---
name: ja4plus 1.0
slug: ja4plus
repo: Crank-Git/ja4plus
status: approved
spec_version: 2
created: 2026-08-06
approved: 2026-08-06
html_generated: 2026-08-06
branch_model: dev-and-live
features:
  - id: foundation
    file: features/00-foundation.md
  - id: spec-conformance
    file: features/01-spec-conformance.md
  - id: correctness-audit
    file: features/02-correctness-audit.md
  - id: concurrency-safety
    file: features/03-concurrency-safety.md
  - id: typed-api
    file: features/04-typed-api.md
  - id: structured-output
    file: features/05-structured-output.md
  - id: live-capture
    file: features/06-live-capture.md
  - id: db-enrichment
    file: features/07-db-enrichment.md
  - id: documentation
    file: features/08-documentation.md
  - id: release
    file: features/09-release.md
  # The file number equals the epic number. Epic 10 (#194) holds no feature document,
  # so `features/10-*.md` does not exist and the sequence loses no file.
  - id: pre-release-validation
    file: features/11-pre-release-validation.md
---

# ja4plus 1.0

## Overview

`ja4plus` is a Python library and command-line program. It reads network packets
and produces JA4+ fingerprints. FoxIO publishes the JA4+ standard. `ja4plus` is an
independent implementation of that standard. Version 0.6.0 is on PyPI now.

FoxIO publishes twelve methods. **`ja4plus` implements eleven of them.** The twelfth is
JA4TScan, and this project declines it by decision rather than omits it by oversight.
`Non-goals` holds the decision and the reason. `docs/specs/foxio/README.md` holds the
inventory of the FoxIO specification material and the transcription procedure.

**`JA4LFingerprinter` writes both JA4L and JA4LS, so ten fingerprinter classes carry eleven
methods.** Read the ten as a count of fingerprinter classes, and never as a count of
methods. #387 records the correction, and `tests/test_documented_method_count.py` holds
every document that states the count against the package.

This spec defines version 1.0.0. Version 1.0.0 makes three promises that version
0.6.0 does not make. The first promise is conformance: every fingerprinter matches
the FoxIO reference output on every FoxIO test vector. The second promise is
safety: the library states a thread-safety contract, holds bounded memory, and
survives malformed packets. The third promise is stability: the public interface is
typed, documented, and does not change again before version 2.0.0.

The reader of this spec is an engineer who works on `ja4plus`. The user of
`ja4plus` is a security engineer. That user reads packet captures, or watches live
traffic, and needs a fingerprint they can compare against a fingerprint another
tool produced. A fingerprint that does not match the reference is worthless to
that user, because comparison is the only thing a fingerprint is for.

## Terms

| Term | Part of speech | Meaning in this project | Do not use |
|---|---|---|---|
| method | noun | One named JA4+ algorithm, for example JA4 or JA4SSH. FoxIO publishes twelve of them. | type, algorithm, mode |
| method name | noun | The lowercase token that names one fingerprinter, for example `ja4h`. `ProcessorStats.method` holds one and `--types` accepts one. The processor holds ten of them. | method key, type name, token |
| fingerprint | noun | The output string of one method for one connection. | hash, signature, ID |
| fingerprinter | noun | The class that implements one method or two. `JA4LFingerprinter` implements JA4L and JA4LS, so ten fingerprinter classes carry eleven methods. | analyzer, parser, engine |
| processor | noun | The `Processor` class that runs every fingerprinter on each packet. | aggregator, pipeline, collector |
| monitor | noun | The `ja4plus watch` command, which reads packets from an interface until the operator stops it. | daemon, sniffer, listener, live capture |
| connection | noun | One network flow, identified by protocol and two endpoints. | session, stream, flow |
| endpoint | noun | One address and one port. | host, peer, side |
| state table | noun | The dictionary a fingerprinter uses to hold per-connection data. | cache, store, map |
| connection table | noun | The state table the monitor owns. It holds the time each connection last sent a packet. | tracker, flow table, session map |
| stop request | noun | The flag that a termination signal sets and that the monitor reads after each packet and after each poll interval. | shutdown flag, kill switch, stop signal |
| poll interval | noun | The count of seconds one `sniff` call reads before it returns to the monitor loop. | tick, poll rate, timeout |
| capture socket | noun | The socket the monitor opens on one interface. It stays open across the `sniff` calls. | listener, raw socket, handle |
| drop count | noun | The count of packets the capture layer dropped. The `dropped` field of the statistics line reports it. | loss, misses, drops |
| packet socket | noun | The `AF_PACKET` socket a Linux capture socket holds in `SuperSocket.ins`. The drop count of a Linux monitor comes from it. | raw socket, AF_PACKET handle |
| capture layer | noun | The code that opens an interface and reports packets to the monitor. `scapy` holds it. | sniffer, driver, backend |
| capture filter | noun | The Berkeley Packet Filter expression the `--bpf` option passes to the capture layer. | BPF, packet filter, filter expression |
| statistics line | noun | The one line the monitor writes to standard error to report its counts. | stats output, status line, summary |
| statistics thread | noun | The thread that writes a statistics line for each interval that passes. | reporter, timer, ticker |
| uptime | noun | The count of whole seconds since the monitor started. | runtime, elapsed time, duration |
| eviction | noun | The removal of one entry from a state table. | cleanup, expiry, purge |
| eviction hook | noun | The callable that a state table calls with the key of every entry it evicts. | callback, listener, observer |
| lockstep | noun | The state where two state tables hold the same keys after every operation. | in step, synchronised, coupled |
| returned connection | noun | One connection that a state table evicted and then saw again. Its new entry holds none of the packets that came before the eviction, so its fingerprint may be incomplete. | revived connection, reappearing flow, re-entry |
| remembered key | noun | One key a state table evicted and holds, so that it recognises a returned connection. A table bounds this memory at its own maximum entry count. The remembered keys of one processor therefore sum to the maximum entry counts of its state tables. | evicted key, ghost key, tombstone |
| lookup cache | noun | The table `JA4DBClient` uses to hold one lookup result for one fingerprint. It is no state table, because it holds no per-connection data. | state table, lookup store, result map |
| resident memory | noun | The memory one process holds in physical memory. `tests/memory_ceiling_run.py` takes two readings of it, and the high-water mark comes from `resource.getrusage(RUSAGE_SELF).ru_maxrss`. The current reading comes from `/proc/self/statm`, or from `ps` on Darwin. A mark never falls, so no reader subtracts two marks. | RSS, memory usage, footprint |
| memory ceiling | noun | The resident memory this package states it holds below, for one named packet run at the shipped defaults. `features/03-concurrency-safety.md` states the number. | memory limit, memory cap, budget |
| memory block | noun | One allocation the interpreter holds. `sys.getallocatedblocks` counts the blocks one interpreter holds live, and no host moves that count. The third control of `TestTheStatedMemoryCeiling` reads it, and #389 records why. | allocation, chunk, object count |
| growth ratio | noun | The growth of the control run divided by the growth of the shipped run, for one packet run. The `resident ratio` reads the resident memory and the `block ratio` reads the memory blocks. | percentage, delta, difference |
| memory pressure | noun | The state where a host holds less free memory than its processes ask for. The kernel then reclaims the pages of a running process, and the resident memory of that process falls while the memory it holds does not. | memory load, swap pressure, low memory |
| lookup service | noun | The third-party service at `https://ja4db.com` that names the application of one fingerprint. | ja4db, remote database, online API |
| remote lookup | noun | One request the client sends to the lookup service for one fingerprint. The operator opts into it. | online lookup, network lookup, remote query |
| disclosure notice | noun | The one output line the command writes to standard error for a run that permits the remote lookup. | warning, banner, message |
| cache directory | noun | The directory outside the installed package that `db update` writes the mapping file to. The platform convention gives its path. | cache dir, download directory, data directory |
| source | noun | The name of the place one mapping file or one lookup result came from. It is `embedded`, `cache` or `remote`. The port publishes the three values, at `lookup.go:31`. | origin, provider, backend |
| vector | noun | One FoxIO packet capture plus its expected-output file. | fixture, sample, test case |
| conformance suite | noun | The test suite that compares `ja4plus` output against every vector. | validation tests, spec tests |
| window | noun | The count of packets JA4SSH reads before it emits a fingerprint. | interval, batch, sample |
| raw form | noun | The unhashed form of a fingerprint, for example `JA4_r`. | expanded form, debug form |
| structurally valid | adjective | The state of input bytes whose headers and length fields agree with the byte count. It describes input bytes and never a fingerprint. | syntactically valid, parseable, sane |
| well formed | adjective | The state of a fingerprint that the published form of its method accepts. It describes a fingerprint and never input bytes. | valid, correct, conformant |
| plausibility guard | noun | A check that rejects input the method reads, because its content describes no real client. | sanity check, heuristic, filter |
| zero sentinel | noun | The literal value `000000000000` that a hash field carries when its list holds no value. | zero marker, zero hash, twelve zeros |
| result | noun | One typed object that carries a fingerprint and its connection. | record, row, output, output line |
| output line | noun | One serialized line that a writer of `ja4plus/output.py` produces for one result. It carries every field of the result, plus `schema_version` and `identified_as`. The `json` format writes it as one JSON object, and the `csv` format writes it as one row. | record, result, entry |
| emit | verb | To return a fingerprint from a fingerprinter. | output, produce, yield |
| evict | verb | To remove one entry from a state table. | clean up, expire, purge |
| conform | verb | To produce the same output as the FoxIO reference. | comply, match spec |
| port | noun | The Go implementation at `Crank-Git/ja4plus-go`. | Go version, sibling, twin |
| parity | noun | The state where this project and the port expose the same interface and emit the same fingerprint. | alignment, sync, consistency |
| hop count | noun | The count of routers a packet crossed, read as the initial TTL minus the observed TTL. | hop distance, TTL delta |
| propagation factor | noun | The JA4L divisor that the FoxIO hop-count table gives for one hop count. | propagation delay, terrain factor |
| measurement point | noun | One packet timestamp that a JA4L value measures from or to. | anchor, marker, reference point |
| Initial packet | noun | One QUIC packet whose long-header type is Initial. RFC 9000 Section 17.2 gives the layout. | first packet, opening packet |
| connection ID | noun | The QUIC identifier that names one endpoint of a connection. The Initial keys derive from the one the client chooses. | CID, connection identifier, DCID |
| Retry packet | noun | One QUIC packet that asks the client to send its Initial packet again with another connection ID. RFC 9000 Section 17.2.5 gives the layout. | retry, redirect |
| Handshake packet | noun | One QUIC packet whose long-header type is Handshake. RFC 9000 Section 17.2.4 gives the layout. | handshake, HS packet |
| key material | noun | One secret that decrypts recorded traffic, such as a TLS session key. | secret, key log, session keys |
| Decryption Secrets Block | noun | The pcapng block that carries key material inside a capture file. | DSB, secrets block, key log block |
| image | noun | One PNG file under `technical_details/` that publishes one method. | diagram, picture, figure |
| transcription | noun | This project's own prose form of one image, held under `docs/specs/foxio/`. | translation, copy, write-up |
| corroboration | noun | One FoxIO-authored source, other than the image, that states the same rule. | confirmation, second opinion, backup |
| baseline | noun | One expected-output file of the FoxIO Zeek package, held under `zeek/tests/Traces/`. | golden file, expected log, snapshot |
| reading | noun | One recorded conclusion about what a source states, with the evidence that supports it. The FoxIO material is one such source. The documentation of a tool is another. | interpretation, take, finding |
| object identifier | noun | One ASN.1 identifier, written as a dotted string such as `2.5.4.3`. JA4X hashes its hex form. | OID, identifier, tag |
| RDN | noun | One relative distinguished name of an X.509 issuer name or subject name. JA4X reads the object identifier of each one. | attribute, name part, DN component |
| tunnel | noun | One connection that carries another protocol inside its payload, for example a SOCKS4 proxy connection. | proxy, wrapper, encapsulation |
| alias | noun | One recorded pair that maps the stream identity a FoxIO reference names to the stream identity `ja4plus` produces. | mapping, translation, synonym |
| value decline | noun | One register entry that records a disagreement with a FoxIO value. An implementation change on either side could close it. The entry carries `"capability": false`. | value dispute, defect entry |
| capability decline | noun | One register entry that records a capability this project chose not to build. No implementation change closes it, because the difference is the scope this project chose. The entry carries `"capability": true`. | scope decline, boundary decline |
| mutation | noun | One change that a sweep applies to one expression of one module under `ja4plus/`. The sweep runs the suite against the change and then reverts it. | mutant, tweak, defect injection |
| sweep | noun | One run of `tests/mutation_sweep.py` over a named module set. It records the result of every mutation it applies. | campaign, mutation testing, sweep run |
| candidate | noun | One test case that no mutation of one sweep makes fail. The sweep does not count it as a failure before it applies a mutation. A candidate belongs to the sweep that named it. | survivor, weak case, untested case |
| body reader | noun | The test file that `tests/mutation_cover.py` names for one module. Its own source holds the most tokens of that module body, so it reads the values the import builds. | cover file, owner, killer |
| clean environment | noun | One virtual environment that holds the installed package, and that holds no source tree on its import path. `features/11-pre-release-validation.md` states the cases that build it. | fresh environment, sandbox, virtualenv |
| throughput | noun | The count of packets one processor reads for each second of elapsed time, for one named packet run on one named host. | packet rate, bandwidth, processing speed |
| recorded transport | noun | The stand-in for `requests` that records every call a lookup makes and sends none. `RecordingRequests` of `tests/test_db_offline.py` holds it. | mock, fake, stub, double |
| package index | noun | The service `pip` resolves one dependency from. The install cases of `tests/test_installed_wheel.py` reach it. | PyPI, registry, repository, mirror |
| pin | noun | The version specifier one dependency entry of `pyproject.toml` carries. An exact pin names one version, and a floating pin lets `pip` resolve a version for each install. | constraint, lock, requirement, bound |
| lint gate | noun | The pair of commands `ruff check ja4plus/ tests/` and `ruff format --check ja4plus/ tests/`. `FR-foundation-7` and `FR-foundation-8` name them. | linter, style check, ruff job |
| documentation tree | noun | The `docs/` directory of this repository and every file below it. `FR-release-11b` keeps it out of the wheel, and `FR-release-11c` keeps it in the source distribution. | doc tree, docs folder, documentation directory |
| assets tree | noun | The `assets/` directory of this repository and every file below it. It holds `logo.png`, which `README.md` renders. The same two requirements hold it as hold the documentation tree. | asset folder, images, media directory |
| end state | noun | The condition a feature holds after the issue that builds it lands. An acceptance criterion of an unbuilt feature states one. | goal state, target state, desired state |
| built criterion | noun | An acceptance criterion that states the count this repository holds today. `tests/test_criterion_counts.py` measures that count. | live criterion, current criterion, satisfied criterion |
| dependency record | noun | One tracked file that states the version of a distribution this project installs. `pyproject.toml` is the one record, and #446 deleted the second one. | requirements file, manifest, dependency list |
| pending criterion | noun | An acceptance criterion that states an end state, and that names the issue which builds it. Its stated count and the measured count differ until that issue lands. | future criterion, aspirational criterion, unmet criterion |
| dependency block | noun | One bracketed array of `pyproject.toml` that states dependency entries, as `dev = [` opens. A dependency record holds one block or more. `tests/dependency_entries.py` reads a block. | dependency list, dependency array, requirements list |
| dry run | noun | The publish that the manual event of `.github/workflows/publish.yml` starts. It reaches TestPyPI alone. `FR-release-13` states it. | test publish, trial run, staging release |
| release body | noun | The text a GitHub release carries. `tests/release_body.py` builds it, and the provider accepts 125000 characters at most. | release notes, description, body text |
| named part | noun | The half of one changelog section that the release body holds: the summary and the two breaking-change tables. `named_part` of `tests/release_body.py` reads it. | excerpt, summary section, head |

## Goals

1. Every method matches the FoxIO reference output on all 37 FoxIO vectors, except
   where the reference holds a proven defect this project declines to reproduce.
   `.claude/rules/conformance.md` states the two shapes that decide it, and every
   exception carries a closed issue and a register entry. #96, #97 and #105 are the
   first three.
2. The conformance suite runs on every pull request and blocks a merge on failure.
3. The library holds bounded memory under a continuous packet stream.
4. The library states one thread-safety contract, and tests prove that contract.
5. No packet input crashes the library, including truncated and corrupt packets.
6. Test coverage is 90 percent or higher, measured by line.
7. The public interface carries type annotations, and `mypy --strict` passes on it.
8. Version 1.0.0 is on PyPI, with documentation published on GitHub Pages.
9. This project and the port emit the same fingerprint for every vector, and the
   typed result carries the same field names.

## Non-goals

- **JA4TScan is declined.** `technical_details/README.md` describes it as an Active TCP
  Fingerprint Scanner. It sends crafted packets to a host the operator names and reads
  the responses. Every other JA4+ method reads traffic that already exists. The reason is
  a capability boundary. A program that sends packets to a host reaches a network the
  operator did not capture. That capability is larger than fingerprint production.
  The user decided it on 2026-08-08, and #197 holds the reading. The decision is
  reversible. The README must stop implying full method coverage while this is true.
- Live packet capture on Windows is out of scope. The daemon mode targets Linux
  and macOS.
- A fingerprint database service is out of scope. `ja4plus` reads FoxIO's
  published mapping file and, when the user opts in, the public `ja4db.com` API.
- Wire-speed performance is out of scope. The project measures throughput and
  reports it, but sets no throughput target for version 1.0.0.
- An asynchronous interface is out of scope. The library stays synchronous.
- Decryption is out of scope. `ja4plus` reads no key material and reads no Decryption
  Secrets Block. It decrypts no TLS record and no QUIC 1-RTT packet. Traffic that a
  capture carries only in encrypted form therefore produces no fingerprint, even when
  the same capture file carries the key material that decrypts it. #129 holds the
  decision, and the decision is reversible.

## Users & personas

| Persona | What they need | What they are allowed to do |
|---|---|---|
| Capture analyst | Fingerprints from a saved packet capture, in a format another tool can read. | Read a capture file. Write output to a file or to standard output. |
| Monitor operator | A long-running process that reads a live interface and never exhausts memory. | Open a capture interface. This needs elevated privileges on the host. |
| Library integrator | A typed, documented Python interface with a stated concurrency contract. | Import `ja4plus` inside their own program. |
| Maintainer | Evidence that a change did not break conformance. | Merge a pull request when every gate passes. |

## Feature map

| Feature set | Spec file | Epic | Mockups |
|---|---|---|---|
| Foundation | `features/00-foundation.md` | Epic 0: Foundation | — |
| Spec conformance | `features/01-spec-conformance.md` | Epic 1: Spec conformance | — |
| Correctness audit | `features/02-correctness-audit.md` | Epic 2: Correctness audit | — |
| Concurrency and resource safety | `features/03-concurrency-safety.md` | Epic 3: Concurrency and resource safety | — |
| Typed public interface | `features/04-typed-api.md` | Epic 4: Typed public interface | — |
| Structured output | `features/05-structured-output.md` | Epic 5: Structured output | `mockups/01-cli-output.html` |
| Live capture | `features/06-live-capture.md` | Epic 6: Live capture | — |
| Database enrichment | `features/07-db-enrichment.md` | Epic 7: Database enrichment | — |
| Documentation | `features/08-documentation.md` | Epic 8: Documentation | — |
| Release | `features/09-release.md` | Epic 9: Release | — |
| Pre-release validation | `features/11-pre-release-validation.md` | Epic 11: Pre-release validation | — |

## Architecture & stack

`ja4plus` is a pure Python package. It has two runtime dependencies.

| Component | Choice | Why this choice beat the alternative |
|---|---|---|
| Language | Python 3.9 or later | Python 3.8 reached end of life in October 2024. Python 3.9 is the oldest line that still receives security fixes. |
| Packet parsing | `scapy>=2.4.0` | `scapy` reads every capture format the project needs and needs no C toolchain. `dpkt` is faster but parses fewer link types. |
| Certificate parsing | `cryptography>=42.0.0` | JA4X reads X.509 structure. `cryptography` is maintained and already a dependency. |
| Build backend | `setuptools` | Already in use. A move to `hatchling` changes nothing a user sees. |
| Test runner | `pytest` | Already in use, and the conformance suite needs its parameterization. |
| Lint and format | `ruff` | One tool replaces `flake8`, `isort` and `black`. It runs in under one second on this codebase. |
| Type check | `mypy --strict` on `ja4plus/` | `mypy` supports the `py.typed` marker that the typed interface needs. `pyright` needs Node.js in CI. |
| Documentation site | `mkdocs-material` with `mkdocstrings` | The existing documentation is Markdown. Sphinx would need a rewrite into reStructuredText. |
| Documentation host | GitHub Pages | The repository is already on GitHub. Read the Docs adds an account and a webhook. |
| Package host | PyPI, through trusted publishing | Already configured in `.github/workflows/publish.yml`. No token is stored. |

Data flow: a packet enters `Processor.process_packet`. The processor calls each of
the ten fingerprinters in a fixed order. A fingerprinter reads the packet, updates
its state table, and emits a fingerprint or returns nothing. The processor collects
every fingerprint into a list of results and returns it.

Verified against: https://github.com/FoxIO-LLC/ja4 (retrieved 2026-08-06, upstream
default branch at commit dated 2026-07-21).

## Parity with ja4plus-go

A Go port of this project exists at `Crank-Git/ja4plus-go`. `ja4plus/processor.py`
already states that it mirrors that port. The two must not drift apart, because a
user who runs both must get one answer.

Three rules govern parity.

1. **FoxIO decides behaviour.** Where FoxIO specifies the output, the vectors
   decide. This rule outranks the port. Where the port disagrees with a vector, the
   port is wrong.
2. **The port decides interface.** Where FoxIO specifies nothing — a field name, a
   subcommand name, a default — the port has already shipped a choice. This project
   adopts that choice rather than inventing a second one.
3. **The gate is the shared vector set.** Both repositories read the same 37 FoxIO
   vectors, pinned to the same upstream commit. No test in this repository builds,
   runs, or imports the port. A cross-language test rig couples two repositories
   that move at different speeds, and it fails for reasons that have nothing to do
   with the change under test.

This spec plans changes to this repository only. It records what the port should
change, and files nothing there.

### Divergence register

| Item | This project today | The port today | Rule | Action |
|---|---|---|---|---|
| Result type | `dict` with 8 keys. | `FingerprintResult` struct with 9 fields. | 2 | Adopt the port's field set, including `Timestamp`. Epic 4. |
| Results per packet | One fingerprint string. | A slice of results. | 2 | Return a list. Epic 4. |
| Parse failures | Swallowed, logged at `DEBUG`. | Returned as a second value. | 2 | Expose the errors the processor collected. Epic 4. |
| Remote lookup | The command-line flag `--lookup` is opt-in, but `JA4DBClient.lookup` then calls `ja4db.com` on every miss with no second opt-in. | The remote fallback is a separate opt-in. | 2 | Separate the local lookup from the remote lookup. Epic 7. |
| Mapping file refresh | `db update` and `db info` already exist. | The same two subcommands. | — | Already at parity. Keep a test that proves it. |
| Shard key format | `tcp:ip:port->ip:port`. | The same format. | — | Already at parity. Keep a test that proves it. |
| JA4SSH window | Emits at `packet_count`, which defaults to 200. | Emits at `min(packetCount, 10)`. | 1 | Fixed here by #28. The port carries the defect at `ja4ssh.go:176`. |
| JA4SSH results the reference produces and this project declines | Reads the mode field from the window alone, emits a value only for a window that holds SSH packets, and emits the trailing window for every connection that closes. | Not measured. | 1 | Three defects of the FoxIO reference produce output that describes the capture and not the connection, so rule 1 does not settle them and a person decided each one. #96, #97 and #105 hold the decision and the measurement. The port must adopt all three, or the two implementations disagree on JA4SSH. |
| The window a connection holds open at the end of a capture | Emits it. `close_open_windows` reads every connection of the state table, and the caller runs it when the packet source ends. | Not measured. The port exposes no equivalent method. | 1 | The specification states no rule that closes the last window, so this is not a case rule 1 settles. `docs/specs/foxio/JA4SSH.md` R11 holds the gap, and #199 proved that the deleted `technical_details/JA4SSH.md` states nothing about it. The references split two against two: `python/ja4.py:554-556` and `wireshark/source/packet-ja4.c:1399-1404` close the last window on a FIN+ACK packet, and `rust/ja4/src/ssh.rs:45-55` and `zeek/ja4ssh/main.zeek:160-164` close it at the end of the capture. `python/ja4.py:610` runs no end-of-capture step, because the line is commented out. The user decided on 2026-08-08 that `ja4plus` follows the Rust reference and the Zeek package, and the decision is reversible. Two FoxIO references hold the value: `tests/foxio_vectors/rust_expected/ja4__insta@ssh2.pcapng.snap:215-217` and the Zeek baseline both give `c36s52_c42s76_c51s2` as the second value of `ssh2.pcapng`, and the `ja4plus` counters already held it. Six JA4SSH comparisons moved and no other value moved. The register lost `ssh2.pcapng/JA4SSH` and gained five entries under #214. #97 stays declined, so a window that holds no SSH packet still emits nothing. #214 holds the decision and the measurement. The port must add the same call, or the two implementations disagree on JA4SSH. |
| The method that emits the window at the end of a capture | `close_open_windows()` on `BaseFingerprinter`, on every fingerprinter and on `Processor`. It returns the entries it appended. | No equivalent method. | 2 | The port has shipped no choice here, so rule 2 names no interface to adopt and this project chose one. The name follows `_close_window`, which `ja4plus/fingerprinters/ja4ssh.py` already held. The port must adopt the name, or the two implementations expose two names for one behaviour. |
| Parity test | 6 tests that assert names exist. | Not applicable. | 3 | Replace with the shared vector suite. Epic 1. |
| JA4 ALPN value for a byte that is not alphanumeric | Writes `99`. | Not measured. | 1 | The FoxIO prose and the FoxIO implementations disagree, so rule 1 settles the value on the vector. #127 holds the decision and the measurement. The port must adopt `99`, or the two implementations disagree on JA4 and on JA4S. #141 owns the condition that triggers the value, which no vector separates. |
| JA4 ALPN value for a byte outside `0x20-0x7E` in a position other than the first | Writes `99`. | Not measured. | 1 | The two FoxIO implementations disagree with each other, so this is a reference split and rule 1 does not settle it. The user decided on 2026-08-07 that `99` stays. Two reasons support the reading. The FoxIO Rust result is an artifact. `rust/ja4/src/tls.rs` reads the tshark escape text, so it reads `h\x1f` as the five characters `h`, `\`, `x`, `1` and `f`. The FoxIO Python result puts `U+FFFD` into a fingerprint, which is a character no byte of the packet holds. #141 holds the measurement, #162 holds the decision, and the decision is reversible. `tests/foxio_vectors/alpn-condition.pcap` streams 2, 4 and 5 carry `h\xab`, `h\x1f` and `h\x0a`, and the register holds twelve entries that state the measured value of both implementations. The port must write `99`, or the two implementations disagree on JA4 and on JA4S. |
| JA4 ALPN value for a first ALPN value of one byte | Repeats the byte and writes `hh`. | Not measured. | 1 | All three sources differ on the input `h`: FoxIO Python writes `h`, FoxIO Rust writes `h0`, and no vector separates them. The FoxIO Python value holds one character, which cannot fill a two-character field. `ja4plus` writes a value that is well formed and stable, and the user decided on 2026-08-07 that it stays. #141 holds the measurement, #162 holds the decision, and the decision is reversible. `tests/foxio_vectors/alpn-condition.pcap` stream 6 carries the input, and the register holds four entries that state the measured value of both implementations. The port must repeat the byte, or the two implementations disagree on JA4 and on JA4S. |
| The JA4 ALPN form that matches no FoxIO implementation | Writes `99` for a first ALPN value that is not alphanumeric, and `hh` for a first ALPN value of one byte. | Not measured. | 1 | **The user ruled on 2026-08-10 that the form of `ja4plus` stands**. The conformance audit of the same date named this the one condition where `ja4plus` matches no FoxIO implementation. The two references disagree with each other, and each one reads its own tooling rather than the packet. `ja4plus` reads the packet bytes, and it writes `99` or `hh`. FoxIO Python writes `U+FFFD`, which is a replacement character that no packet byte holds. FoxIO Rust writes `h9`, which is the escape text of `tshark`. **No FoxIO reading is available to adopt here**. A match with either reference copies an artifact of that reference tool into the fingerprint, and the user declines that. This row moves no fingerprint and it repairs no defect. #141 holds the measurement of the disputed inputs, #162 records the readings of 2026-08-07, and #522 records this ruling. The two rows above state those readings, and this row states the ruling that stands over both. The sixteen entries of `tests/foxio_deviations.json` that name #162 stand, and `tests/test_alpn_ruling_register.py` reads this row and that count. The ruling is reversible. The port must write `99` and `hh`, or the two implementations disagree on JA4 and on JA4S. |
| JA4L client value on the return path of `process_packet` | Returns a client value for every packet that moves the client measurement point. Across the committed vectors the return path reports 105 client values, and the stored list holds 60. | Not measured. | 1 | The reference reports one client value for one connection, and the stored list of `ja4plus` holds one. `tests/conformance_index.py` reads the stored list, so every conformance case measures the correct value and no fingerprint moves. The return path repeats the value, because the reference measures to the last packet that carries both relative numbers. A reader knows which packet is last only when the connection ends. #156 measured three candidate end-of-connection points against the 60 client values. A FIN or a RST reports 26 and loses 34, because 34 connections never close inside their capture. `macos_tcp_flags.pcap` holds no FIN packet and no RST packet at all. Every measurement point read reports 105, which is the behaviour today. A read of the stored list at the end of a capture reports all 60, and that is not a `process_packet` return. That candidate needs a new flush call, and a live capture never ends, so `ja4plus --live` would then report no client value. The user decided on 2026-08-07 to record the divergence and to leave the return path as it is. #156 holds the decision and the measurement, and the decision is reversible. The register holds no entry for this divergence, because the conformance harness reads the stored list and a strict entry would report `XPASS`. `tests/test_ja4l_return_path.py` holds both counts instead, so the divergence is a comparison that runs. The port must report one client value for one connection, or the two implementations disagree on JA4L. |
| The JA4L part count | Writes two timing parts on every connection. A QUIC connection carries a protocol marker as a third part, and no TCP connection carries a third part. | Not measured. | 1 | The specification and two FoxIO implementations write three parts, and two FoxIO implementations write two. `technical_details/JA4L.png` labels three parts. `wireshark/test/testdata/` holds 44 `ja4.ja4l` values and 44 `ja4.ja4ls` values, and every one of the 88 holds three parts. `zeek/tests/Traces/` writes three. `python/test/testdata/` writes two, and `rust/ja4/src/snapshots/` writes two. This is a reference split, so rule 1 does not settle it. **The user decided on 2026-08-08 that the two timing parts stay**, and the reason is the conformance evidence. Every one of the 114 JA4L values in `tests/foxio_vectors/*.json` holds two parts, and `tests/foxio_vectors/wireshark_expected/` holds two files that carry no JA4L key. A three-part form therefore deviates from all 114 comparisons, which moves the register from 116 entries to 230 and leaves no case that measures any JA4L value. **20 of the 114 sit on 10 captures that the Wireshark dissector publishes no JA4L value for**, so those 20 would carry a third part that no reference verifies. A second reading blocks the adoption. The Wireshark part c reads measurement points D, E and F, which `packet-ja4.c:1328`, `packet-ja4.c:1343` and `packet-ja4.c:1366` set on application packets. `ja4l.py` emits the server value on the SYN-ACK and the client value on the first packet that carries both relative numbers, and both points lead D. Adopting part c is therefore an emission-model change and not a format change. #225 holds the decision and the measurement, and the decision is reversible. The port must write two timing parts, or the two implementations disagree on JA4L. |
| The JA4L protocol marker on a QUIC connection | Writes `quic` as the third part of `JA4L-C` and `JA4L-S` on a QUIC connection. 36 values across four captures carry it. | Not measured. | 1 | The FoxIO references disagree, so this is a reference split and rule 1 does not settle it. `packet-ja4.c:1441` and `packet-ja4.c:1447` write `"%d_%d_quic"`, and `wireshark/test/testdata/` publishes the marker on 9 of the 44 `ja4.ja4l` values and on 9 of the 44 `ja4.ja4ls` values. `zeek/ja4l/main.zeek:233` and `zeek/ja4l/main.zeek:252` append the literal `q`. `python/ja4.py:165` writes two parts, and `rust/ja4/src/time.rs` writes two parts. The user decided on 2026-08-08 that a QUIC connection carries the marker, because a published reference value verifies it. **The spelling follows Wireshark.** `.claude/rules/external-apis.md:95` reads "Read no JA4L or JA4LS value of a Zeek baseline as a reference value", and `.claude/rules/external-apis.md:101` names the `q` marker as one of three Zeek divergences. The rule declines the Zeek spelling, so the Wireshark spelling is the one published reading that remains. **The `tcp` literal is not adopted.** `packet-ja4.c:1348` and `packet-ja4.c:1354` write it on an HTTP connection, and none of the 88 published Wireshark values carries it. #225 holds the decision and the measurement, the register holds 16 entries that state the measured value of the Python reference, and the decision is reversible. The port must write the marker on a QUIC connection, or the two implementations disagree on JA4L. |
| The JA4L server value on a retransmitted SYN-ACK | Emits one server value for one connection. A retransmitted SYN-ACK finds point `B` set, and it gives no value. | Not measured. | 1 | The reference publishes one server value for one connection, and a repeat is a defect under `.claude/rules/conformance.md`. `ja4plus` returned the value on every SYN-ACK, so a retransmitted SYN-ACK repeated the value the first one gave. **The repeat described no second measurement, because a retransmitted SYN-ACK moves neither point `B` nor the server TTL.** `ssh2.pcapng` stream 15 is the one vector that reaches the rule: the expected-output file holds `JA4L-S=6252_58` once, and `ja4plus` produced it twice. **A replay of the 38 committed captures moved exactly one value**, and that value is the second `6252_58` of `ssh2.pcapng` stream 15. No other JA4L value moved, on that capture or on any other. The register lost `ssh2.pcapng/JA4L-S` and fell from 135 keys to 134, and the conformance suite moved from 135 `xfailed` to 134. `tests/test_ja4l_connection_state.py::test_a_retransmitted_syn_ack_reports_no_second_server_value` holds the rule, and it fails when the guard goes. #272 holds the decision and the measurement. The port must emit one server value for one connection, or the two implementations disagree on JA4L. |
| The JA4L comparison on a reference file that holds no JA4L key | Produces a JA4L value on `CVE-2018-6794.pcap`, on `https-connect.pcap` and on `tls-handshake.pcapng`. The expected-output file of each of the three holds no JA4L key. | Not measured. | 1 | **This project was never emitting more than the reference. The reference published nothing to compare.** The comparison is unreachable, not satisfied. `python/ja4.py:339` runs `delete_keys(['JA4L-S','JA4L-C'], final)` when the generating run names another method, so the method filter removed the key from the file. The evidence is `python/ja4.py:339` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. A count of the JA4L keys of the 38 expected-output files reports 0 for those three files and a value for 22 others, so the deletion names the three and not the method. #200 measured the mechanism and #272 decided the decline on 2026-08-08. **Five register entries carry the decline**, and each one records a value decline rather than a capability decline. This row changes no fingerprint, so the port needs no change for it. |
| JA4X on a stream that a proxy tunnel carries | Reads the record layer without regard to the tunnel protocol that carries it. Produces three JA4X values on the SOCKS4 tunnel of `socks4-https.pcap` on port 9901, and no FoxIO implementation holds one. | Not measured. | 1 | No FoxIO implementation holds a value, so this is unanimity and not a reference split, and rule 1 does not settle it. The user decided on 2026-08-07 that the three values stay. The same behaviour produces the `https-connect.pcap` values that the FoxIO Rust snapshot and the Wireshark dissector both hold. A gate on the record-layer scan would risk that case. #138 holds the decision and the measurement, the register records the divergence, and the decision is reversible. The port must read the record layer the same way, or the two implementations disagree on JA4X. |
| The JA4H method code for a method outside the nine the Rust reference names | Reads the first two characters of any method token. `PROPFIND` writes `pr`, and `MKCOL` writes `mk`. | Not measured. | 1 | The three FoxIO references disagree, so this is a reference split and rule 1 does not settle it. `python/ja4h.py:9` reads `method.lower()[:2]` and names no method. `rust/ja4/src/http.rs:342` rejects a method outside `co`, `de`, `ge`, `he`, `op`, `pa`, `po`, `pu` and `tr`. `wireshark/source/packet-ja4.c:1087` holds a 40-entry map, so it reads a method the Rust reference rejects and it produces nothing for a method its map omits. The user decided on 2026-08-08 that `ja4plus` follows the Python reference, which is the primary reference for every method in this project. A method absent from the Wireshark map therefore writes a two-character code rather than nothing. The image caption ends with `etc`, so the image closes no method list. No vector in this repository carries a method outside the nine, so no vector separates the three readings and no register entry names this reading. #219 holds the decision and the measurement, and the decision is reversible. `tests/test_ja4h_part_a_readings.py` holds six constructed method cases instead, so the divergence is a comparison that runs. The port must read the first two characters of any method, or the two implementations disagree on JA4H. |

| Part e of JA4TS, the time since the last SYN-ACK | Writes part e. A connection the server answered twice or more carries the delay of each SYN-ACK, in whole seconds, and a connection the server answered once omits part e. | Not measured. | 1 | The FoxIO image is the one source that keeps part e off JA4TS: its caption reads `TCP Retransmission Timings (only on JA4TScan)`. Three FoxIO sources contradict it, and nothing ever corrected them. The deleted `technical_details/JA4T.md` heads its form `__JA4TS and JA4TScan Fingerprint formats:__` and writes `WindowSize_TCPOptions_MSSValue_WindowScale_TimeSinceLastSYNACK`; `b6f3ff4` deleted the file on 2024-02-22 and no commit restored it. `wireshark/source/packet-ja4.c:1595` writes the `JA4TS` field through `ja4t()` with a connection, which appends part e at line 684. `zeek/ja4t/main.zeek:229-235` appends the delay list to `c$conn$ja4ts`. The image's own example value `1-2-4-8-R6` carries the `R` suffix its caption denies. This is a reference split, so rule 1 does not settle it, and the user decided on 2026-08-08 that JA4TS carries part e. **The decision reverses the D6 and D7 ruling of #215 of the same day, which followed the image.** #226 holds the measurement, and the decision is reversible. **The reference value this reading departs from is the Zeek baseline, and the departure measured zero.** The three `conn.log` baselines hold 10 JA4TS values, `Scripts.ja4-conn/conn.log` holds `ja4ts 65535_00_00_00`, and #198 measured 9 of 10 matching. **After part e the reading is 9 of 10, unchanged, and no `ja4plus` value carries part e on any of the ten rows.** Each of the ten baseline connections holds one SYN-ACK, and the deleted file states that a fingerprint omits part e when it sees no retransmission. Across the 38 captures of `tests/foxio_vectors/`, 803 values are produced before and 803 after, and exactly 1 differs: `ssh2.pcapng` packet index 372, which is frame 373 in a one-based reader, moves from `64240_2-1-1-4-1-3_1460_7` to `64240_2-1-1-4-1-3_1460_7_0`. The register holds 116 keys before and after, and the conformance suite reports 116 `xfailed` before and after, because no conformance case compares a JA4TS value. `docs/specs/foxio/JA4T.md` states the rule as R12 and `docs/specs/foxio/zeek.md` records the re-measurement. The RST value of JA4TS separates from part e, and the row below holds it. The port must write part e, or the two implementations disagree on JA4TS. |

| The JA4TS value that a RST produces | Appends `-R` and the delay of the RST to part e, on a connection that already holds a delay. That value reads part a through part d from the first SYN-ACK of the connection. A RST on a connection with no delay produces no value, and a client RST produces no value. | Not measured. | 1 | The deleted `technical_details/JA4T.md` is the primary source and it states the rule: "the final TCP packet, a RST packet, should be appended to the last JA4TS denoted with 'R' and its delay." Its example ends `65535_2-1-3-1-1-4_65495_8_1-2-4-8-R6`, and it states the reason the value reads the stored connection: "Note that RST packets do not contain TCP options or window sizes, as such the program will need to be aware of the previous JA4TS." `b6f3ff4` deleted the file on 2024-02-22 and no commit restored it, so the statement stands as FoxIO-authored under the rule #221 established. **Two implementations corroborate the suffix and each disagrees with the other on one point, so rule 1 does not settle those two points and a person decided each.** `wireshark/source/packet-ja4.c:1608` writes a second `JA4TS` value on the RST packet, and lines 1601 to 1607 restore `window_scale`, `window_size`, `mss_val` and `tcp_options` from the connection. `zeek/ja4t/main.zeek:233` appends `-R%d` to the delay list. Both nest the RST branch inside the delay branch: `wireshark/source/packet-ja4.c:693` reads `rst_time` only when `syn_ack_count > 1`, and `zeek/ja4t/main.zeek:232` reads `rst_ts` only when the delay list holds a value. **The first disagreement is the flag combination, and `ja4plus` reads the RST bit.** `zeek/ja4t/main.zeek:167` tests `rph$tcp$flags & TH_RST`, and `wireshark/source/packet-ja4.c:1296` tests `tcp_flags == 0x004` for equality, so the dissector reads no RST that also carries ACK. The deleted file writes "a RST packet" and names no flag combination, so the prose and the Zeek script agree and the dissector reads one case fewer. A RST that carries ACK is common on real traffic. **The second disagreement is the RST that carries no delay, and `ja4plus` emits nothing.** `wireshark/source/packet-ja4.c:1599` writes the `JA4TS` field whenever `syn == 3`, so the dissector emits the four parts a second time on such a RST. That second value repeats the value the SYN-ACK already produced and describes no packet of its own, which `.claude/rules/conformance.md` declines under its second shape. `zeek/ja4t/main.zeek` writes one JA4TS value for one connection and never a second, so the Zeek package corroborates the decline. **A client RST produces no value**, because every SYN-ACK travels from the server and the connection key names the server first. `zeek/ja4t/main.zeek:189` sets the packet threshold with `F`, so the Zeek script reads responder packets alone, and `wireshark/source/packet-ja4.c:1296` holds no direction guard. **No vector reaches the rule and no value moved.** `tests/foxio_vectors/` holds 10 RST packets across its 38 captures, and none of the 10 sits on a connection with more than one SYN-ACK. A replay of the 38 captures produces 797 values before the change and 797 after, and 0 differ. The register holds 137 keys before and after, and the conformance suite reports 137 `xfailed` before and after, because no conformance case compares a JA4TS value. `tests/build_ja4ts_rst.py` writes the capture from the six capture times the deleted file lists, and `tests/test_ja4ts_reset.py` reads it back. #246 holds the measurement, `docs/specs/foxio/JA4T.md` states the rule as R13, and the decision is reversible. The port must append the RST suffix, or the two implementations disagree on JA4TS. |

| The empty option list, the absent maximum segment size and the zero window scale of JA4T and JA4TS | Writes the two-digit form. An empty option list writes `00`, part c writes two digits, and a window scale of zero writes `00`. `gre-erspan-vxlan.pcap` therefore gives `8192_00_00_00`. | Not measured. | 1 | **Three FoxIO forms exist and rule 1 settles none of them, so this is a reference split and the user decided it.** `rust/ja4/src/tcp.rs` joins an empty option list to the empty string and prints part c and part d with no padding, so its snapshot holds `8192__0_0`. `wireshark/source/packet-ja4.c:664` writes `00` for an empty option list, prints part c with `%02d`, and prints part d with `%02d` when the window scale is zero. `zeek/ja4t/main.zeek:195-211` repeats the Wireshark rule for JA4T and for JA4TS. Two of the three forms agree, and the user followed them on 2026-08-08. The deleted `technical_details/JA4T.md` states the rule in prose and corroborates the form a third time: it reads "If any field does not exist, then the output is 00." and gives `JA4T = 1024_00_00_00`. **The prose and the two implementations part on one input, and `ja4plus` follows the implementations.** The prose reads `00` as the mark of an absent field, and both implementations test the value instead, because each stores the window scale as an integer that defaults to zero. A packet that carries a Window Scale option of value 0 therefore writes `00` in both implementations and `0` under the prose alone, and 7 of the 12 moved values are of that shape. A fingerprint exists so that one tool's output can be compared against another tool's output, so `ja4plus` writes the value the two executable references write. **This diverges from the FoxIO Rust implementation, which is the reference the parity harness compares JA4T against, and the cost was measured before the change.** 12 values across 6 captures take the new form: `CVE-2018-6794.pcap` gives three JA4TS values, and `gre-erspan-vxlan.pcap`, `gre-sample.pcap`, `sshv1.pcap` and `v6.pcap` each give one JA4T value and one JA4TS value, and `ssh2.pcapng` gives one JA4TS value. 5 of the 12 carry an empty option list, and the other 7 move only because the window scale is zero. **Only 1 of the 12 reaches a FoxIO reference comparison**, because this repository holds a Rust snapshot for 7 captures and no reference file holds a JA4TS value. That one is `gre-erspan-vxlan.pcap/0:65174/JA4T.1`, which stays in `tests/foxio_deviations.json` as a decided divergence and keeps its `xfail(strict=True)` marker. The register therefore gains no entry from this reading. #215 holds the decision and the measurement, and the decision is reversible. **D2, D3, D4 and D5 of #215 gain no row of their own, and the reason is stated rather than left out.** Each one moves `ja4plus` towards the FoxIO references and departs from none of them. D2 is the one that needs a word: the reader counts one entry for each End of Option List byte, which `rust/ja4/src/tcp.rs` and `wireshark/source/packet-ja4.c:1458` both do, and `zeek/ja4t/main.zeek:95-98` leaves the option loop at kind 0 and records nothing. Two references against one settles it under rule 1, the Zeek package outranks nothing, and `ja4plus` already recorded one kind 0 entry before #215, so the departure from the Zeek script is older than this change. `docs/specs/foxio/zeek.md` records it. The port must write the two-digit form, or the two implementations disagree on JA4T and on JA4TS. |
| Subfield 2 of JA4D when a message repeats option 57 | Writes the first Maximum DHCP Message Size, in four digits. | Not measured. | 1 | **This declines a proven defect of the dissector.** `wireshark/source/packet-ja4.c:1508-1512` appends `"%04d"` to the same buffer on each occurrence, so a message that repeats option 57 gives an eight-digit subfield and a part a of fifteen characters. R2 of `docs/specs/foxio/JA4D.md` gives part a eleven characters and subfield 2 four of them, and the specification decides schema. Read at the width the image states, the dissector's buffer holds the first occurrence, so this project keeps the first. `zeek/ja4d/main.zeek:55-63` reads one value. **No vector carries a repeated option 57**, so the decline costs zero unmatched cases; `tests/test_ja4d_decisions.py::test_ja4d_keeps_the_first_maximum_message_size` holds the separating packet. The user decided D4 of #231 on 2026-08-08, and the decision is reversible. |
| JA4D on a BOOTP message that carries no option 53 | Emits no value. | Not measured. | 1 | **Two FoxIO references against one.** `wireshark/source/packet-ja4.c:1498` sets the emit flag inside the option 53 block alone, so the dissector emits nothing, and the `00000` default at `wireshark/source/packet-ja4.c:705` stays unreachable for DHCP. `zeek/ja4d/main.zeek:43-45` returns `"00000"`, and `zeek/ja4d/main.zeek:81` records the author's own doubt: `Not sure this is actually possible since you need at least option 53 to be DHCP and not just BOOTP`. **No vector carries a BOOTP message**, so this project stays unmatched against the Zeek script on zero cases; `tests/test_ja4d_decisions.py::test_ja4d_emits_nothing_for_a_bootp_message_that_holds_no_message_type` holds the separating packet. The user decided D2 of #231 on 2026-08-08, and the decision is reversible. |
| Subfield 1 of JA4D6 on a relay message | Writes the outer DHCPv6 message type alone, in five characters, so part a holds eleven characters. | Not measured. | 1 | **This declines a proven defect of the dissector.** `wireshark/source/packet-ja4.c:1537-1546` appends a five-character name for every `dhcpv6.msgtype` field, and a relay message holds two: the outer type and the inner type. The dissector therefore writes `rlayfsolct` and a part a of sixteen characters. R2 of `docs/specs/foxio/JA4D.md` gives part a eleven characters and gives subfield 1 five of them, and the specification decides schema. A consumer that reads part a by position keeps working against the eleven-character form and breaks against the sixteen-character one. **This row departs from the only executable implementation.** No FoxIO implementation other than the Wireshark dissector writes a JA4D6 value. `zeek/README.md:15` states `JA4D6 &rarr; ja4d.log (awaiting Zeek DHCPv6 suppport)`. FoxIO ships no Python and no Rust for the method. No deleted text file covers the method. **No vector carries a relay message, so the decline costs zero unmatched cases.** `tests/foxio_vectors/dhcpv6.pcap` holds no relay message, and all six reference values match before the change and after it. `tests/test_ja4d_decisions.py::test_ja4d6_writes_the_outer_message_type_alone_in_subfield_1` holds the separating packet. A reversal of the ruling fails 5 cases. The user decided D3 of #271 on 2026-08-08, and the decision is reversible. **D1 and D2 of #271 carry no row, because each follows the dissector.** Part a reads the Client DUID, the IA_TA option and the Client FQDN at any nesting depth. Part c reads the Option Request List the same way. |

| The methods the FoxIO License 1.1 covers | The README names each method it implements under the license, and that list holds ten names. It states that FoxIO's list is wider, and it cites `License FAQ.md` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. **The ten stand as a subset**, and the README asserts no equality with FoxIO's list. | Not measured. | — | Record the contradiction, and change the README nowhere. **Three FoxIO records at that commit name a different set of methods**, so no statement of equality with FoxIO's list can be true. `License FAQ.md:5` names twelve methods, and it reads `__JA4S, JA4L, JA4LS, JA4H, JA4X, JA4SSH, JA4T, JA4TS, JA4TScan, JA4Scan, JA4D, JA4D6 and all future additions, (collectively referred to as JA4+)__`. The FoxIO `README.md:293` names nine methods, and it reads `**JA4S, JA4L, JA4LS, JA4H, JA4X, JA4SSH, JA4T, JA4TS, JA4TScan and all future additions, (collectively referred to as JA4+)**`. `LICENSE:3` names thirteen methods, and it reads `Software: JA4S, JA4H, JA4L, JA4LS, JA4X, JA4T, JA4TS, JA4TScan, JA4D, JA4D6, JA4SScan, JA4E, and JA4SSH (Collectively referred to as JA4+)`. **That third record spells the scanner `JA4SScan`, and it names a `JA4E` that neither other record names.** **Each quotation above is verbatim**, because `.claude/rules/ste.md` bars a rewording of text copied from the FoxIO material. **This is a candidate reference defect outside the two shapes `.claude/rules/conformance.md` declines.** It moves no fingerprint, and it makes no fingerprint describe traffic this project never read. That rule sends such a case to the user. **The user ruled on 2026-08-10 that the ten stand and the README does not change.** **The parity rules settle nothing here**, because they govern behaviour and interface, and a license list states another party's terms. #388 measured the three counts on 2026-08-09, and #466 read the three lines again at the same commit on 2026-08-10. `tests/test_foxio_license_register.py` holds this row. The decision is reversible. |
| JA4Scan | Not implemented. **This project has recorded no decision about JA4Scan.** The absence is neither a decline nor a plan. `License FAQ.md:5` names `JA4Scan`, `LICENSE:3` spells it `JA4SScan`, and the FoxIO `README.md:293` names neither spelling. | Not measured. | — | Record the state, and write no reason for a decline. **This project decided nothing about JA4Scan, so a stated reason would assert a decision that no round holds.** The `Non-goals` section above holds the decline of JA4TScan, which is a separate method and a separate decision. `README.md:42` states that reason, and that reason is unrelated to the license. The user ruled on 2026-08-10, and #466 holds the reading. `tests/test_foxio_license_register.py` holds this row. |
| A structurally valid ClientHello whose body describes no real client | Produces a fingerprint. `ja4plus` holds no plausibility guard, so the content of the body changes no outcome. | Not measured. | 1 | **This row records a property of `ja4plus` rather than a divergence from a named source.** The specification decides intent and the vectors decide the exact bytes where intent runs out. No FoxIO material and no vector settles which byte strings are a ClientHello, so rule 1 leaves the question open. **A guard here would be a rule this project invented.** `ja4plus` would then answer differently from every FoxIO implementation on the same bytes. A fingerprint exists so that one tool's output can be compared against another tool's output. **The measurement decides the cost, and #338 produced it and #343 re-measured it.** `tests/measure_random_client_hello.py` draws 5000 random bodies of 256 bytes behind a valid TLS record header and a valid handshake header. All 5000 produce a fingerprint and all 5000 are well formed. 4998 carry the version token `00`. 720 carry neither a cipher suite nor an extension. **The same script reads every committed capture and finds 168 ClientHellos. 0 of the 168 carry the version token `00`, and 0 carry neither a cipher suite nor an extension.** A guard on those two conditions therefore moves no vector. #343 measured that directly. With the guard live the conformance suite reported the same 1531 passed, 143 skipped and 135 xfailed. **It also closes nothing.** 2 of the 5000 draws pass both conditions. The guard therefore costs a permanent divergence and leaves about one fabricated value in twenty-five hundred. **The user decided on 2026-08-08 that `ja4plus` adds no guard, and the decision is reversible.** `tests/fuzz/test_structural_validity.py` holds the decision as four cases that a guard makes fail, and #343 holds the decision and the measurement. The port must produce a fingerprint for the same bytes, or the two implementations disagree on JA4. |

Verified against: https://github.com/Crank-Git/ja4plus-go (`types.go`,
`processor.go`, `lookup.go`, `ja4ssh.go`, retrieved 2026-08-06, default branch
`master` at 2026-05-09).

## Data model

The library holds no database. It holds three kinds of in-memory state.

### Result

The typed object each fingerprinter returns. Epic 4 defines it as a frozen
dataclass named `FingerprintResult`. The field names are the snake-case form of
the port's `FingerprintResult` struct, under parity rule 2. The field that names
the method is called `type`, because the port calls it `Type` and because the
current dictionary already uses that key.

| Field | Type | Constraint |
|---|---|---|
| `type` | `str` | The method name, lowercase. One of the ten values `--types` accepts, and ten values carry eleven methods. |
| `fingerprint` | `str` | The fingerprint string. Never empty. |
| `raw` | `str \| None` | The raw form, when the method defines one. |
| `raw_original_order` | `str \| None` | The original-order raw form, when the method defines one. |
| `src_ip` | `str` | Source address. Empty when the packet carries no address. |
| `src_port` | `int` | Source port. Zero when the packet carries no port. |
| `dst_ip` | `str` | Destination address. |
| `dst_port` | `int` | Destination port. |
| `timestamp` | `datetime \| None` | The packet timestamp. The port carries this field and this project does not. |

### Connection state

Each stateful fingerprinter holds one state table. The key is the connection. The
value holds the data the method needs across packets.

| Method | What the value holds | Lifecycle |
|---|---|---|
| JA4 | QUIC CRYPTO fragments, keyed by connection identifier. | Created on the first fragment. Evicted when the handshake message is complete, on eviction by age, or on eviction by count. |
| JA4H | Reassembled TCP segments. | Created on the first segment. Evicted when the request is complete, on eviction by age, or on eviction by count. |
| JA4X | Reassembled TCP segments. | Same as JA4H. |
| JA4L | Handshake timestamps and observed TTL values. | Created on the first packet. Evicted when the handshake is complete, on eviction by age, or on eviction by count. |
| JA4SSH | Packet lengths, bare-ACK counts, and banner strings. | Created on the first SSH packet. Counters reset at each window. Evicted on eviction by age or by count. |

### Vector

One entry in the conformance suite.

| Field | Type | Constraint |
|---|---|---|
| capture file | path | A file under `tests/foxio_vectors/`. |
| expected file | path | The matching `.json` file FoxIO publishes. |
| upstream commit | string | The FoxIO commit the snapshot came from. |

## Cross-cutting concerns

**Thread safety.** Every fingerprinter and the processor lock their state table by
default. A caller who shards traffic across processes, and needs the fastest path,
constructs the processor with `thread_safe=False`. Feature set
`concurrency-safety` defines the contract and the tests that prove it.

**Bounded memory.** Every state table has a maximum entry count and a maximum age.
The library never holds a reference to a packet object after
`process_packet` returns. Feature set `concurrency-safety` defines the limits.

**The boundary of the state rule.** A state table survives across packets. A structure
that one packet or one request builds and releases is not a state table, and it holds
neither bound. The cookie list of one HTTP request is such a structure, and #175 records
the decision. The boundary removes no bound from a state table. The six unbounded state
tables that #179 records keep their bound.

**Error handling.** A fingerprinter that cannot parse a packet returns nothing. It
does not raise. The processor logs the failure at `DEBUG` and continues with the
next fingerprinter. A caller error, such as a bad argument, raises `ValueError`.
The library defines no exception type of its own before version 1.0.0.

**Validation.** The library treats every packet as hostile input. No parser trusts
a length field it reads from the packet. Feature set `correctness-audit` defines
the malformed-input suite.

**Logging.** Every module uses `logging.getLogger(__name__)`. The library adds no
handler and sets no level. The command-line program configures logging.

**Security posture.** The library performs no network request unless the caller
opts in. Feature set `db-enrichment` makes the `ja4db.com` lookup opt-in, because a
fingerprint sent to a third party discloses traffic the operator observed. The library
also reads no key material, because a program that reads a key log file reads the plain
text of the traffic the operator captured. That capability is larger than fingerprint
production, and this project does not take it. The `Non-goals` section states what the
limitation removes.

**Performance.** The project measures packets per second on a fixed capture and
records the number in the pull request. Version 1.0.0 sets no target. A change that
halves throughput needs a stated reason.

## Environments & config

The library reads no environment variable. The command-line program and the
continuous-integration workflow read the following.

| Name | Where it is read | What it does |
|---|---|---|
| `JA4PLUS_DB_LOOKUP` | Command-line program | When set to `1`, the program allows the remote lookup. The `--lookup-remote` flag does the same. |
| `GITHUB_TOKEN` | Workflows | Provided by GitHub Actions. The documentation workflow uses it to publish to GitHub Pages. |

No secret is stored in the repository. PyPI publishing uses trusted publishing, so
no PyPI token exists.

Seed data: the conformance suite needs the FoxIO vectors. Epic 0 commits them under
`tests/foxio_vectors/`, with a `NOTICE` file that records the upstream commit, the
source URL, and the FoxIO License 1.1 attribution.

Verified against: https://docs.pypi.org/trusted-publishers/ (retrieved 2026-08-06).

## Testing strategy

| Layer | What it covers | Command |
|---|---|---|
| Unit | One function or one class, with synthetic packets. | `pytest tests/ -m "not spec_validation"` |
| Conformance | Every FoxIO vector against the FoxIO expected output. | `pytest tests/ -m spec_validation` |
| Malformed input | Truncated, corrupt and adversarial packets against every parser. | `pytest tests/fuzz/` |
| Concurrency | Many threads against one processor, checked for state corruption. | `pytest tests/test_thread_safety.py` |
| Documentation | Every code sample in the README and in `docs/`. | `pytest tests/test_documentation_samples.py` |
| Packaging | The built wheel installs into a clean environment and runs. | `pytest tests/test_packaging.py` |

A change is done when every layer above passes, `ruff check` reports nothing,
`mypy --strict ja4plus/` reports nothing, and line coverage is 90 percent or
higher.

## Epics

### Epic 0: Foundation

Goal: make the gates exist before any behaviour changes, so every later epic has
something that can fail.

Covers `features/00-foundation.md`. Depends on nothing.

Exit criteria: the `dev` branch exists. Continuous integration runs `ruff`, `mypy`,
the unit suite and the conformance suite on every pull request. The FoxIO vectors
are committed. Coverage is measured and reported. The two failing tests in
`tests/test_ja4db.py` pass.

### Epic 1: Spec conformance

Goal: every method matches the FoxIO reference on every vector.

Covers `features/01-spec-conformance.md`. Depends on Epic 0.

Exit criteria: all 37 vectors pass. The JA4SSH window is 200 packets. `JA4_o` is
exposed. The JA4L propagation factor follows the FoxIO table. The six surface-area
tests in `tests/test_parity.py` are replaced by the vector suite.

### Epic 2: Correctness audit

Goal: remove the defects that produce a wrong fingerprint, or a crash, on input the
vectors do not cover.

Covers `features/02-correctness-audit.md`. Depends on Epic 1, because a conformance
gate must exist before a parser changes.

Exit criteria: the malformed-input suite passes. TCP sequence wraparound is
handled. JA4L emits one client fingerprint per connection. JA4H handles a repeated
cookie name.

### Epic 3: Concurrency and resource safety

Goal: state the concurrency contract, prove it, and bound every state table.

Covers `features/03-concurrency-safety.md`. Depends on Epic 2.

Exit criteria: the thread-safety suite passes. Every state table has a maximum
count and a maximum age. A one-hour packet stream holds flat memory.

### Epic 4: Typed public interface

Goal: give version 1.0.0 an interface that does not change again before 2.0.0.

Covers `features/04-typed-api.md`. Depends on Epic 3, because the constructor
signature carries the `thread_safe` argument.

Exit criteria: `mypy --strict ja4plus/` reports nothing. `py.typed` ships in the
wheel. `Processor.process_packet` returns typed results whose field names match the
port. The processor exposes the parse failures it collected.
`ja4plus/collector.py` is removed.

### Epic 5: Structured output

Goal: give the capture analyst an output format another tool can read.

Covers `features/05-structured-output.md`. Depends on Epic 4, because the output
schema is derived from the typed result.

Exit criteria: the command-line program writes JSON Lines, CSV and a table. The
schema is documented and versioned.

### Epic 6: Live capture

Goal: make a long-running monitor a supported mode rather than an example script.

Covers `features/06-live-capture.md`. Depends on Epic 3 and Epic 5.

Exit criteria: `ja4plus watch` reads an interface, evicts connections by age,
handles a termination signal, and reports its own statistics.

### Epic 7: Database enrichment

Goal: make fingerprint lookup useful offline and safe by default.

Covers `features/07-db-enrichment.md`. Depends on Epic 4.

Exit criteria: no network request happens unless the caller opts in. Bulk lookup
exists. The `db update` and `db info` subcommands exist and match the port. The
mapping file refresh is documented.

### Epic 8: Documentation

Goal: make every published statement about `ja4plus` true and testable.

Covers `features/08-documentation.md`. Depends on every epic above, because the
documentation describes the finished interface.

Exit criteria: the documentation site builds and publishes. Every code sample runs
in continuous integration. The README states the concurrency contract and the real
method coverage.

### Epic 9: Release

Goal: publish version 1.0.0.

Covers `features/09-release.md`. Depends on Epic 8 and on Epic 11. #406 blocks #20, so
version 1.0.0 does not publish before Epic 11 closes.

Exit criteria: version 1.0.0 is on PyPI. The wheel installs into a clean
environment. The GitHub release carries the changelog entry.

### Epic 11: Pre-release validation

Goal: produce the evidence that version 1.0.0 is fit to publish, before Epic 9
publishes it.

Covers `features/11-pre-release-validation.md`. Depends on Epic 8, because the gates and
the documentation it measures must exist first. **The file number equals the epic
number.** Epic 10 (#194) holds no feature document, so `features/10-*.md` does not exist.

Exit criteria: one case installs the built wheel into a clean environment and produces a
fingerprint. Another case does the same for the source distribution. The five gates run
on the granted Linux host, and #410 holds the verbatim output of each one. A sweep
reaches every module whole, and a worker settles every candidate of those modules. One
recorded measurement states the throughput of the processor for a named packet run on a
named host. The conformance suite reports 1532 passed, 143 skipped and 134 xfailed
against 134 keys of `tests/foxio_deviations.json`.

**This epic publishes nothing, it sets no throughput target, and it moves no
fingerprint.**

## Milestones

| Milestone | Epics | What "shippable" means here |
|---|---|---|
| M1 — Gates | 0 | The repository can prove a regression. Nothing user-facing changed. |
| M2 — Correct | 1, 2 | Every fingerprint matches the reference. This alone justifies a 0.7.0 release if version 1.0.0 slips. |
| M3 — Safe | 3 | A monitor can run for a day without memory growth. |
| M4 — Stable | 4, 5, 6, 7 | The interface is typed and will not change again before 2.0.0. |
| M5 — Released | 8, 9 | Version 1.0.0 is on PyPI with published documentation. |

## Assumptions

1. The user accepts a break in the `Processor.process_packet` return type at
   version 1.0.0. A major version is the correct place for it.
2. FoxIO License 1.1 permits redistribution of the vectors with attribution. The
   project already declares `LicenseRef-FoxIO-1.1` in `pyproject.toml`.
3. The FoxIO expected-output files under `python/test/testdata/` are the reference
   the project measures against. The matching files under `wireshark/test/testdata/`
   are not, because `tls12.pcap.json` there is an empty list.
4. `Development Status` moves to `5 - Production/Stable` at version 1.0.0.
5. The default state-table limits are 10000 entries and 300 seconds. Epic 3 tunes
   them against a measurement.
6. The remote lookup becomes opt-in. This changes behaviour for a caller who relies
   on it today, which a major version permits.
7. The port stays at its 2026-05-09 state while this work proceeds. Another session
   works on the port. This spec reads the port and never writes to it.
8. The port adopts the same 200-packet JA4SSH window. Until it does, the two
   implementations disagree on JA4SSH. This spec follows FoxIO, per parity rule 1.

## Risks & open questions

| Item | Why it matters | Blocks approval |
|---|---|---|
| Eleven of the twelve FoxIO methods are specified only in an image, not in text. `technical_details/JA4.md` is the one complete text specification. `technical_details/JA4H.md` is 278 bytes and states one rule, the header count, so JA4H is an image method too. Three methods hold no image at all: JA4LS, JA4TS and JA4TScan. Where a reader reads the image and finds it ambiguous, the project treats the expected-output files as the authority and records the reading in `docs/implementation_notes.md`. An image nobody read does not reach that fallback. `docs/specs/foxio/README.md` holds the inventory and the transcription procedure, and Epic 10 writes the transcriptions. | An ambiguous spec produces a defensible but non-conforming implementation. | No |
| Not every vector exercises a method `ja4plus` implements. The suite must skip a method a vector does not cover, and must never pass by skipping everything. | A silent skip looks identical to a pass. | No |
| The 90 percent coverage target may not be reachable on the `scapy` interface layer without brittle mocks. | An unreachable gate gets lowered under pressure, which defeats it. | No |
| Fixing the JA4SSH window changes the output for every existing user of that method. | It is a breaking behaviour change, correct but disruptive. Version 1.0.0 is the right place, and the changelog must state it plainly. | No |
| The README claims ten methods. FoxIO documents twelve, and counts JA4LS and JA4TScan separately. | A false coverage claim is the kind of documentation error the user asked to fix. | No |
| Fixing the JA4SSH window here, before the port fixes it, breaks parity on that one method until the port catches up. | A user who runs both gets two answers for JA4SSH during that period. | No |

Two items blocked approval. Both are now decided.

- **JA4TScan stays out of scope.** It sends crafted packets and this project reads
  traffic. Epic 8 corrects the coverage claim in the README instead. Round 3 recorded
  this as a permanent non-goal on 2026-08-06. The user restated it on 2026-08-08 as a
  decline that is reversible, and `Non-goals` holds the later form.
- **`dev` becomes the default branch on GitHub.** A pull request then opens against
  `dev` with no manual step. `master` stays the release branch and is protected.
  Epic 0 makes both changes.

## The search for a JA4TS reference value

**A conformance audit of 2026-08-10 read JA4TS as the one method that reached no
FoxIO-produced expected value.** #515 searched every FoxIO source for one. This section
records what each source returned, so a later reader repeats no search this one made.

Every read below ran against a checkout of `FoxIO-LLC/ja4` at the pinned commit
`27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, on 2026-08-10.

| Source | What the search returned |
|---|---|
| `python/test/testdata/` | No `JA4T` key and no `JA4TS` key, in any of the 37 files. |
| `rust/ja4/src/snapshots/` | No `ja4ts` field, in any of the 38 files. 64 `ja4t` fields. |
| `wireshark/test/testdata/` | **58 `ja4.ja4ts` values in 24 of the 37 files. Adopted.** |
| `zeek/tests/Traces/` | **10 `ja4ts` values in three `conn.log` baselines. Nine adopted.** |
| `technical_details/` | `JA4T.png` states the schema and it publishes no value. |
| The deleted `technical_details/JA4T.md` | Prose and example values, which R12 and R13 of `docs/specs/foxio/JA4T.md` transcribe. It is not a produced value. |
| `FoxIO-LLC/ja4tscan` `README.md` | Eight JA4TScan values against named operating systems. No JA4TS value. |
| `FoxIO-LLC/ja4-nginx-module` | An empty `JA4TS` field in each of its four golden files. FoxIO states the implementation is not correct. |

Reproduce the two adopted counts with two commands, from the root of such a checkout.
**Warning: a line count of the key counts the key and not the value**, because the
dissector writes an array and one array holds several values.

```bash
python -c "import json,pathlib;print(sum(len(r['_source']['layers']['ja4.ja4ts']) for p in pathlib.Path('wireshark/test/testdata').glob('*.json') for r in json.loads(p.read_text()) if 'ja4.ja4ts' in r['_source']['layers']))"
python -c "import pathlib;print(sum(1 for p in pathlib.Path('zeek/tests/Traces').glob('*/conn.log') for line in p.read_text().splitlines() if not line.startswith('#') and line and line.split(chr(9))[-1] not in ('','(empty)','-')))"
```

The first prints `58` and the second prints `10`.

**The Wireshark row corrects a wrong measurement this project recorded.**
`docs/specs/foxio/JA4T.md` read `No ja4t value and no ja4ts value` for that directory. The
earlier search read the key `ja4t`, and the dissector writes the key `ja4.ja4t`. The
correction is the reason this issue closes with a vector rather than with a finding.

**JA4TS now reaches two FoxIO implementations, and 61 of its 68 reference values compare
as a pass.** 52 of the 58 Wireshark values match, 9 of the 10 Zeek values match, 6 reach
`tests/foxio_deviations.json` under #246, and 1 falls under the `DLT_NULL` bar.
`docs/specs/foxio/JA4T.md` and `docs/specs/foxio/zeek.md` hold each reading.

## Changelog

| Round | Date | What changed |
|---|---|---|
| 1 | 2026-08-06 | First draft, written from the Phase 1 interview and a read of the codebase at commit `5ab0252`. |
| 2 | 2026-08-06 | Added the parity section and the divergence register after the user named the Go port. Corrected the register: `db update`, `db info`, and the three output formats already exist in `ja4plus/cli.py`. |
| 3 | 2026-08-06 | Approved. JA4TScan confirmed out of scope. `dev` confirmed as the GitHub default branch. |
| 4 | 2026-08-06 | Epic 0 built. #27 was moved into the Epic 0 batch, and stays a child of Epic 1, because #23 cannot gate the conformance suite against a harness that compares one expected value against every fingerprint in a capture. |
| 5 | 2026-08-06 | The `xfail` decision on #23 was revised from five hand-written markers to a deviation register keyed by case, applied as `xfail(strict=True)`, after the measured baseline came to 263 known deviations rather than 5. |
| 6 | 2026-08-06 | The conformance baseline was measured for the first time. It is recorded on #12. The figure of 69 of 74 vectors passing is withdrawn: it came from a harness that matched a value against every fingerprint in a capture and skipped four whole methods. |
| 7 | 2026-08-06 | #78 opened. JA4X fails 35 cases and no issue owned the method. |
| 8 | 2026-08-06 | #80 opened. The conformance harness never runs the JA4L fingerprinter, so 162 of the 263 registered deviations state a harness defect, not non-conformance. JA4L conformance is unmeasured until #80 lands. |
| 9 | 2026-08-06 | Epic 1 batch 1 shipped: #29 `JA4_o`, #80 the JA4L harness, #28 the JA4SSH window. Two are breaking behaviour changes. #80 landed and JA4L conformance is measured for the first time. The harness runs the JA4L fingerprinter, reads the JA4L connection key form, and strips the method prefix from the produced value. The register holds 252 entries, and it held 263. JA4L passes 16 of its 167 cases. #88 opened to own the 114 value cases, and #34 widened to own the 37 occurrence-key cases on both sides of the connection. #89 opened, because the vectors contradict the JA4L requirement in `features/02-correctness-audit.md`. |
| 10 | 2026-08-06 | #88, #92 and #78 opened for the conformance gaps the honest harness exposed. #34 widened to both directions of the connection and its acceptance criterion withdrawn; #89 carries that correction into the feature file. |
| 11 | 2026-08-06 | `JA4S_o` is published with no FoxIO material to validate it. Recorded for the ambiguity register #31 builds. |
| 12 | 2026-08-07 | Epic 1 batch 1 merged into `dev` as `e911d99`, after #94. The conformance suite gave different results on Linux and on macOS: a capture whose link type is `DLT_NULL` starts each frame with the capturing host's address family value, and scapy binds only the reading host's value. `ja4plus` binds 24, 28 and 30 to the IPv6 layer. The suite now reports the same counts on both hosts, and the register count of 240 is host-independent. The rounds below held two entries numbered 9, and #258 folded them into round 9. |
| 13 | 2026-08-07 | #78 landed and JA4X passes 26 of its 35 failing cases. Two defects caused them. The scan read only the first handshake message of a TLS record. The processed certificate table named the certificate alone, so it dropped the value of every stream after the first that carried the same chain. The JA4X register entries fall from 35 to 9, and the register holds 214 entries where it held 240. Seven of the nine state that the reference decrypts the handshake with the secrets the capture carries. Two state that the reference reads no TLS on the proxy port. `socks4-https.pcap/JA4X` is one of the two, and it is a new entry: the scan now reads three certificates that the reference never sees. The project withdraws the claim that JA4X "depends on full TLS dissection which is not always available from pcapng captures". `b9a19a3` removed that claim with `KNOWN_DEVIATIONS`, and the measurement now names a cause for each case. |

| 14 | 2026-08-07 | Goal 1 gains an exception. The FoxIO reference holds three proven defects, and the user decided `ja4plus` reproduces none of them. #96: `dict(ja4sh_stats)` shares one payload list across every window on every connection, so the reference mode field reads the whole capture. #97: a bare ACK after a window boundary writes an occurrence key from a window of zero SSH packets. #105: `finalize_ja4ssh` guards with `if stream:`, and the index 0 is false, so no trailing window reaches the connection at index 0. Each is proven by a measurement of the reference at the pinned commit, not by a reading of its source. `.claude/rules/conformance.md` now states when this project declines a reference defect, and it names the two shapes: a fingerprint that depends on the capture rather than the connection, and a fingerprint that describes no traffic. A defect outside those two shapes stays a question for the user. |
| 15 | 2026-08-07 | #92 landed. `tshark` settled the condition FoxIO applies to the window a connection holds open at close, and the answer was #105. `ssh-r.pcap` now produces the reference occurrence keys on all three streams, and `ssh-scp-1050.pcap` matches with no register entry. Eleven JA4SSH entries remain: #96 owns 4, #97 owns 3, #105 owns 3, #98 owns 1. #105 costs three cases that matched before. `gre-sample.pcap`, `sshv1.pcap` and `v6.pcap` now hold a trailing JA4SSH fingerprint the reference output does not hold, and it is the first divergence where this project emits more than the reference. #98 opened: `ja4plus` counts a TCP segment where the reference counts an SSH message, so the two disagree by one packet for each SSH message that spans two segments. |
| 16 | 2026-08-07 | #88 landed and the JA4L values conform. FoxIO reports one-way latency, so it halves every measured round-trip time, and `JA4L-C` measures to the client's first data packet after the SYN-ACK. The comment at `ja4l.py:218` stated the opposite and is deleted. The register falls from 213 entries to 74, and JA4L conformance failures fall from 151 to 12. #34 shrank as a side effect, from 37 occurrence-key entries to 6, and it is re-scoped before it is scheduled. #101 opened: `gre-erspan-vxlan.pcap` mirrors both directions of the inner connection from one outer address pair, so the SYN and the SYN-ACK reach two different connection keys. #102 opened: `ja4plus` reads the first server Initial packet of a QUIC connection, and the reference reads the Initial packet that completes the ServerHello. |
| 17 | 2026-08-07 | Epic 1 batch 2 ships #78, #92 and #88. #30 was a member and never started; it moves to batch 3, because no fingerprint value reads `calculate_distance` and holding the batch for it delays a register that falls from 240 entries to 74. |
| 18 | 2026-08-07 | #98 landed and JA4SSH counts an SSH message, not a TCP segment. `tshark` proved the boundary: `frame.protocols` of `ssh-r.pcap` frame 399 is `eth:ethertype:ip:tcp`, and frame 400 carries `eth:ethertype:ip:tcp:ssh` with `tcp.segment` `399,400`. `ja4plus/utils/ssh_utils.py` gains `SSHMessageTracker`, which follows the four-byte length field of each plaintext message and turns opaque after `SSH_MSG_NEWKEYS`. Two vectors hold such a message: `ssh-r.pcap` stream 1 holds one, and `ssh-r.pcap` stream 2 holds one. All five windows of stream 2 now hold the reference packet counts, and the register falls from 74 entries to 73. The remaining JA4SSH entries are #96 with 4, #97 with 3 and #105 with 3. No vector that conformed before changed: `ssh.pcapng`, `ssh-scp-1050.pcap`, `ssh2.pcapng` and `ssh-r.pcap` stream 0 hold their values. |
| 19 | 2026-08-07 | #89 landed and the JA4L measurement points move to `features/01-spec-conformance.md`. `FR-correctness-audit-5` claimed that JA4L emits the client fingerprint from the ACK that completes the handshake. Stream 0 of `browsers-x509.pcapng` contradicts it. The SYN-ACK is at `+0.003815s`, the bare ACK at `+0.003927s`, and the Client Hello at `+0.004371s`. The reference `JA4L-C` is `278_128`, and `(4371 - 3815) / 2 = 278`. The project withdraws `FR-correctness-audit-5` and keeps its number, because #34, #35, #36 and #37 quote the numbers below it. `FR-spec-conformance-17` to `FR-spec-conformance-21` replace it, and #88 built them. `FR-spec-conformance-18` to `FR-spec-conformance-21` describe the TCP form of JA4L, because the QUIC form reads no SYN and no relative sequence number. Row 5 of the audit register drops the measurement point and keeps the multiplicity defect. #34 now owns multiplicity alone, and its acceptance criterion that quoted the old requirement is withdrawn. #34 also shrank from 37 register entries to 6 after #88. |

| 20 | 2026-08-07 | Epic 1 batch 3 ships #30, #31, #98 and #89. The register falls from 74 entries to 73. #30 reads the FoxIO hop-count propagation factor and moved no conformance value, which confirms that no fingerprint reads `calculate_distance`. #31 records a reading for every image-published method and replaces the six parity tests that asserted a name exists; each replacement was run against a mutated expected-output file and failed, which proves it compares a value. #98 counts an SSH message rather than a TCP segment, and every vector that conformed before is byte-identical after. Three issues opened from work this batch exposed. #108: the JA4S `raw` value sorts the extensions and the FoxIO `JA4S_r` holds them in wire order; the fingerprint itself conforms. #109: `tests/test_ja4d_foxio.py` and `tests/test_ja4d6_foxio.py` name paths this repository does not hold, so both skip on every run and nothing tests JA4D or JA4D6. #113: `SSHMessageTracker` reads segments in capture order, so a retransmitted segment desyncs the message boundary on live traffic. |

| 21 | 2026-08-07 | #102 landed and the JA4L QUIC server point reads the Initial packet that completes the ServerHello. The cause was one line of `decrypt_initial_payload`. It read the ciphertext to the end of the datagram. The AEAD tag of an Initial packet covers only the bytes the Length field names, so every server Initial packet of the vector set failed the tag. The fingerprinter then fell back to the first one. `_initial_packet_end` now bounds the ciphertext. Both server Initial packets of `chrome-cloudflare-quic-with-secrets.pcapng` stream 50280 decrypt. The packet at `+18569 us` holds an ACK frame, and the packet at `+21981 us` holds the whole 90-byte ServerHello. The register falls from 73 entries to 71. Neither vector splits a ServerHello across two Initial packets. The accumulation across packets therefore rests on RFC 9001 and on synthetic packets, not on a vector. No JA4S value changed, because `ja4s.py` reads a server Initial packet as a client Initial packet. It records the empty connection ID of that packet, and it never reaches the server path. That defect is #118. The self-review found a second one. A CRYPTO frame offset is a 62-bit number, and `reassemble_crypto_fragments` allocates a buffer that reaches the highest offset. `collect_crypto_fragments` now stops every buffer at 16384 bytes, on the JA4L path and on the JA4S path. The JA4 client path holds the same defect and is #122. |

| 22 | 2026-08-07 | Epic 1 batch 4 ships #109, #101, #102 and #108. The register falls from 73 entries to 67, and no entry that remains is an Epic 1 conformance defect: 48 belong to the correctness audit on #13, #35 and #34, 9 state a genuine limitation of a capture on #78, and 10 record a declined FoxIO defect on #96, #97 and #105. #109 found that FoxIO does publish JA4D and JA4D6 reference values, in `wireshark/test/testdata/`, where the Python implementation emits neither method; ten values now run where zero ran, and `.claude/rules/external-apis.md` records the exception to the Wireshark precedence rule plus the fact that nothing corroborates the six JA4D6 values. #101 groups a mirrored capture by its inner connection and reports the outer one. #102 found one line: `decrypt_initial_payload` read the ciphertext to the end of the UDP datagram, and the AEAD tag of a QUIC Initial packet covers only the bytes the Length field names, so every server Initial packet failed the tag. #108 makes the JA4S `raw` key hold the FoxIO wire order, and all 84 `JA4S_r` values now match where 49 did. #121 opened: `tests/conformance_index.py:29` drops every raw key before the comparison, so no raw form of any method has ever been compared against the reference. #115, #118 and #119 opened. |

| 23 | 2026-08-07 | #118 landed and JA4S tells a QUIC server Initial packet from a client one. `_is_quic_client_initial` tested the long-header packet type alone, and a server Initial packet carries the same type. The fingerprinter stored the connection ID of the server packet under the reverse connection key and returned, so it never read the ServerHello. That connection ID was empty, because a server Initial packet names the client with the connection ID the client chose as its source. `ja4s.py` now reads the port that names the server, as `ja4l.py` does, and a server Initial packet replaces no stored value. `chrome-cloudflare-quic-with-secrets.pcapng` stream 50280 produces `q130200_1301_234ea6891581`, which holds the cipher and the extension hash of the `t130200_1301_234ea6891581` value the reference holds for the TCP stream of the same capture. No JA4S value on any other stream changed, and no vector reports a missing occurrence key. The reference reads no QUIC handshake in the committed vectors, so it holds no JA4S value for the nine QUIC streams of `chrome-cloudflare-quic-with-secrets.pcapng`, `ssh2.pcapng` and `tls3.pcapng`. It holds no JA4 value for the same nine streams, and #13 already owned that divergence for JA4. Three register entries record the JA4S form of it, and the register rises from 67 entries to 70. #130 opened: `ja4s.py` reads one server Initial packet at a time, so a ServerHello that spans two Initial packets produces no JA4S value. |
| 24 | 2026-08-07 | #121 landed and the conformance suite compares a raw form for the first time. `tests/conformance_index.py` dropped every key that ends with `_r`, `_ro`, `_o` or `_raw`, so 653 reference values reached no test. The register rises from 67 entries to 220, measured against `epic/12-spec-conformance` at `03c7c02`, and no hashed-form case changed state. `JA4S_r` matches all 84 values, which confirms the #108 measurement. `JA4_r` and `JA4_ro` each match 149 of 160, and every failure sits on a stream whose `JA4` value fails too, so #13 owns all 22. `JA4_o` matches 145 of 160. `JA4H_ro` matches 0 of 89, because `ja4plus` computes no JA4H raw form. Two issues opened. #131 owns the 89 `JA4H_ro` values and the 11 occurrence-key cases with them. #132 owns four `JA4_o` values whose client hello carries SNI as its only extension: the reference gives `000000000000` as the extension hash, and `ja4plus` hashes the original-order extension list, which is the rule that matches the other 156 values. `tests/generate_foxio_baseline.py` now keeps the entry the committed register holds for a key that still fails, because a cause read from a failure message states the symptom and #78, #96, #97 and #105 hold mechanisms a person wrote. |

| 25 | 2026-08-07 | Epic 1 batch 5 ships #119, #118, #115 and #121. **The register rises from 67 entries to 226, and the rise is the point.** 153 entries record a raw form that no test had ever compared, and 3 record the JA4S occurrence keys that #118 exposed. #115 removed the last skip from the unit suite: three tests named a fixture path this repository does not hold, one now compares a committed FoxIO Rust snapshot and passes, and two carry a strict `xfail` naming #127 and #129. #121 found that `tests/generate_foxio_baseline.py` overwrote every cause on each run, so running it would have destroyed 19 hand-written causes, including the ten that record the declined FoxIO defects. Four issues opened from the measurement: #131, `ja4plus` computes no JA4H raw form, so all 89 `JA4H_ro` values fail; #128, 18 register entries state a cause the evidence does not support; #132, four `JA4_o` values whose client hello carries SNI as its only extension; #127, the FoxIO prose and two FoxIO implementations disagree on the JA4 ALPN value for a non-ASCII first byte. #13 grew from 21 entries to 73 and now owns one question: does `ja4plus` emit a fingerprint on a QUIC stream the reference ignores, or does the reference miss it. |

| 26 | 2026-08-07 | #129 settled decryption. `ja4plus` reads no key material, it reads no Decryption Secrets Block, it decrypts no TLS record, and it decrypts no QUIC 1-RTT packet. The reason is a security boundary: a program that reads a key log file reads the plain text of the traffic the operator captured, and that capability is larger than fingerprint production. The decision is reversible. `Non-goals` states the limitation and `Cross-cutting concerns` states the reason. 17 JA4H register entries move from #35 to #129, and they hold the cause the JA4X entries of the same two captures already state. The 17 are `http2-with-cookies.pcapng/JA4H`, the 15 value cases of `http2-with-cookies.pcapng` stream 58847, and `chrome-cloudflare-quic-with-secrets.pcapng/0:57098/JA4H.1`. `http1-with-cookies.pcapng` carries no secrets block and holds two cleartext HTTP messages, so #35 keeps it. No fingerprinter changed and no vector changed state. |
| 27 | 2026-08-07 | #127 landed and the JA4 ALPN value follows the FoxIO implementations, not the FoxIO prose. The prose of `technical_details/JA4.md` states that a first ALPN value whose first byte or last byte is not an ASCII alphanumeric prints the first and the last character of the hex form. The FoxIO Python implementation writes `99` instead, and the FoxIO Rust snapshot holds the same `99`. `tests/foxio_vectors/tls-non-ascii-alpn.pcapng` measures the difference: its first ALPN value is the two bytes `0xba 0xad`, the prose gives `t13d1516bd_8daaf6152771_e5627efa2ab1`, and both FoxIO implementations give `t13d151699_8daaf6152771_e5627efa2ab1`. Only the two ALPN characters differ. The user decided on 2026-08-07 that this project follows the vector, because `CLAUDE.md` rule 1 states that a fingerprint changes only when a FoxIO vector requires it, and because a fingerprint exists so that one tool output can be compared against another tool output. `compute_alpn_value` now returns `99` whenever the first byte or the last byte of the first ALPN value falls outside `0x30-0x39`, `0x41-0x5A` and `0x61-0x7A`. `ja4s.py` reads the same function, so JA4 and JA4S carry one rule. #127 settled the value alone, and #141 owns the condition, because `python/ja4.py`, `rust/ja4/src/tls.rs`, the FoxIO prose and this project apply four different conditions that every vector fires. Four register entries go away, `JA4.1`, `JA4_o.1`, `JA4_r.1` and `JA4_ro.1` on `tls-non-ascii-alpn.pcapng/0:50112`. Measured alone the register falls from 226 entries to 222. Measured on the integration branch, after #129 and #131 landed, it falls from 147 entries to 143. No vector that passed before fails now: the conformance suite reports 1205 passed where it reported 1201. The Go port at `Crank-Git/ja4plus-go` reads the same specification and must adopt `99`. |
| 28 | 2026-08-07 | #113 landed and JA4SSH reads the payload of an SSH direction in sequence order. `SSHMessageTracker` read the segments in capture order and held no sequence number, so a retransmitted segment reached it twice and an out-of-order segment reached it early. Either one moved the tracker off the message boundary for the rest of the direction. The measurement on a synthetic connection: a clean direction gives the packet lengths `[21, 48, 36, 36, 36]`, one retransmission gives `[21, 1448, 48, 36, 36, 36]`, and one reorder gives `[21, 48, 1448, 36, 36, 36]`. `SSHMessageTracker.add_segment` now takes the TCP sequence number, drops a segment the direction already sent, trims a segment that repeats part of the stream, and holds a segment that arrives before its predecessor. The held segments stop at 32 segments or 65536 bytes, and a gap that never fills turns the tracker opaque. The self-review found a second defect in the same code. A sequence number wraps at 2**32, and a plain comparison reads a wrapped number as a number far behind the base. A retransmission that precedes the wrap point would then be held until the buffer reached its bound, and an out-of-order segment that follows the wrap point would be dropped. `_offset` now measures the distance in the shorter direction around the sequence space. Both cases are measured against the plain comparison and both fail it. `ja4plus/utils/tcp_stream.py` compares raw sequence numbers and holds the same defect; JA4H and JA4X read it. `ja4ssh.py` calls `add_segment` and extends the window with the lengths it returns, because the counted segment is the one that completes the message in sequence order, not the one that arrives last. `tests/build_ssh_retransmission.py` builds a capture that holds one retransmitted SSH segment; `tshark -Y "ssh"` labels five client frames and four server frames, and it labels the retransmitted frame 7 `tcp`. The capture produces `c36s36_c5s4_c1s0`. This is a resource bound and not a conformance defect: no FoxIO vector holds a retransmitted or out-of-order SSH segment. The register holds 143 entries before and after, and the conformance suite reports 1284 passed, 141 skipped and 143 xfailed on both. `tests/test_ja4ssh.py` and `tests/test_ja4ssh_message_count.py` gain sequence numbers, because a synthetic segment that repeats sequence number 0 models traffic no direction sends. |
| 29 | 2026-08-07 | #130 landed and JA4S reads a ServerHello that two QUIC Initial packets split. `ja4s.py` read one Initial packet at a time, so neither packet held the whole message, and the connection carried no JA4S value. The fingerprinter now collects the CRYPTO fragments of the server Initial packets of one connection, and it reads the message when `server_hello_is_complete` reports a whole one. The fragment table holds `MAX_QUIC_FRAGMENT_CONNECTIONS` connections for `MAX_QUIC_FRAGMENT_AGE_SECONDS`, which are the two limits `ja4.py` already names, and `cleanup_connection` drops the entry of the connection it names. No vector splits a ServerHello across two Initial packets, so `tests/test_ja4s_quic_split_server_hello.py` carries its own capture. The conformance suite still reports 103 xfailed, and no fingerprint value moved. |
| 30 | 2026-08-07 | #138 landed and the FoxIO Rust snapshot settles the streams the FoxIO Python file omits. The FoxIO Python implementation reads no QUIC handshake, and it reads no TLS on a port it does not know. The FoxIO Rust implementation reads both. Where the two disagree on whether a stream carries a value, the Rust snapshot decides. A value that one implementation holds and another omits is a gap in the implementation that omits it. No fingerprinter changed. `CLAUDE.md` rule 1 holds, because `ja4plus` is right on every stream #138 names. The FoxIO Python file omits 85 streams that the Rust snapshot holds. `ja4plus` produces the exact Rust value on 82 of the 85. It contradicts the Rust snapshot on 0. The user decided the `socks4-https.pcap/JA4X` case on 2026-08-07, and the three values stay. `ja4plus` produces three JA4X values on the SOCKS4 tunnel on port 9901, and no FoxIO implementation holds one. That is unanimity, not a reference split, so the rule above does not reach it. `ja4plus` reads the record layer without regard to the tunnel protocol that carries it. The same behaviour produces the `https-connect.pcap` values that the FoxIO Rust snapshot and the Wireshark dissector both hold. A gate on the record-layer scan would risk the case `ja4plus` provably wins. The decision is reversible, and the `Divergence register` states it. The register ownership figures were stale in two places. The #138 body said 21 entries and the session-4 status digest said 34. The measured count of entries that name the epic #13 is 0. Commit `31b8539` from #128 had already repointed them. 34 entries name #138, and all 34 are now `decided`. The register holds 103 keys before and after, and the key set is unchanged. #138 also found the Rust snapshot rule under-scoped, and it corrects `.claude/rules/external-apis.md`. The rule read at file granularity, so the snapshot decided only where the FoxIO Python file holds an empty array. The unit is the stream. `python/test/testdata/tls3.pcapng.json` is not empty, yet it omits 6 of its 13 streams, and the Rust snapshot holds all 13. The old form covered one capture of the eight. #151 opened from the measurement, and it names three streams where `ja4plus` produces nothing and the FoxIO Rust implementation holds a JA4S value. That is the opposite reading from #138. |
| 31 | 2026-08-07 | #132 landed and the `JA4_o` extension hash reads the sorted extension list for the zero sentinel. FoxIO tests one string, and it sets both extension hashes from that one test. A client hello whose only extensions are SNI and ALPN therefore gives `JA4_o` the zero sentinel. `JA4_ro` still shows the extension in wire order. `technical_details/JA4.md` states the rule and names one field, `JA4_c`, and it describes `JA4_o` by example alone. A probe on `python/ja4.py` at the pinned commit reads `sorted_extensions=''`, `original_extensions='0000'` and `sha_encode(original_extensions)=9af15b336e6a`, which is the value `ja4plus` emitted before this change. The reading is a published rule, not a defect, so `.claude/rules/conformance.md` does not apply and no entry joins the declined set. `JA4_o` now matches all 160 reference values, and the count was 156. The four register entries disappear, so the register falls from 103 keys to 99 and the conformance suite reports 99 xfailed. `docs/implementation_notes.md` holds the reading. |
| 32 | 2026-08-07 | #151 landed and a hello is bounded on its own length field, not on the length of the record that carries it. A TLS handshake record carries one or more handshake messages, so `record_length` bounds the group and not the hello. `parse_tls_handshake` rejected every hello whose enclosing record spans several TCP segments, and a TLS 1.2 server that coalesces the ServerHello, the Certificate and the ServerHelloDone into one record produces exactly that shape. The reader now takes the three-byte handshake length at `offset + 6`, and it stops the slice at the end of the hello. The old form passed `raw_data[offset:]`, so a reader could walk into the Certificate message that follows and report an extension the peer never sent. The three streams #151 names now produce the FoxIO Rust value: `ssh2.pcapng` ports 57374 and 57375, and `tls-handshake.pcapng` port 50167. One bound explains all three, so the fix settles every coalesced-record stream, and four more became readable. The FoxIO Rust snapshots for `browsers-x509.pcapng` and `latest.pcapng` were fetched at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, `ja4plus` reproduces each value exactly, and both snapshots join `tests/foxio_vectors/rust_expected/`. The register rises from 99 keys to 104, and the conformance suite reports 104 xfailed. Four of the five new entries are corroborated by the Rust snapshot. The fifth is `socks4-https.pcap/JA4S`, which no FoxIO implementation holds, and it follows the `socks4-https.pcap/JA4X` decision the user made on 2026-08-07: `ja4plus` reads the record layer without regard to the tunnel protocol that carries it. #151 also found a **third** gap in the FoxIO Python implementation, so `.claude/rules/external-apis.md` is corrected. The rule stated that two gaps produce every case. The implementation also reads no ServerHello whose handshake record spans several TCP segments. `tls-handshake.pcapng` stream 50167 is the proof: the port is 443, so neither known gap explains the omission, and the Rust snapshot holds a value. |
| 33 | 2026-08-07 | #141 landed and the JA4 ALPN condition passes a printable ASCII byte through. The FoxIO prose tests for an alphanumeric byte, and the measurement contradicts it. Both FoxIO implementations read a first ALPN value of `h` and a space as `h `, and not as `99`. The range stops at `0x7E`, because the two implementations agree only inside it. `tests/foxio_vectors/alpn-condition.pcap` holds the separating case, and it is the first capture in the set that reaches the branch with a value the three rules do not agree on. Before it, `tls-non-ascii-alpn.pcapng` held `0xba 0xad`, and all three rules fire on it, so the comparison was never made. The user settled the disputed region on 2026-08-07, and both readings hold the value `ja4plus` wrote before. A byte outside `0x20-0x7E` in a position other than the first keeps `99`, because the two implementations disagree with each other and `CLAUDE.md` rule 1 forbids a move that no vector requires. The FoxIO Rust results on a control byte are an artifact, because `rust/ja4/src/tls.rs` reads the tshark escape text and reads `h\x1f` as five characters. The FoxIO Python result puts `U+FFFD` into a fingerprint, which is a character no byte of the packet holds. A one-byte first ALPN value keeps `hh`. All three sources differ, and the FoxIO Python value holds one character, which cannot fill a two-character field. Both readings are reversible, and #162 records them in the `Divergence register` and adds the disputed inputs to the vector set. The register holds 104 keys before and after, and #141 added none. |
| 34 | 2026-08-07 | #161 landed and the baseline generator keeps the register it rewrites. `tests/generate_foxio_baseline.py` rebuilt every entry from `issue` and `cause`, so a run deleted the `decided` field of 49 entries and reordered the fields of every entry. `decided` records that a person settled a deviation, and the generator measures no such field. `tests/foxio_manifest.py` documents the generator as the way to add a vector, so the next person to add a vector deleted 49 decisions and saw no count change. The entry count held at 104 either way, so the invariant this project checks first did not reach the loss. #141 met the defect, noticed it, and restored the file with `git checkout`. The register run now merges. A key the register already holds keeps its stored entry, byte for byte. The generator writes a new entry only for a deviation the register does not hold, and a key that stops failing still leaves. A run that measures the deviations the register already holds now changes no byte of `tests/foxio_deviations.json`, and the project manager measured that: the file holds md5 `93c8cd5d6800c994c2b5421ac5aa2a91` before the run and after it. The tests fail against the generator as it stood, 6 of 22, so the comparison runs. No fingerprinter changed, and the register holds 104 keys before and after. |
| 35 | 2026-08-07 | #162 landed and the settled JA4 ALPN readings are a comparison the suite runs. #141 measured the disputed region and the user settled it on 2026-08-07, but the readings sat in two issue comments and in `docs/implementation_notes.md`. No capture held a disputed input, so the suite was green because the comparison was never made. This is the seventh instance of that shape in this project. `tests/build_alpn_condition_capture.py` now writes seven streams instead of two. Streams 2, 4, 5 and 6 carry `h\xab`, `h\x1f`, `h\x0a` and `h`, where `ja4plus` matches neither FoxIO implementation. Stream 3 carries `\xabh`, where FoxIO Python writes `99` and `ja4plus` writes `99`, so it conforms and it holds no register entry. The expected-output file holds the FoxIO Python characters #141 measured. The hash halves are invariant across the streams, because the seven client hellos carry the same ciphers and the same extensions. `test_every_stream_differs_from_the_others_in_the_alpn_characters_alone` proves that invariance. The register rises from 104 keys to 120, and the conformance suite reports 120 xfailed. The sixteen new entries are four methods on each of four streams. They all name #162 and they are all `decided`. Each cause states the measured output of both FoxIO implementations. The decided count rises from 49 to 65. A mutation proves the entries bite. Move the stream 6 expected value to `hh`, and four cases report `XPASS(strict)` and the suite fails. `Divergence register` gains two rows, one for the disputed-byte reading and one for the one-byte reading, and both state that the decision is reversible. `compute_alpn_value` is unchanged, and no fingerprint value moved. |
| 36 | 2026-08-07 | #32 landed and the TCP reassembler orders segments across the 32-bit sequence wrap. `get_stream` sorted raw sequence numbers, so a connection that crossed the boundary reassembled in the wrong order. The order now reads from the widest sequence step, which depends only on the sequence numbers. A first implementation compared each segment against a running earliest value, and that comparison is not transitive once segments span more than half the sequence space: three segments at `0x00000000`, `0x60000000` and `0xC0000000` gave three results across the six arrival orders, so a fingerprint would have depended on capture read order. `.claude/rules/conformance.md` forbids that, and the self-review of the issue caught it before the gate. `add_segment` detects a duplicate through a set of `(seq, length)` pairs instead of a scan, so the cost of one segment no longer grows with the segment count, and 10000 segments fell from 0.4511 s to 0.0018 s. The dead `base_seq` dictionary value is removed, and the `base_seq` method stays because `ja4plus/fingerprinters/ja4x.py:166` reads it. **The second acceptance criterion of the issue was itself a comparison that is never made.** "Adding 10000 segments completes in under one second" already passed against the unfixed code at 0.4511 s, so the gate would have reported it met with no change made. A growth-rate assertion replaces it, and that one fails against the unfixed code. No fingerprint moved, and the register holds 120 keys before and after. |
| 37 | 2026-08-07 | #33 landed and one TCP stream is bounded in stored bytes and in segments. It closes #103, which named the same defect. `max_stream_bytes` limited the reassembled output and `max_streams` limited the count of streams, so nothing bounded the list of `(seq, data)` tuples one stream holds, and a sender emitting many distinct small segments grew one stream without limit. `trim_stream` also compared raw sequence numbers, so it held the wrap defect #32 removed from `get_stream`, and it is repaired rather than left. The call shape #78 removed is not restored, because that call passed a byte offset where the method expects an absolute sequence number. A buffer that cannot become an HTTP request now leaves the reassembler, and the removal happens on the second packet that leaves the base sequence number unmoved: removing on the first packet drops a segment an out-of-order request needs, measured on a request whose tail arrives at sequence number 126 and whose head arrives at 100. **The byte cap truncates a real vector stream and the vectors say that is safe.** The largest stream in `tests/foxio_vectors/` holds 1853328 bytes and 1336 segments, on `http2-with-cookies.pcapng`, and the 1048576-byte cap cuts it to 1048554. No reference value stopped matching, because JA4H and JA4X read the head of a stream. A later issue that reads further into a stream must re-measure this cap. **Three tests that were already committed and green asserted nothing.** `test_max_stream_size` read `assertLessEqual(len(result), 20)` against a cap of 10, so it passed whatever the reassembler did. #172 opens to sweep the suite for the shape. |
| 38 | 2026-08-07 | #34 landed and it closes on the stored-list reading. The user decided that on 2026-08-07. Two of the three things the issue asked for were already built, and the issue body did not know it: #126 removed `_src_is_client` in `03c7c02`, and #104 already made the stored list hold one `JA4L-C` per connection through the `client_entry` overwrite. **The defect that survives is on the return value of `process_packet`**, which fires once per matching packet and is what `ja4plus/cli.py`, `ja4plus/collector.py` and `ja4plus/processor.py` read. Every rule a streaming return can compute was measured over the 60 connections in `tests/foxio_vectors/` that produce a client value: the first matching packet is wrong on 44 of 60, the first packet carrying a payload is wrong on 1 and drops 13 connections for which the reference holds a value, and only the last matching packet reproduces all 60. The last rule needs an end-of-connection point that `process_packet` does not have, so the return-path work moved to #156. The worker returned `needs-feedback` rather than choose, which `CLAUDE.md` rule 1 requires: every other candidate moves a fingerprint that no vector requires. A regression test landed, and it fails when the `client_entry` overwrite is removed. The second acceptance criterion stays withdrawn by #89. |
| 39 | 2026-08-07 | #156 landed and the JA4L return-path divergence is a comparison that runs. **The rule the issue stated is wrong, and the vectors settled it.** The reference gates `JA4L-S` and `JA4L-C` independently, and not on all four measurement points: implementing the literal rule breaks 80 conformance cases and deletes `ssh2.pcapng` stream 33 at `16192_57` and `tls3.pcapng` stream 25 at `3583_57`, which are reference values that match today and sit on connections with no client value. `docs/implementation_notes.md` states the measured rule and quotes the failure output. **The return path stays as it is, and the user decided that on 2026-08-07.** A flush interface was measured and rejected: against the 60 client values the vectors hold, a FIN or RST end point reports 26 and loses 34, because 34 connections never close inside their capture and `macos_tcp_flags.pcap` holds no FIN and no RST. Only a read at the end of a capture reports all 60, and a live capture never ends, so the watch command would report no client value until a reader flushed. **The divergence was invisible before this issue**, because `tests/conformance_index.py` reads the stored list, the stored list is correct at 60, and nothing measured the return path. `tests/test_ja4l_return_path.py` now measures it, and the `Divergence register` records the reading as reversible. No entry joined `tests/foxio_deviations.json`, and the worker proved that rather than assumed it: a probe entry for `latest.pcapng/JA4L-C` reported `XPASS(strict)` and failed the suite, which is the reasoning #162 used. One measured fix landed: a QUIC client value now requires the server Initial point, so `quic_mirrored.pcap` moves from `JA4L-C=500_64` to no value, which is what the reference reports. The register holds 120 keys before and after. |
| 40 | 2026-08-07 | #35 landed and both JA4H cookie hashes read one ordered list of pairs. `cookies[k] = v` dropped a repeated cookie name while `cookie_fields` kept it, so part c and part d described different cookie sets, and a repeated cookie name is legal in HTTP. The request-line pattern required `HTTP/\d+\.\d+`, so `GET / HTTP/2` never matched although `_http_version_to_str` handles `2` and returns `20`. The reading follows the FoxIO reference rather than judgment: `python/ja4h.py` sorts a list of cookie pairs, which keeps a repeated name, and the retrieval date is recorded in `docs/implementation_notes.md`. **The issue body named two defects and the file held three code paths.** `extract_http_info`, `_extract_http_info_from_bytes` and the parsed-request path all built the cookie list, and all three now keep a repeated name. Its line numbers were also stale, at 187 and 155 rather than 163 and 131. That is the third issue this session whose body did not match the file, after #34 and #156, and the first where the body asked for less work than the file needed. **No fingerprint moved, and the project manager measured it rather than accepting the verdict:** every capture under `tests/foxio_vectors/` was replayed through `JA4HFingerprinter` on the base and on the branch, 38 captures and 82 values, and 0 captures differ on the return path or the stored list. |
| 41 | 2026-08-07 | #37 landed and the malformed-input suite proves the hostile-input rule. `tests/fuzz/` holds 106 cases over the four committed malformed captures, over truncated copies generated at test time, and over the two length-field cases the requirement names. No generated capture is committed, because a committed copy stops following the capture it came from. **No fingerprinter raises, so no file under `ja4plus/` changed.** The one risk this issue carried was writing a suite that measures nothing: a case that asserts "no exception" passes when the parser is never reached, and a truncated capture that no fingerprinter opens raises nothing and proves nothing. The worker built the reachability proof into the suite, so each case counts the calls into the parser it names and fails when it stops reaching it, and `test_the_harness_reports_a_fingerprinter_that_raises` covers the harness itself because `ja4plus/processor.py:81` catches bare `Exception` and would hide a raise. The project manager measured the suite rather than trusting it: removing the over-declared-length guard from `ja4plus/utils/tls_utils.py` fails 10 of the 106 cases. One finding sits in the harness and not in the project: `truncate_frames` must not cut below the link-layer header, because `scapy` raises `struct.error` dissecting a shorter frame, and such a frame reaches no fingerprinter. **`FR-correctness-audit-13` says the suite runs on every pull request, and `.github/workflows/test.yml` accepts `pull_request` for `[master, dev]` only.** That filter is deliberate and #85 documents it. The worker implemented inside the trigger rather than widening it, which was correct: widening it breaks the batch contract and spends a run on every sub-issue. Every change reaches `dev` through a pull request that runs this suite. |
| 42 | 2026-08-07 | #170 landed and a TCP reassembler stream leaves on a maximum age, which completes the `CLAUDE.md` state rule that #33 half-built. The eviction reads the packet timestamp and never the wall clock, because a capture file replays faster than real time and a wall clock would evict state the capture still needs. `ja4plus/utils/tcp_stream.py` imports no clock at all, and the caller states the time through a new `packet_seconds`. The default is 600 seconds. **The measurement sets it, and the project manager re-derived it independently.** The longest gap between segments of one TCP stream across `tests/foxio_vectors/` is 320.714503 s in `ssh-r.pcap`, on the connection between `192.168.1.169:64980` and `192.168.1.197:22`; a separate measurement keying each direction rather than the whole stream read 320.336760 s on the same connection. **The bound is safe because of what today's fingerprinters read, not because it is inherently safe.** JA4H and JA4X read the head of a stream, which is the same shape as #33's byte cap, and a later issue that reads deeper must re-measure. The worker wrote two tests that asserted nothing, its own self-review caught both, and it reported a third that is still vacuous rather than counting it as evidence: `test_the_module_reads_no_wall_clock` passes against the unchanged base, so it guards a later clock import and proves nothing today. **`docs/specs/features/03-concurrency-safety.md` states a default maximum age of 300 seconds, which is below the longest gap the vectors hold.** The built value stays and the feature file is the artifact that is wrong, so #179 opens to correct it, because a specification edit is its own issue. Nothing breaks at either value, because the one stream above 300 seconds is SSH traffic JA4H and JA4X read nothing from, and that is why the documented number survived until a measurement looked at it. #177 opens for 48 JA4L and QUIC tests that fail under `--cov` on the base commit, which #170 found and did not introduce. |

| 43 | 2026-08-07 | #179 landed and `docs/specs/features/03-concurrency-safety.md` states a default maximum age of 600 seconds, where it stated 300. No file under `ja4plus/` changed, because #170 already built 600 and that value is correct. **A third independent measurement settled which reading governs.** `TCPStreamReassembler` keys a stream by direction, and `add_segment` refuses a segment that carries no payload, so the reading that uses both conditions is the one the default must exceed. That reading is 320.714503 s in `ssh-r.pcap`, on the connection between `192.168.1.169:64980` and `192.168.1.197:22`. **Round 42 and the #179 body both label that number the whole-stream reading and 320.336760 s the per-direction reading, and the two labels are inverted.** 320.714503 s is per-direction over segments that carry payload; 320.336760 s is per-direction over every packet; the whole-connection reading over payload segments is 320.329879 s, and over every packet it is 320.283588 s. All four exceed 300, so no conclusion of #170 or #179 moves. **The file's other numbers described no state table this project holds, and the file now separates what the code holds from what Epic 3 targets.** A new section records the entry count and the maximum age of all ten state tables: JA4H bounds 100 streams and JA4X bounds 50, not the 10000 the file named, and six state tables hold no bound at all and rely on the caller to call `cleanup_connection`. `Processor.__init__` accepts no argument, no lock reaches any module under `ja4plus/`, and no class counts an eviction, so `thread_safe`, `Processor.stats` and the bounded lookup cache are marked as targets rather than as descriptions. The `Interfaces` block also carried 300.0 as `max_connection_age`, which the issue body did not name, and it now reads 600.0 so that one project holds one maximum age. |

| 44 | 2026-08-07 | #36 landed and it closes Epic 2. No fingerprinter holds a packet object after `process_packet` returns. The issue named four writers and a sweep found a fifth at `ja4h.py:170`, so ten fingerprinters now retain zero packet objects, measured at run time with the value counts unchanged. The dead branch in `ja4plus/fingerprinters/ja4.py` is removed: its two arms built one string, and the cipher branch and the extension branch stay, because their arms build the wire order and the sorted order that #132 settled. A test parses the module and reports any branch whose arms hold identical code, and a second test asserts the two surviving branches differ, so a later reader cannot delete them and move `JA4_o` quietly. `interpret_fingerprint` catches the parse errors it expects rather than every exception. **Register row 12 rested on a premise the vectors disprove.** It read that the first banner decides which side is the server, and the first `SSH-` banner comes from the client in 4 of the 11 streams whose SYN is attributable, on `ssh-r.pcap` three times and on `ssh2.pcapng` once. RFC 4253 section 4.2 has both endpoints send an identification string, and a client that does not wait sends first, so the banner is a race and names no side. The worker returned `needs-feedback` rather than build a rule it had disproved. The user decided on 2026-08-07: an endpoint on port 22 decides, else the SYN sender is the client and the SYN+ACK sender is the server, else the lower port decides and the fingerprinter records that it guessed. Every entry carries `server_decided_by`, which reads `port`, `handshake` or `guess`, so a consumer can separate a measured side from a guessed one. That is the fourth issue premise this session that did not survive contact with the code, after #34, #156 and #35, and the first that sat in the specification rather than in an issue body, so this issue corrects rows 12 and its edge-case row rather than leaving a false row beside corrected behaviour. `opens_a_connection` moved to `ja4plus/utils/packet_utils.py` beside a new `accepts_a_connection`, and `ja4l.py` calls the moved function, so one reader of the TCP handshake serves both fingerprinters. The new handshake table caps at 1000 entries, because the state rule binds state a fix introduces and not only state it finds. `tests/conformance_index.py` read the stream identity of a value from the retained packet, taking the innermost address and port layers so a tunnelled capture resolved to the inner header; it now reads stored endpoint fields, and a review found the tunnelled-address defect in the first form. **No JA4SSH value moved, measured at 0 of 17 across 7 captures, and no reference value moved.** The register holds 120 keys before and after. |
| 45 | 2026-08-08 | #182 landed and `interpret_fingerprint` rejects a JA4SSH part that carries no client prefix. The method read a client value with `part[1:]` and never checked that the removed character is `c`. The part `36s36` therefore reported a client packet size of 6, and the caller read a number the fingerprint does not hold. **The issue body named the client packet size alone, and the measurement found the same defect in all three parts.** `c36s36_50s50_c70s30` reported a client SSH ratio of 0, and `c36s36_c50s50_70s30` reported a client ACK ratio of 0. A wrong prefix passed as well: `x36s36_c50s50_c70s30` reported a client packet size of 36. The method now returns `{"error": "Invalid JA4SSH format"}`. That is the error it already returns for a wrong part count, so the change adds no second error shape. **The issue body's line number is stale and the plan corrected it:** the two lines are 479 and 480, not 352. **Line 480 holds no defect, and the plan expected one.** A JA4SSH part names its server value after the `s` separator. It carries no server prefix, so `split("s")[1]` removes no character. A part that holds no `s` already raises `IndexError` and returns the error dictionary. `interpret_fingerprint` reads a fingerprint and produces none, so no FoxIO vector covers it. No fingerprint moved, and `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. **#186 opens for one finding this issue placed out of scope.** A part that carries a second `s` separator, such as `c36s36s99`, passes the method, because nothing checks that the split produced two elements. A comment on #186 records three readings of the same class: `int()` accepts a space, a sign, and a Unicode digit, so `c 36s36`, `c-5s36` and `c٦s36` all pass. |
| 46 | 2026-08-08 | #172 landed and the project holds a way to ask of a case whether it can fail. `tests/mutation_sweep.py` changes one expression in one module under `ja4plus/`, runs the suite, and records which cases fail. The change is a measurement, and the sweep reverts it. The first sweep applied 12 mutations to each of the 26 modules, 275 in all, and each mutation ran the whole suite of 2857 cases. 976 cases failed for no mutation, and 248 of those are cases the baseline reports as skipped or as xfailed, which cannot fail. `docs/mutation_sweep.md` is the generated report, and it lists every mutation with its result and every candidate with its file. **The plan named one candidate and the measurement confirmed it.** `tests/test_ja4l.py` assigned no packet time, so `scapy` stamped the wall clock and the assertions matched the pattern `JA4L-S=\d+_\d+`, which every latency meets. `LATENCY_DIVISOR = 2` changed to `3` survived all 224 mutations of `ja4plus/fingerprinters/ja4l.py` against that file, and the same mutation fails 138 cases of the whole suite: the FoxIO vectors held the divisor and the unit suite did not. The handshake now states its own times, and the two cases compare against `JA4L-S=10000_64` and `JA4L-C=15000_128`. A second case in the same file, `test_ja4l_os_estimation`, failed for none of the 224 mutations, because it read one word of one answer at one TTL of each range; it names the edges 64, 65, 128 and 129 now, and `if ttl <= 64` changed to `if ttl <= 65` fails it. **The sweep also found a defect in itself.** `pytest` prints a failing subtest as `SUBFAILED(hop_count=23)` and prints no `FAILED` line for the case that holds it, so the first form counted a case with subtests as measured by nothing; that produced one false candidate, and the pattern reads both forms now. Three candidates of `tests/test_tcp_stream.py` are correct cases the operators cannot reach: one reads the module attributes, one reads an early `return None`, and one compares a default against a gap the vectors hold. **Two readings stay for other issues.** 173 of the 181 mutations of `ja4plus/utils/x509_utils.py` survive the four JA4X test files, which is a coverage question and #172 excludes coverage. No mutation of `ja4plus/collector.py` failed one case, no module and no test imports it, and it carries its own removal notice for v0.4.0 against a published 0.6.0; #191 owns the removal. No fingerprinter changed. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 47 | 2026-08-08 | #177 closed with no change required, and the reading is recorded here because the issue text stated a cause that measurement contradicts. 48 JA4L and QUIC cases fail under `pytest tests/ -m "not spec_validation" --cov=ja4plus.utils.tcp_stream` and pass without it. **The cause is not the wall clock, and it is not in `ja4plus`.** `coverage` resolves a dotted source name inside a `sys_modules_saved()` block at `coverage/inorout.py:313`, which calls `importlib.util.find_spec` and therefore imports `ja4plus`, scapy and `cryptography`. Leaving the block deletes every module the block added, and the run imports both a second time. The second `cryptography` import builds Python classes the loaded Rust extension does not recognise, so every AES cipher raises `UnsupportedAlgorithm`, and the second scapy import produces duplicate layer classes that change the dissection of a committed capture. A hand-written purge with no coverage in the process reproduces both symptoms, and the JA4L client value count for `ssh2.pcapng` falls from 15 to 14, which is the observed `assert (14, 8) == (15, 9)` at `tests/test_ja4l_return_path.py:111`. 38 of the 48 raise `UnsupportedAlgorithm` in `tests/quic_builder.py:157` before any packet timestamp is read. **The directory form never triggers it**: `--cov=ja4plus` reports 1202 passed and 0 failed on the base, and so does the path form `--cov=ja4plus/utils/tcp_stream.py`. **Two acceptance criteria of #177 already passed against the unchanged code**, so the gate would have reported the issue met with no change made. That is the twelfth instance of a comparison that is never made, and the second that sat in an acceptance criterion after #32. The user declined two candidate repairs on 2026-08-08: pinning `[tool.coverage.run] source` repairs no invocation the issue reported, and importing scapy lazily in `ja4plus/__init__.py` moves the `bind_loopback_ipv6()` and `register_tunnel_dissectors()` side effects that #94 records as load-bearing. #177's third criterion moved to #172, which repaired the two `tests/test_ja4l.py` cases it named. `.claude/rules/conformance.md` states the coverage form to use. |
| 48 | 2026-08-08 | #175 landed as `be3604f` and the user reverted it the same day as `042e8c3`. The change bounded the cookie list at 512 pairs, 256 name bytes and 4096 value bytes on all three parser sites, and past any bound it produced no cookie parse result and therefore no JA4H value. **Two measured reasons reverted it.** Part a and part b of JA4H read the method, the HTTP version, the header count, the `Accept-Language` value and the header names. **None of them reads a cookie, and the policy discarded all five to bound one list.** And the change diverged from the reference with no `Divergence register` row: `docs/implementation_notes.md` quotes `python/ja4h.py`, which sorts the cookie pairs and caps nothing, the Go port holds no bound, and no vector reaches any candidate bound, because the largest vector reading is 14 pairs, a 41-byte name and a 264-byte value. `CLAUDE.md` rule 1 therefore does not settle the change, and every other decision of that class in this project carries a register row. **The measurement stands and #175 keeps it**: all three parser sites build 100000 pairs in 11.0 to 12.1 ms, and the third site, `ja4h.py:_extract_http_info_from_bytes`, is one the issue body did not name. **The project manager's independent measurement found what the verdict overstated.** The boundary cases build their input from the constant, so raising all three constants to 1000000 left 12 of 14 cases green, and nothing asserted that the caps held the values the user decided. The worker added that case before the merge, and changing 512 to 5 then fails it alone. #188 no longer inherits this policy and re-decides from its own measurement. #190 opened for a pre-existing path: `ja4h.py:112` reads a complete header block, produces nothing, and leaves the stream in the reassembler, so every later packet re-reads a buffer that grows to 1048576 bytes. |
| 49 | 2026-08-08 | #195 landed and Epic 10 holds a base. `docs/specs/foxio/README.md` records every file of `technical_details/` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, with its exact byte count, its SHA-256 and the retrieval date 2026-08-08. **Two premises this specification held are false, and the audit of 2026-08-08 measured both.** The first premise read that `technical_details/` holds `JA4.md` and `JA4H.md` as text and publishes seven methods as images. It does hold both files, and `JA4H.md` is 278 bytes: it names the purpose of JA4H and states one rule, the two-digit header count that omits `Cookie` and `Referer`. It builds no fingerprint. **One method of twelve holds a complete text specification, and that method is JA4.** The second premise sat in the fallback rule, which read "where an image is ambiguous". That wording presumes a person read the image, and nobody read one. `.claude/rules/external-apis.md` and `.claude/rules/conformance.md` now state that the fallback needs an image that a reader read and found ambiguous, and that an unread image reaches no fallback. **Three methods hold no image at all: JA4LS, JA4TS and JA4TScan.** `JA4L.png` presumably carries the JA4LS form and `JA4T.png` presumably carries the JA4TS form; this round confirms neither, and #196 and #200 answer it. `JA4SSH.png` and `JA4X.png` report the same rounded size of 90.1K, and they hold 92290 and 92247 bytes with different hashes. **No image enters this repository.** JA4+ carries the FoxIO license and this package publishes to PyPI, so a transcription records the SHA-256 and cites the upstream address. A transcription corroborates every rule against two FoxIO-authored sources that are not the image, cites the source of every statement, and marks a rule with fewer than two corroborations as uncertain, which keeps the vector fallback for that rule. **`CLAUDE.md` rule 1 and `.claude/rules/conformance.md` now state the whole authority rule**, which the user restated on 2026-08-08 and which lived only in round 27: the specification decides intent and schema, the vectors decide the exact bytes where intent runs out, and a provable reference defect is declined and recorded. **`Non-goals` records the JA4TScan decline** with its capability reason, a program that sends packets reaches a network the operator did not capture, and it records that the decision is reversible; #197 holds the reading. The `Overview` states that this project implements eleven of the twelve published methods. No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 50 | 2026-08-08 | #196 landed and `docs/specs/foxio/JA4T.md` holds the first transcription written under the #195 method. **`JA4T.png` titles itself `JA4T/S: TCP Fingerprint`, so one image specifies both JA4T and JA4TS**, and the open question that round 49 left for #196 is closed. The image gives one field layout, `<window size>_<options>_<mss>_<window scale>`, and a fifth part that carries the TCP retransmission timings. **The image states that the fifth part appears only on JA4TScan, and the Wireshark dissector contradicts it**: `wireshark/source/packet-ja4.c:1595` writes the `JA4TS` field through `ja4t()` with a connection, which appends that part. Eleven rules carry two FoxIO-authored corroborations each, and three of them are marked uncertain and keep the vector fallback. **The issue's premise that no Rust snapshot holds a JA4T value is false.** `rust/ja4/src/snapshots/` holds 64 `ja4t` values in 26 files, 19 of them distinct, and **six of those files already sit in `tests/foxio_vectors/rust_expected/` beside the captures that produce them, holding 38 of the 64 values**. No snapshot holds a `ja4ts` value, and no file under `wireshark/test/testdata/` or `python/test/testdata/` holds either. **`tests/test_foxio_rust_parity.py:72` reads `SNAPSHOT_METHODS = (("JA4", "ja4"), ("JA4S", "ja4s"))`, so the harness never reads the `ja4t` field of a snapshot it already parses.** That is the thirteenth instance of a comparison that is never made, and the first at the scale of a whole method. **Two disagreements are measured against local vectors.** `ja4t.py:85` writes `0` for an empty option list, so `gre-erspan-vxlan.pcap` produces `8192_0_0_0` against the FoxIO Rust `8192__0_0` and the Wireshark and Zeek form `8192_00_00_00`; this project matches neither reference. And `ja4t.py:67` reads scapy's parsed option list, which reports one `EOL` entry for the two `0x00` pad bytes of the `chrome-cloudflare-quic-with-secrets.pcapng` SYN, so it produces `65535_2-1-3-1-1-8-4-0_1440_6` against the reference `65535_2-1-3-1-1-8-4-0-0_1440_6`. **Three more disagreements are code readings that no local vector reaches**: `ja4t.py:69` to `ja4t.py:82` drop every option kind the loop does not name, `ja4t.py:71` and `ja4t.py:76` keep the last repeated option where the reference keeps the first, and `ja4t.py` holds no connection state, so `ssh2.pcapng` emits 44 values over the 19 connections the reference reads once each. **JA4TS repeats four of those and adds two of its own**: `ja4ts.py` writes no retransmission part and no value on a RST, and the dissector writes both. **JA4TS reaches no FoxIO reference value except the Zeek baseline**, which #198 owns. **#215 holds the decisions the seven disagreements raise, and #216 adds `("JA4T", "ja4t")` to the parity harness.** No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 51 | 2026-08-08 | #193 landed and JA4H reads a line-feed request and a retransmitted request. **The register recorded the direction of both deviations wrongly, and the measurement contradicts the cause text on one of the two captures.** `technical_details/JA4H.md` holds two facts: JA4H fingerprints the HTTP client "based on each HTTP request", and part a carries the "2 digit number of headers, not counting Cookie and Referer". It names no line ending and no rule that reads the value of a header, so the specification decides that a request is a request and the vector decides the bytes. `http-empty-useragent.pcap` carries `GET / HTTP/1.0\nUser-Agent:\n\n` over three TCP segments. `ja4plus` read the two bytes `\r\n` as the only line ending on all three parse paths. It read the request as one line and produced nothing. **The name of the capture does not state the cause**: the empty header value is legal on both readings, and `b8bcd45ac095` is the hash of the 10 bytes `User-Agent`, so the reference counts a header that carries no value. `split_http_lines` and `header_block_end` now read one line feed as well, on all three paths, which is the shape round 40 records. **`CVE-2018-6794.pcap` fails in the opposite direction from the one the register states.** The register said `ja4plus produces no JA4H fingerprint the reference holds`, and both values matched already: the sender transmits each request six times, the fingerprinter removed the stream from the reassembler after a value, and the retransmission rebuilt the same request. Ten extra occurrence keys failed the case. `JA4HFingerprinter.consumed_seq` now holds the sequence range of the request each stream produced a value for, and a segment that lies inside that range produces nothing. **The self-review found three defects this branch introduced, and it repaired all three.** The first form read the end of the range alone, so a second connection on one address pair and one port pair lost its first request whenever its initial sequence number sat below the stored one, which is about half of all reused connections inside the maximum age. The range now holds the start as well, because a retransmission starts at or after the request it repeats. A distance bound of `max_stream_bytes` was measured and rejected: it still lost a second connection whose initial sequence number sat 500 bytes below the stored one. **The range also ends after the whole buffer, and not after the header block.** A POST request reaches the fingerprinter in a segment that ends past the header block, so a range that ends at the header block let the retransmission produce a second value. **And the new line rule moves a request the old rule read, which `tshark` settles in its favour.** The request `GET / HTTP/1.1\r\nX-Data: line1\n\nHost: example.com\r\nUser-Agent: probe\r\n\r\n` gives `tshark -T fields -e http.request.line` one line, `X-Data: line1\n`. The new rule reports the header names `['X-Data']` and the old rule reported `['User-Agent']`, so the new rule matches the reference and the old one did not. Each of the three readings is a case the suite runs. The table is bounded at 100 entries and at 600 seconds, which the `CLAUDE.md` state rule requires, and `features/03-concurrency-safety.md` records it beside `unusable_base`, which the #179 audit omitted. **Both repairs are proven by reverting them**: without the line-feed rule 6 cases of `tests/test_ja4h_line_endings.py` fail, without the sequence guard 2 cases of `tests/test_ja4h_retransmission.py` fail, and each revert also fails the conformance suite. **The regression reading is zero.** Every capture under `tests/foxio_vectors/` was replayed on `6fc248f` and on the branch, 38 captures and 647 values against 638. Two captures differ and both differ on JA4H alone: `CVE-2018-6794.pcap` falls from 12 values to 2, and the 10 that leave are exact copies of the 2 that stay; `http-empty-useragent.pcap` rises from 0 values to 1. No value that matched before stopped matching. Six register entries leave, 120 keys to 114, and the conformance suite reports 114 xfailed where it reported 120, so the two counts stay equal. **Part 2 marked 43 entries and left 6 unmarked.** Round 26 settled decryption, and all 43 unmarked #129 entries state that the reference decrypts a capture the operator captured secrets for; each cause now names the round. The 6 that stay are the #34 JA4L entries, and **no round settled them**. Round 38 closed #34 on the stored-list reading and moved the return path to #156. Round 39 settled the return path. Neither round reaches an entry that reports a value on a connection the FoxIO Python file holds no JA4L value for. The FoxIO Rust snapshot holds `ja4l_c: 45_64` and `ja4l_s: 13532_57` for `https-connect.pcap` stream 0, which are the two values `ja4plus` produces, so a reading of the round 30 rule may settle two of the six. That reading is a decision this issue did not take. The decided count rises from 65 to 108. |
| 52 | 2026-08-08 | #198 landed and the FoxIO Zeek package is a fourth reference this project has read. `docs/specs/foxio/zeek.md` records it, and `tests/compare_zeek_baselines.py` reproduces the comparison with one command. **Every baseline names its capture, and this project holds all seven captures.** `zeek/tests/btest.cfg` sets `TRACES` to the `pcap/` directory of the same checkout, and the first line of each test script under `zeek/tests/Scripts/` names its capture, so no baseline is unattributable and the page states no guess. The comparison printed 98 rows, of which 62 match and 36 differ. **JA4, JA4S, JA4H and JA4D match on every value**: 20 JA4 values and 20 JA4S values of `tls-handshake.pcapng`, one JA4H value, and the four JA4D values that `tests/foxio_vectors/wireshark_expected/` already holds. **JA4TS matches on nine of ten rows, and it is the first external comparison the method has ever had**; #196 cites this reading, and JA4T matches on eight of ten. **Two JA4T differences have a cause and neither moves a fingerprint.** `zeek/ja4t/main.zeek:66-68` returns an empty option record when the link layer is not Ethernet, and the link type of `ipv6.pcapng` is `DLT_NULL`, so the Zeek baseline reads `65535_00_00_00` for a SYN packet that carries eight options. `zeek/ja4t/main.zeek:95-98` leaves the option loop at TCP option kind 0 and records nothing for it, where `ja4plus` records it; the `README.md` of `FoxIO-LLC/ja4tscan` gives `65535_2-1-3-1-1-8-4-0-0_1460_6` and corroborates `ja4plus`. **The Zeek script is the outlier on JA4L, and the FoxIO Python reference agrees with `ja4plus` on every value.** `zeek/ja4l/main.zeek:158` converts the halved interval with `double_to_count`, which rounds half to even, and `ja4plus/fingerprinters/ja4l.py:324` truncates toward zero; four half-intervals of `tls3.pcapng` land exactly on `.5` and each Zeek value equals the half-to-even form. The Zeek script also appends a third part that `python/test/testdata/` does not publish, and it marks a QUIC connection with a `q` part. The authority rule decides all three, so no fingerprint moves. **`ja4l_delta` and `ja4ls_delta` are ratios, not fingerprints**; `zeek/ja4l/main.zeek:269` and `zeek/ja4l/main.zeek:274` write each one with `fmt("%.1f", ...)`, and the audit that opened #198 read them as a possible gap. **The one open decision is JA4SSH on `ssh2.pcapng`, and #214 carries it.** The Python reference holds `c36s36_c0s0_c2s0` as its second value, which #97 declines because it describes no traffic; the Zeek baseline holds `c36s52_c42s76_c51s2` for the same trailing window, which describes 42 client packets and 76 server packets and therefore falls outside the shape #97 declined. `ja4plus` emits neither. **The Zeek package implements no JA4X and no JA4D6**: `zeek/ja4x/__load__.zeek` holds the single line `# empty`, and `zeek/config.zeek:29` disables the method. **`.claude/rules/external-apis.md` records `FoxIO-LLC/ja4tscan` at `d01bfec4e64366d37ae95982a5068a5b41ca43b0` and `FoxIO-LLC/ja4-nginx-module` at `7eeee6202b9b65f5ccf85572957a816ade8cb0bc`.** The first holds prose and no baseline. **FoxIO states that the second is not correct**, in a `# NOTICE` section that reads "This version of JA4 has known issues and bugs and may not produce correct JA4 values. Use at your own risk.", so this project reads none of its four golden files. **No baseline joins `tests/foxio_vectors/` in this round.** `docs/specs/foxio/zeek.md` states which are usable and what adding one needs, and it states that no JA4L or JA4LS value of any Zeek baseline is usable. No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 53 | 2026-08-08 | #203 landed and `docs/specs/foxio/JA4H.md` holds the second transcription written under the #195 method. **`technical_details/JA4H.md` is 278 bytes and it builds no fingerprint**, so it corroborates the image rather than replacing it; where the two agree, that is one corroboration and not two. The image titles itself `JA4H: HTTP Client Fingerprint` and labels four parts, `JA4H_a` to `JA4H_d`, with six captioned fields inside part a. Twenty-four rules carry two FoxIO-authored corroborations each, and **six of them are marked uncertain and keep the vector fallback**: the empty header list, a cookie that carries no `=`, the order of two cookies that carry one name, a header name that begins with `cookie`, where the primary language token ends, and an HTTP/2 pseudo-header. Each uncertain rule holds a disagreement between two FoxIO implementations, and the image settles none of them. **R24 is the widest.** `rust/ja4/src/http.rs:127` counts and hashes the four HTTP/2 pseudo-headers, and `python/ja4h.py:47` and `wireshark/source/packet-ja4.c:1203` drop them, so the Rust count is four higher on every request of `http2-with-cookies.pcapng`: the snapshot holds `ge20cn17enus`, `ge20cn23enus` and `ge20cr22enus` where `python/test/testdata/` holds `ge20cn13enus`, `ge20cn19enus` and `ge20cr18enus`. This project follows the Python reader. **Six disagreements with this project are reported, and four are measured.** `ja4h.py:288` reads the parsed cookie list where R5 states that all three references read the Cookie header, so `Cookie: sessionid` writes field a3 as `n` against the reference `c`. `ja4h.py:289` reads the Referer value where R6 states that the references read the header, so an empty Referer writes `n` against the reference `r`. `ja4h.py:291` counts a list that `ja4h.py:321` does not hash, so a header whose name is empty gives `ge11nn020000_4a823118b9ba_...`, which reports two headers and hashes one name. **`ja4h.py:92` adds every TCP segment to the reassembler, so a retransmitted request produces a second fingerprint**: each of the two streams of `CVE-2018-6794.pcap` carries 6 request packets and 1 distinct TCP sequence number, and this project emits 12 values where the reference holds 2. **That measurement also proves a register entry wrong.** `CVE-2018-6794.pcap/JA4H` and `/JA4H_ro` state `ja4plus produces no JA4H fingerprint the reference holds`, and the suite reports `10 extra occurrence key(s) [...]; 0 missing occurrence key(s) []`, so the values match and the count does not. `http_utils.py:17` names nine request methods where `python/ja4h.py:9` reads any method, and `ja4h.py:62` states that FoxIO publishes no `JA4H_r` key while `wireshark/test/testdata/http1-with-cookies.pcapng.json` publishes one. **Two whole captures agree, value for value**: `http1-with-cookies.pcapng` gives `ge11cr04da00_8ddaef5d77af_280f366eaa04_c2fb0fe53442` in the Python reference, the Wireshark reference and this project, and `https-connect.pcap` gives `co10nn010000_b8bcd45ac095_000000000000_000000000000`. **JA4H is the method the FoxIO material covers most**: 89 values in `python/test/testdata/`, 89 in `rust/ja4/src/snapshots/`, 126 in `wireshark/test/testdata/`, and **this repository already holds 89 of them across 11 captures**. `tests/foxio_vectors/rust_expected/` holds 6 `ja4h` values that `tests/test_foxio_rust_parity.py:72` never reads, which is the same shape #196 found for JA4T. **42 of the 120 register entries name JA4H**, over four captures; the specification explains the `CVE-2018-6794.pcap` pair through R18, explains the `http2-with-cookies.pcapng` schema through R4 and not the TLS decryption, and explains neither the `http-empty-useragent.pcap` pair, which #193 owns, nor the QUIC pair. **#219 holds the six decisions.** No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 54 | 2026-08-08 | #199 landed and `docs/specs/foxio/JA4SSH.md` holds the third transcription written under the #195 method. Eleven rules carry two FoxIO-authored corroborations each, and one is marked uncertain. **FoxIO published a text specification of JA4SSH, and one commit deleted it.** `technical_details/JA4SSH.md` exists at commit `16850cc2c8bcb8328c1a43a851a3a9a6eaa56103` as blob `e9bb04b11a06ee1dfd3dbeaa18b45a0138bcdf48`, 2288 bytes, and commit `b6f3ff4` titled `Update README.md` removed it. **The pinned commit points at the deleted file**: `rust/ja4/src/ssh.rs:283` cites it for the mode rule, so FoxIO's own implementation treats it as the statement of intent. **The same commit deleted seven files**: `JA4.md`, `JA4H.md`, `JA4L.md`, `JA4S.md`, `JA4SSH.md`, `JA4T.md` and `JA4X.md`. The first two exist again at the pinned commit, and the other five do not. **Five methods therefore hold a deleted text specification that no transcription has read.** #221 owns that work. A deleted file corroborates and never outranks the image, and `docs/specs/foxio/README.md` states the rank. **The deleted text states two rules the image only draws.** The image captions part a `Mode of Client Packet Length`, and the text states `Reminder: We’re looking at the TCP payload lengths, not the packet length. In wireshark this is under “tcp.len”.`; a reader of the image alone would read the frame length. The text also excludes a retransmission from the packet count, with the words `TCP replays`. **The three declined reference behaviours all stand, and the specification contradicts two of them.** #96 is confirmed at `python/ja4ssh.py:8-15`, with `python/ja4ssh.py:88` and `python/ja4ssh.py:128`. `dict(ja4sh_stats)` is a shallow copy, so one pair of payload lists serves every window of every connection. The deleted text heads the rule `How to measure the mode for TCP payload lengths across 200 packets in the session`. #97 is confirmed at `python/ja4ssh.py:125`, which runs `(entry['count'] % ssh_sample_count) == 0` on every packet. `python/ja4ssh.py:98-99` advances the count only for an SSH packet. A bare ACK that arrives between packet 200 and packet 201 therefore writes a second occurrence. `rust/ja4/src/ssh.rs:271-274` holds the same empty-window guard that `ja4plus/fingerprinters/ja4ssh.py:342-343` holds, so the Rust reference contradicts the Python one. #105 is confirmed at `python/ja4.py:370-377`, where `if stream:` reads the stream index `0` as false. **The specification states the window boundary and states nothing about what closes the last window**, which is the reading #214 needs. The image states `(runs every 200 SSH packets by default)` and the deleted text states `Runs every n packets per SSH TCP stream. n = 200 by default but is configurable.`; neither names a FIN packet, a connection that closes, or the end of a capture. **The references split two against two.** `python/ja4.py:554-556` and `wireshark/source/packet-ja4.c:1399-1404` close the last window on a FIN+ACK packet, and `rust/ja4/src/ssh.rs:45-55` and `zeek/ja4ssh/main.zeek:160-164` close it at the end of the capture; `ja4plus/fingerprinters/ja4ssh.py:221-222` follows the first pair. **`python/ja4.py:610` runs no end-of-capture step at all**, because the line `#finalize_ja4ssh() if 'ja4ssh' in output_types else None` is commented out. **`ssh2.pcapng` carries no FIN+ACK packet on port 22.** Its connection holds its last window open when the capture ends. That is the whole mechanism of #214. **`ja4plus` already counts that window, and it matches two references byte for byte.** It holds 42 client SSH packets, 76 server SSH packets, 51 client ACKs and 2 server ACKs. That window would give `c36s52_c42s76_c51s2`. **`tests/foxio_vectors/rust_expected/ja4__insta@ssh2.pcapng.snap:215-217` already sits in this repository and holds that exact value**, which equals the Zeek baseline value; #198 did not read it. The Python second value `c36s36_c0s0_c2s0` is the #97 defect and not a trailing window. **`tests/test_foxio_rust_parity.py:72` reads `SNAPSHOT_METHODS = (("JA4", "ja4"), ("JA4S", "ja4s"))`, so the harness parses that snapshot and never reads its `ja4ssh` field.** That is the fourteenth instance of a comparison that is never made. The page records four disagreements with `ja4plus`, and none moves a fingerprint. D1 is the trailing window, which #214 holds. `ja4ssh.py:135` reads SSH on any port, where `python/ja4.py:549` requires port 22. `ja4ssh.py:284-326` decides the client endpoint from three rules that no specification states. `ja4ssh.py:214` tests `>=` where the Python and Rust references test equality, and the Zeek script tests `>=` too. No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 55 | 2026-08-08 | #175 landed and the state rule of `CLAUDE.md` names its boundary. **A state table survives across packets, and a structure that one packet or one request builds and releases is not a state table.** The second holds no maximum entry count and no maximum age. The `Conventions` list of `CLAUDE.md`, the `## State rules` block of `.claude/rules/conformance.md`, and the `Cross-cutting concerns` block of `docs/specs/spec.md` and of `docs/specs/spec.html` carry one wording. The rule stated the bound and never named the object it binds, so round 48 read the cookie list of one HTTP request as a state table and bounded it. **The user decided on 2026-08-08 that the cookie list carries no bound.** The FoxIO reference holds no cookie bound: `docs/implementation_notes.md` quotes `python/ja4h.py`, which sorts the cookie pairs and holds no bound. The Go port holds no cookie bound. No FoxIO vector reaches any candidate bound, because the largest vector readings are 14 pairs, a 41-byte name and a 264-byte value. `ja4plus` matches all three. **No file under `ja4plus/` changed.** The bound merged as `be3604f` and the user reverted it as `042e8c3`, so the parser already held the decided state, and this round is documentation and one test. **The measurement that round 48 produced stands and this round records it** in `docs/specs/features/02-correctness-audit.md`: all three parser sites build 100000 pairs in 11.0 to 12.1 ms, the third site is `ja4h.py:_extract_http_info_from_bytes`, and the memory returns when the request ends. **The `Divergence register` gains no row, and the file states why.** The register records a reading that diverges from the reference; the reference holds no cookie bound and `ja4plus` holds none, so the two agree and a reader who looks for a row finds none missing. **The boundary weakens no bound Epic 3 targets, and all four files say so.** The six unbounded state tables that #179 records survive across packets, so the rule reaches them and they keep their bound. **One test stops the policy being rebuilt by accident.** `tests/test_ja4h_large_cookie_header.py` holds 5 cases, each with a Cookie header past all three reverted bounds: 5000 pairs, a 1024-byte name and a 8192-byte value. Each case asserts a complete JA4H value, part a through part d, and one case asserts that part a and part b of a 5000-pair request equal those of a one-pair request. **The cases are proven to fail, against both candidate policies.** Applying `be3604f` to the two parser files fails all 5, with `assert None is not None` on 4 and an `AttributeError` on the fifth. Changing that same parser to truncate rather than to refuse fails 4 of the 5, and the case that passes is the one that reads part a and part b alone, which truncation keeps. |
| 56 | 2026-08-08 | #200 landed and `docs/specs/foxio/JA4L.md` holds the fourth transcription written under the #195 method. **`JA4L.png` specifies the client form alone, and the open question round 49 left is answered.** The image titles itself `JA4L: Light Distance/Location Fingerprint`, it labels its one example `JA4L=`, and it states no server rule; `technical_details/README.md:9` and `README.md:50` name `JA4LatencyServer` / `JA4LS` as a separate method, and `technical_details/` holds no `JA4LS.png`. **The two halves of the round 49 presumption have opposite answers**: #196 found `JA4T/S` in the title of `JA4T.png`, and JA4LS reaches no image at all. **The image labels three parts, and this project publishes two.** Part a is `One-way TCP latency in µs`, part b is `Observed TTL`, and **part c is `One-way application handshake latency`**, in the example `JA4L=5191_42_45014`. The Wireshark dissector writes the same three-part form at `wireshark/source/packet-ja4.c:1371-1374` and `:1381-1386`, and `zeek/ja4l/main.zeek:191-192` and `:132-133` write a third part too, so R4 carries two corroborations. **The other two references contradict it**: `python/ja4.py:157` writes `f"{diff}_{ttl}"` and `rust/ja4/src/time.rs:18-22` holds two fields, and every one of the **114 JA4L values in `tests/foxio_vectors/*.json`, across 24 files, holds two parts**. `wireshark/test/testdata/` holds **44 `ja4.ja4l` values and 44 `ja4.ja4ls` values, 88 in all, in 15 files, and every one of the 88 holds three parts**, 9 of each key with the literal `quic` as the third. The count comes from a walk of each file's JSON and not from a line pattern, because the two keys sit side by side and a line pattern reports a number whose key set the reader cannot see. R4 is therefore uncertain and the vector fallback keeps the two-part form, so no fingerprint moves. **#198 answered the other two questions and this page cites it rather than re-deriving it**: Zeek is the outlier on the part count against the reference values, and `ja4l_delta` and `ja4ls_delta` are ratios. The image corroborates the second answer from the other side: the delta it names is a subtraction, `45014-5191 = 39823µs`, it feeds the distance formula, and `wireshark/source/packet-ja4.c:1377` and `zeek/ja4l/main.zeek:269` both write a **ratio** in a field separate from the fingerprint, so the image specifies neither column. **Eleven rules are transcribed and four are uncertain, and none of the four builds a fingerprint**: the initial TTL table of 64, 128 and 255, the hop count formula, the distance formula `D = jc/p` with 0.128 miles per microsecond, and the propagation factor table each rest on the image alone. Measured on 2026-08-08 at the pinned commit outside `zeek/`: `grep -rn "propagation"` and `grep -rn "Hop Count"` report no match, and the one FoxIO function that subtracts a TTL, `python/ja4.py:140-147`, reads `initial_ttl = 54` for the low band and nothing calls it. **`ja4l.py` follows the image on all four, and the worked example reproduces**: `calculate_distance(5191, ttl=42)` returns 415.28 miles, because TTL 42 gives 22 hops and the table row for 22 gives 1.6, which is the factor both distance lines of the image use. **Four disagreements with this project are reported and three are measured.** `ja4l.py` writes two parts where the image states three, so `https-connect.pcap` gives `45_64` against the Wireshark `45_64_66`; `calculate_distance` therefore cannot compute the VPN case the image works; `ja4l.py:549` writes no `q` part and no `_quic` part on a QUIC connection, which matches the Python reference; and **`ja4l.py:416-425` returns a second server value for a retransmitted SYN-ACK**, so `ssh2.pcapng` gives 11 `JA4L-S` values against the reference 10, the extra being `JA4L-S=6252_58` on `tcp_172.16.225.48:57380_184.150.157.177:80`. **The six unmarked register entries all name #34, and the specification explains none of them.** The image states the schema of one value and the distance a reader derives; it states no emission rule. **Round 38 settled the stored list and the return path, and it did not settle these six.** **Five of the six compare against an expected-output file that publishes no JA4L key at all**: `CVE-2018-6794.pcap` and `https-connect.pcap` hold `JA4H` keys alone, `tls-handshake.pcapng` holds `JA4`, `JA4S` and `JA4X` keys alone, and `python/ja4.py:339` deletes `JA4L-S` and `JA4L-C` when the run names another method, so the reading is that the three files were produced with a method filter. The upstream files agree: `grep -c '"JA4L-'` reports 0 for each of the three and 19 for `python/test/testdata/ssh2.pcapng.json`. **`ssh2.pcapng/JA4L-S` is the one entry of the six that compares two measured counts.** **This round also corrects a reading of round 52.** `docs/specs/foxio/zeek.md` stated that the Zeek script adds a third part that no other reference holds, and #198 compared the Python reference against the Zeek script without reading the image or `wireshark/test/testdata/`. **Zeek is not the outlier on the part count**: the specification labels three parts, all 44 `ja4.ja4l` values and all 44 `ja4.ja4ls` values of `wireshark/test/testdata/` hold three, and the Python reference and this project hold two. #198's measurement stands and the conclusion it drew from two sources of five does not. The rounding reading of round 52 is unaffected, because there the Python reference does agree with `ja4plus` and the Zeek script is the outlier. **#225 holds the decisions the four disagreements and the five register entries raise.** No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 57 | 2026-08-08 | #221 landed and `docs/specs/foxio/deleted-text-specifications.md` reconciles all seven deleted FoxIO text files against the material at the pinned commit. **The user set the rule: a deleted file may record a correction rather than a suppression.** The measurement answers it, and the answer differs per file. **`JA4.md` came back as the same blob.** `b6f3ff4^:technical_details/JA4.md` and `ad2e13c:technical_details/JA4.md` are both `02c493ad032c8828ccfa67dd60d607c4ee89a95c`, 6922 bytes, five days apart, so that deletion corrected nothing. **`JA4H.md` never came back.** The 9137-byte blob `15f0b265456210462c7a456cdb7c8e0e35b5dfe7` was deleted on 2024-02-22, and a different 337-byte file took the name on 2024-09-25, which `eee9a48` trimmed to the pinned 278 bytes. **That replacement corrected a defect**: the deleted file states the header cap as `99 = anything > than 100 headers`, which names two thresholds, and the 278-byte file states `If there are more than 99, the output is 99.` **A deletion therefore carries no single meaning, and `docs/specs/foxio/README.md` now ranks a statement rather than a file.** **#214 gains a negative result and no answer.** The deleted `technical_details/JA4SSH.md`, blob `e9bb04b11a06ee1dfd3dbeaa18b45a0138bcdf48`, is the exact blob `rust/ja4/src/ssh.rs:283` cites, and it names no FIN packet, no connection that closes, no end of a capture and no trailing window. R11 of `docs/specs/foxio/JA4SSH.md` stays uncertain, the vector fallback stays, and the user still decides #214. **The deleted `JA4T.md`, 6423 bytes, is the richest source and it carries the one contradiction.** Its JA4TS form reads `WindowSize_TCPOptions_MSSValue_WindowScale_TimeSinceLastSYNACK`, and the image captions part e `TCP Retransmission Timings (only on JA4TScan)`; the deleted text agrees with `wireshark/source/packet-ja4.c:1595` and the image outranks it, so R2 stays uncertain. **It also states the empty-field form that the image does not state**: `If any field does not exist, then the output is 00.`, with `JA4T = 1024_00_00_00`. `wireshark/source/packet-ja4.c:664` and `zeek/tests/Traces/Scripts.ja4-conn/conn.log` corroborate it, `rust/ja4/src/tcp.rs` writes `8192__0_0`, and `ja4plus` writes `8192_0_0_0`; #215 item 1 gains the FoxIO prose it lacked. It states four more rules the image does not state, and one of them, the bound of 10 retransmissions with a timeout of 2 minutes, rests on the deleted file alone and stays uncertain. **The deleted `JA4H.md` names exactly nine request methods** where the image's caption ends with `etc`, which #219 item 5 owns, **and its worked example computes 11 headers and publishes 13**, a defect the image repeats and no implementation reads. **The deleted `JA4L.md` corroborates the whole distance model this project already builds**: `0.128` miles per microsecond, the six propagation factors, and the three initial TTL values at `ja4l.py:57`, `ja4l.py:60`, `ja4l.py:72` and `ja4l.py:260`. It states no rounding rule and no third part, so it settles neither Zeek divergence. `technical_details/JA4L.png` also changed after the deletion, from 198175 bytes to 162323 bytes, so #200 reads a different image. **The deleted `JA4S.md` and `JA4X.md` state nothing the pinned material contradicts**, and the `(q or t)` of `JA4S.md` predates the DTLS addition that `ja4s.py:276` already builds. **One defect against the pinned specification is found and not repaired.** `technical_details/JA4.md` at the pinned commit states `0x0002 = SSL 2.0 = “s2”` after `3e02a27` titled `Fix SSL version fields: SSL 2.0 is 0x0002, SSL 1.0 never existed`, and `ja4plus/fingerprinters/ja4.py:127`, `ja4.py:250` and `ja4s.py:393` write `0x0200`. No vector carries an SSL 2.0 hello, so no case fails today. **#227 owns the repair, and #226 owns the one contradiction**; #226 names #215 item 4 as the issue a triager may fold it into. No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 58 | 2026-08-08 | #201 landed and `docs/specs/foxio/JA4S.md` holds the fifth transcription written under the #195 method. **JA4S is the method with the most published reference values, so this page measures where #196 reasoned.** The three FoxIO implementations that publish an expected value hold 329 JA4S values between them: 84 in `python/test/testdata/` across 14 files, 120 `ja4s` values in 15 files under `rust/ja4/src/snapshots/` with 29 distinct, and 125 `ja4.ja4s` values in 15 files under `wireshark/test/testdata/`. The Zeek baseline #198 read adds 20 more. **This repository already holds 194 of them**, all 84 Python values and 110 Rust values in 7 files, and `tests/test_foxio_rust_parity.py:72` already reads every one. **JA4S is therefore the one method of this epic where the comparison is made rather than merely available**, which is the opposite of what #196, #203 and #199 found. **The image's own worked example reproduces byte for byte.** `JA4S.png` draws `JA4S=t120400_c030_4e8089b08790`, that value belongs to `tls-handshake.pcapng`, which this repository holds, and `ja4plus` produces `t120400_c030_4e8089b08790` with the raw form `t120400_c030_0005,0017,ff01,0000` that the deleted `technical_details/JA4S.md` states. Fourteen rules carry two FoxIO-authored corroborations each, and **three are marked uncertain and keep the vector fallback**: the DTLS protocol character, an ALPN byte that is not ASCII alphanumeric, and GREASE in the server extension list. **R13 is the widest split, and it is two against two.** `python/ja4.py:182` carries the comment `# get extensions in hex in the order they are present (include grease values)` and `rust/ja4/src/tls.rs:604-613` applies no GREASE test, where `wireshark/source/packet-ja4.c:757` and `zeek/ja4s/main.zeek:91` drop a GREASE extension from the list and from the count. `ja4plus` includes it and follows the two references the authority rule ranks first. **No vector measures it**: measured on 2026-08-08, 0 ServerHello messages across the 38 captures of `tests/foxio_vectors/` carry a GREASE extension type. **Five disagreements with this project are reported and none is measured against a reference value.** `ja4s.py:276` writes a `d` protocol character for DTLS where the image names only `t` and `q`; `wireshark/source/packet-ja4.c:732` and `technical_details/JA4.md:24` support `d`, and `rust/ja4/src/tls.rs:434` reads any UDP packet as QUIC so the Rust reference writes `q` for the same ServerHello. Measured on 2026-08-08, `ja4plus` produces 126 JA4S values over the 38 captures, of which 97 open with `t`, 29 open with `q` and **0 open with `d`**. `ja4s.py:281-283` reads the first supported version where the three references read the largest, and it falls back to the legacy version where `rust/ja4/src/tls.rs:563` emits nothing; `tls_utils.py:267-269` stores one value, so a conformant ServerHello reaches the same answer. `ja4s.py:300-302` writes no value for a ServerHello that names no cipher, where `python/ja4.py:191` writes an empty part b. `ja4s.py:393` maps the retracted `0x0200`, which **#227 owns and this page only cites**. **Fourteen register entries name JA4S, and the specification explains none of them and contradicts none of them.** Nine name #138 and five name #151, and every one records a stream that the FoxIO Python expected-output file omits; the image states the schema of one value and no rule about which stream a reader reaches. **The plan's premise that 16 register entries under #162 concern JA4S is false.** Those 16 name `JA4.1`, `JA4_o.1`, `JA4_r.1` and `JA4_ro.1` on four streams of `alpn-condition.pcap`, and none names JA4S. Measured on 2026-08-08: the capture holds 7 packets and 0 ServerHello messages, its expected-output file holds 0 JA4S keys, and `ja4plus` produces 0 JA4S values for it. **The deleted `technical_details/JA4S.md` gains one reading from the image.** #221 read its `(q or t)` as a file that predates the DTLS addition; the image at the pinned commit states the same two values, so that reading now rests on `technical_details/JA4.md` and the Wireshark dissector rather than on the age of the deleted file. The deleted text contradicts the image on nothing, and it corroborates the single cipher, the wire order, the 12-character truncation and the absent sorted form. **#201 raises five decisions and #227 already owns the fifth.** No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 59 | 2026-08-08 | #204 landed and `docs/specs/foxio/JA4D.md` holds the transcription of `technical_details/JA4D.png` and `technical_details/JA4D6.png`, the last two images of the twelve methods. **JA4D and JA4D6 are the only two methods for which FoxIO has never published prose.** The seven files `b6f3ff4` deleted are `JA4.md`, `JA4H.md`, `JA4L.md`, `JA4S.md`, `JA4SSH.md`, `JA4T.md` and `JA4X.md`, and `git log --all --diff-filter=ADR -- '*JA4D*'` reports one commit, `6239c08` titled `Adding JA4D/6`, dated 2025-11-07, which adds the two images and nothing else. **The two-corroboration rule is therefore satisfied from implementations alone**, and an uncertain rule on that page is a rule one implementation states while no second implementation states anything. **The images also postdate the deletion by 21 months**, so no statement of either image corrects a deleted statement. **The issue's premise holds and the search corrects nothing.** `grep -ril 'ja4d|dhcp' python/` and `grep -ril 'ja4d|dhcp' rust/` each report no file, `python/test/testdata/dhcp.pcapng.json` and `python/test/testdata/dhcpv6.pcap.json` each hold `[]`, `ja4plus-mapping.csv` carries no column for either method, and no FoxIO blog post was read because this reading reaches no network. **JA4D reaches two FoxIO implementations and JA4D6 reaches one.** `zeek/README.md:15` states `JA4D6 &rarr; ja4d.log (awaiting Zeek DHCPv6 suppport)`, so `zeek/ja4d/` builds JA4D alone. The reference values are the four in `wireshark/test/testdata/dhcp.pcapng.json`, the four in `zeek/tests/Traces/Scripts.ja4-dhcp/ja4d.log`, the six in `wireshark/test/testdata/dhcpv6.pcap.json`, and one documented value per method at `README.md:154` and `README.md:155`. **Twenty-two rules are transcribed and five are uncertain.** The image captions decide two JA4D6 rules by differing from the JA4D captions: subfield 2 reads `Client DUID Length` rather than `Maximum DHCP Message Size`, and the part b caption carries no exclusion clause where the JA4D caption reads `DHCP Options List, ignoring options 50, 53, 81, and 255`. **The image's skip set matches the Zeek script exactly and the dissector matches neither.** `zeek/ja4d/consts.zeek:25-30` holds `{53, 255, 50, 81}` and `wireshark/source/packet-ja4.c:1526` tests `val != 0 && val != 53 && val != 50 && val != 81`, which names code 0 and omits code 255; the ten reference values separate neither set. **The Zeek baseline also proves that Zeek never sees code 255**, because `zeek/utils/common.zeek:30-32` appends the separator whenever the index is below the last index, so a skipped last element would leave a trailing `-`, and `ja4d.log` holds `disco0000in_61-55_1-3-6-42` with none. **R10, R15, R16, R19 and R20 are uncertain and each keeps the vector fallback**, and R16, R19 and R20 are uncertain for one reason: JA4D6 reaches one implementation. **Eleven disagreements are reported with a `file:line` and both readings, and not one moves a value on any vector.** Measured on 2026-08-08, `ja4plus` matches all four JA4D reference values and all six JA4D6 reference values, and it emits no extra value. The eleven rest on packets no vector carries: a DHCP message on another port, a BOOTP message with no option 53, an option 81 with an empty name, a repeated or split option 55, 57, 1 or 6, a DHCPv6 container this project does not recurse into, and a DHCPv6 message type of 0. **One is an internal contradiction of this project**: `ja4plus/fingerprinters/ja4d6.py:74-75` names DHCPv6 option 17 in a comment and `ja4d6.py:76-82` holds no entry for it. **The comment at `ja4plus/fingerprinters/ja4d.py:42-44` cites FoxIO pull requests 267 and 270, which no checkout at the pinned commit can read, and R9 replaces that citation with the image caption and `zeek/ja4d/consts.zeek:25-30`.** **`tests/foxio_deviations.json` holds no key that names either method, and the reason is structural**: the conformance suite walks the top level of `tests/foxio_vectors/` and the two DHCP reference files sit under `tests/foxio_vectors/wireshark_expected/`, so `tests/test_ja4d_foxio.py` and `tests/test_ja4d6_foxio.py` compare all ten values inside the unit suite instead. **This is the opposite of the shape `docs/specs/foxio/JA4T.md` reports**, where the local snapshots carry `ja4t` values no harness reads. **#231 holds the eleven readings and the five uncertain rules.** No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys before and after, against 120 `xfailed` cases. |
| 60 | 2026-08-08 | #202 landed and `docs/specs/foxio/JA4X.md` holds the seventh transcription written under the #195 method. **The image states nothing about the transport that carries the certificate**, and that negative answers the named question of the issue. The user decided on 2026-08-07 that `ja4plus` reads the record layer without regard to the tunnel protocol that carries it, and the specification neither supports nor refutes the three `socks4-https.pcap` values. Eleven rules carry two FoxIO-authored corroborations each. **R8 is the one uncertain rule, and the image contradicts itself on it.** The image's two `Sliver, Havoc C2` rows write the zero sentinel in part a, and its `Qakbot` row writes `e3b0c44298fc` in part c, which is the truncated SHA-256 of the empty string. `README.md` line 146 at the pinned commit writes `000000000000` for the same certificate, so the image records the FoxIO Python output and the `README.md` records the FoxIO Rust output. `ja4plus` writes the zero sentinel, and **no local vector reaches the case**, so no fingerprint moves either way. **Two measurements prove the schema.** `ja4plus` reproduces the image's example value `96a6439c8f5c_96a6439c8f5c_aae71e8db6d7` from the lists the deleted `JA4X.md` states, and the wire-order extension hash of the first `https-connect.pcap` certificate is `5e17a2514980`, which the FoxIO Rust snapshot holds, while the sorted hash `fdc32d844f5f` is not. **Nine register entries name the method, and the image explains none of them.** The deleted `JA4X.md` does explain the seven #129 entries: it states that a certificate travels in clear text under TLS 1.2 and encrypted under TLS 1.3, nothing at the pinned commit corrects that sentence, and #221's rule therefore keeps it as FoxIO-authored evidence. **One register claim is false.** The cause of `https-connect.pcap/JA4X` reads `tests/test_foxio_rust_parity.py measures the match.`, and `tests/test_foxio_rust_parity.py:72` holds `SNAPSHOT_METHODS = (("JA4", "ja4"), ("JA4S", "ja4s"))`, so no test compares a JA4X value against a Rust snapshot. #229 owns that harness change and #228 holds the two decisions. No file under `ja4plus/` changed and no fingerprint moved. |
| 61 | 2026-08-08 | #235 landed, and the five Epic 10 register statements now read the register `batch/193-register-and-state-rule` holds. **Two integration branches were live at once, and neither could see the other.** Every page of Epic 10 was written against a register of 120 keys. #193 repaired two JA4H defects, and it removed the six entries behind them. The register falls from 120 keys to 114. **Only the total and the JA4H count move.** This round measures the counts on 2026-08-08 over `git show origin/batch/193-register-and-state-rule:tests/foxio_deviations.json`. The measurement parses the JSON and counts the method component of each key. It reads the method component and not a text pattern, because the pattern `/JA4S` also matches a `/JA4SSH` key. JA4H with `JA4H_r` and `JA4H_ro` falls from 42 to 36. `JA4S` with `JA4S_r` holds 14, and JA4X holds 9. `JA4L-C` with `JA4L-S` holds 6, JA4SSH holds 10, and JA4D with JA4D6 holds 0. **The six removed keys are `CVE-2018-6794.pcap/JA4H`, `CVE-2018-6794.pcap/JA4H_ro`, `http-empty-useragent.pcap/JA4H`, `http-empty-useragent.pcap/JA4H_ro`, `http-empty-useragent.pcap/0:57722/JA4H.1` and `http-empty-useragent.pcap/0:57722/JA4H_ro.1`.** **#193 repaired the defect behind each one, and it reclassified none of them.** The FoxIO reference holds the JA4H values it always held. This project now produces them too, so the comparison passes and the register needs no entry. `docs/specs/foxio/JA4H.md` therefore keeps both rows, under a heading that names the repair and beside the reading each row already carried. Two further statements of that page reason from the removed entries, and each one now names the branch it describes. **The earlier rounds of this table are not rewritten.** Each one records the count its own branch measured, and that count stays correct for that branch. **The five pages state the count as of #193**, so a later reader knows which register each page describes. No file under `ja4plus/` or `tests/` changed, and no fingerprint moved. `tests/foxio_deviations.json` holds 120 keys on this branch, against 120 `xfailed` cases, because #193 reaches `dev` on its own branch. |
| 62 | 2026-08-08 | #227 landed and the SSL 2.0 version value follows the pinned specification. **FoxIO retracted the value this project wrote.** `technical_details/JA4.md:65` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` states `0x0002 = SSL 2.0 = “s2”`, and FoxIO commit `3e02a27`, dated 2024-08-23, is titled `Fix SSL version fields: SSL 2.0 is 0x0002, SSL 1.0 never existed`. `ja4.py:127`, `ja4.py:250` and `ja4s.py:393` held `0x0200`, which the deleted 2024-02-22 form states, and #221 found all three. **The author corrected the statement, so the retracted form carries no authority**, which is the rule `docs/specs/foxio/deleted-text-specifications.md` states: rank a statement, and not a file. **The specification settles what `0x0200` now means, so this issue needed no decision.** `technical_details/JA4.md:65` names nine values and states `Unknown = “00”` for every other value, `wireshark/source/packet-ja4.c:72-81` holds `{0x0002, "s2"}` and no `0x0200` entry, and the `TLS_MAPPER` of `python/common.py` holds `'0x0002': "s2"` and no `'0x0200'` key. All three FoxIO sources write `00` for `0x0200`, so **the repair replaces the row and adds no alias**. `ja4plus` held no `0x0100` row, so the SSL 1.0 half of the correction needed nothing, and `0x0100` already reached the `00` fallback. **Two parser paths present the value and the SSL 2.0 record format presents none.** `_parse_client_hello` reads the legacy version field verbatim from bytes 9 and 10 of a handshake record, `_parse_supported_versions_client` reads any 16-bit value, and `parse_tls_handshake` returns `None` for a genuine SSL 2.0 ClientHello, because that message opens with a two-byte length whose high bit is set. **The repair therefore reaches a hello that names the value inside a TLS record, and it reaches no genuine SSL 2.0 handshake.** **No fingerprint moved.** No capture under `tests/foxio_vectors/` carries an SSL 2.0 hello; measured on 2026-08-08, the 38 captures produce 1494 values before the repair and 1494 after, and zero values differ. `tests/foxio_deviations.json` holds 114 keys before and after, against 114 `xfailed` cases. **The repair is proven by its reversal**: `tests/test_ja4_ssl2_version.py` holds fifteen cases, ten of them fail against the base form of the two modules, and twelve cases of the unit suite fail when that form is restored. **Two independent methods converged on this one defect, months apart in the tracker and from opposite directions.** #172 mutated `ja4s.py:393` in the batch 15 sweep and recorded the result at `docs/mutation_sweep.md:257` as `| 393 | number | `0x0200` | `513` | survived | 0 |`; the mutation killed zero cases, so the sweep proved the line was untested and it stated nothing about the value being wrong. #221 read a deleted FoxIO text specification and found that the author had retracted the value, and it stated nothing about the line being untested. **The first method measured this project against its own suite, and the second measured this project against FoxIO.** Both named the same line. The mutation now dies: `test_ja4s_writes_s2_for_the_ssl_2_0_version_value_0x0002` fails when `ja4s.py` maps any value other than `0x0002`. **#206 therefore needs no further reading of this candidate**, and `docs/mutation_sweep.md:257` stays as written, because a sweep checkpoint belongs to the commit it measured. |
| 63 | 2026-08-08 | #216 landed and the parity harness compares JA4T against every JA4T value the local FoxIO Rust snapshots hold. **The gap #196 measured is closed.** `tests/test_foxio_rust_parity.py` read `ja4` and `ja4s` from a snapshot and never read `ja4t`, so the six local snapshots carried 38 JA4T values that no case compared, and the suite reported green on a comparison it never made. `SNAPSHOT_METHODS` now holds `("JA4T", "ja4t")`, `tests/conformance_index.py` runs the JA4T fingerprinter, and `TestTheJa4tValuesTheRustSnapshotHolds` collects 38 value cases and 6 occurrence-key cases. **The revert proves the cases run**: remove `("JA4T", "ja4t")` and 44 cases stop running, and three checks fail and name the loss. **37 of the 38 values reproduce exactly and two disagreements report.** D2 is `chrome-cloudflare-quic-with-secrets.pcapng`, where the SYN carries two End of Option List bytes and scapy reports one `EOL` entry for the pair, so `ja4plus` writes `65535_2-1-3-1-1-8-4-0_1440_6` against the reference `65535_2-1-3-1-1-8-4-0-0_1440_6`. D4 is `ssh2.pcapng`, where `ja4plus` holds no connection state and fingerprints every SYN, so 10 of the 19 streams carry more than one value against the reference count of one; the distinct values agree, so the count differs and the value does not. **Each disagreement is a register entry that names #215, and neither is `decided`**, because #215 decides the repair. **JA4TS stays uncovered and the page says so.** No file under `tests/foxio_vectors/rust_expected/` writes a `ja4ts` field, and no FoxIO Python expected-output file holds a `JA4T` key or a `JA4TS` key; the Zeek baseline is the one reference value, and #198 owns it. **D1 reaches no local snapshot value, and #216 declined to add the one that measures it.** `rust/ja4/src/snapshots/ja4__insta@gre-erspan-vxlan.pcap.snap` names the stream by the inner tunnel addresses `10.16.27.12` and `10.16.27.131`, and `python/test/testdata/gre-erspan-vxlan.pcap.json` and `ja4plus` both name the outer pair `100.20.9.2` and `100.20.9.1`, so a case built from that snapshot would report the address layer rather than D1. The D1 reading stands on its own: `ja4plus` produces `8192_0_0_0` where the snapshot holds `8192__0_0`. **No file under `ja4plus/` changed and no fingerprint moved.** `tests/foxio_deviations.json` holds 114 keys before and 116 after, against 114 and then 116 `xfailed` cases. |
| 64 | 2026-08-08 | #219 landed and the four JA4H part-a readings that `docs/specs/foxio/JA4H.md` measured now follow the FoxIO references. **The user decided all four on 2026-08-08.** Field a3 read the parsed cookie list and field a4 read the Referer header value, so a Cookie header whose value holds no `=` wrote `n` and an empty Referer value wrote `n`. R5 and R6 state that both fields read the header, and all three references read it, so the two are defects and the `Divergence register` gains no row for either. Field a5 counted the whole header list while part b hashed a shorter one, so a header named with a space raised the count and changed no hash. R9 states that the count and the hash read one list. `http_utils.py` named nine request methods, so a request that carries `PROPFIND` produced no JA4H value. `python/ja4h.py:9` reads the first two characters of any method, `rust/ja4/src/http.rs:342` rejects a method outside the nine, and `wireshark/source/packet-ja4.c:1087` holds a 40-entry map. The three disagree, so **the `Divergence register` gains one row** and it names what the reading departs from. **No FoxIO vector reaches any of the four readings**, so the repair is proven by what does not move and by what fails when it is reverted. The 38 captures under `tests/foxio_vectors/` produce 791 values before the repair and 791 after, and 0 differ. 73 of them are JA4H values. The conformance suite reports 114 `xfailed` before and 114 after, and `tests/foxio_deviations.json` holds 114 keys before and after. **A test built only from constructed cases proves nothing about the old behaviour, so each repair is reverted and measured.** `tests/test_ja4h_part_a_readings.py` holds 42 cases. 28 of them fail on the base commit. Reverting field a3 alone fails 3, field a4 alone fails 5, field a5 alone fails 5, and the method pattern alone fails 15. **The self-review found two more gaps and both are repaired.** The request-line test the reassembly gate runs read the whole stream buffer, and `ja4l.py:365` calls that gate for every TCP payload; the test now reads 8192 leading bytes, and 200 calls on a buffer of 1000003 bytes fall from 9.2 ms to 0.3 ms. R8 states the cap of 99, the D4 repair rewrote the expression that applies it, and no case in the suite held it; removing the cap now fails 2 cases. The three parse paths now read one request-line pattern, so `parse_http_request` reads the same method list and the same version list as the other two. That repairs two readings of that path, which split the request line on one space and read no version: `GET / HTTP/11` wrote the version token `HTTP/11`, and `GET  /  HTTP/1.1` wrote the path `` and the version `/`. **The reassembly gate keeps a narrow test.** `is_http_request` admits a method the nine tokens omit only when the buffer holds a whole request line, because `ja4l.py:365` reads the same gate and a wider test would admit an SSH banner. Coverage holds at 81%. |
| 65 | 2026-08-08 | #229 landed and the parity harness compares JA4X against every JA4X value the local FoxIO Rust snapshots hold. **The register stated a measurement that did not happen.** `tests/foxio_deviations.json` held for `https-connect.pcap/JA4X` the cause `tests/test_foxio_rust_parity.py measures the match.`, and it marked the entry `decided`, while `read_rust_snapshot` took the two-space level of a snapshot alone and every `ja4x` value sits under `tls_certs:` at a deeper indent. Five of the ten local snapshots hold a certificate, 19 streams hold at least one value, and those streams carry 43 JA4X values that no case compared. The reader now enters the `tls_certs` block, and `TestTheJa4xValuesTheRustSnapshotHolds` collects 43 value cases and 5 count cases. **The revert proves the cases run**: remove the `tls_certs` branch and 48 cases stop running, and three checks fail and name the loss. **All 43 values reproduce exactly, in snapshot order, and no stream differs in its value count**, so the register gains no entry and the sentence in the cause becomes true rather than corrected. **One stream carries a register key of the parity module.** `tests/test_spec_validation.py` builds the key form `<capture>/<stream>:<port>/JA4X.<n>` from the FoxIO Python file, and that file holds a JA4X value for 18 of the 19 streams; `certificate_key` keys `https-connect.pcap` stream 0 on port 54723 alone, and a new check keeps the two key sets apart, because one entry that matches a case of each module marks both and neither fix reports itself. **D1, D2 and R8 stay unmeasured, and none is a mismatch**: no snapshot field holds a raw form, no capture carries a certificate with a duplicate attribute, and no capture carries a certificate with no extension. **The `ja4ssh` values of the snapshots stay unread**: `tests/foxio_vectors/rust_expected/ja4__insta@ssh2.pcapng.snap:215` holds a `ja4ssh:` block with two values, no other local snapshot holds the field, and #199 owns that reading. **No file under `ja4plus/` changed and no fingerprint moved.** `tests/foxio_deviations.json` holds 116 keys before and 116 after, against 116 `xfailed` cases. |
| 66 | 2026-08-08 | #225 landed and JA4L keeps two timing parts, while a QUIC connection gains the `quic` marker. **The three-part adoption was proposed, measured and reversed.** `JA4L.png` labels three parts, `wireshark/test/testdata/` holds 44 `ja4.ja4l` values and 44 `ja4.ja4ls` values that all carry three, and the Zeek `conn.log` baselines carry three; `python/test/testdata/` and `rust/ja4/src/snapshots/` carry two. The user first decided for three parts on the authority rule, because a part count is schema. **The measurement changed the decision.** Every one of the 114 JA4L values in `tests/foxio_vectors/*.json` holds two parts, and `tests/foxio_vectors/wireshark_expected/` holds `dhcp.pcapng.json` and `dhcpv6.pcap.json`, neither of which carries a JA4L key. A three-part form therefore deviates from all 114 comparisons, which moves the register from 116 entries to 230 and **leaves no case that measures any JA4L value**. **20 of the 114 sit on 10 captures the dissector publishes no JA4L value for**, so those 20 would carry a third part no reference verifies. **A second finding blocked the adoption outright: the Wireshark part c is not computable where `ja4plus` writes a value.** `packet-ja4.c:1370` reads the interval from D to E for `ja4ls` and `packet-ja4.c:1382` reads the interval from E to F for `ja4l`, where `packet-ja4.c:1328`, `:1343` and `:1366` set D, E and F on application packets; `ja4l.py:423` emits the server value on the SYN-ACK and `ja4l.py:439` emits the client value on the first packet carrying both relative numbers, and both points lead D. **Adopting part c is an emission-model change and not a format change.** The dissector also publishes 44 values on each key where the Python reference publishes 46 `JA4L-C` and 48 `JA4L-S`, so the two references emit on different connections. **The QUIC marker survives because a published reference value verifies it**, and it is the one part of the three-part reading that does. **The spelling follows Wireshark and the sources settle it.** `packet-ja4.c:1441` and `:1447` write `quic` on 18 published values, `zeek/ja4l/main.zeek:233` and `:252` write `q` on 14, and `.claude/rules/external-apis.md:95` reads "Read no JA4L or JA4LS value of a Zeek baseline as a reference value" while `:101` names the `q` marker as one of three Zeek divergences. The rule declines the Zeek spelling. **The `tcp` literal is not adopted**: `packet-ja4.c:1348` and `:1354` write it on an HTTP connection and **none of the 88 published Wireshark values carries it**. **The change touches two format strings.** The replay over the 38 captures produces 143 JA4L values before and 143 after; 36 move, every one on a QUIC connection, and **107 TCP values do not move**. `chrome-cloudflare-quic-with-secrets.pcapng` moves 2, `ssh2.pcapng` moves 3, `tls-handshake.pcapng` moves 20 and `tls3.pcapng` moves 11. **16 of the 36 reach a conformance case**, because `tls-handshake.pcapng` carries no JA4L key in its expected-output file and its existing occurrence entry already covers its 20. **On all 16 the two timing parts agree, so the marker is the whole difference.** The repair is proven by its reversal: restore `ja4plus/` from the base and 32 cases fail, 16 in the conformance suite and 16 in the unit suite, every one a QUIC JA4L case and none a TCP one. `tests/foxio_deviations.json` holds 116 keys before and 132 after, against 116 and then 132 `xfailed` cases. **D2 and D4 stay open.** #225 ruled on the part count and on the marker alone; `ssh2.pcapng/JA4L-S` stays under #34 unchanged, and `docs/specs/foxio/JA4L.md` records that both await a decision. |
| 67 | 2026-08-08 | #226 landed and JA4TS carries part e, the time since the last SYN-ACK. **The user reversed the D6 and D7 ruling of #215 of the same day**, which followed the FoxIO image caption `TCP Retransmission Timings (only on JA4TScan)`. Three FoxIO sources put part e on JA4TS and nothing ever corrected them: the deleted `technical_details/JA4T.md`, which `b6f3ff4` removed on 2024-02-22 and no commit restored, `wireshark/source/packet-ja4.c:1595` through `ja4t()` at line 684, and `zeek/ja4t/main.zeek:229-235`. The image's own example `1-2-4-8-R6` carries the suffix its caption denies. **The deleted file states the omission rule and this round read the blob rather than inferring it**: "If no retransmissions are seen, as there shouldn't be in normal network communications, the fingerprint will omit section e." Part e is absent, and it is not `00`. Both implementations corroborate the omission, because each appends part e only when a delay exists. **The user accepted a fall in the Zeek comparison in advance, and the measurement reports none.** #198 measured JA4TS at 9 of 10 rows matching against the three `conn.log` baselines, and `Scripts.ja4-conn/conn.log` holds `ja4ts 65535_00_00_00`. **The reading after part e is 9 of 10, unchanged, and no `ja4plus` value carries part e on any of the ten rows**, because each of the ten baseline connections holds one SYN-ACK. `docs/specs/foxio/zeek.md` records the new reading beside #198's rather than rewriting it. **One value moved in the whole vector set.** The 38 captures produce 803 values before and 803 after, and `ssh2.pcapng` packet index 372, which is frame 373 in a one-based reader, moves from `64240_2-1-1-4-1-3_1460_7` to `64240_2-1-1-4-1-3_1460_7_0`. It is the one connection of the set the server answered twice, and the interval of 0.005829 s rounds to `0`. No value of another method moved. The register holds 116 keys before and after and the conformance suite reports 116 `xfailed` before and after, because no conformance case compares a JA4TS value. **Three further rules come from the deleted file and each carries a second corroboration**: a delay rounds to the nearest whole second, which `timediff` in the dissector matches by calling the C `round` and which `zeek/ja4t/main.zeek:180` contradicts by truncating; a fingerprint grows with each SYN-ACK; and the state holds ten retransmissions with a timeout of two minutes, which `zeek/ja4t/main.zeek:185` corroborates and which `MAX_SYN_ACK_TIMES` in the dissector reads one lower at nine. **The RST value separates from part e and #246 owns it.** Both implementations nest the RST branch inside the delay branch, so a delay list is complete with no RST, and the RST value needs a reader of RST packets and stored parts a to d that the RST packet does not carry. **Seven mutations prove the cases bite.** Dropping part e fails 8 of 17, removing the omission rule fails 7, the Python built-in `round` fails 1, a cap of nine fails 2, a one-hour timeout fails 2, an unbounded connection table fails 2, and keying part e on the server address alone fails 3. **The self-review found a vacuous case and repaired it**: three bound cases read the constant they asserted, so a cap of nine changed nothing; they now state the literal values and a separate case pins the three constants. `Divergence register` gains one row, and the decision is reversible. |
| 68 | 2026-08-08 | #214 landed and JA4SSH emits the window a connection holds open when the capture ends. **The user decided it on 2026-08-08, and the decision is reversible.** The specification settles nothing here: `docs/specs/foxio/JA4SSH.md` R11 records the gap, and #199 read the deleted `technical_details/JA4SSH.md`, the exact blob `rust/ja4/src/ssh.rs:283` cites, which names no FIN packet, no connection that closes, no end of a capture and no trailing window. **The references split two against two.** `python/ja4.py:554-556` and `wireshark/source/packet-ja4.c:1399-1404` close the last window on a FIN+ACK packet, and `rust/ja4/src/ssh.rs:45-55` and `zeek/ja4ssh/main.zeek:160-164` close it at the end of the capture. `python/ja4.py:610` runs no end-of-capture step at all, because the line `#finalize_ja4ssh() if 'ja4ssh' in output_types else None` is commented out. **Two FoxIO references agree on the value, and this repository already held the proof.** `ssh2.pcapng` carries 452 TCP packets on port 22 and zero FIN+ACK, so no FIN+ACK rule fires on it; `tests/foxio_vectors/rust_expected/ja4__insta@ssh2.pcapng.snap:215-217` and the Zeek baseline both hold `c36s52_c42s76_c51s2`, and the `ja4plus` counters already held that value. **`ssh2.pcapng` now produces `c36s52_c42s76_c51s2` as its second value.** `close_open_windows` reads every connection of the state table. The caller runs it when the packet source ends. `tests/conformance_index.py` runs it after the last packet, and `ja4plus/cli.py` runs it after the file reader and after a live capture stops. **#97 stays declined and its reasoning is unchanged.** A window that holds no SSH packet describes no traffic, and the new rule reaches the guard at `ja4plus/fingerprinters/ja4ssh.py:368-369`. **The guard is proven by its removal**: replace the guard condition with `False` and 11 cases fail, 5 of them new cases of this issue. Four cases name the decline directly: a connection of bare ACKs alone, the empty window that follows a full window, the same window through the processor, and the absence of `c36s36_c0s0_c2s0` from `ssh2.pcapng`. **Six JA4SSH comparisons moved and no value of another method moved**, measured by a replay of all 38 committed captures. The values the end of the capture adds are `c36s52_c42s76_c51s2` on `ssh2.pcapng`, `c36s52_c42s76_c0s0` on `ssh.pcapng`, `c0s1460_c0s53_c6s0` on `ssh-scp-1050.pcap`, `c16s23_c7s6_c3s4` on `ssh2-malformed.pcap` and on `ssh2-moloch-crash.pcap`, and `c144s48_c10s11_c6s4` on `tcpdump-geneve.pcap`. **`gre-sample.pcap`, `sshv1.pcap` and `v6.pcap` did not move**, because each one carries a FIN+ACK packet and each already produced a trailing value; the rule is now one rule for every capture. **The register moved by four keys.** It lost `ssh2.pcapng/JA4SSH`, whose occurrence keys now equal the reference, and it gained five entries under #214, one for each other vector, because the FoxIO Python reference emits no trailing window for a connection that sends no FIN+ACK packet. `tests/foxio_deviations.json` holds 116 keys before and 120 after, against 116 and then 120 `xfailed` cases. **The change is proven by its reversal**: restore the body of `close_open_windows` to `return []` and 13 cases fail, 10 of the unit suite and 3 of the conformance suite. A revert of the whole method breaks `tests/conformance_index.py`, which calls it on every fingerprinter, so that measurement reports 1556 failures and states nothing about the behaviour. **The state table holds no entry longer.** `close_open_windows` emits the open window and evicts nothing, so `JA4SSHFingerprinter.connections` holds the same keys after the call; that table is one of the six unbounded state tables #179 records, and Epic 3 owns its bound. `ja4plus/cli.py` also lost a bare `except Exception` around the call, which `CLAUDE.md` forbids. Coverage holds at 82%. |
| 69 | 2026-08-08 | #251 landed and the 16 QUIC marker entries carry the `decided` marker. **The register stated that nobody had read them, and Changelog round 66 had settled every one.** #193 repaired the same fault on 43 entries under #129, and batch 18 put it back. A reader of `dev` counted 24 open entries where 8 are open. **The undecided count falls from 24 to 8, and the key count does not move.** Each of the 16 causes now names Changelog round 66 and states that the decision is reversible. Each cites the published reference value that carries the marker. `wireshark/source/packet-ja4.c:1441` and `:1447` write it, and a walk of `wireshark/test/testdata/` reports 18 of 88 `ja4.ja4l` and `ja4.ja4ls` values that hold it. **The eight entries that stay open now state why.** #34 owns six and #215 owns two. **Five of the six #34 entries compare against an expected-output file that publishes no JA4L key at all.** `grep -c '"JA4L-'` reports 0 for `CVE-2018-6794.pcap.json`, `https-connect.pcap.json` and `tls-handshake.pcapng.json`, and 19 for `ssh2.pcapng.json`. `python/ja4.py:339-340` deletes `JA4L-S` and `JA4L-C` when the run names another method, so a method filter produced the three files. **Round 56 cited `python/ja4.py:339` alone, and line 339 is the guard while line 340 is the call.** **`ssh2.pcapng/JA4L-S` is the one entry of the six that compares two measured counts, and this round re-measured it.** The FoxIO Python file holds 10 `JA4L-S` values, and a replay of the capture produces 11. The extra value is `JA4L-S=6252_58` on `172.16.225.48:57380` to `184.150.157.177:80`, where packets 369 and 372 carry the same SYN-ACK. **Round 56 cited `ja4l.py:416-425` for that path, and the code has moved since.** `ja4plus/fingerprinters/ja4l.py:430` returns the second value today, and the entry cites the current line. Round 66 cited `packet-ja4.c:1441` and `:1447`, and a `grep -n` of `%d_%d_quic` confirms both. **The invariant this repair needs is still not enforced, and #253 owns the gate.** **A second defect is found and not repaired: the owner snapshot is stale.** `tests/foxio_deviation_owners.json` records #34 and #225 as `open`, and `gh issue view` reports both closed. The six #34 entries therefore name a closed owner that only the stale snapshot hides. #255 owns the refresh and the ruling. `tests/test_foxio_deviations.py` gains two classes that hold the counts. `TestTheQuicMarkerDeviations` names the 16 keys, and `TestTheOpenRegisterEntries` names the eight. **The change is proven by its reversal**: restore `tests/foxio_deviations.json` from the base and seven cases fail. No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 136 keys before and after, against 136 `xfailed` cases. |
| 70 | 2026-08-08 | #211 landed and three files state the FoxIO image count the inventory measures. #195 corrected four files and scoped its work to them, so `docs/implementation_notes.md`, `docs/specs/features/01-spec-conformance.md` and `docs/specs/spec.html` kept the superseded premise for fifteen rounds. **This round re-measured `technical_details/` rather than trusting the count the issue body carries.** `shasum -a 256 technical_details/*`, run against a checkout at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, reports twelve files: three text files and nine images. All twelve hashes and all three byte counts, 1567 for `README.md`, 9153 for `JA4.md` and 278 for `JA4H.md`, reproduce `docs/specs/foxio/README.md` exactly. **Eleven of the twelve methods carry no complete text specification, and JA4 is the one method that holds one.** `technical_details/README.md` lists twelve methods and embeds nine images, so JA4LS, JA4TS and JA4TScan reach no image of their own name. **The `Interfaces` table of `docs/specs/features/01-spec-conformance.md` now holds one row for each of the twelve methods**, where it held three rows and omitted JA4LS, JA4TS and JA4TScan. It records that #200 read `JA4L.png` and found no server rule, and that #196 read `JA4T.png`, which titles itself `JA4T/S: TCP Fingerprint` and specifies JA4TS too. It also records the seven deleted text files that `b6f3ff4` removed on 2024-02-22, which `docs/specs/foxio/deleted-text-specifications.md` reconciles, so this page contradicts #221 nowhere. **The `Out of scope` bullet states the JA4TScan decline rather than an open question.** The user declined JA4TScan on 2026-08-08, the decision is reversible, and #197 holds the reading. **One premise of the issue body is false and the measurement disproves it.** The issue states that `docs/specs/spec.html` holds no Changelog round after 44 where `spec.md` holds 48; the page holds no Changelog section at all. Two commits wrote the page, and the later one is `f6d808b` of #175, while `docs/specs/spec.md` records 68 rounds before this one. **`docs/specs/spec.html` is corrected and it is not removed, and it is not generated.** No generator command exists in the repository, so the page gains a note that a person maintains it by hand, that it lags `docs/specs/spec.md`, and that `docs/specs/spec.md` decides wherever the two disagree. **Whether the page survives at all is a question for the user, and this round decides nothing about it.** The page's two contradicted statements are corrected: the image count, and the JA4TScan card, which held the round 3 form and now holds the round 49 decline with its capability reason. **A new case set guards the count, because prose carries no other gate.** `tests/test_documentation_image_count.py` holds 14 cases. It forbids the superseded count phrase, which the case file holds as the constant `SUPERSEDED_COUNT`, in every Markdown page under `docs/`, in `.claude/rules/`, in `CLAUDE.md`, in `README.md` and in `docs/specs/spec.html`; it reads the first table of the `Interfaces` section and requires a row for each of the twelve methods; and it requires the `Out of scope` bullet to cite #197 and to drop the words `open question`. **The cases were written first and they bite**: against the base they failed 5 of 14, one for the count and four for the table and the bullet, and they pass after the corrections. Round 49 records the superseded premise in different words, so that history survives the forbidden phrase. No file under `ja4plus/` changed, no fingerprint moved, and `tests/foxio_deviations.json` is untouched, because #251 owns it in this batch. |
| 71 | 2026-08-08 | #191 landed and `ja4plus/collector.py` is removed. **The module carried its own removal notice for v0.4.0, and the project publishes 0.6.0.** #48 closed as absorbed by #191, decided by the user on 2026-08-08, so `FR-typed-api-11` now points at #191. **The claim that nothing imports the module was measured over every tracked file, not over `ja4plus/` alone.** `git grep -i collector d147592` reports 53 matching lines in 12 files outside the removed file, and no line imports it. The command names the base commit, because this row adds a match to the count it reports. `ja4plus/__init__.py` re-exports it nowhere. `pyproject.toml` declares one console script, `ja4plus = "ja4plus.cli:main"`, so no entry point reaches the module. The six files under `examples/` name it nowhere, the three files under `.github/workflows/` name it nowhere, and `README.md` names it nowhere. **Every remaining match holds the word for another thing.** `tests/generate_foxio_baseline.py` and `tests/test_generate_foxio_baseline.py` name the `_CaseCollector` class, `tests/test_quic_server_initial.py` names the QUIC fragment collector, `tests/test_comprehensive.py::TestJA4TSComprehensive::test_collector_uses_base` reads `JA4TSFingerprinter`, `docs/specs/features/03-concurrency-safety.md` names the Go garbage collector, and the `processor` row of the `## Terms` table rejects `collector` as a synonym. `docs/specs/features/06-live-capture.md` names a remote collector, which is delivery and not this module. The other matches sit in `docs/mutation_sweep.md`, `docs/implementation_notes.md`, `docs/specs/features/02-correctness-audit.md`, `docs/specs/features/04-typed-api.md` and `docs/specs/spec.html`. Those five files record the sweep that found the module and the Epic 4 goal that removes it, and they stay as written. **Both suites report the counts they reported before the removal.** The unit suite reports 1349 passed, 1743 deselected, 8 xfailed and 93 subtests passed, before and after. The conformance suite reports 1464 passed, 143 skipped, 1357 deselected and 136 xfailed, before and after. **Coverage rises from 82% to 85%, and no case gained coverage.** The module held 119 statements and every one was unreached, so the removal drops the total from 3586 statements to 3467 and the misses from 643 to 524. `mypy ja4plus/` reads 27 source files before and 26 after, and reports success on both. `python -c "import ja4plus.collector"` now raises `ModuleNotFoundError`. **The removal answers the JA4SSH divergence #214 recorded on the module.** `collector.py:195-217` held its own `sniff` loop and ran no `close_open_windows()` call, so it emitted no trailing window where `ja4plus live` emits one. No file under `ja4plus/fingerprinters/` and no file under `ja4plus/utils/` changed. **The Go port was not read.** This round holds no reading of `Crank-Git/ja4plus-go`, and the parity rule reaches no removal of a module this project deprecated two minor versions ago. |
| 72 | 2026-08-08 | #242 landed and the parity harness names the stream of a tunneled capture. **The two FoxIO references disagree, so no vector decides.** `python/test/testdata/gre-erspan-vxlan.pcap.json` names the outer pair `100.20.9.2` and `100.20.9.1`, and `rust/ja4/src/snapshots/ja4__insta@gre-erspan-vxlan.pcap.snap` names the inner pair `10.16.27.12` and `10.16.27.131`. The measurement of 2026-08-08 reads three address layers in every packet, GRE and ERSPAN over the outer pair, VXLAN over the middle pair `172.16.27.131` and `172.16.27.121`, and the session over the inner pair; both references name the inner ports `65174` and `80`. **`ja4plus` reports the outer pair with the inner ports, which is the pair the FoxIO Python file reports**, so rule 1 of `CLAUDE.md` and the #162 settlement keep the present behaviour and no fingerprinter changes. **The harness records the pair rather than loosening the match.** `SNAPSHOT_ADDRESS_ALIASES` in `tests/test_foxio_rust_parity.py` maps the identity the snapshot names to the identity `ja4plus` produces, and `read_rust_snapshot` reads it. A port pair alone would match any of the 19 JA4T streams of `ssh2.pcapng`, and a value-only comparison would drop stream attribution; the recorded pair stops measuring nothing, because a wrong address on either side finds no entry and the case reports `ja4plus=<none>`. `TestTheStreamIdentityOfTheTunneledCapture` holds seven checks, and `test_a_stream_the_alias_does_not_name_finds_no_entry` substitutes a wrong address, a wrong source port and a wrong destination port against six parameter sets. **#216 declined to commit `ja4__insta@gre-erspan-vxlan.pcap.snap` and #242 commits it**, so the seven local snapshots now hold 39 JA4T values against 38, and D1 of `docs/specs/foxio/JA4T.md` reaches a conformance case for the first time: `ja4plus` writes `8192_0_0_0` for an empty option list and the snapshot holds `8192__0_0`. The reader guard moved from a check for the `JA4` name to a check for any value the module compares, because the snapshot holds `ja4t` alone. **No file under `ja4plus/` changed and no fingerprint moved.** `tests/foxio_deviations.json` holds 136 keys before and 137 after, against 136 and then 137 `xfailed` cases, and the one new key `gre-erspan-vxlan.pcap/0:65174/JA4T.1` names #215, which owns the D1 decision in batch 20. |
| 73 | 2026-08-08 | #186 landed and `interpret_fingerprint` rejects every JA4SSH part the form denies. The method split a part on `s` and read element 1 as the server value, and it never counted the elements. **The issue body named the packet size part, and the measurement found the same unchecked split in all three parts.** `c36s36s99_c50s50_c70s30`, `c36s36_c50s50s1_c70s30` and `c36s36_c50s50_c70s30s7` each reported a session type and no error, and each dropped the text after the second separator. That is the second issue on this method whose body named one part of three, after #182 in round 45. **The issue body's line number is stale again.** It names `ja4plus/fingerprinters/ja4ssh.py:486`, and the line is 514. Round 45 records the same correction for #182, which named 352 against a true 479. **The three readings a comment on #186 records all reproduce, and the measurement adds four more.** `int()` accepts a space, a sign and a Unicode digit on the server side as well as on the client side, so `c36s 36`, `c36s-5` and `c36s٦` pass. `c36s+36` passes on a plus sign. A trailing line feed and a trailing space pass, because the last part reaches `int()` whole. **One guard replaces the split, the `int()` call and #182's prefix guard together.** `JA4SSH_PART` is `re.compile(r"c(\d+)s(\d+)", re.ASCII)`, and the method calls `fullmatch` on each part. The regular expression subsumes the prefix guard, and #182's five cases pass unchanged. **`fullmatch` is the comparison and an anchored `match` is not.** Python `$` matches before a trailing line feed, so `c70s30\n` passes `^c(\d+)s(\d+)$`. **Three mutations prove the parts of the guard bite.** `match` in place of `fullmatch` fails 5 cases, a pattern without `re.ASCII` fails 2, and the pre-#186 parser fails all 13 new cases. **The self-review found a comment that states a false fact, and a fourteenth case now pins the truth.** The comment read that the part guard rejects a malformed string before `IndexError` and `ValueError` can reach the handler. CPython converts 4300 digits at most, so a part of 5000 digits matches the pattern and `int()` raises `ValueError`. The method returns the error dictionary, and a mutation that drops `ValueError` from the handler fails the new case. **The self-review found one further gap and #262 owns it.** A `bytes` fingerprint raises `TypeError` from `fingerprint.split("_")` at `ja4ssh.py:503`, and the same input raises the same error at the base commit `07f5a7a`. **The regular expression carries no backtracking risk.** It holds no nested quantifier, and the literal `s` separates the two groups; a 200000-digit match runs in 0.00045 s and a 100001-character failure runs in 0.00072 s. **One error message changes and no second error shape appears.** `c36_c50s50_c70s30` returned `Failed to interpret: list index out of range` and it now returns `{"error": "Invalid JA4SSH format"}`, which is the error the method already returns for a wrong part count. The handler still names `AttributeError`, `IndexError` and `ValueError`, so row 14 of `docs/specs/features/02-correctness-audit.md` stays true; the guard now rejects a malformed string before the last two can fire. **One reading stays open and this round changes nothing for it.** A leading zero passes, so `c0036s36_c50s50_c70s30` reports a client packet size of 36. The regular expression the issue comment states accepts it, no FoxIO reference states a rule for it, and no emitter of this project writes one. **No fingerprint moved.** `interpret_fingerprint` reads a fingerprint and it produces none, so no FoxIO vector covers it. The conformance suite reports 1464 passed, 143 skipped and 136 `xfailed` before and after, and `tests/foxio_deviations.json` holds 136 keys before and after. Coverage holds at 82%, and `ja4plus/fingerprinters/ja4ssh.py` holds 98% before and after. |
| 74 | 2026-08-08 | #190 landed and JA4H removes a stream whose complete header block produces nothing. The fingerprinter removed a stream after a value and after a buffer that starts no HTTP request. It removed no stream when a complete header block failed the parse, so the request line `GET / HTTP/11` left the buffer in the reassembler. Every later packet on that connection read the whole buffer again, and the buffer grows to 1048576 bytes. **The measurement reproduces the issue.** One refused request and 300 further packets of 1400 bytes cost 35.5 ms and hold 420055 stream bytes on the base. The same traffic costs 20.1 ms and holds 1400 bytes after the change, and an accepted request costs 20.4 ms and holds none. **The premise of the issue is wrong for a byte that precedes the buffer, and a test proves it.** The issue states that a complete header block admits no later byte that changes its parse. A segment that arrives out of order puts the middle of a request at the start of the buffer. The head of the request lowers the base sequence number when it arrives. The removal waits for one further packet that leaves the base sequence number in place. That rule is the one #33 built for a buffer that starts no HTTP request, and `_drop_a_stream_whose_base_holds` now holds it for both paths. **The stream therefore leaves on the second packet, and not on the refused packet itself.** A stream that receives one packet and nothing else leaves through `max_stream_age`, which is 600 seconds. **An immediate removal fails two cases.** One of them is `test_a_reordered_request_whose_head_arrives_late_produces_a_fingerprint`. Its request carries the head `POST /a HTTP/1.1\r\nX-Note: ` at sequence number 100, and the tail `GET / HTTP/11\r\n\r\n` arrives first. **No JA4H value moved.** A replay of all 38 captures under `tests/foxio_vectors/` gives 73 values, 73 raw forms and 293 streams left behind. The replay gives the same result before the change and after it. **The count of 82 that round 40 and the second acceptance criterion state is stale.** The base commit of batch 20 produces 73, and no work of this issue moved that number. **Every count of this row is measured against `batch/259-interpret-fingerprint` at `f0ef8fc`, after #186 landed.** The register holds 136 keys before and after, and the conformance suite reports 136 xfailed. Batch 19 raises the register to 137 keys, because #242 commits the snapshot that measures D1, and that batch is not on this base. Coverage of `ja4plus/fingerprinters/ja4h.py` rises from 90% to 91%, and the whole project holds at 82%. |
| 75 | 2026-08-08 | #262 landed and `interpret_fingerprint` returns the error dictionary for a fingerprint of type bytes. **The project manager's premise holds and the measurement confirms it.** `interpret_fingerprint(None)` returns `{'error': "Failed to interpret: 'NoneType' object has no attribute 'split'"}` at the base commit `f0ef8fc`, so the method already answers a value that is not a string with the error dictionary. A value of the wrong type is a malformed fingerprint, and `TypeError` joins the handler beside `AttributeError`, `IndexError` and `ValueError`. **The issue body's line number is correct this time.** It names `ja4plus/fingerprinters/ja4ssh.py:503`, and the line held `fingerprint.split("_")` at the base commit. Rounds 45 and 73 each record a stale line number on this same method, so this round breaks that run of two. **The issue body names bytes, and the measurement adds bytearray.** Thirteen types reached the method. `bytes` and `bytearray` raised `TypeError: a bytes-like object is required, not 'str'`, because each holds a `split` method that denies a separator of type str. **Nine other types already returned the error dictionary through `AttributeError`**: `None`, `memoryview`, `int`, `float`, `bool`, `list`, `tuple`, `dict` and `set` each hold no `split` attribute. `memoryview` is the one bytes-like type that answers this way. **A second place raises `TypeError` and one handler covers both.** The part guard `JA4SSH_PART` holds a string pattern, so `fullmatch` raises `TypeError` on a part of type bytes. A stand-in whose `split` returns `[b"c36s36", b"c50s50", b"c70s30"]` reaches it, and the method now returns the error dictionary. **The handler names four errors and it names no bare `Exception`.** `CLAUDE.md` forbids the wider form, and row 14 of `docs/specs/features/02-correctness-audit.md` now records the four. **The guard is proven by its removal.** Drop `TypeError` from the handler and 4 cases fail: the three bytes-like cases and the case whose parts hold bytes. No other case of the unit suite fails, so nothing depended on the stack trace. **The error message follows the shape the method already writes.** A bytes fingerprint returns `{'error': "Failed to interpret: a bytes-like object is required, not 'str'"}`, which is the `Failed to interpret:` form that `None` returns. **No fingerprint moved.** `interpret_fingerprint` reads a fingerprint and it produces none, so no FoxIO vector covers it. The conformance suite reports 1464 passed, 143 skipped and 136 `xfailed` before and after, and `tests/foxio_deviations.json` holds 136 keys before and after. The unit suite grows from 1352 to 1356 passed. Coverage holds at 82%, and `ja4plus/fingerprinters/ja4ssh.py` holds 98% before and after. |
| 76 | 2026-08-08 | #253 landed and a gate enforces the register marker rule. **Round 69 recorded that the invariant is not enforced, and this round enforces it.** `tests/foxio_deviations.json` gained 21 entries in batch 18, and 16 of them named a decision the user had made while carrying no marker. #193 repaired the same fault on 43 entries under #129, and #251 repaired the 16. No case caught either fault. **The gate reads the register and fails when an entry cites a decision and carries no `decided` marker.** `unmarked_decisions` in `tests/test_foxio_deviations.py` names every offending key and the citation it holds, so the failure states the repair rather than a count. **The gate reads four forms of citation as evidence that a person decided an entry.** The first is a Changelog round named by number, as `Changelog round 66`. The second is a decision recorded against an issue or a date, as `decided on #105` or `decided on 2026-08-07`. The third is an issue named as the place the decision was made, as `#138 decided`. The fourth is an issue named as the record of the decision, as `#162 records the decision`, which 16 entries of #162 hold today. **Every form is past tense.** A decision that was made is the only evidence that somebody read the entry. **A rule on the words `Changelog round` alone marks six open entries.** The six #34 entries close their cause with `no Changelog round settled it`, so the rule requires a number, and a lookbehind drops a denied citation. **The present tense is not a citation.** The three #215 entries read `#215 decides whether ja4plus reads the raw option bytes`, and naming the issue that will decide an entry is not naming a decision. **The rule was read against the open entries before it was written, and it flags none of them.** **The register holds nine open entries and not eight.** The issue body states eight, and #242 added `gre-erspan-vxlan.pcap/0:65174/JA4T.1` after that body was written. #34 owns six and #215 owns three. **The rule runs in one direction.** It recognizes a citation in 90 of the 128 decided entries. It reads no citation in 38 of them. The 38 are the 34 entries of #138 and 4 of the 5 entries of #151. A FoxIO Rust snapshot settled those by measurement, and no person decided them. The fifth #151 entry cites `#138 decided`. A decided entry that cites nothing is therefore not a defect. **The gate is proven by a failure, and the proof was reverted.** A marker-free entry keyed `253-proof.pcap/0:1234/JA4.1`, whose cause names Changelog round 66, makes `test_the_committed_register_hides_no_decision` fail with `253-proof.pcap/0:1234/JA4.1 names Changelog round 66, and the entry carries no decided marker`. The marker alone clears the failure, which proves the gate reads the marker and not the key. `git checkout` restored the register, and `git status` reports it unchanged. **The first ten cases were written before the check existed, and every one failed.** Self-review added two more. One proves that a denied citation hides no decision the same cause states later. The other proves that the fourth citation form fails an unmarked entry. Eleven of the twelve read a register the case builds, and three of those eleven hold the phrasing of an open entry. **No entry was marked and the register was not edited.** No file under `ja4plus/` changed and no fingerprint moved. `tests/foxio_deviations.json` holds 137 keys before and after, against 137 `xfailed` cases before and after. |
| 77 | 2026-08-08 | #228 landed and an empty JA4X list writes the zero sentinel `000000000000`. **The user decided it on 2026-08-08, and the decision is reversible.** **No file under `ja4plus/` changed, because the project already wrote the decided form.** `ja4plus/fingerprinters/ja4x.py:65` to `:71` hold one guard for each of the three parts, and the round measured that rather than assuming it. **The reason the question needed the user is that `technical_details/JA4X.png` contradicts its own example.** Its two `Sliver, Havoc C2` rows write `000000000000` in part a, and its `Qakbot` row writes `2bab15409345_af684594efb4_e3b0c44298fc`, so the image draws both forms for the same condition. `python3 -c "import hashlib; print(hashlib.sha256(b'').hexdigest()[:12])"` prints `e3b0c44298fc`, reproduced in this worktree, so the Qakbot part c is the truncated SHA-256 of the empty string and not a sentinel. **The decided form carries three FoxIO sources at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`.** `rust/ja4x/src/lib.rs:166` to `:176` return the twelve zeros on an empty input, and the unit test at `:181` asserts `assert_eq!(hash12(""), "000000000000");`. `wireshark/source/packet-ja4.c:590` to `:592` write the same literal for each of the three parts. `README.md` line 146 reads ```JA4X=2bab15409345_af684594efb4_000000000000``` for the same Qakbot certificate, and its first two parts match the image row exactly. **The rejected form carries two**, `python/ja4x.py:87`, which hashes the join with no guard, and the image row. **The project already uses the same sentinel where JA4H signals a request that carries no cookie**, so the ruling is consistent with the package. **No fingerprint moved.** A replay of all 38 committed captures produces 60 JA4X values before and 60 after, every value identical, and 0 `JA4X_r` values before and after, because `ja4plus` publishes no raw form. `tests/foxio_deviations.json` holds 137 keys before and after, against 137 `xfailed` cases, and the round added no register entry, because no local vector reaches the case. **The gate is proven by its reversal**: remove the three guards, so that each part hashes the empty join, and 7 unit cases fail, 4 of them new. **The conformance suite reports no failure under the same reversal**, which is the measurement behind the claim that no local vector reaches R8. **`tests/test_ja4x_empty_ext.py` is the new gate.** It holds 7 cases, it reproduces the arithmetic, it pins the sentinel on part a, part b and part c, and it reads one DER certificate that carries no extension. Two of its cases stop the file measuring nothing: one requires part a and part b to hold a hash on that certificate, so a file of three sentinels cannot pass, and one requires a list of one entry to hash. **One docstring stated the rejected form and it is corrected.** `tests/test_edge_cases.py::TestX509EdgeCases::test_generate_ja4x_empty_lists` read "Empty RDN/extension lists should produce hashes of empty strings" while it asserted the sentinel. **`docs/specs/foxio/JA4X.md` records the contradiction beside R8** rather than hiding it, with both forms, both source sets and the arithmetic, so a later reader does not derive it again. **This issue asked two questions and the user answered one.** D1, the JA4X raw form, moved to #267 and it is `status:needs-feedback`. #228 ruled nothing on it. Coverage holds at 85%, and the unit suite moves from 1358 passed to 1365 passed. |
| 78 | 2026-08-08 | #231 landed and the eleven JA4D and JA4D6 readings follow the image. **The user decided all eleven together on 2026-08-08, on the authority rule: the specification decides intent and schema.** **Every item is latent, and no capture under `tests/foxio_vectors/` separates the two readings of any one**, so the conformance suite proves none of them. `tests/test_ja4d_decisions.py` holds twenty-two cases that build the packets the vector set lacks. Fourteen of the twenty-two fail against the base, and a reversal of each ruling fails at least one case. **This round re-measured the two port premises of the issue table, and one is false.** The table states that the reference applies no port test for either method. That is true of `wireshark/source/packet-ja4.c` and false of the reference as a whole, because the dissector reads fields Wireshark already produced and Wireshark selects a dissector by port. `epan/dissectors/packet-dhcp.c` states `#define DHCP_UDP_PORT_RANGE  "67-68,4011"` and `epan/dissectors/packet-dhcpv6.c` states `#define UDP_PORT_DHCPV6_RANGE      "546-547"`, both at Wireshark 4.4.2. **The pinned FoxIO checkout carries no core Wireshark dissector**, so `.claude/rules/external-apis.md` gains a row for the Wireshark repository and pins it to that release. **D1 therefore adds UDP port 4011, Proxy DHCP, and D7 changes nothing**, because the JA4D6 port set already matched. A reading that removed the test would emit a value the reference does not emit. **This round re-measured all eleven `file:line` references of the issue table, and all eleven are current.** **D2 keeps the present reading**: `wireshark/source/packet-ja4.c:1498` sets the emit flag inside the option 53 block alone, so the dissector and this project agree against `zeek/ja4d/main.zeek:43-45`, two references against one. **D3 reads the name inside option 81 rather than the option**, because the image caption reads `Has a Domain name (d) or No domain (n)` and `wireshark/source/packet-ja4.c:1521` reads `dhcp.fqdn.name`; RFC 4702 puts the name after three bytes. The existing case `tests/test_ja4d.py::TestGenerateJA4D::test_discover_with_fqdn_flag` built an option 81 with no name and it failed, which is the separating packet the suite already held and nobody read. **D4 and D9 keep the first occurrence, and D5 and D10 join every occurrence.** RFC 3396 lets a long option split, and part c holds the list the image names. The dissector concatenates every option 57 into a four-character subfield, which gives eight digits and breaks R2; **that is a defect the schema declines**, and read at the width the image states the dissector's buffer holds the first occurrence, which is also the guard `wireshark/source/packet-ja4.c:1547-1549` applies to the Client DUID length. **D6 replaces a citation no checkout can read.** The comment cited FoxIO pull requests 267 and 270, and R9 of `docs/specs/foxio/JA4D.md` now holds the FoxIO statement; two source-text cases guard it. **D8 adds DHCPv6 option 9, Relay Message**, which carries a whole inner message; a relay message puts its options after 34 bytes rather than 4, and before the ruling part b of the case packet read the link address as options and held `0-0-0-0-0-0-0-0` where it now holds `18-9-1-6`. The round also repairs the comment that named option 17, because Wireshark reports a vendor sub-option under another field name and the table holds no entry for it. **D11 emits the five-digit form for message type 0**, which `wireshark/source/packet-ja4.c:1537-1538` does. **The round also repairs two latent faults that the self-review found in the code it touched.** A crafted relay chain nested one message inside another without bound, and Python raises `RecursionError` near 1000 frames, so `_walk_options` stops at 32 containers; a 1200-level chain returns a value where it raised before. The DHCP option parse also trusted a length byte it read from the packet: `opt_data` came from a slice that shortens silently, so a truncated option 81 gave `d` where it carries no name, and a truncated option 57 or option 53 raised `IndexError`. The parse now stops when an option claims more bytes than the payload holds, which is the guard `_walk_options` already applies to a DHCPv6 option. Three cases cover the three paths and all three fail without the guard. **The five uncertain rules stay uncertain and this round resolves none.** R10, R15, R16, R19 and R20 keep the vector fallback, and #271 holds the two JA4D6 divergences D8 uncovered: the dissector reads `dhcpv6.duid.bytes`, `dhcpv6.iata`, `dhcpv6.client_domain` and `dhcpv6.requested_option_code` at any nesting depth where this project reads them at the top level, and it writes a ten-character subfield 1 for a relay message. **All ten reference values match before the rulings and after them**, measured by a replay of all 38 captures: four JA4D values on `dhcp.pcapng` and six JA4D6 values on `dhcpv6.pcap`, byte for byte unchanged. The unit suite reports 1377 passed on the base and 1399 after, with 8 `xfailed` on both. The conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after. Coverage holds at 85%, and the two modules report 95% and 94%. **`tests/foxio_deviations.json` is untouched**: 137 keys and no key that names either method, before and after, because no reference value moved. |
| 79 | 2026-08-08 | #246 landed and JA4TS writes the value that a RST produces. **Changelog round 67 split this rule from part e, and this round builds it.** The deleted `technical_details/JA4T.md` is the primary source and this round read the blob rather than inferring the text: `b6f3ff4` removed the file on 2024-02-22 and no commit restored it. It reads "the final TCP packet, a RST packet, should be appended to the last JA4TS denoted with 'R' and its delay", and its example ends `65535_2-1-3-1-1-4_65495_8_1-2-4-8-R6`. It states the reason the value reads the stored connection: "Note that RST packets do not contain TCP options or window sizes, as such the program will need to be aware of the previous JA4TS." **Two implementations corroborate the suffix.** `wireshark/source/packet-ja4.c:1608` writes a second `JA4TS` value on the RST packet, and lines 1601 to 1607 restore `window_scale`, `window_size`, `mss_val` and `tcp_options` from the connection. `zeek/ja4t/main.zeek:233` appends `-R%d` to the delay list. Both nest the RST branch inside the delay branch, so a RST on a connection with no delay produces nothing: `wireshark/source/packet-ja4.c:693` reads `rst_time` only when `syn_ack_count > 1`, and `zeek/ja4t/main.zeek:232` reads `rst_ts` only when the delay list holds a value. **Every `file:line` of the issue body and the plan was re-measured, and three were stale.** The dissector tests the RST flag combination at line 1296 and not 1297. The Zeek script stores part a through part d at lines 177 and 178, not 181 and 182, and it reads the timeout at line 162, not 161. **The two implementations disagree twice, so rule 1 settles neither point and this round decided each.** The first is the flag combination. `zeek/ja4t/main.zeek:167` tests `rph$tcp$flags & TH_RST` and `wireshark/source/packet-ja4.c:1296` tests `tcp_flags == 0x004` for equality, so the dissector reads no RST that also carries ACK; the deleted file writes "a RST packet" and names no flag combination, so `ja4plus` reads the bit. The second is the RST that carries no delay. `wireshark/source/packet-ja4.c:1599` writes the `JA4TS` field whenever `syn == 3`, so the dissector emits the four parts a second time; that value repeats the value the SYN-ACK already produced and describes no packet of its own, which `.claude/rules/conformance.md` declines under its second shape, and `zeek/ja4t/main.zeek` writes one JA4TS value for one connection. **`ja4plus` stores part a through part d of the first SYN-ACK**, which `zeek/ja4t/main.zeek:177-178` also reads and never replaces; the dissector overwrites the three scalars on each SYN-ACK and keeps the option list of the first. A retransmission repeats the SYN-ACK, so the two readings agree on real traffic. **No bound moved and no table was added.** The stored parts live in the connection table that round 67 bounded at 1000 entries and two minutes, and `prefixes` holds the same keys as `times`. **No vector reaches the rule, and no value moved.** The 38 captures hold 10 RST packets and none of the 10 sits on a connection with more than one SYN-ACK. A replay of the 38 captures produces 797 values before and 797 after, and 0 differ. `tests/foxio_deviations.json` holds 137 keys before and after, against 137 `xfailed` cases before and after, because no conformance case compares a JA4TS value. The register gains no entry, so the marker gate of #253 stays satisfied. **The capture is built, because none exists.** `tests/build_ja4ts_rst.py` writes the six capture times the deleted file lists, and `tests/test_ja4ts_reset.py` writes the file to a temporary directory and reads it back, the way `tests/test_ja4ssh_message_count.py` reads `tests/build_ssh_retransmission.py`. `.gitignore` commits no capture other than the FoxIO vectors. **The 22 cases were written first and 16 of them failed.** The 6 that passed assert that a RST produces no value, and the unchanged code produced no value at all. **A revert of `ja4plus/fingerprinters/ja4ts.py` fails 17 cases**, and 13 mutations prove that all 22 can fail. Dropping the delay gate fails 1, reading the first SYN-ACK for the RST delay fails 10, storing the parts of the latest SYN-ACK fails 14, the equality test on the flag combination fails 1, dropping the timeout fails 1, keying the connection on the server address alone fails 2, a normalized key fails 3, a crash on an unknown connection fails 1, the empty-string guard of part e fails 1, removing the RST branch fails 13, omitting the suffix fails 10, a `clear` that keeps the parts fails 1, and a `drop` that keeps them fails 2. `docs/specs/foxio/JA4T.md` states the rule as R13, and `Divergence register` gains one row. The decision is reversible. |
| 80 | 2026-08-08 | #255 landed and the owner list of the register records the state the forge reports. **This is the fifteenth recorded instance of a comparison that cannot fail, and it is the first where the check is sound and the data disarms it.** `TestTheRegisterOwners.test_no_entry_names_an_epic_or_a_closed_issue` rejects an undecided entry whose owner is closed. `tests/foxio_deviation_owners.json` recorded #34 as open, #34 closed on 2026-08-07, and the stale record kept the check quiet. **The `retrieved` field detects nothing here.** It read `2026-08-08` before the round and it reads `2026-08-08` after, because #34 closed on the day the file records. **The measurement is worse than the issue body states.** The issue names #34 and #225, and the round measured every owner the file holds. Eleven of the fifteen moved from open to closed, and only #215 stays open. `gh issue list --state all --limit 500 --json number,state,labels,title` is the measurement, and it names 151 issues. **The refresh turns the suite red on six keys and no more.** The register names ten closed owners, and the nine that are not #34 hold decided entries alone. A decided entry records a permanent divergence, so a closed owner is correct for it. The register holds nine undecided entries: the six JA4L entries of #34 and the three JA4T entries of #215. **The six move to #272, which is open and `status:needs-feedback`.** #272 holds the question the user answers, and it decides nothing today. **No entry gains the `decided` marker.** #193 refused that repair and #251 refused it again, and the decided count holds at 128 of 137. **The six causes name #272 in the present tense**, as `#272 decides whether this project declines the deviation`, so the #253 marker gate reads no decision in them. The three #215 causes already hold that form. **The register holds 137 keys before and after, against 137 `xfailed` cases before and after.** Its SHA-256 moves from `c85e73d480df59832c42a666ae9edc11b856a3d19db6ebde9693d3ff11c8d5be` to `b8daf04737c777d1db5d9d3c596e6ec659d6b4ec254c7a0697b8f6f4569fae45`, and 12 lines change: the `issue` field and the cause of six entries. No other entry moves. **The refreshed check is proven by a failure.** `ssh2.pcapng/JA4L-S` pointed at the closed #34 makes three cases fail, and the first message reads `ssh2.pcapng/JA4L-S names the closed issue #34, and the entry is not decided`. `git checkout` restored the register, `git status` reports it unchanged, and the SHA-256 matches. **The new case fails before the repair and passes after it.** `TestTheOpenRegisterEntries::test_every_ja4l_entry_names_the_open_issue_that_holds_the_question` reads the six keys and requires the owner #272. **The module docstring now warns that a refresh turns the suite red whenever an owner closed since the last refresh.** The red suite is the true state, and the repair is to point the entry at an open issue. The owner list grows from 15 owners to 16. #34 stays in it as the record of where the six entries came from. **No file under `ja4plus/` changed and no fingerprint moved.** Coverage holds at 85%, and the unit suite moves from 1377 passed to 1378 passed. |
| 81 | 2026-08-08 | #38 landed and `ja4plus/utils/state_table.py` holds `BoundedStateTable`, the one shape the six unbounded state tables of #179 move onto. **The issue body states a maximum age of 300 seconds and the module ships 600.** The worker re-measured the longest gap between two packets of one connection over all 38 captures under `tests/foxio_vectors/`, in four readings. The directional reading over the packets that carry payload gives **320.714503 seconds**, in `ssh-r.pcap`, between `192.168.1.169:64980` and `192.168.1.197:22`. The three other readings give 320.336760, 320.329879 and 320.283589 seconds, on the same capture and the same connection. Every reading reproduces #170, and all four sit above 300. Three flow keys hold a gap above 300 seconds, and none holds a gap above 600. **A default of 300 seconds therefore evicts state that a committed capture still needs.** A default of 600 seconds evicts none. **The 300 of the issue body is stale, and the 600 of the feature file governs.** `docs/specs/features/03-concurrency-safety.md` line 117 reads "The default maximum age is 600 seconds. A measurement sets it." Line 205 publishes `max_connection_age: float = 600.0` on `Processor`, and line 211 states that the value matches `DEFAULT_MAX_STREAM_AGE`. #170 is that measurement, and #179 corrected the file. The issue body was written before both, and nobody updated its number. **The specification decides intent and schema, so no later reader re-derives this.** The module therefore reads the value the feature file publishes, and `DEFAULT_MAX_CONNECTION_AGE` equals `DEFAULT_MAX_STREAM_AGE`. One project holds one maximum age, and a case asserts that the two numbers match. The correction is a comment on #38. **The measurement is a case, not a note.** A replay of `ssh-r.pcap` at 600 seconds evicts nothing, and the same replay at 300 seconds evicts. Remove the age comparison and both cases fail. **The table evicts on two schedules, and the reason differs.** The count bound removes the least recently used entry on the insert that reaches `max_connections`. A count bound that waits overshoots the count it states. The age bound runs one pass for every 1000 packets, because a pass reads every entry. `on_packet` carries the packet timestamp, and the table reads the wall clock only when a packet carries none. Both clocks read seconds since the epoch, so the two compare. **A read of one key holds that entry, and a pass over the table holds none.** `__getitem__`, `get` and the `in` operator each refresh the entry against both bounds; `keys`, `values`, `items` and iteration refresh none. `__iter__` returns a snapshot, because a read inside a loop moves an entry and an `OrderedDict` refuses that move during its own iteration. **`pop`, `del` and `clear` raise no eviction count.** Each is the caller's removal, and `cleanup_connection` is the caller's way to evict early. **The cases were written first and 27 of 28 failed against an empty stub.** The one that passed reads the capture and not the module. **Twenty mutations of the module were then applied by hand, and the first round left two alive.** No case asserted that the `in` operator holds an entry. No case asserted that a second write to one key holds that entry against the count bound. Three cases now cover both, and **every one of the twenty mutations kills at least one case**. **The memory baseline is measured, and the ceiling stays open.** `features/03-concurrency-safety.md` asks #38 to measure the baseline and to propose the ceiling. #38 measured it. **#279 holds the ceiling, because no measurement forces one.** The measurement forces a floor near 239 MiB, and nothing forces the distance above that floor. That distance is a product judgement, and a ceiling is a number this package states to its users. **No constant names a ceiling and no case asserts one.** An idle `Processor()` with scapy imported holds 91.03 MiB resident. One full table of 10000 entries shaped like `JA4LFingerprinter.connections` holds 10.69 MiB, at 1121 bytes for each entry. The same shape holds 102.36 MiB over 100000 entries, at 1073 bytes for each entry. One full table of 10000 entries shaped like `JA4SSHFingerprinter.connections` holds 51.72 MiB at a 200 packet window. That reading is 5423 bytes for each entry, and it is the largest of the twelve tables. Ten tables at the default bound therefore project 91.03 + 51.72 + 96.21 = **238.96 MiB**. That projection is the floor for the one-million-packet case of #41, and #279 carries the ceiling to the user. The projection holds whatever the connection count is, because the table never passes 10000 entries. 100000 distinct connections therefore reach no more than 10000 entries in one table. **The self-review found one defect and two operations that read against the dictionary they replace.** `keys` returned the live `OrderedDict` view, so `for key in table.keys(): table[key]` raised `RuntimeError: OrderedDict mutated during iteration`. `__iter__` already returned a snapshot and `keys` did not. `keys` now returns a list, and a case covers the loop. `dict(table)` reads each key through `__getitem__`, so it holds every entry against both bounds, and `items` is the pass that holds none. `popitem` removes the least recently used entry, and a dictionary removes the entry it received last. Neither operation reaches a call site today, the class docstring states both, and a case pins each. **The review also read the class against the six tables of #179, and two of them need something else.** `BaseFingerprinter.fingerprints` is a list that `ja4l.py` indexes and `ja4ssh.py` reads at index -1, so a mapping keyed by connection serves neither call site. `JA4DBClient._cache` reads no packet, so nothing calls `on_packet` and its age pass never runs on its own. Its count bound holds, because `__setitem__` applies that bound at once. #39 and #42 own the two answers, and the module docstring records both. **One further reading is recorded and not repaired.** One `on_packet()` call that states no timestamp moves the table from the capture clock to the wall clock. A replay of a capture recorded in the past then ages out whole. That is the rule `features/03-concurrency-safety.md` states, so the module keeps it, and a warning plus a case record the consequence. **No fingerprinter moved and no caller changed.** #39 performs that move. The unit suite rises from 1379 passed to 1416 passed, and 8 `xfailed` holds. The conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after, and `tests/foxio_deviations.json` holds 137 keys before and after. Coverage of the project holds at 85%, and `ja4plus/utils/state_table.py` reaches 100%. |
| 82 | 2026-08-08 | #39 landed and every state table a fingerprinter holds is a `BoundedStateTable`. **The list of six tables that #179 records is stale, and a re-measurement replaces it with thirteen.** A walk of `ja4plus/fingerprinters/` and `ja4plus/utils/` names every mapping that survives across packets, and each one carries a row in `docs/specs/features/03-concurrency-safety.md`. The thirteen are `JA4Fingerprinter._quic_fragments` and `_quic_dcid_to_tuple`; `JA4SFingerprinter._quic_dcids` and `_quic_server_crypto`; `JA4HFingerprinter.consumed_seq` and `unusable_base`; `JA4LFingerprinter.connections` and `grouping_keys`; `JA4XFingerprinter.processed_certs` and `scan_offsets`; `JA4SSHFingerprinter.connections` and `_handshake_clients`; and `SynAckTracker.times`, which JA4TS holds. **Four of the five names the plan lists moved since #179 measured them.** `JA4LFingerprinter.grouping_keys`, `JA4XFingerprinter.scan_offsets`, `JA4SSHFingerprinter._handshake_clients` and `SynAckTracker.times` reach no row of the #179 table, and each is a state table. `SynAckTracker.times` already held both bounds, at 1000 entries and 120 seconds, so the #179 row that omits it also hides a bound. **The issue body names `ja4plus/fingerprinters/ja4.py:299` and the line is stale.** `_quic_fragments` sat at line 351 of the base commit `849f58d`. **Two tables leave, because the state table holds the age of each entry.** `_quic_fragment_seen` and `_quic_server_crypto_seen` each mapped one connection to the time of its last fragment, and `BoundedStateTable` stores that time inside the entry. **`TCPStreamReassembler.streams` stays an `OrderedDict`, and the reason is recorded.** It holds both bounds already, and `add_segment` applies `max_stream_bytes` and `max_stream_segments` to one entry, which no mapping models. **`JA4DBClient._cache` is untouched, because #42 owns it.** **The age pass runs on each packet for five of the thirteen tables, and once per 1000 packets for eight.** A pass that waits for 1000 packets is longer than a 30-second age on a connection that sends few packets, so a table whose age sits below the default states `eviction_interval=1`. The five are `_quic_fragments`, `_quic_dcid_to_tuple`, `_quic_server_crypto`, `times` and `consumed_seq`. The last one keeps the schedule the base code ran, and each of the five holds 1000 entries at most, so the cost of the pass stays flat. **The self-review moved three tables off that schedule.** `processed_certs` reads up to 1000 entries and every TCP packet that carries payload reaches it, and neither `unusable_base` nor `scan_offsets` ran an age pass before this change. Each of the three now holds the default interval. **No fingerprint moved, and the comparison is a replay of every capture.** A replay of all 38 captures under `tests/foxio_vectors/` dumps every per-packet value, every trailing window, every collected entry and every HASSH value: **1626 values before the change and 1626 after, and the two dumps are byte-identical.** The comparison drops one key, `timestamp`, which `_close_window` fills from the wall clock and which describes no fingerprint. **The first comparison was not identical, and it found a real defect.** `BoundedStateTable` orders its keys by the last read, because its count bound evicts the least recently read entry. `JA4SSHFingerprinter.get_hassh_fingerprints` and `close_open_windows` each read that order, so the four HASSH values of `ssh2.pcapng` published in a new order. Each connection now stores an arrival index, `_connection_keys_in_arrival_order` sorts on it, and both readers publish the order the capture opened the connections. **The move of each table is proven by its reversal.** A sweep rewrote one construction site at a time to a plain dictionary that answers `on_packet` and holds no bound, then ran both suites. **Every one of the thirteen reversals fails at least four cases**, and the count by table is: `_quic_fragments` 4, `_quic_dcid_to_tuple` 4, `_quic_dcids` 4, `_quic_server_crypto` 4, `consumed_seq` 5, `unusable_base` 4, `JA4LFingerprinter.connections` 4, `grouping_keys` 4, `processed_certs` 4, `scan_offsets` 4, `JA4SSHFingerprinter.connections` 4, `_handshake_clients` 4, and `times` 7. **The first sweep left four tables with one behaviour case each, and four new cases repair that.** `_quic_dcids`, `unusable_base`, `grouping_keys` and `scan_offsets` each held a count case and no age case, so a reversal killed the two class cases and one count case. Each now holds an age case too. **No reversal fails one case of the conformance suite.** No committed capture reaches a bound, which is the result the age measurement of round 81 predicts. **The first acceptance criterion is measured, and no table passes its bound.** A run of 1000000 packets across 100000 distinct connections, which mixes a TCP handshake, SSH data, an HTTP request, a TLS record and a QUIC Initial packet, reports a peak resident memory of **419.27 MiB** and **zero tables above their bound**. `ja4l.connections` holds 10000 entries against 10000 and counts 390000 evictions, and `ja4x.scan_offsets` holds 50 against 50 and counts 499950. The reading includes the packets the script builds, so it names no library ceiling. **#279 still holds the ceiling, and this issue states none.** A committed case runs the same traffic over 2000 connections and reads all thirteen tables against their bounds. **Each case states its bounds as literal values.** #175 derived a boundary case from the constant it asserted, so raising that constant left the case green. `tests/test_bounded_state_table_adoption.py` holds 43 cases, and its `TABLES` list names the two bounds of each table as numbers. **A case for each stateful method replays a connection whose span passes the maximum age.** The JA4L handshake spans 1000 seconds in gaps of 500, the JA4SSH connection spans 2000 seconds in gaps of 100, and each produces every value it produces today. **One hazard is recorded, and two cases carried it.** A packet that states no timestamp moves a table to the wall clock, and the entries that packets with a timestamp wrote then age out at once. Two cases of `tests/test_ja4s_quic_split_server_hello.py` mixed the two clocks, and each now states a time on every packet. The unit suite rises from 1416 passed to 1459 passed, and 8 `xfailed` holds. The conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after, and `tests/foxio_deviations.json` holds 137 keys before and after. Coverage of the project reports 85% before and after. The count of missed statements holds at 523, and the total falls from 3555 to 3525, because the change removes 30 covered statements of bespoke eviction. |
| 83 | 2026-08-08 | #42 landed and `ja4plus.ja4db.lookup` builds one client, and its lookup cache holds 100000 entries at most. **The three line numbers of the issue body were re-measured, and all three hold on the base commit `849f58d`.** `ja4db.py:132` held `_default_client = None`, `:138` held the unguarded build, and `:68` held `self._cache = {}`. **The guard is a module-level `threading.Lock`, and the function reads the client again inside the lock.** The port guards the same construction with `sync.Once`, at `lookup.go:29-37` of commit `d06dfd9f8b68dd2cb637bc8c2514dc0795cf50cc`. FoxIO specifies nothing here, so parity rule 2 decides, and a lock plus a second read is the Python equivalent. **The port holds no lookup cache at all**, because it reads the whole mapping file into a map that is read-only after load; `ja4plus` keeps its cache, because `JA4DBClient` also reads `ja4db.com`. **The race is forced, and it is not sampled.** The case holds the first caller inside the constructor until the second caller runs, so the second caller is guaranteed to arrive while the first still builds. **The guard is proven by its removal**: the case reports 0 failures out of 100 runs with the guard, and 100 failures out of 100 runs without it, each one `AssertionError: the module built 2 clients`. Each run is one process, because pytest deduplicates a repeated test id and reports 20 identical ids as one run. **The bound is 100000 entries, and `features/07-db-enrichment.md` states it** at line 112 and publishes it at line 134 as the `cache_size` default. **The 10000 of `features/03-concurrency-safety.md` describes a different object.** The `## Terms` table names a state table the dictionary a fingerprinter uses to hold per-connection data, and `JA4DBClient` is no fingerprinter. FR-concurrency-safety-14 and FR-db-enrichment-9 state one requirement, and one number satisfies both. The `## Terms` table gains a `lookup cache` row, because `cache` alone is a rejected synonym of `state table`. **The cache is a `BoundedStateTable`, and the module holds no second bounding mechanism.** The maximum age is 600 seconds, the `DEFAULT_MAX_CONNECTION_AGE` of #38, because one project holds one maximum age. **One lookup announces itself as one arrival, so the lookup cache runs its own age pass.** The module docstring of `ja4plus/utils/state_table.py` recorded that nothing calls `on_packet` for this table and that its age pass never runs, and it now records the call this issue built. **A measurement sets the eviction interval at 100000, and no default sets it.** A pass reads every entry, so 200000 lookups on a full cache of 100000 entries cost 1.91 s at an interval of 1000, 0.36 s at 10000 and 0.19 s at 100000. An interval below the entry count therefore costs more than one entry read for each lookup. **The client reads the smaller of 100000 and its own `cache_size`**, so a client that holds a smaller lookup cache runs its age pass too. A fixed interval of 100000 left `JA4DBClient(cache_size=50)` with an age bound that runs once for every 100000 lookups, and the self-review found it. **The client holds a lock over the cache read and over the cache write, and no thread holds it across `_do_lookup`.** A remote lookup takes up to 5 seconds. Two threads that miss on one fingerprint each read the result, and the second write replaces an equal value. **A cached miss cannot go stale under a live client.** `JA4DBClient` reads the mapping file once, in `__init__`, and no code path reloads it. The `db update` command of #57 writes a file that the next client reads at construction, so a caller that replaces the mapping file builds a new client and receives an empty cache. The age bound also removes a miss that receives no read for 600 seconds. **The two acceptance criteria of #60 hold now.** One case looks 200000 distinct fingerprints up and the cache holds `cache_size`, and one case proves that the cache evicts the least recently used entry. #60 keeps its dependency on #57 for the `source` value, and the user decides whether #60 closes. **The self-review found one more reading, and the read of a hit now holds the entry once.** The `in` operator holds an entry and the read that follows holds it again, so `lookup` reads the entry through `get` against a sentinel value. A cached miss holds None, so no plain default separates a miss from an absent key. **The lock over the lookup cache is proven by its removal too, and its first case measured nothing.** That case gave each of eight threads 2000 distinct fingerprints, and it reported 0 failures out of 30 runs both with the lock and with a no-op in its place. The case now shares one client between eight threads over a lookup cache of two entries that ages every entry out, so an eviction runs beside every read. It reports 0 failures out of 20 runs with the lock, and 17 failures out of 20 runs without it, each one a `KeyError` that a concurrent eviction raises. **The cases were written first, and 11 of the 12 failed against the base commit.** The tenth reads the lazy build, which the base already held. **No fingerprint moved.** A replay of all 38 committed captures produces a byte-identical dump, at sha256 `368fe3d831e6640017e52a56e7af306475b744353fb63a6d38be80b9eb2900ee` before and after. No fingerprinter imports `ja4plus/ja4db.py`, and `ja4plus/cli.py` is its one importer under `ja4plus/`. The unit suite rises from 1416 passed to 1428 passed, and 8 `xfailed` holds. The conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after, and `tests/foxio_deviations.json` holds 137 keys before and after. Coverage of `ja4plus/ja4db.py` rises from 82% to 85%, and the project holds at 85%. |
| 84 | 2026-08-08 | #40 landed and `Processor.__init__` accepts `thread_safe`, which defaults to True. **The lock is one `threading.RLock` for each fingerprinter, and ten fingerprinters hold ten locks.** `BaseFingerprinter.__init__` builds it, and a processor that reads `thread_safe=False` gives every fingerprinter the shared `NULL_LOCK`, an object whose `__enter__` acquires nothing. **The thirteen tables of round 82 were re-measured against the code, and the count holds.** A recursive walk of `Processor()` reports 13 `BoundedStateTable` instances over 7 fingerprinters, plus 2 `TCPStreamReassembler.streams` and 10 `BaseFingerprinter.fingerprints` lists. A first walk reported 12, because it read the attribute name `tracker` and JA4TS names its tracker `syn_ack_times`. **The reentrant lock is required, and a plain lock deadlocks on the first packet.** `Processor.process_packet` holds the lock of a fingerprinter and then calls `process_packet` on that fingerprinter, which holds it again. A run with `threading.Lock` in its place times out on 3 of 3 runs. **The processor holds that lock across the call and across the read of `last_raw` that follows it.** The raw form describes the value the call produced, so a second thread that runs between the two pairs one fingerprint with the raw form of another packet. **Three methods of `BaseFingerprinter` hold no lock, and the measurement decided it.** `add_fingerprint`, `get_fingerprints` and `reset` each run one operation over the value list, and a lock on the three failed 0 of 20 runs with it removed. `ja4l.py` holds the one composite over that list, and its `process_packet` holds the lock. **Every lock this issue ships is proven by its removal, and each removal is one run of `tests/test_thread_safety.py` twenty times.** Every fingerprinter lock at once fails 20 of 20. The processor lock fails 20 of 20. The `process_packet` lock of JA4, JA4S, JA4H, JA4X, JA4SSH and JA4TS each fails 20 of 20, and the JA4L one fails 20 of 20. The JA4L `reset` lock fails 20 of 20. **The first sweep found six locks that nothing measured, and each reported 0 of 20.** Every case then reached the fingerprinter through `Processor.process_packet`, which holds the lock of that fingerprinter across every call, so the case measured the processor and not the fingerprinter. Seven cases now call `process_packet` on the fingerprinter itself and count the threads inside its guarded region, and each reports one thread. **The forced reset case parked on the wrong packet and reported 9 of 20.** It patched `packet_endpoints` and the first call came from the SYN-ACK, which reports `JA4L-S` and reads no stored index. The case now runs the SYN and the SYN-ACK before the patch, and it parks the writer on the ACK, which reads the stored index and writes at that position. It reports 20 of 20. **The first acceptance criterion reads a partition by connection, and a concatenation of captures disproves the literal reading.** Eight threads over five concatenated captures produce a fingerprint set that differs from one thread on 4 of 5 runs, and the cause is the eviction order of the bounded tables, not a race. One capture at a time, with `get_shard_key` sending every packet of one connection to one thread, matches the single-thread list exactly: `latest.pcapng`, `ssh2.pcapng`, `browsers-x509.pcapng` and `http1.pcapng` each match on 3 of 3 runs. **The 60-second criterion runs 2112 rounds and every round matches.** The committed case reads `JA4PLUS_THREAD_SOAK_SECONDS` and defaults to 2.0 seconds, so the unit suite pays two seconds and not sixty. **The criterion that no lock is acquired is measured by a pair, and the control proves the pair can fail.** A case replaces `threading` inside `ja4plus/fingerprinters/base.py` with a namespace whose `RLock` returns an object that raises when a caller acquires it. `Processor(thread_safe=False)` replays 209 packets and raises nothing, and `Processor(thread_safe=True)` raises on the first packet. **A criterion that reads an exception must read the log, because `Processor.process_packet` catches every exception a fingerprinter raises.** The two cases that feed eight threads beside a `reset` read `caplog` for the `DEBUG` record, so neither can pass on a swallowed failure. **The lock costs 1.22% of one single-threaded replay.** A replay of 1963 packets over four captures reports a median of 263.9 ms with `thread_safe=True` and 260.7 ms with `thread_safe=False`, which is 1.63 microseconds for each packet. The default is True, because 1.63 microseconds buys a contract and a caller who shards pays nothing for it. **The lock of a fingerprinter and the module lock of #42 never meet.** No fingerprinter calls `ja4plus.ja4db.lookup`; `ja4ssh.py` holds `lookup_hassh`, which reads a local dictionary, and `ja4plus/cli.py` calls the lookup client after `process_packet` returns. No thread holds one lock while it waits for the other, so the two cannot deadlock. **`SynAckTracker` holds no lock**, because every caller of it runs inside a method of `JA4TSFingerprinter` that holds the lock of that fingerprinter. **No fingerprint moved.** A replay of all 38 committed captures dumps 809 values before the change and 809 after, and the two dumps are byte-identical. The dump holds no wall-clock field, and two runs on the base commit produced identical bytes, which is the reading that proves it. **The brief states the two suite results in the wrong order, and both were re-measured.** The unit suite reports 1472 passed and 8 `xfailed` on the base commit, and the conformance suite reports 1477 passed, 143 skipped and 137 `xfailed`. The unit suite rises to 1494 passed and 8 `xfailed` holds. The conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after, and `tests/foxio_deviations.json` holds 137 keys before and after. Coverage holds at 85%, and the missed statement count falls from 523 of 3539 to 522 of 3581. |
| 85 | 2026-08-08 | #41 landed and `Processor.stats()` returns one `ProcessorStats` for each of the ten methods. **The set of state tables was re-measured, and it is fifteen and not thirteen.** A walk of `Processor()` reports the 13 `BoundedStateTable` instances of round 82, and it reports 2 `TCPStreamReassembler` instances beside them, on `JA4HFingerprinter.reassembler` and `JA4XFingerprinter.reassembler`. Each reassembler holds per-connection data across packets, so the `## Terms` table names each one a state table, and FR-concurrency-safety-11 covers it. Round 82 recorded the reassembler as an `OrderedDict` that keeps its own bounds, and that reading holds; #41 leaves the bounds where they are and adds the counts. **`TCPStreamReassembler` therefore inherits `StateTable`, the new base class both shapes hold.** `BaseFingerprinter.state_tables` finds a table by that type and by no list, because #39 found that a list of tables written in an issue named six where the code held thirteen. The search descends one level, which reaches `SynAckTracker.times` under the name `syn_ack_times.times`. A first walk that read only `vars(fingerprinter)` reported 14 and missed that table. **`TableStats` reports six counts and not two.** FR-concurrency-safety-11 asks for the entry count and FR-concurrency-safety-12 asks for the eviction count; `max_entries`, `inserts`, `removals` and `returned_connections` sit beside them. The four make the report readable and testable: a reader who sees 10000 entries cannot otherwise tell a table that filled once from a table that evicted a million connections, and the six hold the invariant `inserts == entries + evictions + removals`. **A returned connection is counted, and it is distinguished from a first sighting by the memory of the evicted keys.** `StateTable` holds an `OrderedDict` of the keys it evicted and has not seen since, least recent first. A key that arrives and sits in that memory is a return; a key that arrives and does not is a first sighting. **A key the caller removed leaves no memory**, so a connection that returns after `cleanup_connection`, `del`, `pop` or `clear` counts as a first sighting, because the caller asked for that removal and no eviction lost its packets. Nothing that survives across packets grows without a limit, so the memory holds the entry bound of its own table. **The memory costs 8.3 MiB at its worst, and the cost was measured.** The fifteen tables hold 46400 remembered keys between them, and only 4 of the 15 carry the 10000 default: `ja4s._quic_dcids`, `ja4l.connections`, `ja4l.grouping_keys` and `ja4ssh.connections`. `tracemalloc` reports 187 bytes for one remembered key in an `OrderedDict`. **The counters cost 1.8% of one single-threaded replay.** A replay of 1963 packets over four captures reports a median of 272.8 ms with the counters and 267.9 ms with each counter replaced by a no-op, which is 2.50 microseconds for each packet. **The order of the three steps of an insert was decided by a failing case.** A first form read the memory inside the counter call, after the eviction pass, and `test_a_stream_that_returns_after_an_eviction_counts_one` reported 0 where it expects 1 on a table of one entry: the eviction that the arrival drove had already pushed the arriving key out of a one-key memory. A table now calls `take_evicted_key` before it evicts, then stores the entry, then counts the insert. **The packet count lives on the processor and not on a fingerprinter.** `Processor.process_packet` raises it inside the lock of that fingerprinter, and it counts a packet the method ignores, because the method still received it. `Processor.reset` returns every count to zero, because a reset drops the tables the counts describe. **`stats()` holds the lock of one fingerprinter across the read of that fingerprinter, and it holds two locks never.** **The guard is proven by its removal, and the case forces the race rather than sampling it.** `BoundedStateTable.__setitem__` stores the entry and counts the insert after that store, so a reader between the two sees `inserts == entries + evictions + removals - 1`. `tests/test_processor_stats.py` holds a writer inside that window with a `threading.Event`, then reads the statistics from a second thread. With the guard the reader returns early 0 times out of 20 and reports a broken snapshot 0 times out of 20. With the guard removed, through `Processor(thread_safe=False)`, the reader returns early 20 times out of 20 and reports a broken snapshot 20 times out of 20. **No fingerprint moved.** A replay of all 38 committed captures dumps 809 values before the change and 809 after, and the two dumps are byte-identical. The dump holds no wall-clock field, and two runs on the base commit `0d3480e` produced identical bytes, which is the reading that proves it. **The register is untouched.** `tests/foxio_deviations.json` holds 137 keys before and after, and the conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after. The unit suite rises from 1494 passed to 1531 passed, and 8 `xfailed` holds. Coverage rises from 85% to 86%, at 522 missed statements of 3676. **The tests followed the reporting code rather than preceding it, and the practice is only partly met.** The eviction, return and guard cases came after the surface they measure. Two of them then failed against that surface and moved it, and the removal proof above is the evidence that they measure something. |
| 86 | 2026-08-08 | #43 landed and it closes Epic 3. It adds `tests/test_memory_bounds.py`, adds `TestTheConcurrencyContract` to `tests/test_thread_safety.py`, and states the contract in `README.md` and `docs/api_reference.md`. **The premise of round 84 is disproven.** Round 84 records that eight threads over five concatenated captures differ from one thread on 4 of 5 runs, and it names the entry count bound as the cause. A re-measurement over the same 2047 packets of `latest.pcapng`, `ssh2.pcapng`, `browsers-x509.pcapng`, `http1.pcapng` and `tls-sni.pcapng` reads **5 of 5**, and it names the age bound instead. **Four readings separate the two bounds: both bounds as shipped 5 of 5, the entry count bound raised 4 of 5, the age bound raised 0 of 5, both raised 0 of 5.** The third reading still evicts 168 entries of `JA4XFingerprinter.scan_offsets` on its entry count while the two value lists stay equal, so the entry count bound causes none of the difference. **The mechanism is the clock of the concatenation, and it is no race.** The five captures were recorded across 183 days, so the concatenation jumps the packet timestamp by months, and `BoundedStateTable.evict_aged` measures an age against the timestamp of the most recent packet. A concatenation moved onto one timeline reads **0 of 5** and produces 310 values where the jumping concatenation produces 309. **One thread loses that value too**, which is the reading that removes the race: the committed case runs on one thread. **Every reading above was taken twice**, once against `0d3480e` and again against `0e377b0` after #41 sub-merged, and the conclusion holds on both. **Clause 1 of the contract holds on every capture.** Eight threads that route on `Processor.get_shard_key` match one thread on 3 of 3 runs for each of the five captures, which reproduces round 84. **Clause 1 needs no lock, and this is why `thread_safe=False` exists.** Every fingerprinter lock removed fails **0 of 20** runs of `TestTheConcurrencyContract`, and the lock of `Processor.process_packet` removed fails 1 of 20, because a thread that owns whole connections touches keys no other thread touches. The locks guard the caller who shares one processor without that arrangement, and round 84 records 20 of 20 for those cases. **The two removals that do fail name what the class measures**: the direction sort of `get_shard_key` fails 20 of 20 with 4 of 7 cases, and the age pass fails 20 of 20 with 1 of 7. **The first sweep found one case shape that could not fail.** The memory file held no case that measured the age bound, and removing the age pass failed 0 of 3 runs. `TestTheAgeBoundFreesAnIdleConnection` repairs it, and it holds a control that replays one connection in steps below the age. **Five removals now fail the memory file**: the entry count eviction 3 of 3 with 8 of 20 cases, the age pass 3 of 3 with 1 of 20, the `max_streams` eviction of `TCPStreamReassembler.add_segment` 3 of 3 with 4 of 20, the returned connection count of `StateTable.count_insert` 3 of 3, and the nested descent of `BaseFingerprinter.state_tables` 3 of 3. **A walk replaces a hardcoded table list.** `test_the_processor_holds_no_mapping_without_a_bound` walks a live `Processor()` and fails on any mapping that no `StateTable` bounds, so a table a later change adds cannot pass unread. **That case earned its keep during this issue.** An earlier form named the two stream tables in an allowlist, and the rebase onto #41 failed it on `_evicted_keys`, a mapping that is in fact bounded. The rule now reads the type of the holder rather than a name. **The table count was re-measured and it is fifteen**, which matches round 85: `Processor.stats()` reports 15 tables across 10 methods, and the walk of the memory file reads 15 by an independent route. `test_the_walk_of_this_file_agrees_with_the_report_of_the_processor` compares the two, so a change that hides a table from one of them fails. **The suite states no memory ceiling.** #279 owns the number, and round 81 records that the measurement forces a floor and forces no ceiling. The file proves the bound through entry counts instead: a second flood of 400 connections raises the entry count by zero while the eviction count rises. **No fingerprint moved, and no file under `ja4plus/` changed.** This issue adds tests and prose only, and `git diff -- ja4plus/` is empty, so the replay of the 38 captures needs no reading. **The unit suite reports 1532 passed on the base commit `0e377b0`, and round 85 records 1531.** The reading here excludes the 20 cases of `tests/test_memory_bounds.py` and the 7 of `TestTheConcurrencyContract`, and it repeats. The suite rises to 1559 passed with this change, which is the 27 cases this issue adds, and 8 `xfailed` holds. The default run rises from 24.8 seconds to 30.1 seconds. The conformance suite reports 1477 passed, 143 skipped and 137 `xfailed` before and after, and `tests/foxio_deviations.json` holds 137 keys before and after. Coverage holds at 86%, and the count of missed statements falls from 522 of 3676 to 520 of 3676. |
| 87 | 2026-08-08 | #285 landed and `dev` merges into `epic/14-concurrency-and-resource-safety`. **Two live integration branches cannot see each other, and the merge orphaned a state table.** #246 added `SynAckTracker.prefixes` on `dev`, where `_expire` and `_evict` bounded it through `drop(key)`. #39 replaced `SynAckTracker.times` with a `BoundedStateTable`, which evicts inside itself and called no caller hook, so the merge left `prefixes` with nothing that removes a key `times` evicted. The merge commit `fe83bbb` records the defect: `tests/test_memory_bounds.py::TestTheProcessorHoldsNoUnboundedStateTable::test_the_processor_holds_no_mapping_without_a_bound` reads `AssertionError: these mappings hold no bound: ['ja4ts.syn_ack_times.prefixes']`, and `tests/test_ja4ts_reset.py::TestTheResetBounds::test_the_stored_parts_hold_one_entry_for_each_tracked_connection` reads `AssertionError: 1100 not less than or equal to 1000`. **The repair adds an eviction hook to `BoundedStateTable`**, which is a change to the structure #38 built. `SynAckTracker.prefixes` is now the fourteenth `BoundedStateTable`, and the hook of `times` removes from it every key `times` evicts. Three other shapes were read and declined. A second table that holds its own bounds alone loses lockstep, because a read moves the least recently used order of one table and not of the other. One table that holds a pair keeps lockstep at no cost and reports fifteen tables rather than sixteen, so `Processor.stats()` would report no prefix table. A sweep that compares the two tables on each packet costs the entry count on each packet. **The table count rises from 15 to 16, re-measured and not assumed**: `Processor.stats()` reports 16 across 10 methods, and the walk of `tests/test_memory_bounds.py` reads 16 by an independent route. The hook is proven by its removal, which fails five cases and reads `KeyError: ('10.0.0.2', 443, '10.0.0.1', 50000)` on the RST value of #246. A replay of the 38 committed captures produces 797 values on `dev` and 797 after the repair, and every value matches; the record holds no timestamp, so it holds no wall-clock field. `tests/foxio_deviations.json` holds 137 keys against 137 `xfailed`, and neither moved. |
| 88 | 2026-08-08 | #276 landed and every citation of the Zeek part e gate names the measured line. **Line 227 holds no gate.** At the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, `zeek/ja4t/main.zeek:227` reads `c$conn$ja4ts += fmt("%d", c$fp$ja4t$synack_opts$window_scale);`. That line appends the window scale. A reader who opens it finds no evidence for the rule the citation carries, and `.claude/rules/conformance.md` reads a `file:line` as evidence. `zeek/ja4t/main.zeek:229` opens the `if` block that tests the length of `c$fp$ja4t$synack_delays`, and that line holds the gate. Line 231 appends the delay list. **The issue body names two stale citations and this round measures six.** `docs/specs/foxio/JA4T.md` holds two, one in R2 and one in R12. `docs/specs/spec.md` holds the part e row of the `Divergence register` and the Changelog round 67 row. `docs/specs/foxio/deleted-text-specifications.md` holds one, and `docs/implementation_notes.md` holds one. **The issue body gives the range as 229 to 236, and the measurement gives 229 to 235.** Line 235 closes the delay block that line 229 opens. Line 236 closes the JA4TS block that line 213 opens, so the range `229-236` crosses one brace of an enclosing block. Every range citation now reads `229-235`, and every single-line citation now reads `229`. **The reading does not move, and this round corrects the evidence rather than the rule.** The Zeek script appends part e only when the delay list holds a value, which is the statement round 67 records. No fingerprint moves, because no file under `ja4plus/` changes. `tests/foxio_deviations.json` holds 137 keys against 137 `xfailed`, and neither moved. |
| 89 | 2026-08-08 | #46 landed and `mypy --strict ja4plus/` reports no error. **The base commit reported 720 errors in 25 of the 28 source files, and the count is now zero.** That count is this issue's red-to-green measurement. An annotation carries no test of its own, so the error count is the only observable the change moves. The breakdown of the 720 was 437 `no-untyped-call`, 274 `no-untyped-def`, 8 `var-annotated` and 1 `type-arg`; the first group is a consequence of the second, because a call reads as untyped until the function it names carries annotations. **The package uses one annotation form.** `ja4plus/types.py` chose `from __future__ import annotations` with the `X | None` form in #44, and every module now follows it, so the Python 3.9 floor holds and no module writes `Optional`. **No annotation changes a value.** Both suites report the counts the base commit reported: 1673 passed, 8 xfailed and 93 subtests passed for the unit suite, and 1477 passed, 143 skipped and 137 xfailed for the conformance suite. Where mypy reported `Returning Any`, the fix annotates a local and returns it, because a converter such as `int(...)` around a scapy field would change a value. **`scapy` needed no new configuration and `cryptography` needed none either.** The `follow_imports = "skip"` override that Epic 0 wrote for `scapy.*` already covers `--strict`, and `cryptography` ships its own type information, so no call site holds a cast. **The change adds three type aliases and five `# type: ignore` comments, and every ignore names its error code and its reason.** `BaseFingerprinter.Lock` names the guard, because typeshed publishes no name for the object `threading.RLock` returns and every call site uses `with` alone. `ja4db.LookupResult` and `cli.ResultRow` name two shapes the code already built. **The gate command changed, so three files that state it changed with it.** `.github/workflows/test.yml` now runs `mypy --strict ja4plus/`, `CLAUDE.md` states the same command, and `[tool.mypy]` of `pyproject.toml` holds `strict = true` with `files = ["ja4plus"]` so an editor reports what the gate reports. **`ProcessorStats` stays in `ja4plus/processor.py`.** `docs/specs/features/04-typed-api.md` lists it under `ja4plus/types.py`, and neither #46 nor #47 names the move, so this issue annotates it where it sits and leaves the move to the issue that names it. **`docs/specs/features/00-foundation.md` is unchanged.** Every sentence it holds about `mypy` describes Epic 0 and stays true, and FR-foundation-9 asks that `mypy` run on every pull request, which the strict command satisfies. |
| 90 | 2026-08-08 | Epic 4 shipped and version 1.0.0 has a typed public interface. #44 added the frozen `FingerprintResult`, which supports item access by field name and emits a `DeprecationWarning` on it, so code written against the dictionary keeps working for one major version. #45 made `Processor.process_packet` return those results and added `process_packet_with_errors`, which returns the parse failures the processor collected. **#45 records one decision that costs the caller something.** A returned exception carries a `__traceback__` whose frames hold the packet as a local, so a program that keeps the errors it reads holds every packet it read. That breaks the rule that no code holds a reference to a packet object after `process_packet` returns. **The returned errors carry no traceback.** The type, the message and the `__cause__` and `__context__` chain are preserved, and the stack is not. #46 turned on `mypy --strict` for `ja4plus/`, which reported 720 errors in 25 files and now reports none. The gate command changed with it. #47 shipped the `py.typed` marker as package data and declared `__all__` with 25 names. **A name in `__all__` is the interface this project promises and it does not change before version 2.0.0. A name absent from it is not promised.** `ProcessorStats` is public at `ja4plus.processor.ProcessorStats` and the class moves nowhere; `docs/specs/features/04-typed-api.md` lists it under `ja4plus/types.py`, and no issue of this epic named the move. No fingerprint moves in this epic. The conformance suite reports the same 1477 passed, 143 skipped and 137 xfailed at every member. |
| 91 | 2026-08-08 | #267 landed and `ja4plus` publishes `JA4X_r`. **The user decided the form on 2026-08-08, and the decision is reversible.** The value holds the three unhashed lists of the JA4X fingerprint, joined with `_`. Two of the three FoxIO implementations publish it: `rust/ja4x/src/lib.rs` writes `let ja4x_r = with_raw.then(|| parts.join("_"));`, and `wireshark/source/packet-ja4.c:1726` registers `ja4.ja4x_r`. The FoxIO Python implementation writes none, so parity rule 1 decides the form. R11 of `docs/specs/foxio/JA4X.md` holds the transcription, and D1 of that page closes. **No JA4X value moves.** The conformance suite reports 1477 passed, 143 skipped and 137 xfailed before, and 1529 passed, 143 skipped and 137 xfailed after. The register holds 137 keys before and after. **The new value is compared, and the comparison is proven by its reversal.** No expected-output file under `tests/foxio_vectors/` holds a `JA4X_r` key, and no field of a Rust snapshot holds one either, because the FoxIO Rust implementation writes `ja4x_r` only when the caller asks for it. The same function hashes the three parts for `ja4x` and joins the same three parts for `ja4x_r`, so the raw form is the preimage of the snapshot value. `TestTheJa4xRawFormTheRustSnapshotImplies` in `tests/test_foxio_rust_parity.py` hashes each part of each produced raw form and reads the snapshot value: 43 raw forms over 19 streams of 5 captures, 43 exact, 0 differ, 0 streams whose count differs. **Three mutations of `generate_ja4x_raw` each make the conformance suite fail**, measured on 2026-08-08: a `|` join of the three parts fails 46 cases, a `;` join of the issuer list fails 44, and a sort of the extension list fails 43. Two further checks measure the same two directions inside the suite, so a reader who deletes the mutation record still holds the measurement. **The empty list writes an empty part and not the zero sentinel**, because `hash12` of the FoxIO Rust implementation runs on the hashed form alone; R8 keeps `000000000000` for the hashed form. **A JA4X result holds one raw value under two keys**, because R10 sorts no list, which is the shape JA4S already holds. `read_certificate` is now the reader of one certificate, and `fingerprint_certificate` returns its first value, so one parse serves both forms. The output schema version is #50 and this issue does not write it. |
| 92 | 2026-08-08 | #271 landed and every part of JA4D6 reads the DHCPv6 options at any nesting depth. **The user decided the three readings on 2026-08-08, and each is reversible.** **D1 and D2 follow the dissector.** `wireshark/source/packet-ja4.c:967-969` calls `proto_all_finfos(tree)` and walks every field of the whole dissection tree. The fields `dhcpv6.duid.bytes`, `dhcpv6.iata`, `dhcpv6.client_domain` and `dhcpv6.requested_option_code` each reach an inner relay message. #231 had already made part b recurse into DHCPv6 option 9, so all three parts now read the same way. **D3 declines the dissector.** `wireshark/source/packet-ja4.c:1537-1546` appends a five-character name for every `dhcpv6.msgtype` field. A relay message therefore gives it `rlayfsolct` and a part a of sixteen characters. R2 of `docs/specs/foxio/JA4D.md` gives part a eleven characters, and the divergence register holds the row. **JA4D6 rests on one source and this round adds no second one.** No FoxIO implementation other than the Wireshark dissector writes a JA4D6 value. R16, R19 and R20 therefore gain a settled nesting depth, keep the `uncertain` mark and keep the vector fallback. `docs/specs/foxio/README.md` now states that a decision settles a reading and clears no mark. **No reference value moves.** `tests/foxio_vectors/dhcpv6.pcap` carries no relay message. The conformance suite reports 1529 passed, 143 skipped and 137 `xfailed` before the change and after it. `tests/foxio_deviations.json` holds 137 keys before and after. **One walk now reads all six inputs.** `_read_subfield_option` holds the four subfield reads that a second top-level loop held before, and `_walk_options` calls it at every depth. D9 and D10 keep their rulings, because the walk reads the outer option before the inner one. **The conformance suite measures none of the three readings, so each reversal is the evidence.** `tests/test_ja4d_decisions.py` gains 7 cases, and the three reversals fail 3, 2 and 5 of them. No reversal fails one case of the conformance suite. **Two of the 7 cases pass against the base, and each guards a reading the merged walk could otherwise lose.** The outer Client DUID length wins over the inner one, and part a holds eleven characters. The unit suite rises from 1652 passed to 1659 passed, and 8 `xfailed` holds. Coverage of the project holds at 86%, and the count of missed statements falls from 519 of 3755 to 516 of 3742. Coverage of `ja4plus/fingerprinters/ja4d6.py` rises from 94% to 96%. |
| 93 | 2026-08-08 | #215 landed and JA4T and JA4TS write the form the user decided. **The user ruled on D1, D2 and D4 on 2026-08-08, after #216 and #242 built the conformance evidence, and D3 and D5 ride with them as plain defects.** **D1 — the two-digit form.** An empty option list writes `00`, part c writes two digits, and a window scale of zero writes `00`. `wireshark/source/packet-ja4.c:664` and `zeek/ja4t/main.zeek:195-211` write that form, and `rust/ja4/src/tcp.rs` writes one digit, so two of the three FoxIO forms agree. The deleted `technical_details/JA4T.md` states the rule in prose and corroborates them a third time: it reads "If any field does not exist, then the output is 00." and gives `JA4T = 1024_00_00_00`. **The prose and the two implementations part on one input, and the worker measured it rather than assuming.** The prose reads `00` as the mark of an absent field, and both implementations test the value, because each stores the window scale as an integer that defaults to zero. A packet that carries a Window Scale option of value 0 therefore writes `00` in both implementations and `0` under the prose alone, and 7 of the 12 moved values are of that shape. `ja4plus` follows the two executable references the user cited, and `docs/specs/foxio/JA4T.md` R11 records the reading. **The cost of D1 was measured before the change, and the project manager's premise that it might reach one capture was wrong.** 12 values across 6 captures take the new form, and all 12 diverge from the FoxIO Rust implementation: 5 carry an empty option list and the other 7 move only because the window scale is zero. Only 1 of the 12 reaches a FoxIO reference comparison, and it is `gre-erspan-vxlan.pcap/0:65174/JA4T.1`, which stays in the register as a decided divergence and keeps `xfail(strict=True)`. **D2 — the reader takes the raw TCP option bytes.** Scapy reports one `EOL` entry for several `0x00` pad bytes, and `rust/ja4/src/tcp.rs` and `wireshark/source/packet-ja4.c:1458` count one entry for each byte. 16 values move to the reference form, and `chrome-cloudflare-quic-with-secrets.pcapng` moves from `65535_2-1-3-1-1-8-4-0_1440_6` to `65535_2-1-3-1-1-8-4-0-0_1440_6`. **D3 — part b holds every option kind.** The loop named six kinds and dropped the rest, and both references append every kind. No vector carries such an option, so a constructed case holds the reading. **D4 — one connection produces one JA4T value.** `ssh2.pcapng` falls from 44 values to 19, which is the count the Rust snapshot holds, and the distinct values do not move. **D5 — a repeated option keeps the first value.** No vector carries a repeated option, so a constructed case holds the reading. **The new connection table carries both bounds**, 10000 entries and 600 seconds, which `connections` in `ja4plus/fingerprinters/ja4ssh.py` already held. The processor now walks 17 state tables and 15 of them are a `BoundedStateTable`. **One reader serves both methods.** `ja4plus/utils/tcp_options.py` holds part a through part d, so `ja4ts.py` no longer repeats the body of `ja4t.py` and one repair covers D1 through D5 on both. **The whole vector set was replayed before and after.** The 38 captures produce 809 values before and 784 after, and 28 differ. Every one of the 28 is a JA4T value or a JA4TS value, and no value of another method moved. The 25 values the count loses are the duplicate JA4T values of `ssh2.pcapng` that D4 stops. **The register falls from 137 keys to 135**, against 135 `xfailed` cases, because the D2 entry and the D4 entry resolve to a pass and leave the register. The undecided entries fall from nine to six, and the six are the JA4L entries #272 owns. **Each ruling is proven by its reversal.** The `Divergence register` gains one row, for D1 alone, because D2, D3, D4 and D5 each move `ja4plus` towards the FoxIO references and depart from none of them. `docs/specs/foxio/JA4T.md` records the readings, and #246 keeps the RST value it built. The decision is reversible. |
| 97 | 2026-08-08 | #294 landed and the three X.509 handlers name the errors they expect. **Each handler wrote `except (ValueError, TypeError, Exception) as e:`, and `Exception` is a superclass of the other two names.** Each one therefore caught every error while it read as a narrow catch, which is worse than a plain bare catch, because a reader who checks the rule sees two named errors and moves on. `CLAUDE.md` and `.claude/rules/conformance.md` each state the rule: a fingerprinter catches the parse errors it expects, and it catches no bare `Exception`. **The line numbers of the issue body predate several merges, and this round measured the three sites.** They are `ja4plus/fingerprinters/ja4x.py:460`, `ja4plus/fingerprinters/ja4x.py:490` and `ja4plus/utils/x509_utils.py:267`. **The `cryptography` documentation names the errors, and this round read it rather than the code.** `load_der_x509_certificate` raises `ValueError`. `Certificate.extensions` raises `DuplicateExtension` and `UnsupportedGeneralNameType`. `Certificate.version` raises `InvalidVersion`. Verified against: https://cryptography.io/en/latest/x509/reference/ (`cryptography` 50.0.0, retrieved 2026-08-08). **None of the three `cryptography` errors inherits `ValueError`**, which a read of the class hierarchy confirms, so a list of `ValueError` alone would drop them. **`JA4XFingerprinter.get_cert_details` names `ValueError`, `DuplicateExtension` and `UnsupportedGeneralNameType`.** **`JA4XFingerprinter.read_certificate` names `ValueError` and `TypeError`**, because `bytes()` raises `TypeError` for input that is no byte string and `load_der_x509_certificate` raises it for the same input. `get_cert_details` returns nothing rather than raise, and `generate_ja4x` and `generate_ja4x_raw` each hold their own errors. **The direct parse of `extract_certificate_info` names `ValueError` and all three `cryptography` errors**, because the `get_cert_details` of that module holds `ValueError`, `TypeError` and `AttributeError` and lets the three pass. **No wide catch is load-bearing, and the vector set is the evidence.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it. `tests/foxio_deviations.json` holds 135 keys against 135 xfailed cases, before and after. The 43 `JA4X_r` cases and the 105 JA4X cases of `tests/test_foxio_rust_parity.py` pass. **The rule that a parser returns nothing and does not raise still holds**, and five hostile inputs measure it: an empty string, a text string, a truncated DER header, 5000 zero bytes and 200 `0xff` bytes. **The 18 cases were written first and three of them failed**, one for each site. Each of the three sends an error the narrow list omits to one handler and requires the handler to hold it no longer. **The three repairs are proven by their reversal, and the reversal was proven live.** Putting `Exception` back into one tuple fails the one case of that site, three times of three. Following #51, `inspect.getsource` of the loaded function in a fresh interpreter reports the mutation present before each run and absent after the restore, and `PYTHONDONTWRITEBYTECODE=1` with a cleared `__pycache__` removes the stale bytecode that #50 met. **Coverage rises.** The project holds 87% before and 88% after, and the missed statements fall from 516 of 3909 to 483 of 3909. `ja4plus/fingerprinters/ja4x.py` rises from 93% to 94%, and `ja4plus/utils/x509_utils.py` rises from 18% to 34%. The unit suite rises from 1760 passed to 1778 passed, and 8 xfailed holds. **One defect is found and not repaired.** `extract_certificate_info` imports `x509` inside a branch, so `ja4plus/utils/x509_utils.py:277` raises `UnboundLocalError` for every packet that carries a certificate the reader found, and the `except Exception` of that function hides it. #309 records the measurement. No file that a fingerprint reads changed its output. |
| 98 | 2026-08-08 | #309 landed and `extract_certificate_info` reads the module `x509`. **The line numbers of round TBD above are stale, because the #294 merge moved them, and this round measured the site.** On the base commit `74de3d7`, `ja4plus/utils/x509_utils.py:262` and `:263` held `from cryptography import x509` and `from cryptography.hazmat.backends import default_backend`, inside the branch that a payload of more than 100 bytes enters. **A branch-local import makes the name a local of the whole function.** The parse at `ja4plus/utils/x509_utils.py:285` therefore raised `UnboundLocalError` for every packet whose payload carried a certificate the reader found. **The failure is on the path that succeeds, and not on a failure path.** The `except Exception` below that parse returned nothing and wrote no log record, so the function reported "no certificate" for the one input it exists to read. **The defect is measured and not asserted.** The verbose form of the unrepaired function writes `Direct certificate parsing failed: cannot access local variable 'x509' where it is not associated with a value` for a TLS Certificate record that carries one self-signed certificate. **The module imports both names at line 14 and line 15, so the branch-local import bought nothing**, and the repair deletes the two lines and states the reason above the module import. **The repair is proven by its reversal, and the reversal was proven live.** Putting the two lines back fails `test_the_packet_reader_returns_the_details_of_a_certificate_it_found`, three times of three. Following #51 and #294, `inspect.getsource` of the loaded function reports the branch-local import present after each reversal and absent after each restore, with `PYTHONDONTWRITEBYTECODE=1` and a cleared `__pycache__`. **The second question of the issue is open, and it belongs to the user.** No caller inside `ja4plus/` calls `extract_certificate_info`, and no case reached its certificate path before this round, which is why the defect survived a green suite. That is the sixteenth recorded instance of a comparison that is never made. **`__all__` of `ja4plus/__init__.py` names 25 public names for version 1.0.0, and `extract_certificate_info` is not one of them.** `docs/api_reference.md:540` documents it under `ja4plus.utils.x509_utils`, so the two readers disagree, and the project manager takes the removal question to the user. **The repair is safe under either answer, and the removal is not.** **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against 135 xfailed cases. **Coverage rises.** The project holds 88% before and 89% after, and `ja4plus/utils/x509_utils.py` rises from 34% to 54%. The unit suite rises from 1778 passed to 1781 passed, and 8 xfailed holds. **Two wide handlers remain in that function and this round left both.** `except Exception` reads the parse and the whole body, and #294 narrowed neither, because each one wrote the plain form rather than the deceptive form. |
| 99 | 2026-08-08 | #314 landed and four X.509 helpers leave `ja4plus/utils/x509_utils.py`. **The user decided on 2026-08-08 that `extract_certificate_info` leaves the package before version 1.0.0, together with the sibling helpers no caller uses.** #309 returned the question and reported three siblings. **This round re-measured the list rather than trust the count, and it confirms three.** One grep of the whole repository, excluding `.git/`, `.venv/` and `ja4plus.egg-info/`, reports every reference to each of the seven names the module held. `get_certificate_issuer` and `get_certificate_subject` each hold one reference, their own definition, so no caller reads either. `get_name_attribute` holds four references: its own definition and three calls, and all three sit inside the two functions above. **The three form a closed group that nothing outside reaches**, which is why the count is three and not two. `extract_certificate_info` holds its definition, two cases of `tests/test_x509_certificate_info.py`, five cases of `tests/test_ja4x_named_exceptions.py`, and `docs/api_reference.md:540`. **No caller inside `ja4plus/` reads any of the four.** **Three helpers stay, and each one holds a caller.** `oid_to_hex` holds callers in `ja4plus/fingerprinters/ja4x.py` and in three test files. `extract_certificate_from_bytes` holds a caller in `tests/test_x509_certificate_reader.py`, and the issue body names it a survivor. `get_cert_details` held two callers inside `extract_certificate_info` at the time of the measurement, so the removal rule of the issue does not reach it, and `docs/api_reference.md:538` documents it. **A reader should know that `get_cert_details` now holds no caller at all**, and a decision on it belongs to the user rather than to this round. **`__all__` of `ja4plus/__init__.py` names 25 entries before the change and after it, and the 25 are unique.** An import of the package, followed by a `getattr` of each of the 25 names, reports no missing name. **`docs/api_reference.md` was checked in both directions.** It documented one of the four removed names, at line 540, and that row is gone. It documents `oid_to_hex`, `get_cert_details` and `extract_certificate_from_bytes`, and the package still holds all three. **Two plain `except Exception` handlers leave with the function.** #294 narrowed neither, because it targeted the deceptive `(ValueError, TypeError, Exception)` form and each of these wrote the plain form. **Two plain `except Exception` handlers survive in the module, and this round left both.** They sit at `ja4plus/utils/x509_utils.py:148` and `ja4plus/utils/x509_utils.py:154`, inside `extract_certificate_from_bytes`. The outer one holds the whole body, and a narrow list there changes what the reader raises for hostile input, which needs its own cases and its own issue. **The second copy of the branch-local import shape is repaired.** `extract_certificate_from_bytes` imported `x509` and `default_backend` inside the ASN.1 branch, and the module imports both names already, so the two lines bought nothing. **The shape is latent here, and no case turns it red.** The block that imports the two names also reads them, so the read follows the import for every input, and the repair therefore changes no behaviour. **The path carried no case at all, which is the defect the module has now paid for twice**, so this round covers it. `test_the_asn1_reader_finds_a_certificate_that_no_record_carries` builds one certificate that the record reader rejects, and it requires the ASN.1 reader to return that certificate. The fixture builds certificates until the record reader rejects one, because a certificate body carries the byte pair the record reader scans for by chance. Six runs of the file report 4 passed. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against 135 xfailed cases. **Coverage rises, as the removal of untested code should.** The project holds 88% before and 90% after, and the missed statements fall from 463 of 3904 to 402 of 3842. `ja4plus/utils/x509_utils.py` rises from 44% to 66%. **The unit suite falls from 1781 passed to 1776 passed, and 8 xfailed holds.** Eight cases leave with the function they measured, two of `tests/test_x509_certificate_info.py` and six of `tests/test_ja4x_named_exceptions.py`, and three new cases cover the ASN.1 reader. `tests/test_x509_certificate_info.py` is now `tests/test_x509_certificate_reader.py`, because the function it named is gone and it measures `extract_certificate_from_bytes`. **`mypy --strict ja4plus/` reports no issue in 29 source files**, which is the reader that catches a reference to a removed name. `binascii`, `hashlib` and `ExtensionOID` stay unread in the module, because they were unread before this round and #297 owns the F401 findings. |
| 100 | 2026-08-08 | #316 landed and the two handlers of `extract_certificate_from_bytes` name the errors they expect. **#294 narrowed neither, because it targeted the deceptive `(ValueError, TypeError, Exception)` form and each of these wrote the plain form.** **The `cryptography` documentation names one error, and this round read it rather than the code.** `load_der_x509_certificate(data)` raises `ValueError` "If a certificate cannot be parsed from the provided data", and it documents no other error. Verified against: https://cryptography.io/en/latest/x509/reference/ (`cryptography` 50.0.0, floor `cryptography>=42.0.0`, retrieved 2026-08-08). **The documentation is incomplete, and a measurement found the second error.** A run of the loader over malformed candidates raised `ValueError` 8693 times and `InvalidVersion` 10 times, and it raised no other error. **`InvalidVersion` inherits `Exception` and not `ValueError`**, which `x509.InvalidVersion.__mro__` confirms, so a list of `ValueError` alone would drop it. **The inner handler therefore names `ValueError` and `x509.InvalidVersion`.** **`UnsupportedAlgorithm` reaches no handler here.** The loader raises `ValueError` for a certificate whose public-key algorithm OID or signature algorithm OID names nothing, and the extensions parse is lazy, so `DuplicateExtension` and `UnsupportedGeneralNameType` need an attribute read that this reader does not make. The reader discards the certificate it loads, because the caller wants the raw bytes. **The outer handler holds the whole body, so a narrow list there changes what the reader raises for hostile input.** The body reads one byte at a time and guards every index, so it raises nothing for a byte string. `len()` raises `TypeError` for input that is no sequence, and `JA4XFingerprinter.read_certificate` already names `TypeError` for that reason. **The outer handler therefore names `TypeError` alone.** **The outer handler is not load-bearing, and 13680 calls are the evidence.** The narrowed reader was called over an empty string, plain text, 5000 zero bytes, 5000 set bytes, 5000 sequence tags, every truncation of one certificate, 1500 damaged certificates, 1500 random strings, 1500 sequences that carry the OID pattern, 1000 spliced certificates and 1000 damaged TLS Certificate records, each one with `try_asn1` false and true. **No call raised.** **The 12 new cases were written first, and the first one failed.** `test_the_reader_holds_no_error_that_its_two_handlers_do_not_name` sends an error that neither list names to the loader and requires the reader to hold it no longer. It fails on the base commit with `Failed: DID NOT RAISE ReaderProbeError`. **Five reversals prove each name, and each reversal was proven live.** Naming `Exception` in the inner list fails that case, naming `Exception` in the outer list fails it, dropping `x509.InvalidVersion` fails `test_the_asn1_reader_returns_nothing_for_a_certificate_of_an_unknown_version`, dropping `ValueError` fails `test_the_asn1_reader_returns_nothing_for_a_sequence_that_is_no_certificate`, and dropping `TypeError` fails `test_the_reader_returns_nothing_for_input_that_is_no_sequence_of_bytes`. Following #51, `inspect.getsource` of the loaded function in a fresh interpreter reports the mutation present before each run and absent after the restore, and `PYTHONDONTWRITEBYTECODE=1` with a cleared `__pycache__` removes the stale bytecode that #50 met. **No plain `except Exception` survives in `ja4plus/utils/x509_utils.py`.** One grep of the module reports no match. **`get_cert_details` is unchanged.** It still holds no caller, `docs/api_reference.md:550` still documents it, and the question of its removal stays with the user. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against 135 xfailed cases. `__all__` of `ja4plus/__init__.py` names 25 unique entries before the change and after it. **Coverage rises.** The project holds 90% before and 91% after, and the missed statements fall from 377 of 3887 to 367 of 3887. `ja4plus/utils/x509_utils.py` rises from 66% to 75%. The unit suite rises from 1855 passed to 1867 passed, and 8 xfailed holds. `mypy --strict ja4plus/` reports no issue in 30 source files. |

| 94 | 2026-08-08 | #51 landed and the command-line program runs on `Processor`. **The program held two copies of the per-packet loop and each caught every error with `except Exception` and continued without a word.** `cmd_analyze` and `cmd_live` each built a dictionary of fingerprinters through `_build_fingerprinters` and each ran its own loop. One `Processor` replaces both, so the program gets the connection eviction of Epic 3 and the errors of Epic 4. FR-structured-output-11 and FR-structured-output-12 close. **The premise that `process_packet_with_errors` alone can carry the diagnostic is wrong, and this round corrects it.** The issue asks for one line that names the method, and `process_packet_with_errors` returns `list[Exception]`. An exception names no method, and the list holds an entry only for the methods that raised, so no caller can recover the name from the order. `Processor.process_packet_with_method_errors` therefore returns the same results and one pair of the method name and the exception. `process_packet_with_errors` keeps its signature and drops the name, so every case of `tests/test_processor_typed_results.py` holds, including the three that compare exception identity. **The program keeps no exception.** It writes each line and holds nothing, so nothing grows across a capture, and the rule that no code holds a reference to a packet object after `process_packet` returns is untouched. #45 already cleared the traceback of every returned exception. **`--types` now selects the results and no longer selects the methods.** The processor runs all ten methods whatever the option names, so it evicts the connections of a method the user filtered out and it reports that method's errors. The program still reports in the order the user wrote, because `--types ja4t,ja4` wrote the JA4T value first through version 0.6.0 and the fixed method order of FR-typed-api-3 would move it. **No fingerprint moved, and the reading is byte-level.** A replay of the 43 committed captures under `tests/foxio_vectors/` and `tests/data/` produces 863 records before the change and 863 after, and the two dumps are byte-identical across all 43 files. A second matrix of 6 captures against the 3 formats and 6 `--types` orderings, two of them the ten method names written forward and backward, produces 2453 lines before and 2453 after, byte-identical. **The diagnostic is proven by its reversal.** `tests/test_cli_processor.py` holds 13 cases and 11 of them fail against the base commit. With the swallow restored on the new code, by replacing the body of `_report_errors`, 5 of the 13 fail. **The mutation was proven live before it was read.** #50 found that a length-preserving edit with a preserved mtime lets CPython load a stale `.pyc`, so a mutation reports a green suite while the mutated module never runs. This measurement cleared every `__pycache__` outside `.venv/`, set `PYTHONDONTWRITEBYTECODE=1`, and read `inspect.getsource` of the loaded `_report_errors`, which reported the mutation present and the write to standard error absent. The revert was proven the same way. **The self-review found one order that moves, and no capture could have caught it.** `--types ja4t,ja4,ja4t` repeats a name. Version 0.6.0 built a dict of fingerprinters, so the repeated key kept the position of its first write and the program reported `ja4t` first. A first form of `_reporting_order` read the last position and reported `ja4` first. The map now keeps the first position, and `test_a_method_the_user_wrote_twice_keeps_its_first_position` fails against the first form. No committed capture reaches this, because no replay writes a repeated name. **`ja4plus.cli._packet_endpoints` is gone, because the processor now reads the endpoints and the copy in the program became unreachable.** The three cases of `tests/test_output_schema.py` that measured it now read `ja4plus.processor._packet_endpoints`, which returns the two addresses first and the two ports second. The unit suite rises from 1801 passed to 1814 passed, which is the 13 cases this issue adds, and 8 `xfailed` holds. The conformance suite reports 1531 passed, 143 skipped and 135 `xfailed` before and after, and `tests/foxio_deviations.json` holds 135 keys against those 135. **The base commit `50ec718` carried two failures that this issue neither caused nor repaired, and #50 repaired them before this issue merged.** The merge of `dev` into `epic/16-structured-output` brought the JA4T form that #300 shipped, and two cases of `tests/test_output_schema.py` still expected `8760_2-1-1-4_1460_0` where the program writes `8760_2-1-1-4_1460_00`. `afd0e56` moved them onto the two-digit form, so #305 is redundant and closes unbuilt. **One case of `tests/test_thread_safety.py` flaked once during this measurement.** `TestTheConcurrencyContract::test_eight_sharded_threads_read_one_timeline_the_way_one_thread_reads_it` failed on one run of the base source and passed on the next run of the same source and in isolation. No file this issue changes reaches it. |
| 95 | 2026-08-08 | #52 landed and it closes Epic 5. The command-line program separates results from diagnostics, gains `--output` and `--force`, and exits quietly when the reader of standard output goes away. **The premise that the table header is a diagnostic was re-measured and declined.** The brief of #52 names the table header as a line that moves to standard error. The `Screens & states` table of `docs/specs/features/05-structured-output.md` gives the table format the states `Header and rows; no results; error on standard error`, and its edge-case table gives the `csv` format `the header only` for a capture with no fingerprint. The header is therefore part of the output of each format, and it goes to the result stream. A header on standard error would leave the file that `--output` names without its `csv` header, which no parser could read. `tests/test_cli.py::TestAnalyzePcap::test_analyze_table_has_header` states the same reading and it is unchanged. **The progress message that did sit on standard output is the one line of `db update`**, `Downloading latest fingerprint database from FoxIO...`, and it now goes to standard error. `cmd_analyze` held no progress message on the base commit `4c798d0`; the line numbers of the brief were re-measured and `_report_errors` sits at `ja4plus/cli.py:169` as the brief states. **Every option now runs after the subcommand name, because two acceptance criteria write it that way.** `ja4plus analyze <capture> --format csv` and `ja4plus analyze <capture> --output existing.json --force` both name the option after the subcommand, and `argparse` reads an option of the main parser before the subcommand alone. `_add_output_options` therefore registers `--format`, `--types`, `--lookup`, `--output` and `--force` on the main parser with their defaults and on the `analyze`, `live` and `cert` subparsers with `argparse.SUPPRESS`. **The `argparse.SUPPRESS` default is the whole mechanism.** A subparser that carries a real default writes that default over the value the main parser already parsed, so `ja4plus --format json analyze capture.pcap` would report the table format. `test_the_analyze_command_keeps_the_format_option_written_before_the_name` holds that reading. **The self-review found that the first form of the guard wrote through a symbolic link, and no race was needed.** `os.path.exists` follows a link and reports False for a link whose target is absent, so a link planted at the path the operator names passed the guard and `open(path, "w")` created the target. `cmd_live` runs as root, which makes that reading a privilege boundary. The guard now opens with the mode `x` when the user passes no `--force`, which fails on an existing path and on a symbolic link, and it closes the check-to-open race at the same time. `test_the_command_writes_through_no_symbolic_link` and `test_the_command_refuses_a_file_that_arrives_after_the_test` hold both readings. **The refusal to overwrite is proven by its removal.** With the guard, the eight cases of `TheProgramRefusesToOverwriteAFileThatExists` report 8 passed. With `if os.path.exists(path) and not force:` replaced by `if False:` and the mode `x` replaced by `w`, they report 5 failed and 3 passed. The five are `test_the_command_exits_with_the_status_1`, `test_the_command_leaves_the_file_byte_for_byte_unchanged`, `test_the_command_names_the_file_on_standard_error`, `test_the_command_refuses_a_file_that_arrives_after_the_test` and `test_the_command_writes_through_no_symbolic_link`. The two `--force` cases pass by design, because `--force` overwrites with the guard and without it, and `test_the_command_writes_nothing_to_standard_output` measures the redirection rather than the guard. **The mutation was proven live before the run was trusted.** Every `__pycache__` outside `.venv/` was removed and `PYTHONDONTWRITEBYTECODE=1` was set, and `inspect.getsource` on the loaded `_result_stream` reported `GUARD_PRESENT`, then `GUARD_ABSENT`, then `GUARD_PRESENT` again after the revert. Round 86 records why that reading is needed. **A first run of that check reported `GUARD_ABSENT` three times**, because the check still read the text of the earlier guard; the check was repaired and the run repeated, which is the reason a liveness check is read rather than assumed. **The closed-pipe handler is proven by its removal too.** `main` catches `BrokenPipeError` and calls `_exit_on_broken_pipe`, which points the file descriptor of standard output at the null device and exits with the status 1. https://docs.python.org/3/library/signal.html#note-on-sigpipe gives that recipe, retrieved 2026-08-08. With the handler, the five cases of `TheProgramExitsQuietlyWhenTheWriteFails` and `TheProgramExitsQuietlyWhenTheReaderClosesThePipe` report 5 passed; with `except BrokenPipeError` changed to `except KeyboardInterrupt`, they report 5 failed. `inspect.getsource` on the loaded `main` reported the mutation live. **`cmd_analyze` re-raises `BrokenPipeError` before its `except Exception` catch**, which #294 owns, because that catch would otherwise report a closed pipe as `Error reading PCAP file: [Errno 32] Broken pipe`. No new bare `Exception` catch was added. **The pipe case needs a capture larger than the pipe buffer.** `tests/data/http.cap` writes its whole output in one write and never reaches the closed pipe, so `tests/foxio_vectors/tls-handshake.pcapng` carries the case at 644 lines and 87.6 kilobytes. **A capture the command cannot read leaves a partial output file**, which is what a shell redirection of standard output also produces, and `docs/output-schema.md` states it and asks the reader to read the exit status. **No fingerprint moved.** A replay of the 40 committed captures across the three formats writes 2649 standard-output lines before the change and 2649 after, and `diff -r` reports no differing file across the 120 runs, which covers the standard-output text, the standard-error text and the exit status of each run. A matrix of 6 captures by 3 formats by 6 `--types` orderings gives 108 runs and 3684 standard-output lines before and after, and `diff -r` reports no differing file. **No line moved between the two channels for any committed capture**, because every one of them produces at least one result. **The tests came first and 18 of the first 23 failed against the base commit.** The 5 that passed are the regression guard on the option written before the subcommand name, two cases that the base satisfies because it rejects `--output` before it opens a file, and two pipe cases that the base satisfies because its `except Exception` catch turns the traceback into a message. The 2 cases the self-review added bring the file to 25. **The register is untouched.** `tests/foxio_deviations.json` holds 135 keys before and after, against 135 `xfailed` in the conformance suite, and that suite reports 1531 passed and 143 skipped before and after. The unit suite rises from 1814 passed to 1839 passed, and 8 `xfailed` holds. `mypy --strict ja4plus/` reports no issue. Coverage holds at 88%, and `ja4plus/cli.py` rises from 59% to 65%. |
| 96 | 2026-08-08 | Epic 5 shipped and the capture analyst has an output another tool can read. **#49 gave every format the source address, the source port, the destination address and the destination port as separate fields.** `ja4plus/cli.py` packed all four into one `source` string, so a downstream tool parsed it apart again, and it added `identified_as` only when `--lookup` was passed, so the CSV header changed with the command line. Both stop. **#50 wrote `docs/output-schema.md` and made the version rule enforceable rather than only readable.** The version starts at 1; it rises when a field is removed or its meaning changes; adding a field does not raise it; a JSON object omits no field and a field with no value is `null`; a new CSV column is appended, never inserted. **JSON Lines and CSV carry a stability promise and the table format carries none.** **#50 also records a finding about proof itself.** Its first mutation harness reported that swapping two CSV columns left the suite green. The tests were right and the harness was wrong: a length-preserving edit keeps the file size, the copy back preserved the modification time, and Python reused a stale `.pyc`, so the mutated module never loaded. **Every reversal in this project now clears `__pycache__`, sets `PYTHONDONTWRITEBYTECODE=1`, and proves the mutation is live before it trusts a green suite.** Rounds 94 and 95 record two workers doing that. No fingerprint moves in this epic. The conformance suite reports the same 1531 passed, 143 skipped and 135 xfailed at every member, and the register holds 135 keys against 135 xfail. |
| 101 | 2026-08-08 | #53 landed and `ja4plus watch <interface>` is the supported monitor. `ja4plus live` stays as an alias of it, and one parser serves both names, so the two accept the same options and write the same result. **The command owns the connection table, and version 0.6.0 owned nothing.** `cmd_live` built ten fingerprinters, handed each packet to all of them, and called `cleanup_connection` never, so a monitor on a busy interface grew until the host stopped it. `ja4plus/watch.py` now holds the loop and the table. **The table is a `BoundedStateTable` and not a second form.** Epic 14 shipped that class, `CLAUDE.md` states that a state table holds a maximum entry count and a maximum age, and the brief of this issue asked for no new one. The eviction hook of the table calls `Processor.cleanup_connection`, so one eviction drops the entry of the connection table and the per-connection state of all ten methods together. An eviction that dropped the entry alone would be the leak wearing a bound as a disguise. **The key reads the outer address layer and the innermost port layer**, because `ja4plus/utils/packet_utils.py` gives the ten methods the same two layers. A key read from other layers would name a connection whose state `cleanup_connection` never drops, and a tunnelled connection is where the two readings differ. The two directions of one connection sort to one key, so the table holds one entry for one connection. **The bound is proven at the size the acceptance criterion states.** 100000 packets across 50000 connections leave the table holding 10000 connections, which is the default `--max-connections`, and the eviction count rises. A second measurement of 1000000 packets across 50000 connections reports 10000 tracked connections, 990000 evictions and 7.20 MiB held, against the 512 MiB ceiling that #279 states for the one-million-packet case. **The age bound reads capture time and not a wall clock**, so no case waits. A connection that sends one packet and stops leaves the table after the age pass that follows its timeout, and a connection that keeps sending stays. **Eviction runs on packet arrival, so the age pass runs on the packet schedule.** The default runs one pass every 1000 packets, which `ja4plus/utils/state_table.py` states, and a monitor on an interface that goes quiet holds an aged connection until the next 1000 packets arrive. The command starts no thread, which the behaviour rules of `docs/specs/features/06-live-capture.md` require. **Three reversals prove the three mechanisms, and each was proven live before it was read.** With `_drop_connection` replaced by `return`, 3 cases fail. With `max_connections` replaced by 1000000000, 4 cases fail. With the `on_packet` announcement replaced by `pass`, 2 cases fail. Every `__pycache__` outside `.venv/` was cleared, `PYTHONDONTWRITEBYTECODE=1` was set, and `inspect.getsource` on the loaded member reported the text present, then absent, then present again after the revert. Round 86 records why that reading is needed. **The tests came first, and the whole file failed to collect against the base commit** `250a2ac`, because `ja4plus/watch.py` did not exist. **No test opens a capture interface.** Every case drives the loop from an injected packet source, and the two command-level cases replace `read_interface` with a call that replays a list. A background worker can open no interface, so the capture layer itself is unmeasured here. **The `--max-connections` default of 10000 matches the library and the `--connection-timeout` default of 300 no longer does.** `DEFAULT_MAX_CONNECTION_AGE` in `ja4plus/utils/state_table.py` is 600, because #179 raised it above the 320.714503 second gap of `ssh-r.pcap`. The issue body and the feature file both state 300 and both call it the library value, so the command ships 300 and this round records that the reason is stale. A monitor therefore sheds an idle connection before the state tables of the ten methods do, which is the stricter direction. **Three options of this feature are absent, and each belongs to a sibling.** #54 owns the termination signals and the flush, #55 owns `--stats-interval` and the statistics line, and #56 owns `--bpf` and the privilege check. The privilege check therefore still reads `os.geteuid`, which `features/06-live-capture.md` asks #56 to replace with a failed capture attempt. `examples/monitoring_daemon.py` stays until the command carries all three. **The `## Terms` table gains `monitor` and `connection table`.** `monitor` is a noun, so the help text of the subcommand reads `Read packets from a network interface`. **No fingerprint moved.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before and after, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 1839 passed to 1858 passed, which is the 19 cases this issue adds, and 8 xfailed holds. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 102 | 2026-08-08 | #54 landed and the monitor stops on `SIGINT` and on `SIGTERM`, each with the status zero. **The handler sets a flag and returns.** It calls `sys.exit` never, because a signal arrives at any point, including the point where the output holds half a line, and an exit there truncates that line. Version 0.6.0 called `sys.exit` inside the handler of `ja4plus/collector.py`, and Epic 4 removed that module. `ja4plus/watch.py` now holds `StopRequest`, whose `stop_after` method `ja4plus/cli.py` passes to `sniff` as `stop_filter`, and `stop_on_signal` installs the handler of each signal and restores the handler it replaced. **`scapy` applies `stop_filter` after it reports the packet**, which `AsyncSniffer._run` holds at the line `if (stop_filter and stop_filter(p))`, so the loop finishes the packet it holds and the command flushes after the loop returns. **The flush is now unconditional, and it was conditional.** `cmd_watch` flushed only when it wrote a result, so a monitor that produced no fingerprint left its header in the buffer. **A flush that the interpreter runs at shutdown is no flush this project implemented.** Python flushes every stream at shutdown and `close` flushes a file, so a case that ends the program and then reads a complete file passes against a command that flushes never. The cases therefore read a recorder that records the order of the calls, and they assert `flush` and then `close`. The one case that fails without this change writes the `csv` header and no fingerprint. **The cases also prove that the handler does not exit.** With the flag assignment replaced by `raise SystemExit(0)`, 8 cases fail, and 3 of them read output the loop must still write after the signal arrives. A handler that exited would pass a case that asked only whether the capture stopped. **Four reversals, each proven live first.** With the final `stream.flush()` removed, 1 case fails. With the `stop_after` argument removed from the `read_interface` call, 1 case fails. With the flag assignment replaced by `pass`, 5 cases fail. With it replaced by `raise SystemExit(0)`, 8 cases fail. Every `__pycache__` outside `.venv/` was cleared, `PYTHONDONTWRITEBYTECODE=1` was set, and `inspect.getsource` on the loaded member reported the text present, then absent, then present again after the revert. **No case opens a capture interface**, so the capture layer stays unmeasured, as round 97 records. Each case replaces `read_interface` with a call that replays a list and applies `stop_filter` in the order the `scapy` source holds. The three signal cases send a real `SIGTERM` or `SIGINT` to the test process, and each case installs a handler of its own first, so a build that installs no handler fails the assertion rather than ending the test run. **A finding: the monitor reads the flag on packet arrival alone.** `scapy` blocks in `select` with no timeout, so an interface that carries no traffic holds the monitor until the next packet arrives. `features/06-live-capture.md` names `stop_filter` as the mechanism, so this issue ships it and #320 records the gap. **A second finding: the pinned `scapy` is 2.7.0 and the issue body cites 2.6.** The documentation page the body names is the `latest` page, its `sniff` entry states the same `stop_filter` behaviour, and `.claude/rules/external-apis.md` pins 2.4 or later, so the citation holds. **No fingerprint moved.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before and after, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 1858 passed to 1875 passed, which is the 17 cases this issue adds, and 8 xfailed holds. Coverage holds at 88%, and `ja4plus/watch.py` rises from 94% to 96%. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 103 | 2026-08-08 | #55 landed and the monitor reports statistics on exit and on a schedule. **The statistics thread is the only thread the command starts, and `--stats-interval` is the only thing that starts it.** `report_statistics` yields nothing and starts no thread when the interval is None, and `cmd_watch` wraps `read_interface` in it, so the thread ends when the capture returns and a termination signal stops the monitor and the thread together. The thread waits on a `threading.Event` rather than on a sleep, so `stop` ends it inside a millisecond and no exit waits a whole interval. **Every statistics line goes to standard error, which #52 established as the diagnostic channel.** A line on standard output would break a pipe that reads fingerprints. **`MonitorStats` holds the four counts under one lock, and the capture thread publishes the two table counts.** `Monitor.handle_packet` calls `record_packet(len(table), table.evictions)` after it writes the table and before it reports the packet, so the statistics thread reads `MonitorStats` and never the connection table; the table is a dictionary that the capture thread rewrites on each packet. **A case that reads an invariant beside a writer measures nothing on CPython, and this round measured that before it trusted it.** One reader against one writer, over 20000 writes, read 97635 snapshots and found 0 broken invariants with the lock and 0 without it, at the default switch interval and at one microsecond. CPython leaves one thread inside three attribute stores that hold no call and no backward jump, so the reader never samples the gap. `test_a_write_waits_while_a_read_holds_the_guarded_region` replaces that case: a clock that blocks holds the reader inside the guarded region, and the writer reaches no count until the reader leaves. It is the case that fails when either lock is removed. **Twelve mutations prove the wiring, each proven live first.** A swap of the two table counts fails 5 cases, a packet counter that counts nothing fails 6, a snapshot that acquires no lock fails 1, a record that acquires no lock fails 1, a reporter that writes before the first interval fails 1, an exit summary on standard output fails 5, a fingerprint counter that counts one for each packet fails 1, a `report_statistics` that starts the thread without the option fails 1, a `stop` that sets no event fails 7, and an uptime that reads the packet count fails 1, a trailing window that reaches no count fails 1, and an interval guard that admits a value that is no finite number fails 2. Every `__pycache__` outside `.venv/` was cleared, `PYTHONDONTWRITEBYTECODE=1` was set, and `inspect.getsource` on the loaded member reported the marker absent, then present, then absent again after the revert. The one deletion mutation was probed in the other direction, present then absent. **The counts are measured against known traffic and no two of them are equal.** Seven packets across five connections, with a table of three, give 7, 4, 3 and 2, and one TCP handshake through the command gives 2 packets, 3 fingerprints and 1 connection, so a field wired to another counter fails. **The self-review found two defects and a case now pins each one.** `cmd_watch` wrote the trailing JA4SSH window that #214 emits, and it counted that window never, so the exit summary under-reported every capture that ends with an open window. `ssh.pcapng` produces two fingerprints through the monitor, and the summary reported one. **The second defect is the interval guard.** `--stats-interval nan` passed the guard, and `Event.wait` returns at once for `nan`, so the thread writes a statistics line without an end. `--stats-interval inf` passed the guard, and `Event.wait` raises `OverflowError` for `inf`, so the thread ends and the monitor keeps running. The guard now reads `math.isfinite`. **The `dropped` field reads `null`, and that is a measurement.** `scapy` 2.7.0 reports no drop count to a caller of `sniff`. On macOS `_L2bpfSocket.get_stats` at `scapy/arch/bpf/supersocket.py:297` reads the `BIOCGSTATS` ioctl, and `AsyncSniffer._run` at `scapy/sendrecv.py:1205` holds the socket it opens in a local name, so a caller reaches that socket through the `opened_socket` argument alone. On Linux `scapy` defines `PACKET_STATISTICS = 6` at `scapy/arch/linux/__init__.py:95` and calls `getsockopt` with that option nowhere. **The macOS reading ran on macOS 25.6.0, and no Linux host ran the second reading.** #326 owns the socket work that reports a count, and it follows #56. `MonitorStats` accepts a `dropped_source` and `ja4plus/cli.py` passes none. **No case opens a capture interface**, so the three lines of `read_interface` stay uncovered, as rounds 97 and 98 record. **No fingerprint moved.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before and after, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 1875 passed to 1916 passed, which is the 41 cases this issue adds, and 8 xfailed holds. Coverage rises from 88% to 89%, and `ja4plus/watch.py` rises from 96% to 98%. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 104 | 2026-08-08 | #56 landed and `ja4plus watch` applies a capture filter and reads the capture failure. **The privilege check reads no user identity.** Version 0.6.0 read `os.geteuid() != 0` at `ja4plus/cli.py:452`, and the line number of the issue body predates #53, #54 and #55, so it was re-measured on the base commit `855fb50`. That check held two defects. `os.geteuid` is absent on Windows, so the command raised `AttributeError` there rather than reporting that Windows carries no monitor. A Linux host also grants `CAP_NET_RAW` without granting the user identity zero, so the check refused a permitted operator. The command now attempts the capture and reads the failure. **The classification is separable from #320, which follows this issue.** `describe_capture_failure` reads one exception object and four strings. It calls no capture function and it knows nothing about the way the socket opens, so the change #320 makes to `read_interface` leaves the classification where it is. The `Raises:` section of `read_interface` states the contract between the two. **The failures of `scapy` 2.7.0 were read from its own source, and the pinned version is 2.7.0 and not the 2.6 the interface table names.** A refused `/dev/bpf` device raises `Scapy_Exception` at `scapy/arch/bpf/core.py:59`, an interface the host does not hold raises `ValueError` at `scapy/interfaces.py:434`, and a refused filter raises `Scapy_Exception` at `scapy/arch/common.py:129`. Both socket classes wrap that filter failure as `Cannot set filter:`, at `scapy/arch/bpf/supersocket.py:218` and at `scapy/arch/linux/__init__.py:232`. The Linux socket calls raise `OSError` and `scapy` wraps neither, so `EPERM` and `EACCES` name a refused privilege and `ENODEV` names an absent interface. **The privilege reading comes first, because a host that refuses the privilege refuses it before it reads the interface name or the filter.** **Three of the four failures were produced on this host, and none of the three opens a capture interface.** `get_dev_bpf()` reported `Permission denied: could not open /dev/bpf0. Make sure to be running Scapy as root ! (sudo)`, `resolve_iface('nosuchif0')` reported `Interface 'nosuchif0' not found !`, and `compile_filter('tcp port', linktype=1)` reported `Failed to compile filter expression tcp port (-1)`. Each case feeds the real exception object to the classifier, so no case reads a message a person typed. **The host is macOS 25.6.0 and it granted no capture privilege.** The `/dev/bpf` case skips on any other platform and under the user identity zero. **The Linux behaviour is a reading of the `scapy` source and no Linux host ran it.** The `CAP_NET_RAW` refusal, the `EPERM` number and the `ENODEV` number are therefore claims and not measurements, and the two cases that cover them construct the `OSError` by hand. **The interface list needs no privilege**, which is the reading that makes the second acceptance criterion answerable: `get_if_list()` reported 20 interfaces on this host without the capture privilege. **`--bpf` reaches `sniff(filter=...)`, and one case measures the call.** It replaces `scapy.all.sniff` and reads the arguments, so `read_interface` runs for real and opens no interface. `filter=None` is safe on both platforms: `scapy/arch/bpf/supersocket.py:213` and `scapy/arch/linux/__init__.py:228` each attach a filter only where one is not None. **Three reversals prove the three mechanisms, and the probe discriminates in both directions.** With the privilege reading replaced by `if False`, 5 cases fail. With `filter=capture_filter` replaced by `filter=None`, 1 case fails. With the Windows reading replaced by `if False`, 5 cases fail. Every `__pycache__` outside `.venv/` was cleared, `PYTHONDONTWRITEBYTECODE=1` was set, and `inspect.getsource` on the loaded member reported PRESENT under each mutation and ABSENT after each revert. **The tests came first, and the whole file failed to collect against the base commit** `855fb50`, because `ja4plus/watch.py` held no `CAPTURE_FAILURES`. **`examples/monitoring_daemon.py` is removed, and every reference to it was repaired.** `git grep` over every tracked file reported two references outside the file: the Purpose section of `docs/specs/features/06-live-capture.md`, which this issue rewrote, and Changelog round 97, which records that the file stays until the command carries all three options and which stays as written. No file under `tests/`, under `.github/` and no line of `README.md` named it. **The bare catch of `cmd_watch` stays.** The self-review removed it and then restored it: #319 owns the fourteen bare catches of this module, and a removal here would turn every unclassified failure into a stack trace. The new clause sits above it and reads `CAPTURE_FAILURES` alone. `BrokenPipeError` inherits `OSError`, so the clause that re-raises it precedes both. **No fingerprint moved.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before and after, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 1916 passed to 1950 passed, which is the 34 cases this issue adds, and 8 xfailed holds. `__all__` of `ja4plus` names 25 entries before and after. **Coverage holds at 89% and no file falls.** The total misses fall from 476 of 4141 statements to 466 of 4178. `ja4plus/watch.py` rises from 98% to 99%, because the case that measures `sniff(filter=...)` covers the three lines of `read_interface` that rounds 97 and 98 record as uncovered. `ja4plus/cli.py` rises from 75% to 78%. The removed example carried no coverage, because the measurement reads `ja4plus/` alone. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 105 | 2026-08-08 | #320 landed and the monitor stops on an interface that carries no traffic. **`read_interface` opens the capture socket and calls `sniff` with `opened_socket` and a short `timeout`, in a loop.** The user chose that option on 2026-08-08 and declined the two others. `AsyncSniffer` runs the capture on its own thread, and the behaviour rules of `docs/specs/features/06-live-capture.md` state that the statistics thread is the only thread the command starts, so that option changes a stated rule. A monitor that ignores `SIGTERM` reaches `SIGKILL`, and a killed process skips the flush #54 built. **The socket stays open across the calls, and that property is what makes the option work.** `AsyncSniffer._run` closes the sockets it opened itself and no other socket, at `scapy/sendrecv.py:1391`, so the host buffer holds every packet that arrives between two calls. A loop that reopened the socket would drop each one. **The loop reads the stop request after each call, and `scapy` reads it after each packet.** `scapy` applies `stop_filter` to a packet and to nothing else, and its capture loop waits in `select` with `remain` set to None while the caller states no timeout, at `scapy/sendrecv.py:1331`. Round 98 records the finding. **The default poll interval is 0.25 seconds**, which holds a margin below the one-second bound the acceptance criterion states. **The one-second bound is measured and not reasoned about.** Two cases send a real `SIGTERM` and a real `SIGINT` to the test process from inside the wait of the capture socket, and each one reads `time.monotonic` at the send and again after `read_interface` returns. Two more cases measure the same bound through the whole command-line program. **The interface carries no traffic at all in those cases**, and each one asserts that the capture loop called `recv` never. A case that needed a packet to pass would measure the mechanism #54 already shipped. **The loop boundary is measured with a socket that a reopen would replace.** `open_socket` builds a new socket for each call, the packet reaches the queue of the first socket at the instant its timeout expires, and the case asserts the report holds one packet and the run opened one socket. Under the reopen mutation that case reports `0 != 1 : the loop boundary lost the packet`, which is the failure the second acceptance criterion names. **A self-review defect: `scapy` closes a socket whose read raised and drops it from its list**, at `scapy/sendrecv.py:1370`, so a loop that called `sniff` again would wait on a closed socket for as long as the monitor runs. The loop now reads `capture_socket.closed`, which `SuperSocket` holds as a class attribute, and returns. One case pins it. **A second self-review defect: a poll interval of zero turns the loop into a spin.** `AsyncSniffer._run` breaks before it waits while the remaining time is at or below zero, so `sniff` would return at once and the loop would call it without a wait. `read_interface` now refuses a poll interval that is no positive finite number, which is the guard #55 shipped for `--stats-interval` after `nan` produced the same defect. Three cases pin it. **A third reading: the loop holds no file descriptor across the calls.** `AsyncSniffer._run` builds one `ObjectPipe` for each `sniff` call against a socket that reports a blocking select, and that pipe holds two file descriptors. A monitor calls `sniff` four times every second and runs for weeks, so `test_three_hundred_calls_hold_the_open_descriptor_count` reads `/dev/fd` after each of 300 calls and reports the same count as before the loop. **`--bpf` still applies, and `libpcap` now compiles the expression when the socket opens.** `read_interface` passes no `filter` to `sniff`, because `AsyncSniffer._run` reads that argument only where it opens the socket itself and an expression stated there would reach no socket. The three failure classes of #56 still travel out of the same call, because `open_capture_socket` calls `resolve_iface(name).l2listen()(type=ETH_P_ALL, iface=name, filter=...)`, which is the call `AsyncSniffer._run` makes at `scapy/sendrecv.py:1268`. **Six reversals prove the six mechanisms, and the probe discriminates in both directions.** With the stop-request check of the loop replaced by `if False and ...`, 11 cases fail. With the socket reopened inside the loop, 3 cases fail. With `timeout=poll_interval` replaced by `timeout=None`, 11 cases fail. With the closed-socket guard replaced by `if False and ...`, 1 case fails. With `stop_requested=stop.requested` removed from the `read_interface` call of `ja4plus/cli.py`, 4 cases fail. With the poll-interval guard replaced by `if False and ...`, 3 cases fail. Every `__pycache__` outside `.venv/` was cleared, `PYTHONDONTWRITEBYTECODE=1` was set, and `inspect.getsource` on the loaded member reported PRESENT under each mutation and ABSENT after each revert. Four of the six mutations run the monitor without an end, so those runs were bounded by `pytest-timeout`, which the virtual environment holds and the project does not declare. **The tests came first, and the whole file failed to collect against the base commit** `8b2bad7`, because `ja4plus/watch.py` held no `DEFAULT_POLL_INTERVAL` and no `open_capture_socket`. **No case opens a capture interface**, and the three lines of `read_interface` that rounds 97, 98 and 99 record as uncovered are now zero. Every case supplies the capture socket, so the real `scapy` capture loop runs against a socket the test file controls. `ja4plus/watch.py` reports one uncovered line, line 131 of `connection_key`, which this issue did not touch. **The real socket open is covered and it is not measured.** The three cases that read `open_capture_socket` replace `scapy.interfaces.resolve_iface`, so no case builds a real `L2bpfListenSocket` and no case reads a real interface. The host is macOS 25.6.0 and it granted no capture privilege. **A reading the criteria do not cover: a packet the host buffered when the signal arrives reaches no reader.** The monitor closes the socket and exits, and the mechanism #54 shipped drops the same buffer. The second acceptance criterion reads the loop boundary, which the issue body states, and no stop point of either design reads that buffer. **No fingerprint moved.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before and after, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 1950 passed to 1974 passed, which is the 24 cases this issue adds, and 8 xfailed holds. `__all__` of `ja4plus` names 25 entries before and after. **Coverage holds at 89% and no file falls.** The total misses hold at 466, of 4178 statements before and 4202 after. `ja4plus/watch.py` holds 99% and `ja4plus/cli.py` holds 78%. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 106 | 2026-08-08 | #338 landed and the malformed-input suite reads what the fingerprinter produced, and not only that it raised nothing. `tests/test_edge_cases.py::TestMalformedTLS::test_random_bytes` and `::test_very_large_packet` asserted nothing about the result, and the comment `# Either None or a result, but should not crash` stated the defect. **The current behaviour was measured before any test changed.** Both named inputs produce nothing today, so this round tightens the two cases and moves no file under `ja4plus/`. `tests/fuzz/test_synthetic_hostile_input.py` widens the input set to the six shapes the issue names and adds 17 cases. **The cases are proven live in both directions.** With `process_packet` changed to return a fabricated JA4 value where it returns nothing, 22 cases fail; with the same mutation live, the two cases as they were written on the base commit both pass. `inspect.getsource` on the loaded `process_packet` reported `MUTATION_LIVE`, then `MUTATION_ABSENT` after the revert, with every `__pycache__` outside `.venv/` removed and `PYTHONDONTWRITEBYTECODE=1` set. **One finding sits outside the six shapes and it is open.** A payload of a valid TLS record header, a valid handshake header and a random body produces a well-formed JA4 value on 5000 draws of 5000, and 4999 of them carry the version token `00`. #343 owns it, because the repair decides which byte strings are a ClientHello. |
| 107 | 2026-08-08 | #337 landed and the QUIC version 2 Initial cases assert the contract their docstring names. `tests/test_quic_utils.py::test_v2_initial_type_not_rejected` called `parse_quic_initial` and discarded the return, and it held no assertion. **The contract already holds, so this round moves no file under `ja4plus/`.** **The rejected packet and the undecryptable packet both return nothing, so the return alone tells the two outcomes apart for no packet of dummy bytes.** The repair therefore builds a packet that decrypts: `tests/quic_builder.py` gains `client_hello` and `client_initial_crypto`, and `_protect` now holds the payload protection and the header protection that `server_initial` held. A version 2 Initial packet that carries a protected ClientHello produces that ClientHello, with the server name `v2.example.com`. **The version 2 Handshake case carries the same protected ClientHello under the type code 3**, so it measures the type check and nothing else. **The cases are proven live in both directions.** With `if packet_type != 0x01` changed to `0x99`, which rejects every version 2 Initial packet, `test_v2_initial_yields_the_client_hello` fails and the other two pass; with the same check changed to `if packet_type != packet_type`, which accepts every version 2 type, `test_v2_non_initial_rejected` fails and the other two pass. Under each mutation the two cases as they were written on the base commit both pass. `inspect.getsource` on the loaded `parse_quic_initial` reported the marker `MUTATION-337-LIVE`, which `grep` found nowhere in the repository beforehand, with every `__pycache__` outside `.venv/` removed and `PYTHONDONTWRITEBYTECODE=1` set. **The comment that read "We verify by checking a v2 non-Initial IS rejected." named a case that exists, and that case could not fail either.** #348 opened: the length guard, the short-header guard and the version negotiation guard of `parse_quic_initial` and `parse_quic_server_initial` hold the same shape, and three mutations prove it. |
| 108 | 2026-08-08 | #348 landed and every QUIC Initial header guard holds a case that can fail. **The issue named four guards, and the count is eleven.** `parse_quic_initial` holds five rejection conditions and `decrypt_quic_server_initial_crypto`, which `parse_quic_server_initial` calls, holds six. The packet type guard carries one arm for version 1 and one for version 2 in each reader, and the server length guard at `ja4plus/utils/quic_utils.py:440` reads `if len(udp_payload) < 5 or not client_dcid:`, which holds two conditions. **The base commit measured one of the eleven.** Each of the other ten came out of the reader in turn, and the whole QUIC suite of 53 cases stayed green under every one. Round 107 left the version 2 type guard of `parse_quic_initial`, and that guard is the one exception. **A packet of dummy bytes makes the reader return nothing whichever guard runs**, so each guard case now carries a protected handshake message the reader decrypts. **The AEAD tag covers the header, so each case builds the rejected header rather than changing a byte of a built packet.** `tests/quic_builder.py` gains a `long_header` flag that clears bit 7, a `key_version` parameter that protects a packet under the version 1 salt while the header carries version 0, and a `type_code` parameter on `server_initial`. **The length guard admits no case of the shape the issue asks for, and arithmetic decides it.** A client Initial packet that carries a ClientHello holds at least 33 bytes. The count is 10 header bytes, a 16-byte AEAD tag, a 3-byte CRYPTO frame header and a 4-byte handshake header. The guard rejects at 20 bytes, so no packet under the threshold can produce a result. **The two readers unpack the version field outside their `try` block**, so each length guard holds a case that measures the exception it prevents: `parse_quic_initial(b"\xc0\x00\x00\x00")` returns None today and raises `struct.error` with the guard removed. **The cases are proven live.** Each of the eleven conditions came out of the reader in turn, and each one killed the case that names it. The six server conditions each killed two cases, because `tests/test_quic_utils.py` measures the parse level and `tests/test_quic_server_initial.py` measures the decrypt level. The five client conditions each killed one. `inspect.getsource` on the loaded module reported the marker `MUTATION-348-LIVE` after each mutation and reported it absent before and after, with every `__pycache__` outside `.venv/` removed and `PYTHONDONTWRITEBYTECODE=1` set. `grep` found the marker nowhere in the repository beforehand and nowhere afterward. **No file under `ja4plus/` changes and no fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against 135 xfailed cases. The unit suite rises from 2020 passed to 2028 passed, and 8 xfailed holds. **Coverage rises.** `ja4plus/utils/quic_utils.py` holds 89% before and 93% after, and the project holds 92% before and after, with 336 missed statements before and 324 after. **The first acceptance criterion of #348 is open for the length guard**, because the criterion asks for a packet that would produce a result, and no such packet exists. |
| 109 | 2026-08-08 | #339 landed and every `cleanup_connection` case reads the state the call leaves. The cases asserted that the call raised nothing, so a call that removed the wrong key, that left the entry, or that removed a neighbouring connection passed every one. **The memory ceiling #279 states rests on this call**, because #53 evicts a connection from its bounded table and calls `Processor.cleanup_connection` to drop the per-method state. **The contract already holds, so this round moves no file under `ja4plus/`.** `tests/state_readers.py` reads the key set of every state table through `BaseFingerprinter.state_tables`, so one reader serves every method. Every case builds a target connection and a neighbouring connection that share no address, and it compares the whole key set after the call against the key set a correct call leaves. That comparison covers the three failure modes at once. `tests/test_cleanup_connection.py` grows from 8 cases to 32 and covers JA4TS and JA4 as well. **Every state table of every stateful method holds a key for each of the two connections**, which the builders report as JA4L 2 tables, JA4SSH 2, JA4H 3, JA4X 3, JA4S 2, JA4TS 2 and JA4 2. **Seven mutations are proven live, each discriminating in both directions.** A `cleanup_connection` that removes nothing fails 29 cases, 16 of them new. One that removes the most recent key of each table rather than the target fails 23 cases, **all 23 of them new**: no case in the repository caught a call that removes a neighbouring connection before this round. One that removes the target from the first state table and restores the rest fails 23 cases, 15 of them new. One that raises `KeyError` for a connection it holds no entry for, which is what `del table[key]` does in place of `table.pop(key, None)`, fails 15 cases, **all 15 of them new**. Outside `cleanup_connection`, a reassembler that keeps the arrival order fails 8 cases, a stateless `cleanup_connection` that empties the fingerprint list fails 5, and a `get_ip_layer` that reports no layer for IPv6 fails 18. **One case of the 32 no mutation kills**, and it is `test_the_base_class_holds_the_cleanup_method`, which reads `hasattr` and which only the removal of the method fails. **Coverage of `ja4plus/` holds at 92 percent, 336 statements of 4135 unread, which is the reading of the base commit.** **Two cases in `tests/test_ja4x_reassembly.py` and two in `tests/test_ipv6_support.py` asserted nothing either**, and each now reads the measured value. **One state stays unobservable from outside.** `JA4XFingerprinter.processed_certs` reads a whole certificate, and `JA4SFingerprinter._quic_server_crypto` reads a ServerHello that two Initial packets split, so each case writes one entry of those two tables by hand. A capture that carries both would replace the two writes. |
| 110 | 2026-08-08 | #343 landed and the `Divergence register` records what a fingerprint is evidence of. **`ja4plus` adds no plausibility guard, and a structurally valid ClientHello produces a fingerprint whatever its body holds.** The user decided it on 2026-08-08, and this round records the behaviour and changes none of it, so no file under `ja4plus/` moves. #338 left the finding open, and this round re-measured every number of it. **Two of the four premises the brief carried were wrong, and the measured values stand instead.** `tests/measure_random_client_hello.py` draws 5000 random bodies of 256 bytes behind a valid TLS record header and a valid handshake header. All 5000 produce a well formed fingerprint. **4998 carry the version token `00`, where the brief said 4999.** **720 carry neither a cipher suite nor an extension, where the brief said 784.** The vector side holds: the script finds 168 ClientHellos across the committed captures, and 0 of them carry either condition. **A guard on the two conditions therefore moves no vector, and 2 of the 5000 draws still pass it.** The guard costs a permanent divergence from every FoxIO implementation and leaves about one fabricated value in twenty-five hundred. `tests/fuzz/test_structural_validity.py` holds the decision as four cases, and a guard on either condition makes at least two of them fail. **The new file agrees with the two cases the brief named.** `tests/fuzz/test_synthetic_hostile_input.py` feeds bytes that carry no readable ClientHello and asserts nothing comes back. `tests/fuzz/test_length_fields.py::test_an_extension_that_declares_more_bytes_than_the_packet_produces_no_exception` asserts that a readable hello with one inflated length field still produces a value. Both read the structure and neither reads the content, which is the reading this round records. `docs/output-schema.md` and `README.md` now state the property where a user reads it. **One mutation is proven live and it discriminates in both directions.** The proof added the narrow guard to `parse_tls_handshake`, and `inspect.getsource` reported `MUTATION_LIVE`. 3 of the 4 new cases failed. The fourth case passed, because a guard reaches no vector ClientHello. **The conformance suite reported the same 1531 passed, 143 skipped and 135 xfailed with the guard live.** That run measures the claim that a guard moves no vector. The proof removed every `__pycache__` outside `.venv/` and set `PYTHONDONTWRITEBYTECODE=1`. After the revert `inspect.getsource` reported `MUTATION_ABSENT`, and all 4 cases passed. |
| 111 | 2026-08-08 | #306 landed and the `## Terms` table holds one word for the serialized output line. **The table listed `record` in the `Do not use` column of `result`, and `ja4plus/output.py` used it as a noun in five places.** #50 found that, reworded `docs/output-schema.md` to `output line`, and left the shared table alone, because a term changed as a side effect of a feature diff is a term nobody agreed. **A result and an output line are two things, and that is why the word matters.** A result is one fingerprint one method produced. An output line carries every field of the result, plus `schema_version` and `identified_as`, which the command-line program owns and the library result holds neither of. **The term is `output line`, and its `Do not use` column names `record`, `result` and `entry`.** `record` was already denied. `result` names the smaller thing. `entry` names a state-table row in this project. **`row` and `object` stay, because each names the shape of one output line in one format**, which `docs/output-schema.md` already states: the `json` format writes one JSON object on each line, and the `csv` format writes one row. The identifier for a parsed output line is therefore `json_object`. **The word `record` keeps two meanings that this term does not touch.** The first is the verb, as in `#215 records the reading`. The second is the TLS record of RFC 8446, which `ja4plus/utils/tls_utils.py` and four fingerprinters read. **`tests/conformance_index.py` and the FoxIO parity cases keep the word for one expected-output entry of a FoxIO file**, which is the reference's data and no output line of this program. **The rename is proven by measurement, because a rename is easy to believe and easy to get wrong.** A replay of the 38 committed captures in the `table`, `json` and `csv` formats is 114 runs; every run exits 0, and `diff -r` of the 2466 output lines against the base reports no differing byte. `import ja4plus` succeeds and all 25 names of `ja4plus.__all__` resolve. **No field name and no column name moved.** `SCHEMA_VERSION` is 1 and `CSV_COLUMNS` holds the same eleven names in the same order, so `docs/output-schema.md` needs no edit and the schema version rule is not reached. The unit suite reports 1839 passed, 8 xfailed and 93 subtests passed before and after. The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before and after, against 135 keys in `tests/foxio_deviations.json`. Line coverage is 88 percent before and after, and no file under `ja4plus/` gained or lost a statement. `mypy --strict ja4plus/` reports no issue in 30 source files. **One defect outside this scope is found and not repaired.** `tests/compare_zeek_baselines.py:155` reads the `source` field that #49 removed, so the script raises `KeyError` on the first output line it parses. #324 owns it. |
| 112 | 2026-08-08 | #324 landed and `tests/compare_zeek_baselines.py` runs again, under a case that would catch the next break. **The script read the composite `source` field that #49 removed, so it raised `KeyError` on the first output line it parsed.** The reader now reads `src_ip`, `src_port`, `dst_ip` and `dst_port`, and `split_source` goes away because the four fields need no split. `docs/output-schema.md` states that the `json` format writes each port as a number, so the key reads the string form of both. **The reader also reads `schema_version` first and stops on a version above 1**, which the same page states as the rule for a parser. **This is repaired rather than removed, which is the opposite of what #314 did, because the readings it produced are still cited.** `docs/specs/foxio/zeek.md` rests on it, and #198, #226 and #276 reason from it. A comparison tool that does not run is worse than absent. **No gate ran it, and that is why it broke in silence.** `grep -rn compare_zeek_baselines tests/ ja4plus/ .github/` returned the file alone. `tests/test_compare_zeek_baselines.py` holds four cases. Three run the script over `dhcp.pcapng`, and one reads the schema version. **The end-to-end cases write their own baseline in the Zeek TSV form, because this repository holds no Zeek baseline**; they measure the reader, the connection key and the comparison, and they measure no FoxIO value. **The cases are proven able to fail, and the mutation is proven live.** With `__pycache__` cleared outside `.venv/` and `PYTHONDONTWRITEBYTECODE=1` set, `inspect.getsource(ja4plus_readings)` holds `json_object["source"]` and no `src_port` under the reintroduced defect, and the opposite after the revert; the marker sits inside the member, which is the reading #54 recorded. The reintroduced defect fails 3 of the 4 cases, and the revert passes all 4. **The re-run against the pinned FoxIO checkout `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` exits 0 and prints 98 rows, of which 63 match and 35 differ.** Round 52 read 98 rows, 62 match and 36 differ. **The one row that moved is JA4SSH, because #214 landed**: `ssh2.pcapng` now produces `c36s52_c42s76_c51s2` as its second value, which the Zeek baseline holds. JA4 reads 20 of 20, JA4S 20 of 20, JA4H 1 of 1, JA4D 2 of 2, JA4T 8 of 10, JA4TS 9 of 10, JA4L 1 of 17 and JA4LS 1 of 17, all unchanged from round 52. **The search for another reader of the removed `source` field found none.** `tests/test_output_schema.py:324` and `tests/test_cli.py:64` each assert that the field is absent, `tests/test_ja4ssh_deep.py:235` and `ja4plus/fingerprinters/ja4ssh.py:664` name the source of a HASSH lookup, and `tests/foxio_deviation_owners.json:85` names the provenance of a register. None of those five is the composite field. `examples/live_traffic_fingerprinting.py:121` and `examples/tcpdump_workflows.py:231` build a display string from four separate fields and read no `source` field. **No file under `ja4plus/` changed, so no fingerprint moved and no output field moved.** `SCHEMA_VERSION` stays 1 and `ja4plus.__all__` names the same 25 entries. The unit suite rises from 1839 passed to 1843 passed, with 8 xfailed and 93 subtests passed. The conformance suite reports 1531 passed, 143 skipped and 135 xfailed, against 135 keys in `tests/foxio_deviations.json`. Line coverage is 88 percent before and after. `mypy --strict ja4plus/` reports no issue in 30 source files. **One stale reading outside this scope is found and not repaired.** `docs/specs/foxio/zeek.md` still states that `ja4plus` produces no second JA4SSH value and that the baseline is blocked on #214, which closed. #327 owns it. |
| 113 | 2026-08-08 | #327 landed and `docs/specs/foxio/zeek.md` reads the current comparison. **The page read as though #214 were open, in three places, and #214 closed.** #214 made JA4SSH emit the window a connection holds open when the capture ends, so `ssh2.pcapng` now produces `c36s52_c42s76_c51s2` as its second value, which the Zeek baseline holds. **The re-run of #327 against the pinned FoxIO checkout `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` exits 0 and prints 98 rows, of which 63 match and 35 differ.** The count agrees with the re-run of #324, and round 52 read 98 rows, 62 match and 36 differ. JA4 reads 20 of 20, JA4S 20 of 20, JA4H 1 of 1, JA4D 2 of 2, JA4T 8 of 10, JA4TS 9 of 10, JA4SSH 1 of 1, JA4L 1 of 17 and JA4LS 1 of 17. **JA4SSH is the one row that moved since round 52, and the page read it as 1 of 0.** **The page now names the Changelog round of every run it cites**, in a table of the three runs, because round 52's counts sat unmarked until #324 re-ran the tool and no reader could tell a stale count from a current one. That is the defect this issue repairs. **One vector-adoption decision no longer follows from its evidence, and #327 declines to replace it.** `Scripts.ja4-ssh2/ja4ssh.log` carried the value rating `Blocked. Its second value is the open decision on #214.` The block is gone, and the replacement rating is a precedence question: `ja4plus` and the Zeek baseline now agree on both JA4SSH values of `ssh2.pcapng`, `python/test/testdata/ssh2.pcapng.json` holds `c36s36_c0s0_c2s0` as the second value, and `tests/foxio_deviations.json` declines that value under #97 at the key `ssh2.pcapng/14:57377/JA4SSH.2`. `.claude/rules/external-apis.md` states that `python/test/testdata/` decides where it and a Zeek baseline both hold a value for one method on one connection, and it names no exception for a value the register declines. **The user decides whether a Zeek baseline may hold the reference JA4SSH value for a connection whose Python value the register declines.** The rating reads `Undecided` until then, and no baseline of the seven is adopted as a vector, which the page already stated. **No file under `ja4plus/` changed, so no fingerprint moved and no output field moved.** The unit suite reports 1843 passed, 8 xfailed and 93 subtests passed before and after. The conformance suite reports 1531 passed, 143 skipped and 135 xfailed, against 135 keys in `tests/foxio_deviations.json`. `ja4plus.__all__` names the same 25 entries. `mypy --strict ja4plus/` reports no issue in 30 source files, and `ruff check` and `ruff format --check` report no issue. **This issue writes no test, and that is the reading rather than an omission.** The change is prose over a measurement, so the red-to-green is the re-run of `tests/compare_zeek_baselines.py`, which #324 already put under `tests/test_compare_zeek_baselines.py`. |
| 114 | 2026-08-08 | #332 landed and the precedence rule carries one exception. **A FoxIO Python value that `tests/foxio_deviations.json` declines under a decided entry forfeits its precedence.** Where the register declines it, a Zeek baseline may hold the reference value for that method on that connection. **Python keeps its precedence everywhere else**, so this adds one exception and weakens no rule. **The user decided it on 2026-08-08, after #327 returned the question rather than answering it.** **The reason is that precedence breaks a tie between two sources this project trusts, and a value this project already ruled wrong is not a tie.** `.claude/rules/external-apis.md` named no exception because the case had not arisen when the rule was written. **The condition is a recorded decline that names an issue, which a reader checks in the register.** The rule states three facts: the key is the value form, so the FoxIO Python file holds a value to decline; the entry carries `"decided": true`; and a Zeek baseline holds a value for the same method on the same connection. **An entry the register leaves undecided is an open question, and the exception passes over it.** **No reading of which value looks right reaches the exception**, which is the trap the issue names. **The exception reaches 1 row of the 135 the register holds, and the search that proves it is in the tests.** `tests/test_zeek_precedence_exception.py` intersects every decided register key with the methods each of the seven Zeek baselines holds a value for, read at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. 20 keys name a capture and a method a baseline covers. 13 are JA4L keys under #225, and the standing bar on a JA4L or JA4LS value of a Zeek baseline holds them out. 6 are `tls-handshake.pcapng` keys under #138, which carry the occurrence form `<vector>/<method>`; that form records a stream the Python file omits and declines no value, and the Rust snapshot rule already decides it. The 1 that remains is `ssh2.pcapng/14:57377/JA4SSH.2`, declined under #97, which is the case that raised the question. **`docs/specs/foxio/zeek.md` now rates `Scripts.ja4-ssh2/ja4ssh.log` Medium**, where #327 left it `Undecided` and wrote the question into the page. **The rating is Medium and not High, because a third FoxIO reference already holds both values**: `tests/foxio_vectors/rust_expected/ja4__insta@ssh2.pcapng.snap:215-217` holds `c36s36_c76s124_c74s5` and `c36s52_c42s76_c51s2`, which round 68 records and #327 did not weigh. **No baseline is adopted as a vector.** The exception states which source may hold a reference value, and adoption is its own decision. **No file under `ja4plus/` changed, so no fingerprint moved and no output field moved.** `ja4plus.__all__` names the same 25 entries. The unit suite rises from 1843 passed to 1847 passed, with 8 xfailed and 93 subtests passed, and the 4 new cases are the reach measurement. The conformance suite reports 1531 passed, 143 skipped and 135 xfailed, against 135 keys in `tests/foxio_deviations.json`. Line coverage is 88 percent before and after. `mypy --strict ja4plus/` reports no issue in 30 source files, and `ruff check` and `ruff format --check` report no issue. **The red-to-green is the rating case.** `test_the_zeek_page_rates_the_baseline_the_exception_reaches` fails against the page #327 left, and passes against the page this issue writes. |
| 115 | 2026-08-08 | #334 landed and the precedence exception is source-neutral. **Where the register declines the FoxIO Python value under a decided entry, any other FoxIO implementation may hold the reference value for that method on that connection.** #332 wrote the exception for the Zeek baseline alone, because that was the case #327 raised, and nothing in its reasoning depends on which source contests the declined value. **The user decided it on 2026-08-08, with two bars.** **Bar 1: a capability decline bars the row.** The exception reaches a row only where the decline records a disagreement about the value, and #129 is the other kind: `ja4plus` reads no encrypted request by decision, the Wireshark file holds the decrypted values, and naming those as the reference would create permanent divergences no implementation change could close. **Bar 2: a disagreement between the remaining sources bars the row.** Where the remaining sources hold different values, no source holds the reference and the row stays declined. A standing ranking among Rust, Zeek and Wireshark was declined, because this project has found each of the three wrong in different places. **The widened rule before the bars reaches 58 of the 135 register keys**, and the two bars leave 6. The measurement read all 135 keys against 38 Rust snapshots, 37 Wireshark testdata files and 7 Zeek baselines at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. **The Wireshark testdata records `frame.number` and no connection**, so the search resolved every frame against its capture to attribute each value to one connection. The 6 rows are `ssh-r.pcap/2:46396/JA4SSH.1`, `ssh-scp-1050.pcap/0:49237/JA4SSH.3` and `ssh-scp-1050.pcap/0:49237/JA4SSH.4` under #96, where Rust and Wireshark agree; `ssh2.pcapng/14:57377/JA4SSH.2` under #97, where Rust and Zeek agree, which is the row #332 already found; and `ssh2.pcapng/33:51810/JA4L-S.1` and `tls3.pcapng/25:61884/JA4L-S.1` under #225, where the Wireshark dissector stands alone. **Three findings came out of the search.** **First, the register records no field that separates a value decline from a capability decline**, so the check names the issue and the rule states a proposal: a `"capability": true` field that `tests/foxio_deviations.py` reads beside `decided`. The user decides whether to build it. **Second, one value-form key declines no Python value.** `gre-erspan-vxlan.pcap/0:65174/JA4T.1` carries the value form, the FoxIO Python file holds no JA4T value, and #215 declines the FoxIO Rust value `8192__0_0`. The rule now reads that fact from the vectors rather than from the key form, and the exception passes over the row. **Third, the bar on a Zeek JA4L value reaches a source it does not name.** The Wireshark dissector appends a third part to every JA4L and JA4LS value it writes, a delta on a TCP connection and the marker `quic` on a QUIC connection, which is the second of the three rules the bar states. A bar that reached the dissector would remove the two #225 rows and leave 4. **#225 records that this project adopted the `quic` marker from the dissector on purpose**, so widening the bar would contradict a recorded decision, and #334 changed nothing there. **No vector is adopted, no register entry changes, and no file under `ja4plus/` changes.** `tests/test_zeek_precedence_exception.py` becomes `tests/test_precedence_exception.py`, because it no longer reads the Zeek baseline alone. `ja4plus.__all__` names the same 25 entries. The unit suite rises from 1847 passed to 1862 passed, with 8 xfailed and 93 subtests passed, and the 15 new cases are the reach measurement, the two bars and the third-part finding. The conformance suite reports 1531 passed, 143 skipped and 135 xfailed, against 135 keys in `tests/foxio_deviations.json`. Line coverage is 88 percent before and after. `mypy --strict ja4plus/` reports no issue in 30 source files, and `ruff check` and `ruff format --check` report no issue. **The red-to-green is the reach case.** Remove `ssh2.pcapng/14:57377/JA4SSH.2` from `SOURCE_VALUES` and `test_the_exception_reaches_six_register_keys` fails, with `inspect.getsource` reporting 0 occurrences of the row under the mutation and 1 after the revert. |
| 116 | 2026-08-08 | #341 landed and the register records the kind of every decline. **`tests/foxio_deviations.json` carries a `capability` field on all 135 entries**, and `CAPABILITY_DECLINES` is gone from `tests/test_precedence_exception.py`. #334 shipped bar 1 of the source-neutral precedence exception as the constant `{129}`, and reported that the register held no field the check could read. **A rule stated where nothing checks it is the failure mode this project keeps meeting**, so a new capability decline would have fallen into the exception unless somebody remembered to edit a test that named it nowhere near the decline. **`Deviation` reads the field beside `decided`, and the default is a statement rather than an absence**: an entry with no field is a value decline, which `tests/foxio_deviations.py` states in the schema. **The gate requires the field on every decided entry.** `unrecorded_kinds` in `tests/test_foxio_deviations.py` reads the register file itself, because the reader turns an absent field into the default and no caller could then tell an omission from a decision. An undecided entry declines nothing, so it may omit the field; the six #272 entries carry `false` today. **The kind of all 135 entries came from the recorded cause of each one, and not from the issue number.** A cause that states `reads no encrypted request` or `reads no encrypted certificate` records a capability decline. **43 entries carry `"capability": true`, and all 43 name #129.** No entry of #96, #97, #105, #138, #151, #162, #214, #215, #225 or #272 records a capability this project chose not to build, and no entry was undeterminable. #96, #97 and #105 decline a reference defect; #138 and #151 record a stream the FoxIO Python file omits; #162 records an ALPN byte the two FoxIO implementations disagree on; #214, #215 and #225 record a form this project chose; #272 stays open on a count. **The field and the constant agree exactly, and the reach is 6 rows before and after.** The six rows are unchanged. **One premise of the issue body is false, and the measurement disproves it.** The issue states that admitting a #129 entry would raise the reach, and it does not: `SOURCE_VALUES` holds no #129 key at all, because #334 measured the 35 source values of #129 and left them out under the bar. With every one of the 43 #129 entries admitted, the reach stays 6, because fact 4 finds no source value and bars each row before fact 3 matters. **The capability bar is therefore proven in the other direction.** `test_the_bar_reads_the_field_and_not_the_issue_number` copies the #96 row `ssh-r.pcap/2:46396/JA4SSH.1`, sets `"capability": true` on the copy, and the reach falls from 1 row to 0. A live mutation of the committed register reproduces it: `"capability": true` on that one row drops the whole reach from 6 to 5. **No fingerprint moves, no file under `ja4plus/` changes, and no vector is adopted.** `ja4plus.__all__` names the same 25 entries. The register holds 135 keys before and after, against 135 `xfailed` cases. |
| 117 | 2026-08-08 | #57 landed and `JA4DBClient` performs no network request by default. **The defect is a disclosure, and it is not a failure.** On the base commit `79f8c66`, `_do_lookup` called `_remote_lookup` for every fingerprint the bundled mapping file held no entry for, so `ja4plus --lookup analyze capture.pcap` sent each unknown fingerprint to `https://ja4db.com/api/read/<fingerprint>`. A fingerprint describes traffic the operator observed, so each request told a third party what the operator watched, and no caller asked for that. **`JA4DBClient(allow_remote=True)` is now the only way to reach the lookup service.** The constructor holds `allow_remote` first and `cache_size` second, which is the order `docs/specs/features/07-db-enrichment.md` line 131 publishes. `ja4plus/cli.py` builds `JA4DBClient()`, so the command-line program now makes no network request, and #58 owns the option that permits one. **The default is measured by the attempt, and not by the result.** The `lookup` extra is absent from this environment, so `_remote_lookup` returns None on `ImportError` and a case that blocks every outbound socket passes against the unrepaired client. `tests/test_db_offline.py` therefore installs a recording stand-in for `requests` in `sys.modules` and asserts that the call list is empty. **The opt-in gate is proven by its reversal.** With `if not self._allow_remote` replaced by `if False and not self._allow_remote`, 3 cases fail, among them `test_the_default_client_looks_1000_fingerprints_up_while_every_socket_fails`. **The response reader is proven by its reversal too.** With the version 0.6.0 body reader restored, 5 cases fail. That reader answered an object without an `application` field with the application name `Unknown`, and the client now reads every shape it does not know as a miss. It accepts one shape: an object that carries a non-empty `application` string. It reads `type` and `notes` as strings and substitutes an empty string for a field of another type, so no unchecked value of the lookup service reaches a caller. **The timeout is a module constant and not a parameter.** `_REMOTE_TIMEOUT` holds 5.0, which `docs/specs/features/07-db-enrichment.md` line 113 states, and that line states that no operator configures the interval before version 1.0.0. **A divergence the project manager owns: line 136 of the same file publishes a `timeout` parameter on the constructor, and line 113 refuses one.** The issue body of #57 states the second reading, and this round shipped it. **The two wide handlers of `ja4plus/ja4db.py` remain, and this round added none.** #319 owns them, and a comment above the handler of `_do_lookup` now names that issue. **A self-review defect: the parameter order reintroduced the disclosure it repairs.** `cache_size` was the first parameter before this round, so `JA4DBClient(100)` asked for a lookup cache of 100 entries. Against the first form of the repair that call set `allow_remote` to 100, which is truthy, and it kept the default entry count. The client now raises `TypeError` for an `allow_remote` value that is no bool, and five cases pin it. **A second self-review defect: the fingerprint reached the request path unescaped.** `lookup` accepts any string, and a string that carries `/` or `?` named another path on the lookup service. `_remote_lookup` now calls `quote(fingerprint, safe="")`. A JA4+ fingerprint holds no character the escape changes, and one case pins that too. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against those 135. **Coverage rises.** The project holds 92% before and 92% after, and `ja4plus/ja4db.py` rises from 86% to 93%. The unit suite rises from 2002 passed to 2032 passed, which is the 30 cases this issue adds, and 8 xfailed holds. `__all__` of `ja4plus` names 25 entries before and after. **The tests came first**, and `tests/test_db_offline.py` reported 20 failed and 3 passed against the unrepaired client. The `## Terms` table gains `lookup service` and `remote lookup`. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 118 | 2026-08-08 | #58 landed and the command asks for the remote lookup with an option or a variable. **The command reached no service before this round and it named no way to reach one.** #57 built `JA4DBClient()` in `_init_lookup`, so `ja4plus --lookup analyze capture.pcap` performed the local lookup and the operator had no opt-in. **`--lookup` stays local, FR-db-enrichment-3.** **`--lookup-remote` permits the disclosure, FR-db-enrichment-4**, and it asks for the lookup too, so an operator who passes it needs no `--lookup`. **`JA4PLUS_DB_LOOKUP=1` permits the same disclosure, FR-db-enrichment-5**, for an operator who runs a command line another program builds. **The precedence follows the two requirements and invents nothing.** Each source permits the disclosure and neither one refuses it, so `JA4PLUS_DB_LOOKUP=0` cancels no option, and the variable permits the request on the value `1` and on no other value. **The variable asks for no lookup**, so `JA4PLUS_DB_LOOKUP=1 ja4plus analyze capture.pcap` looks nothing up and discloses nothing. **The disclosure notice is written once for each run, FR-db-enrichment-6.** `_init_lookup` writes it, and each command calls that function once, so the count of lookups moves it not at all. It goes to standard error, which #52 established, because a notice on standard output would enter the pipe that carries the results. It names the lookup service and the two ways to stop the request. **The absent package ends the run rather than the lookup.** Where the operator permits the remote lookup and `requests` is absent, the command names the `lookup` extra and exits with the status 1, which the edge-case table of `docs/specs/features/07-db-enrichment.md` states. **The privacy property is measured by the attempt and not by the result.** `tests/test_cli_lookup_opt_in.py` installs the recording stand-in of `tests/test_db_offline.py` in `sys.modules` and asserts an empty call list, so a run that tried a request fails the case. **The notice count is measured against several lookups.** One lookup cannot separate one notice per run from one notice per lookup, so the case asserts more than one recorded request beside the one notice. **The tests came first**, and the file reported 24 failed and 4 passed against the unrepaired command, at the 28 cases it held then. The self-review added the 29th, for the operator who passes both options. **Three reversals prove the three gates.** `JA4DBClient(allow_remote=True)` in place of the option gate fails 7 cases. The notice moved to the lookup site fails 7 cases. A variable read as any value that is not empty fails 5 cases. The restored form passes 29 of 29 each time, with `PYTHONDONTWRITEBYTECODE=1`. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against those 135. **Coverage holds and the command rises.** The project holds 92% before and 92% after, and `ja4plus/cli.py` rises from 78% to 80%. The unit suite rises from 2032 passed to 2061 passed, which is the 29 cases this issue adds, and 8 xfailed holds. `__all__` of `ja4plus` names 25 entries before and after. **This round adds no wide handler.** The one `except Exception` of `_init_lookup` is the handler #319 owns, and a comment now names that issue. The `## Terms` table gains `disclosure notice`. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 119 | 2026-08-08 | #354 landed and `docs/specs/features/07-db-enrichment.md` states one reading of the remote timeout. **The file stated two.** Line 113 states that the remote timeout is 5 seconds and that no operator configures it before version 1.0.0. Line 135 of the `Interfaces` block published a constructor parameter `timeout: float = 5.0`, and **a published parameter is configurable by definition**, so the two could not both hold. #57 found the divergence, shipped the narrower reading as the module constant `_REMOTE_TIMEOUT`, and reported the question rather than settling it. **The narrower reading wins, and the Go port is the evidence.** `CLAUDE.md` parity rule 2 gives the port the interface where FoxIO specifies nothing, and a timeout is that kind of choice. `lookup.go` of `Crank-Git/ja4plus-go` publishes `RemoteLookupConfig`, which names two fields, `Endpoint` and `HTTPClient`, and it names no timeout. Its lookup entry point is `LookupFingerprintRemote(ctx context.Context, cfg *RemoteLookupConfig, fingerprint string)`, so the caller carries the deadline in the context and the published surface names no timeout. **The 30 second interval of `cmd/ja4plus/main.go:332` is no counter-example.** It sits inside `runDBUpdate`, it covers the mapping file download and not the lookup, and a command internal publishes nothing. **The repair removes the parameter from the `Interfaces` block, and no file under `ja4plus/` moves**, because `JA4DBClient.__init__` already names `allow_remote` and `cache_size` alone. **The settled reading now carries a check, following #332 and #334.** `tests/test_db_offline.py` gains `test_the_constructor_publishes_no_timeout_parameter`, which reads `inspect.signature(JA4DBClient.__init__)` and pins the parameter list at `self`, `allow_remote` and `cache_size`. **A specification statement that a test can contradict outranks one that only a reader can.** **The case is proven by its reversal.** With `timeout: float = 5.0` added to the constructor, it fails, and with the constructor restored it passes, under `PYTHONDONTWRITEBYTECODE=1`. **Test-driven development applies oddly to a prose repair and this round says so rather than inventing a test for prose.** The prose carries no behaviour, so the case pins the code the prose describes, and it did not come first. **The block now names why it publishes no timeout**, so the next reader of it resolves the question from this record and not from its own judgment. #59 edits the same block. **Line 113 does not move**, so the comment of `ja4plus/ja4db.py:53` that cites it stays accurate. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 2061 passed to 2062 passed, which is the one case this issue adds, and 8 xfailed holds. **Coverage does not fall.** `__all__` of `ja4plus` names 25 entries before and after. The `## Terms` table gains no word. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 120 | 2026-08-08 | #60 asks for a bound that the base commit `3c0ce5f` already holds, so this round measures the bound and proves it rather than building it. **Round 83 built the bound under #42, and both acceptance criteria of #60 pass on the base commit.** `tests/test_ja4db.py::TestTheLookupCacheBound` reports 10 passed there, and it holds one case for each criterion. The issue body names `ja4plus/ja4db.py:68` and a plain dictionary, and neither survives. The line predates #42, #57 and #58. `JA4DBClient.__init__` builds a `BoundedStateTable` of 100000 entries. **Neither criterion is vacuous, and two reversals prove it.** The first reversal makes the count bound evict the newest entry rather than the least recently used one. `test_the_cache_evicts_the_least_recently_used_entry` then fails alone. The second criterion therefore separates a least-recently-used eviction from an arbitrary one. The second reversal makes the count bound remove nothing. 4 cases fail, and the first criterion is one of them. Each reversal ran with `PYTHONDONTWRITEBYTECODE=1`, and each read the loaded source back before the run. **A full lookup cache holds about 47 MiB, and the entries are the smaller part of it.** `tracemalloc` measured a client whose remote lookup returns None, across 100000 distinct lookups and then 200000, and it reads 25.34 MiB and then 47.06 MiB. A run of 200000 distinct lookups fills two structures, and 100000 lookups fill the entries alone. The second structure is `StateTable._evicted_keys`, which remembers 100000 keys beside the 100000 entries, and no key of the one is a key of the other. **Two methods agree on the total, and they split it differently.** `sys.getsizeof` reads 46.95 MiB over the two structures, against the 47.06 MiB of `tracemalloc`, so the total is firm. It gives the entries 30.89 MiB and the evicted keys 16.06 MiB. The `tracemalloc` difference gives 25.34 MiB and 21.72 MiB. `sys.getsizeof` reads the capacity of a hash table rather than the allocation, so the two splits differ. The total is the figure this round rests on, and it reads no split as exact. **The figure is reported against a proposal and not against a decided ceiling.** #38 proposed 512 MiB and #279 is open, so this package publishes no ceiling yet. Read against that proposal, the lookup cache is 9.2% of it, and the 238.96 MiB projection of #279 rises to 286.02 MiB. The lookup cache therefore fits, and this round decides no ceiling. **The evicted-key memory of the lookup cache buys a count that nothing reads.** `Processor.stats` collects the state tables of the fingerprinters, and `JA4DBClient` is no fingerprinter. No code under `ja4plus/` calls `JA4DBClient._cache.stats()`. `returned_connections` therefore reaches no reader for this table. The bound of that memory is measured, and one case failed when the bound was removed: `tests/test_processor_stats.py::TestTheReturnedConnections::test_the_memory_of_the_evicted_keys_holds_the_entry_bound`. Whether the lookup cache needs that memory is a product question, and this round opens it rather than answering it. **The issue names a maximum entry count and no maximum age, and the client holds both.** `DEFAULT_CACHE_AGE` is 600 seconds, which is the `DEFAULT_MAX_CONNECTION_AGE` of #38, because one project holds one maximum age. The `## Terms` table names the lookup cache no state table. The `CLAUDE.md` rule therefore reaches it through the reading of round 83 rather than through the table. The client satisfies the rule under either reading. **This round changes no file under `ja4plus/`, so no fingerprint moves.** The unit suite reports 2061 passed and 8 xfailed before and after. The conformance suite reports 135 xfailed, and `tests/foxio_deviations.json` holds 135 keys against those 135. `__all__` of `ja4plus` names 25 entries before and after. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 121 | 2026-08-08 | #61 landed and `ja4plus db update` writes the mapping file to the cache directory. **The command wrote inside the installed package, and that is the defect this round repairs.** On the base commit `6d6861a`, `cmd_db` called `os.makedirs(os.path.dirname(_BUNDLED_CSV))` and wrote the download over `ja4plus/data/ja4plus-mapping.csv`. `features/07-db-enrichment.md` line 114 refuses that, because a package directory may be read-only, several users may share it, and the next `pip install` discards the file. **The defect is not theoretical, and the mutation run proved it.** One mutation returned the write to `_BUNDLED_CSV`, and the run replaced the bundled mapping file of the worktree with the two-line file of the case. `git checkout -- ja4plus/data/ja4plus-mapping.csv` restored it. **The command now writes `$XDG_CACHE_HOME/ja4plus/ja4plus-mapping.csv`, `~/.cache/ja4plus/ja4plus-mapping.csv` or `~/Library/Caches/ja4plus/ja4plus-mapping.csv`.** `cache_dir_path` reads `sys.platform` and holds the two conventions the feature file states. **The port corroborates both names.** `CachedDatabasePath` of `lookup.go:210` joins `os.UserCacheDir()`, `ja4plus` and `ja4plus-mapping.csv`, and `os.UserCacheDir` reads `$XDG_CACHE_HOME` or `~/.cache` on Linux and `~/Library/Caches` on macOS. One `db update` therefore serves both implementations. Verified against: https://github.com/Crank-Git/ja4plus-go/blob/master/lookup.go (retrieved 2026-08-08). **The command writes a temporary file and renames it**, as `runDBUpdate` of `cmd/ja4plus/main.go:352` does, so a reader reads the whole new file or the file the last run wrote. **The client prefers the cached mapping file, and the round proves both directions.** `load_mapping_file` returns the mapping, the source and the path, and `JA4DBClient.__init__` and `db info` both read it. A cache file that holds one entry answers a fingerprint the bundled file holds no entry for, and a run with no cache file answers the bundled fingerprint and not the cached one. **A mapping file that this program did not write is hostile input, and `CLAUDE.md` rule 2 reaches it.** An empty file, a file of arbitrary bytes and a file whose mode denies the read each fall back to the bundled file, and each writes one `WARNING` record. **A file of arbitrary bytes raises `UnicodeDecodeError`, which is no `OSError`**, so `_read_mapping_file` names it beside `OSError` and `csv.Error`. The handler names its errors and catches no bare `Exception`. **`db info` reports the source, and the word it prints is `embedded`.** An earlier form of `features/07-db-enrichment.md` stated that a source value is `bundled`, `cache` or `remote`, and the fourth acceptance criterion of #61 stated `bundled`. `lookup.go:31` sets `dbSource = "embedded"` and `runDBInfo` of `cmd/ja4plus/main.go:378` prints it. **The two disagreed on one user-visible word**, so `CLAUDE.md` parity rule 2 and the acceptance criterion pointed opposite ways. This round reported the question rather than settling it, and **the user decided on 2026-08-08 that the port wins.** The port already corroborates every other interface choice of this feature, so one disagreement against five agreements is a specification that drifted. The feature file, its fourth criterion, the `## Terms` row, `CHANGELOG.md` and `docs/api_reference.md` now publish `embedded`, and the feature file names the port as the authority and cites `lookup.go:31`. **The word `bundled` stays in the prose that describes the file inside the package**, and `_BUNDLED_CSV`, `_load_bundled_db` and `test_db_update_leaves_the_bundled_mapping_file_unchanged` keep their names, because only the published value changes. **Two sites keep the old word and #59 owns them**: the `source` comment of the `LookupResult` block, and the acceptance criterion that names `LookupResult`. #59 adds `source` to `LookupResult`, and this round changes no line of that interface. **`db info` gains a `Path` line and its `Source` line changes meaning.** The line carried the FoxIO address through version 0.6.0, and the address moves to a `Mapping` line. `runDBInfo` of `cmd/ja4plus/main.go:389` prints the `Source`, `Path` and `Entries` lines and the hint that names the cache file, and this command prints all four. **The cases came first and they failed for the stated reason**, because `ja4plus.ja4db` published no `cache_file_path`. **The self-review found one defect and the round repairs it.** `db info` printed `No cache file at <path>` for a cache file that exists and holds no entry, and the line contradicted the file on disk. The command now names all three states the `Screens & states` table of the feature file states. **Three mutations prove the cases measure behaviour.** With the cache branch removed, 3 fail. With the empty-cache guard removed, 4 fail. With the write returned to the installed package, 7 fail. **No test reaches the network and no test writes outside its own temporary directory.** Each case takes the `no_network` fixture of #57, and the `cache_home` fixture points `HOME` and `XDG_CACHE_HOME` at `tmp_path`. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 2062 passed to 2084 passed, and 8 xfailed holds. The 22 new cases are the whole rise, and 2 of them skip on macOS because they read the Linux convention. **Coverage rises from 92% to 93%.** `__all__` of `ja4plus` names 25 entries before and after. **The `## Terms` table gains `cache directory` and `source`.** `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 122 | 2026-08-08 | #59 landed and it closes Epic 7. `JA4DBClient.lookup_many` accepts a sequence of fingerprints and returns one entry per fingerprint, FR-db-enrichment-7, and every lookup result records its source, FR-db-enrichment-8. **`LookupResult` was a type alias for `dict[str, str]` and it is now a frozen dataclass.** It carries the three fields that `LookupResult` of `lookup.go:23` carries, plus `source`. A caller therefore reads `result.application` where it read `result["application"]` before. Verified against: https://github.com/Crank-Git/ja4plus-go/blob/master/lookup.go (retrieved 2026-08-08). **The source names a provenance and not a location.** A hit from the mapping file inside the package records `embedded`, a hit from the file that `db update` wrote records `cache`, and a hit from the lookup service records `remote`. `_read_mapping_file` takes the source of the file it reads, so one entry cannot report a file the client did not read. **A case that reads `embedded` alone cannot separate a client that reads the source from a client that writes one value**, so the round reads all three. **`lookup_many` multiplies no network.** It reaches the lookup service under the rule that `lookup` holds and under no other rule, because FR-db-enrichment-1 and FR-db-enrichment-2 govern the client and this file grants the bulk call no separate permission. A client built with `allow_remote=False` therefore sends nothing over 100000 fingerprints. A client built with `allow_remote=True` sends one request for each fingerprint the mapping file holds no entry for, and the lookup cache holds a miss as well as a hit, so 1000 occurrences of one fingerprint cost one request. The lookup service publishes no bulk interface, so no smaller count is reachable. **`features/07-db-enrichment.md` read two ways and this round repairs it.** #61 published `embedded` in the behaviour rule, the `## Terms` table, `CHANGELOG.md` and `docs/api_reference.md`, and it left the `Interfaces` block and one acceptance criterion reading `bundled`. Both now read `embedded`. The word `bundled` stays where it describes the file inside the package, and `_BUNDLED_CSV` and `_load_bundled_db` keep their names. **`__all__` of `ja4plus` names 25 entries before this round and after it.** It names no name of `ja4plus.ja4db`, so it names neither `JA4DBClient` nor `lookup`. A `LookupResult` added there alone would promise a type that no promised function returns, and FR-typed-api-13 makes a name there permanent until version 2.0.0. The question of whether version 1.0.0 promises the lookup interface is one the issue that names the move settles. **Three mutations prove the cases measure behaviour.** With the remote result reporting `embedded`, 5 fail. With `lookup_many` reading past the lookup cache, 1 fails. With every mapping file entry reporting `embedded`, 2 fail. **No test reaches `ja4db.com`.** Every case takes the `no_network` fixture of #57 or the `RecordingRequests` stand-in of the same module. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 2084 passed to 2099 passed, and the 15 new cases are the whole rise. Coverage holds at 93%. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 123 | 2026-08-08 | #364 landed and a `LookupResult` reads by field name, FR-db-enrichment-16, and that read emits a `DeprecationWarning`, FR-db-enrichment-17. **The user ruled the difference a specification gap on 2026-08-08.** #59 replaced the dict of version 0.6.0 with a frozen `LookupResult`, so `result["application"]` raised `TypeError: 'LookupResult' object is not subscriptable`. Epic 4 met the same situation and answered it the other way, because `FR-typed-api-5` gives `FingerprintResult` item access and `FR-typed-api-6` makes it warn. #59 reported the difference and shipped what the specification said, rather than inventing a requirement. **The two new requirements are worded from those two and not freshly invented.** **This project holds one spelling of the behaviour, and this round adds no second one.** `LookupResult.__getitem__` copies `FingerprintResult.__getitem__` of #44: it reads a module-level frozen set of field names, it raises `KeyError` before it warns, it passes `stacklevel=2` so the caller's line is reported, and it returns `getattr(self, key)`. **The check #44 could not make is made, and it clears the design.** `git show v0.6.0:ja4plus/ja4db.py` holds the dict at `_load_bundled_db` and at `_remote_lookup`, and each site writes the same three keys: `application`, `type` and `notes`. Each one names a field of `LookupResult`, so no key of version 0.6.0 returns the value of another field, and none of them raises `KeyError`. **That is the point where this differs from `FingerprintResult`, which renamed the method key to `type` and therefore raises `KeyError` for `"method"`.** The `source` field of FR-db-enrichment-8 is an addition and no rename, so it reaches no version 0.6.0 caller. **The tests came first**, and `tests/test_db_item_access.py` reported 6 failed and 4 passed against the unrepaired dataclass, at the 10 cases it held then. The first failure read `TypeError: 'LookupResult' object is not subscriptable`. **Four mutations prove the cases measure behaviour, and each one was proven live by reading the loaded source back with `PYTHONDONTWRITEBYTECODE=1`.** With the warning moved before the membership test, 3 fail, among them `test_an_unknown_key_raises_a_key_error_and_emits_no_warning_first`. With the warning removed, 2 fail. With `stacklevel=1`, `test_the_warning_reports_the_line_of_the_caller` fails alone. With `getattr(self, key)` replaced by `self.application`, `test_item_access_returns_the_value_the_attribute_holds` fails alone. The restored form passes 11 of 11 after each mutation. **A case asserting only that item access works cannot separate a deprecated accessor from a permanent one**, which is why the warning and the ordering each carry a case. **The self-review found one consequence of the method that no case recorded.** A `__getitem__` makes the old iteration protocol reach the object, so `list(result)` calls `result[0]` and raises `KeyError` where the dict of version 0.6.0 iterated its keys. `FingerprintResult` behaves the same way and records it nowhere. The eleventh case records it here. **No caller inside `ja4plus/` iterates a lookup result.** `ja4plus/cli.py:385` is the one site, and it reads `match.application`. The method adds no `__len__`, so `if match:` keeps its meaning. **No fingerprint moves.** The conformance suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, and `tests/foxio_deviations.json` holds 135 keys against those 135. The unit suite rises from 2099 passed to 2110 passed, which is the 11 cases this issue adds, and 8 xfailed holds. **Coverage holds at 93%**, and `ja4plus/ja4db.py` reads 95%. **`__all__` of `ja4plus` names 25 entries before and after.** #59 published no `ja4plus.ja4db` name there, because promising a type that no promised function returns would be worse than promising neither, and this round leaves that reasoning alone. `docs/api_reference.md` gains the deprecated item access under the lookup client, and the `Interfaces` block of `features/07-db-enrichment.md` publishes the method. The `## Terms` table gains no word. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 124 | 2026-08-09 | #368 landed and `examples/monitoring_daemon.py` is absent again. **A merge restored a file that a case asserts is absent, and no merge tool reported it.** #56 removed the example and added `tests/test_watch_capture.py::TheExampleDaemonIsAbsent`, and that commit reached `dev` at `79f8c66`. `batch/322-hygiene-two` forked from `dev` before `79f8c66`, so its own run never collected the case. Both parents of the merge `8b87c20` were green and the merged tree was not, because the file and the case live in different directories. `dev` then ran red across `35bdfab` and `8ef8acc`, and **`8ef8acc` merged pull request #366 over six failing test jobs**. **The case was measured in the order the issue asks.** It fails on the base commit `8ef8acc` with `AssertionError: True is not false` at `tests/test_watch_capture.py:469`, and it passes after the removal. **This round moves no file under `ja4plus/`**, so no fingerprint moves and no published field changes. This round reads no FoxIO material. **Six mentions of the file survive, and this round rewrites none of them.** Two are rounds of this table, 97 and 104, and round 104 already decided that round 97 stays as written. Two are entries of `CHANGELOG.md`, at lines 105 and 291. Three are lines of `docs/specs/features/06-live-capture.md`: the `Purpose` paragraph, the `Data touched` entry and the acceptance criterion that states the file is absent. **Each one is a past-tense record or an absence claim, and none of them directs a reader to run the file**, so no reference misleads a reader. **Acceptance criterion 3 of the issue forbids every mention, and no reading of it is satisfiable.** This row names the file, the round table is append-only, and a criterion cannot state the absence of a file without naming it. The user decides how far criterion 3 reaches, and the removal above stands whichever way that decision falls. |
| 125 | 2026-08-09 | #369 landed and `TheReporterWritesOneLinePerInterval` states the schedule of the statistics reporter rather than samples it. **The class measured how promptly the host schedules a thread, and it reported that as a defect of the package.** Each case started `StatisticsReporter` at a 0.05 second interval, slept a fraction of a second, and asserted a lower bound on the count of lines that arrived. The `macos-latest, 3.12` job of the run for `8ef8acc` read `AssertionError: 2 not greater than or equal to 3` and turned `dev` red, and the five other jobs of the same run passed. **The race was reproduced before the repair, by shrinking the sleep rather than by loading the host.** The sweep prints 6 lines at a 0.35 second sleep, 4 lines of 5 due at 0.25, and `AssertionError: 2 not greater than or equal to 3` at 0.15, on an idle host. The lost wakeup at 0.25 is the measurement that settles it: the count is a function of the host and not of the reporter, because `Event.wait` restarts the whole interval after each line, so the write cost and the scheduling latency accumulate. **The issue left two readings open and this round chose the injected wait, on evidence and not on preference.** The other reading reads the gap the reporter asks for rather than the gap the host delivered, and the only way to observe that gap is to intercept the wait, so both readings cost the same seam. Given the seam, the injected wait holds everything the other reading holds. It asserts the gap the reporter asks for, which is the whole of the other reading. It also reads an exact line count instead of a lower bound, and it removes the wall clock from all four cases rather than one. **Read that as a superset of the other reading, and not of the earlier form.** The earlier form asserted the elapsed time between two lines, and no case asserts that now. **This round removes that assertion on purpose**, because criterion 1 of the issue bars it: the host decides that time, and the `macos-latest, 3.12` job proved it. **The seam is one parameter and the shipped behaviour does not move.** `StatisticsReporter` gains `wait`, which receives the interval and returns True when the stop arrives, matching `threading.Event.wait`. The default is `self._stop.wait`, `ja4plus watch` passes no other call, and `_run` is the one line that changed. **A case binds the seam to the shipped path**, because an injected wait proves nothing about FR-live-capture-9 unless the default wait is the real one, and `TheReporterStopsCleanly` then measures that the stop ends the thread. **The tests came first**, and the six cases reported `TypeError: StatisticsReporter.__init__() got an unexpected keyword argument 'wait'` against the unrepaired class. **Five mutations prove the cases discriminate, and each one was proven live by reading the loaded source back with `PYTHONDONTWRITEBYTECODE=1`.** A reporter that writes one line before it waits fails 4 cases. A reporter that asks the wait for a constant 0.05 fails the interval case with `AssertionError: Lists differ: [0.05, 0.05, 0.05, 0.05] != [1.0, 1.0, 1.0, 1.0]`, which is why that case runs two intervals. A reporter that writes two lines for one interval fails 3. A reporter that ignores the injected wait fails 6. A `write_statistics` that flushes nothing fails the flush case alone. The restored form passes 6 of 6 after each mutation. **The count assertions are exact and no longer a lower bound**, so a reporter that writes an extra line now fails, where the earlier `assertGreaterEqual` passed. **The class runs 20 times in a row and passes 20 times**, and it holds no `time.sleep`, where the earlier form slept 1.10 seconds per run. **No fingerprint moves**, and the conformance suite reports 1531 passed, 143 skipped and 135 xfailed, with 135 keys in `tests/foxio_deviations.json` against those 135. **Coverage holds at 93%**, and `ja4plus/watch.py` reads 99% before and after. The unit suite rises from 2194 passed to 2196 passed, which is the two cases this issue adds net of the four it replaces. **One case of the same fault stays, outside the class this issue names.** `TheStatisticsGoToStandardError::test_the_periodic_line_reaches_standard_error` sleeps 0.3 seconds and asserts at least 2 lines, and it reaches the reporter through the command, so a deterministic form needs a seam through `cmd_watch`. #371 records it. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 126 | 2026-08-08 | #279 landed and this package states a memory ceiling of **512 MiB** for the one-million-packet case at the shipped defaults. **The user decided the number on 2026-08-08**, and #38 proposed it. `docs/specs/features/03-concurrency-safety.md` states the number, the defaults it holds at, the traffic the case feeds, and the boundary of the claim. **This round re-measured the package rather than quoting a projection.** The reading is 383.47 MiB, 388.25 MiB, 392.05 MiB and 394.94 MiB across four runs on macOS 26.6.1 with Python 3.14.3, and the highest is 77 percent of the ceiling. **The table states every run and not the best one**, because a resident memory reading moves with the machine. **Three earlier figures describe no branch that exists.** #38 projected 238.96 MiB from an idle `Processor` at 91.03 MiB, one JA4SSH-shaped table at 51.72 MiB and nine JA4L-shaped tables at 10.69 MiB each. Round 82 read 419.27 MiB for the whole run. #60 put the projection at 286.02 MiB with the lookup cache included. This branch reads an idle `Processor` at about 100 MiB, 40200 entries across seventeen state tables, and 400000 values across ten value lists. **The ceiling covers the stated case and no longer run.** `BaseFingerprinter.fingerprints` grows without a limit, so resident memory rises about 23 MiB for each 100000 packets past the 200000th, and **this round measured the crossing at 1500000 packets and 513.06 MiB** rather than projecting it. `README.md` and `docs/api_reference.md` each held the sentence `This package states no memory ceiling.`, and each now states the ceiling and its boundary. **The measurement runs in an interpreter of its own, and that is part of the reading**, because `resource.getrusage(RUSAGE_SELF).ru_maxrss` reports the high-water mark of the whole process and a reading taken inside the pytest session measures every case that ran before it. New file `tests/memory_ceiling_run.py` feeds the packets and writes one JSON object, and `TestTheStatedMemoryCeiling` of `tests/test_memory_bounds.py` reads it. **The case feeds 30000 packets by default and `JA4PLUS_MEMORY_CEILING_PACKETS` sets the count**, because the full run costs 481 seconds and a case of that length costs every later run of the unit suite; `tests/test_thread_safety.py` holds the same arrangement for the 60-second soak of #40. **The premise that a raised table limit turns the case red is wrong, and this round measured it three ways.** With `ja4ssh.connections` raised from 10000 to 100000 the run reads 383.00 MiB and stays green. With both JA4L bounds raised to 100000 it reads 412.06 MiB and stays green, because the age bound then holds those two tables at 24000 entries rather than 100000. With all fifteen entry counts raised to 100000 no run produced a reading: the operating system killed two, and a third reached 2500 connections in ten minutes. **The age bound and the traffic mix hold this ceiling, and the entry count bound holds two tables of seventeen.** `ja4ssh.connections` never reaches its shipped maximum on this traffic, holding 6000 entries against 10000, and the 5423 bytes for each JA4SSH entry that #38 measured describe a full 200-packet window where the case gives each connection two SSH packets. **The committed class carries three controls, because the default size holds 3000 connections and the ceiling comparison alone cannot fail there.** With the control bound raised from 100 to the shipped 10000, `test_a_smaller_entry_count_holds_less_resident_memory` fails and reads `a bound of 10000 added 38.26 MiB and the shipped bounds added 39.08 MiB, a ratio of 0.979, so the entry count changed nothing`. **That control compares a ratio and not a MiB figure**, because the absolute reading moves with the platform while the ratio measures the bound itself, and an absolute margin that suits macOS can sit above the whole signal elsewhere. Five runs at the default size held the ratio between 0.746 and 0.769 and the raised bound reads 0.955 and 0.979, so the threshold of 0.85 sits between the two. **No file under `ja4plus/` changes and no fingerprint moves.** The `## Terms` table gains `resident memory` and `memory ceiling`. **Batch #362 repaired the two controls, because both subtracted two high-water marks.** Pull request #384 ran the suite on Ubuntu for the first time, and four jobs failed on Python 3.9, 3.11, 3.12 and 3.13. `lint`, `fuzz`, `conformance` and `macos-latest 3.12` passed. **A high-water mark rises and never falls, so the difference between two of them states `max(0, later mark - earlier mark)` and not the growth between the two readings.** The run read `idle_mib` after the import of scapy and `peak_mib` after the traffic, so the import mark swallowed the run. The four jobs read `154.7`, `176.18`, `178.56`, `179.56` and `185.47` for both numbers, and `test_the_reading_measures_the_traffic_and_not_the_interpreter` and `test_a_smaller_entry_count_holds_less_resident_memory` failed. **The ceiling case passed on every job, and it is the one legitimate high-water question here.** The file already stated the mechanism at line 320 and then contradicted it in code. **A second measurement process was proposed and the readings refute it.** An idle process and a run process each hold the import mark, so the difference between their two marks stays zero. The cause is the import transient and not the count of processes. **The repair reads the current resident set, from `/proc/self/statm` on Linux and from `ps` on Darwin, and `proc_pid_statm(5)` states the field order the reader uses.** The run reports `idle_resident_mib` and `final_resident_mib` beside the unchanged `peak_mib`, `traffic_growth_mib` refuses a high-water pair as void, and `TestTheGrowthReading` holds three cases that start no interpreter. **The mechanism was reproduced on macOS, because no Linux host was available.** A probe allocated 80 MiB of small objects and freed them before the idle reading, which reproduces the Ubuntu shape. Under that transient the mark read a growth of **0.00 MiB** and the current reading read **35.58 MiB**, at 30000 packets and the shipped bounds. The freed pages left the resident set, from 243.86 MiB to 111.86 MiB, so a freed allocator arena absorbs no part of the traffic. **The ratio holds under the transient and it discriminates.** The control bound of 100 read 26.05 MiB against 35.58 MiB, a ratio of 0.732, and a control bound raised to the shipped 10000 read 34.03 MiB, a ratio of 0.956. Without the transient the two runs read 38.62 MiB and 28.81 MiB, a ratio of 0.746, which reproduces the range this round first measured. **Two mutations prove the repaired controls discriminate.** The control bound raised to 10000 fails the third control at a ratio of 0.969, and a run that reports a flat pair fails the second at `the run added 0.00 MiB, so the reading measures no traffic` and the third at `the shipped run added 0.00 MiB, which is too little to read`. The ceiling case passes under both mutations. **The Linux reading stays unverified until Ubuntu runs it**, and the mechanism forces it: the mark proves the run stayed below the import mark, and it states nothing about the current set. **No file under `ja4plus/` changes in the repair.** |
| 127 | 2026-08-09 | #347 landed and the `capability` field alone bars the 35 measured rows of #129. **The bar stood twice, and one of the two enforcers was an absent measurement.** #341 shipped the field on all 135 register entries and reported that the one thing it should prove, it could not: #334 measured the 35 source values of #129, deliberately left them out of `SOURCE_VALUES`, and `test_no_measured_row_records_a_capability_decline` asserted the exclusion. **Flipping the field on a #129 entry therefore moved nothing, and no case failed.** That is the project's signature defect, a comparison that is never made, and this one was recorded rather than discovered. **This round measured the 35 rows against the pinned FoxIO checkout** `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` and put them in the table. It read 37 decided value-form keys of #129 against the Rust snapshots and the Wireshark testdata of the two captures those keys name, and it resolved every Wireshark frame number against its capture with `scapy` to read the 5-tuple, which is the method #334 recorded. **A capture-level reading would mis-key these rows.** **The reading corroborates itself**: all 16 Wireshark JA4H values and all 3 Wireshark JA4X values equal the FoxIO Python values at the same connection, method and occurrence, which is what a correct frame-to-connection resolution produces. **No Zeek baseline reaches these rows.** `zeek/README.md` states that the JA4X module is off, no baseline names `http2-with-cookies.pcapng`, and the one baseline that names `chrome-cloudflare-quic-with-secrets.pcapng` is a `conn.log` that holds JA4L and JA4T columns alone. **Two of the 37 rows carry no source value**, and they are `chrome-cloudflare-quic-with-secrets.pcapng/0:57098/JA4X.1` and `JA4X.2`, because neither the Rust snapshot nor the Wireshark file holds a JA4X value for that capture. That figure of 35 is the one #334 recorded, reached by an independent run. **The reach holds at 6 rows before the change and after it.** **The measurement the field now permits reads 25.** With `capability` false on all 43 #129 entries, written to `tests/foxio_deviations.json` on disk, the reach rises from 6 to 25 and returns to 6 after the restore; the register file digest is `6273fc7ba15d3558` before, `713fe5d2c556f72c` with the mutation, and `6273fc7ba15d3558` after. **The same flip on the base file reads 6 and 6**, which is the measurement #341's acceptance criterion asked for and could not have. The 19 rows that enter are the 16 JA4H_ro rows and the 3 JA4X rows. **The 16 JA4H rows stay out under the disagreement bar**, because the Rust value and the Wireshark value differ on every one of them, so the field is not the only bar on the family. **The renamed case holds the one meaning the exclusion did not carry.** `test_no_measured_row_records_a_capability_decline` becomes `test_every_measured_capability_row_names_the_issue_the_bar_exists_for`: a measured capability row names #129, and a row under another issue would reach a bar no measurement has read. Its sibling `test_every_measured_row_names_a_decided_register_key` already carried the `decided` half. **Neither new case can pass vacuously, and five live mutations prove it.** Dropping the capability check from `exception_reach`, dropping one JA4X row from the expected rise, dropping one JA4X row from the measured table, reading the measured count as 34, and reading the capability issue as 130 each fail the case they target, and each case passes clean and after the restore. Every run cleared every `__pycache__` outside `.venv/` and set `PYTHONDONTWRITEBYTECODE=1`, and `inspect.getsource` reports `if deviation.capability:` once in the loaded `exception_reach`. **No file under `ja4plus/` changes, no register entry changes, no vector is adopted and no fingerprint moves.** The conformance suite reports 135 xfailed against 135 register keys. |
| 128 | 2026-08-09 | #258 landed and every Changelog round number names one row. The Changelog carried the number 9 on two rows. **The two rows are not two rounds.** One row records the shipment of Epic 1 batch 1, and it names #80 as a member. The other row details #80 on the same date. **The repair folds the detail row into the shipment row, and it renumbers no row.** The fold preserves every sentence of the folded row. **A round number is an identifier, and it states no order.** The date column carries the order. Rounds 10 through 124 are all taken, so no free number sits beside the number 9. A renumber of the rows above it would break every citation of a round number. Those citations carry weight: round 67 holds the #226 part e decision, and #215 and #226 cite it. **Three premises of the issue were stale, and this round measured all three again.** The ruling named `spec.md:542` and `:546`, and the rows were at 569 and 573. The ruling named 88 as the highest round, and the highest was 124. The ruling searched `[Rr]ound 9\b` and found no citation. That pattern misses the one citation that exists. Round 12 closed with "The rounds below hold two entries numbered 9". The fold makes that sentence false, so round 12 now records the repair. `tests/test_specification_changelog.py` holds the invariant. Both of its cases fail when the duplicate returns. No file under `ja4plus/` changes, no fingerprint moves, and the register holds 135 keys against 135 xfailed. |
| 129 | 2026-08-09 | #359 landed and the lookup cache remembers no key it evicts. **The issue asked for a decision and it named two shapes. This round therefore measured both shapes, and it reasoned about neither.** `StateTable` remembers the key of every entry it evicts, and that memory buys `returned_connections`. `Processor.stats` collects the state tables of the fingerprinters, `JA4DBClient` is no fingerprinter, and no code under `ja4plus/` reads the count for this table. **Shape 1 recovers the whole saving, so shape 2 is unnecessary and this round does not build it.** `BoundedStateTable` and `StateTable` now take `track_evictions`, it defaults to True, and `JA4DBClient` is the one caller that states False. **The saving is 16.06 MiB, and the two methods #60 used agree on it to 0.00 MiB.** A full lookup cache of 100000 entries falls from 47.06 MiB to 31.00 MiB under `tracemalloc`, and from 44.66 MiB to 28.60 MiB under `sys.getsizeof`. **The before figure reproduces #60 exactly**, which is the reading that ties this measurement to that one. Each run cleared every `__pycache__` outside `.venv/` and set `PYTHONDONTWRITEBYTECODE=1`. One run measured both settings in one process, so no difference in the run conditions reaches the difference. **Eleven callers build a state table and ten of them keep the statistic.** `ja4.py` builds the QUIC fragment table. `ja4h.py` builds the reassembly table. `ja4l.py` builds `connections` and `grouping_keys`. `ja4s.py` builds `_quic_dcids`. `ja4ssh.py` builds `connections` and `_handshake_clients`. `ja4t.py` builds `connections`. `ja4ts.py` builds `times` and `prefixes`. `ja4x.py` builds `processed_certs` and `scan_offsets`. `watch.py` builds `_connections`. `tcp_stream.py` inherits `StateTable` for the two reassemblers. Each one states no `track_evictions`, so each one costs nothing and reports what it reported before. **The eviction count stands for the lookup cache**, so FR-concurrency-safety-12 holds for it and the invariant `inserts == entries + evictions + removals` holds. **The new parameter sits before `on_eviction` and breaks no caller.** Every caller of `on_eviction` states it by keyword. `ja4ts.py:111` and `watch.py:449` are the two callers, and `tests/test_state_table.py` holds three more. **#279 measured the 512 MiB ceiling case without a lookup cache**, so this saving moves none of its four runs of 383.47, 388.25, 392.05 and 394.94 MiB. It moves the worst case of a program that runs a monitor and a full lookup cache together, from 442.00 MiB to 425.94 MiB. **Ten cases came before the change and nine of them failed against it.** The tenth, `test_the_lookup_cache_still_counts_the_entries_it_evicts`, is a control that passes on the base commit. **The reversal of each new case proves it.** With `track_evictions=False` restored to tracking, `test_a_table_that_tracks_no_eviction_remembers_no_key`, `test_a_table_that_tracks_no_eviction_counts_no_returned_connection`, `test_the_age_bound_of_a_table_that_tracks_no_eviction_remembers_no_key` and `test_the_lookup_cache_remembers_no_evicted_key` each fail. **A first form of the lookup cache cases turned the ceiling control red, and the cases and not the code were the cause.** `tests/test_memory_bounds.py::TestTheStatedMemoryCeiling::test_a_smaller_entry_count_holds_less_resident_memory` failed 2 of 2 full runs under `--cov=ja4plus` on this branch and passed 3 of 3 on the base commit. A run that deselects the two new case classes passes. A run that deselects the three `JA4DBClient` cases alone passes too, so those three client constructions carry it. Each one loaded the whole mapping file into the session, and the control reads the resident memory of the session. The cases now patch `load_mapping_file` to an empty mapping, because each one reads the lookup cache and no mapping entry. **The production change causes none of it**, and the deselect run is the reading that proves so. **No fingerprinter changes and no fingerprint moves.** |
| 130 | 2026-08-09 | #272 landed and the six JA4L register entries that #34 left open now hold a live owner. **#272 declines five of the six, and the cause states why in exact words, because the wrong words read as a pass.** `CVE-2018-6794.pcap/JA4L-C`, `CVE-2018-6794.pcap/JA4L-S`, `https-connect.pcap/JA4L-C`, `https-connect.pcap/JA4L-S` and `tls-handshake.pcapng/JA4L-S` carry a value that no reference value opposes. **This project was never emitting more than the reference. The reference published nothing to compare.** The comparison is unreachable, not satisfied. `python/ja4.py:339` runs `delete_keys(['JA4L-S','JA4L-C'], final)` when the generating run names another method, and the round re-read that line at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` rather than cite #200 unread. A count of the JA4L keys of the 38 expected-output files reports 0 for `CVE-2018-6794.pcap.json`, `https-connect.pcap.json` and `tls-handshake.pcapng.json`, which corroborates the mechanism against the vectors. The five entries now carry `"decided": true` and `"capability": false`, so each records a value decline. **The sixth is a defect of this project, and the round measured the repair before it changed any code.** `ssh2.pcapng/JA4L-S` compares two measured counts: the FoxIO Python file holds 10 JA4L-S values and `ja4plus` produced 11. **The extra value is a duplicate and not a new reading.** Stream 15, the connection to `184.150.157.177:80`, produced `6252_58` twice where the reference holds it once. `ja4plus/fingerprinters/ja4l.py` returned the server value on every SYN-ACK, so a retransmitted SYN-ACK repeated the value the first SYN-ACK gave. A retransmitted SYN-ACK moves neither point `B` nor the server TTL, so the repeat described no second measurement. **#272 set three conditions that stop the repair, and the round tested each one and found each one false.** The repair removes no case that measures a JA4L-S value: 142 JA4L values stand after it, and `ssh2.pcapng` alone keeps 10. The repair moves no JA4L value on any other capture: a replay of all 38 committed captures reports exactly 1 moved key, and it is the duplicate itself. The extra value is no artifact of the same `delete_keys` call, because `ssh2.pcapng.json` publishes 10 JA4L-S keys and the call deletes the key whole. **The register falls from 135 keys to 134 and the conformance suite falls from 135 `xfailed` to 134**, so the register invariant holds on both sides of the change. The suite reports 1532 passed against 1531 before, and the one case that moved from `xfail` to pass is the `ssh2.pcapng` JA4L-S occurrence comparison. `tests/test_ja4l_connection_state.py::test_a_retransmitted_syn_ack_reports_no_second_server_value` came before the change and failed against the base, and it passes against the repair. The case feeds a SYN-ACK that a second SYN-ACK follows, which is input the guard alone rejects. **No entry is marked `decided` to make a check pass**, which is what #193 refused and #251 refused again. The run cleared every `__pycache__` outside `.venv/`, set `PYTHONDONTWRITEBYTECODE=1`, and confirmed the loaded guard with `inspect.getsource`. |
| 131 | 2026-08-09 | #297 landed and `ruff` enforces F401. **Every count the issue recorded is stale, and this round measured each one again.** The issue read 58 findings across 28 files on `issue/47-py-typed` at `ruff 0.14.5`. This round reads **54 findings across 27 files** on `batch/362-hygiene-three` at `ruff 0.16.2`. `ja4plus/utils/x509_utils.py` holds 3 findings and not the 5 the issue recorded. **The `ignore` comment recorded 82 I001 findings, and the rule reports 76.** The comment now states 76. **`ruff --fix` decides no removal here, because an unused import is not always unused.** This round classified all 54 before it removed one. The five shapes are a side-effect import, a re-export, a `TYPE_CHECKING` import, a test helper, and a genuinely unused name. **All 54 are genuinely unused.** No consumer imports a flagged name from a flagged module, so no removal breaks a re-export. `parse_http_request` is the one flagged name that another module reads. Every reader takes it from `ja4plus/utils/http_utils.py`, and none takes it from `ja4plus/fingerprinters/ja4h.py`. No file that holds a finding declares `__all__` or a `TYPE_CHECKING` block. **No import statement under `ja4plus/` loses every name.** Every `scapy.all` line keeps `Packet`, so no module-level side effect disappears. `tests/test_ja4x.py` is the one file whose whole `scapy.all` statement goes, and `ja4plus.fingerprinters.ja4x` still imports `scapy.all` for it. **15 findings sit under `ja4plus/fingerprinters/` and `ja4plus/utils/` across 7 files, and the conformance suite proves each removal inert.** That suite reports 1531 passed, 143 skipped and 135 xfailed before the change and after it, against 135 register keys. The unit suite reports 2212 passed, 2 skipped, 8 xfailed and 96 subtests before and after. **Coverage holds at 93.** The missed line count stays at 287. The statement count falls from 4231 to 4226, because five removed import lines were covered statements. Every measurement cleared every `__pycache__` outside `.venv/` and set `PYTHONDONTWRITEBYTECODE=1`, because a stale `.pyc` lets a removed import keep working. **No fingerprinter changes behaviour and no fingerprint moves.** |
| 132 | 2026-08-09 | #345 landed and a case holds the two counts the marker rule states. **The `## The marker rule` section of `tests/foxio_deviations.py` read `38 of the 128 decided entries`, and the register holds 134 decided entries.** The denominator went stale twice under a sentence nothing measured. #341 found the first drift at 129. **#272 moved the register again inside this batch.** It removed `ssh2.pcapng/JA4L-S` and it decided the five method filter entries. **This round re-measured all three numbers on its own base commit `f238c15`, and it carried none of them from the issue body.** The register holds 134 keys, 134 decided entries and 38 decided entries that cite no decision. **The numerator was correct and the issue said so.** The 38 are still the 34 entries of #138 and 4 of the 5 entries of #151. **The register now holds zero undecided entries, so the denominator equals the key count.** **The correction is the smaller half of this round.** A count in prose that nothing measures goes stale on the next entry. That is the failure mode this project keeps meeting. The repair therefore states the rule as a condition a case tests. `TestTheMarkerRuleCounts` in `tests/test_foxio_deviations.py` reads the two counts out of the sentence and measures them against the register. It reads them with the pattern `which (\d+) of the (\d+) decided entries do`. `measured_marker_rule_counts` counts the decided entries, and the decided entries that `DECISION_CITATION` reads no citation in. That reader is the one `unmarked_decisions` uses, so one definition of a citation serves both gates. **The case came first and it failed on the unchanged prose** with `assert 128 == 134`. **The case discriminates in both directions, and three live mutations prove it.** A decided entry added to `tests/foxio_deviations.json` whose cause names `Changelog round 66` fails `test_the_marker_rule_states_the_decided_count_the_register_holds` with `assert 134 == 135`. A decided entry whose cause cites no decision fails that case and `test_the_marker_rule_states_the_uncited_count_the_register_holds` too, with `assert 38 == 39`. A prose that reads `133`, against an unchanged register, fails the first case with `assert 133 == 134`. Each run restored the file it moved, and the eight cases pass clean before each mutation and after it. **This round added no register entry, it removed none and it edited none.** `git status` reports `tests/foxio_deviations.json` unchanged. Five further cases hold the reader itself. Two measure the count of a register the case builds. Two reject a document that states the counts on no sentence, or on two. **`test_the_register_holds_six_undecided_entries` no longer exists, and #272 already treated it.** It reads `test_the_register_holds_no_undecided_entry` today and asserts zero. That case asserts an empty set, and it still bites, because a new undecided entry fails it. **This round changed no case of #272, and it reports the finding instead.** The treatment of a gate that asserts an empty set is a question for the user. **No file under `ja4plus/` changed and no fingerprint moved.** The conformance suite reports 134 `xfailed` against the 134 keys of `tests/foxio_deviations.json`, before and after. |
| 133 | 2026-08-09 | #319 landed and the fourteen bare `Exception` handlers hold a recorded reading. **The reading is the deliverable, and it justifies every clause this round changes.** `grep -rn "except Exception" ja4plus/` reads **14 sites across the same five files** on `batch/362-hygiene-three`, and every per-file count of the issue table holds. **Seven of the fourteen line numbers moved and seven held.** `ja4plus/ja4db.py` reads 396 and 434 against the recorded 162 and 189. `ja4plus/cli.py` reads 450, 508, 619, 669 and 735 against the recorded 368, 419, 474, 510 and 558. `ja4plus/processor.py` holds 223, 259 and 323, `ja4plus/utils/quic_utils.py` holds 381, 471 and 622, and `ja4plus/__init__.py` holds 83. **Group 1 holds four sites, group 2 holds three, and group 3 holds seven.** Group 1 is the three QUIC readers and `compute_ja4x_from_pem`. **The issue put `ja4plus/__init__.py` in group 3 and the reading moves it to group 1.** That function reports nothing to an operator and it exits nothing. It reads a caller-supplied certificate and returns nothing, which is the shape `CLAUDE.md` binds. **Four sites meet the sizing bound of the issue, so this round narrows all four and stops.** Group 2 is the three sites of `ja4plus/processor.py`, where #45 made the processor hand each failure to its caller. Group 3 is the five sites of `ja4plus/cli.py` and the two of `ja4plus/ja4db.py`, where each site reports a failure and returns. **Each of the ten sites this round leaves wide now carries a comment that states why the wide catch is the design.** **Each named error rests on measurement and not on documentation.** A fuzz of 90000 datagrams through the three QUIC try-bodies returned 0 times and raised `IndexError` 81153 times, `InvalidTag` 8598 times and `ValueError` 249 times. **`InvalidTag` inherits `Exception` and not `ValueError`**, which is the shape #316 found in `InvalidVersion`, so a list of `ValueError` alone drops the common case. **The random fuzz never decrypts, so it never reaches the read behind the decryption, and a second fuzz measured that part.** 40005 attacker-controlled payloads through the post-decryption body of `parse_quic_initial` raised `IndexError` 15 times and nothing else. **`struct.error` is unreachable at that site, and a bound proves it.** `MAXIMUM_CRYPTO_BUFFER_BYTES` is 16384, so the reassembled ClientHello never reaches the 65536 that the `!H` length field would refuse. **The PEM loader raises `InvalidVersion` too, and this round measured it rather than inferring it from the DER loader.** A self-signed certificate whose version field carries 18 in place of 2 raises `18 is not a valid X509 version`. A fuzz of 8003 malformed PEM inputs raised `ValueError` alone, so the constructed case is the one that reaches the second name. **Each named error is proven by its reversal, and each reversal ran live.** Eleven drops of one name from one clause each turn at least one case red. The file returns to its changed form after each drop. `inspect.getsource` of each of the four loaded readers reports the narrowed clause, with `PYTHONDONTWRITEBYTECODE=1` set and every `__pycache__` outside `.venv/` cleared. **The rule is now a condition a case tests, and not prose alone.** `test_a_group_one_reader_names_no_bare_exception` reads the loaded source of all four readers. Four group 2 and group 3 cases assert that the caller returns rather than raises. **`ja4plus/cli.py:619` carries no case, and the comment at the site records why.** This host grants no capture socket, so no case reaches the report path of a live capture. **The self-review found one defect, and the project manager widened the scope of this round to absorb it.** #382 reported it and this round repairs it. `decrypt_quic_initial_crypto` and `decrypt_quic_server_initial_crypto` call `parse_crypto_frames` outside the handler, and the CRYPTO branch of that reader guarded no index. A datagram of 76 bytes that authenticates, and whose plaintext ends on a CRYPTO frame type byte, made the first reader raise `IndexError`. **That is the failure `CLAUDE.md` rule 2 forbids, and it is remotely triggerable by construction.** RFC 9001 Section 5.2 derives the Initial keys from the destination connection ID, and the Initial packet carries that value in the clear, so any sender builds a datagram that decrypts. **The defect predates this round and a measurement proves it.** The base commit `60b179c` holds `except Exception as e:` at that site, and the same datagram raises the same error against it. The wide catch never covered the call either. **The reading of the callers decided the shape of the repair, and not a preference.** `parse_crypto_frames` holds three callers, and two of them call it outside a handler, so a guard inside the reader repairs every caller where a moved return repairs one. **The guard is the one the ACK branch of the same function already holds**, which keeps the repair to the shape the module already uses. **No random fuzz found it, because a random datagram never authenticates.** The 90000 fuzzed datagrams and the 1734 real payloads of the committed captures all stop at the AEAD, and a case must build a datagram that decrypts. `build_authenticating_initial` of `tests/test_bare_exception_reading.py` builds it, and a comment names the construction, because no fuzz will find that datagram again. **The reversal is proven live.** With the guard removed the two new cases fail, and with it restored all 20 pass; `inspect.getsource` reports the guard in the loaded CRYPTO branch. **The repair silences no packet the package reads today.** The 51 genuine QUIC Initials of the committed captures still produce their results, and the second fuzz of 40005 decrypted payloads falls from 15 escaping `IndexError` to 0. **No fingerprint moves.** The conformance suite reports 1532 passed, 143 skipped and 134 xfailed before the change and after it, against 134 register keys. The unit suite rises from 2215 passed to 2233 passed, and 2 skipped, 8 xfailed and 96 subtests hold. **Coverage holds at 93** and the missed line count falls from 287 to 276. |
| 134 | 2026-08-09 | #302 landed and every entry of `CHANGELOG.md` states the round its specification row states. **Eight entries read `Round TBD` and the specification had already assigned their round.** A reader who follows such an entry reaches nothing, and version 1.0.0 is close. **This round invents no number, adds no row and moves no number.** Three entries of Epic 4 name round 90, which is the Epic 4 shipment row and already records all three pieces of work in the words the entries use: it states that #45 made `Processor.process_packet` return the results and added `process_packet_with_errors`, and that #47 shipped the `py.typed` marker and declared `__all__`. #258 settled that shape on 2026-08-09, because a repair folds a detail into the shipment row and renumbers no row. The other five name rounds 122, 118, 122, 104 and 102, and each match reads what the two records state rather than the issue number. #59 carries two entries against round 122. Rounds 123 and 124 are the rival candidates for #59 and #56, and each is already taken, 123 by #364 and 124 by #368, so no match is ambiguous. **Five of the eight orphans hid behind a defect in the guard this round wrote.** `CHANGELOG_ROUND` read a literal space, the file wraps at 90 columns, and `Round` therefore ends one line and `  TBD.` opens the next on five entries. The parser read 36 entries where 41 exist, `_changelog_entries` skipped the five, and no case reached them. **A comparison that never runs reads as a comparison that passes**, which is the fault this project records sixteen times, and it appeared inside the guard built to stop it. **The floor recorded the fault rather than reporting it.** `MINIMUM_CHANGELOG_ENTRIES` read 36, which is the count the broken parser produced, so a floor taken from the wrong side of the defect could not detect it. It now reads 41 from the corrected parser. The three other patterns were read for the same fault and all three already carry `\s*`. **Six premises of the issue were stale, and this round measured each one again.** The issue named three orphans and its 2026-08-09 comment named eight; eight is right, and the count of three was this round's own parser defect. **The comment reported that #47 holds no Changelog row, and round 90 records #47 by name**; the project manager accepted that correction. The comment expected seven unassigned rows and the branch holds eight. `origin/dev` carries round 125 and a duplicate round 9, and this branch stops at 124 and holds the #258 repair, so a new number at the end of the table would collide at the merge. **The guard bars no `TBD`**, because the batch model requires a member to write it and the project manager to assign the number at the batch gate, so a guard that barred the word would fail on every integration branch. The ten unassigned entries of this branch stay untouched. Seven mutations prove the guard fails on a disagreeing number, on an entry that reads `TBD` against an assigned row, on an assigned entry against an unassigned row, on an entry whose row is absent, on a wrapped round sentence that names a round no row records, and on a parser that matches nothing; a wrapped sentence that names the right round stays green. Each run restored the file it moved. Run against the two files of `origin/dev`, the corrected guard reports all eight orphans where the first form reported three. No file under `ja4plus/` changes, no fingerprint moves, and the register holds 134 keys against 134 xfailed. |
| 136 | 2026-08-09 | #62 landed and the README states the method coverage, the concurrency contract, the default bounds and the owner of the standard. **The README claimed "all ten JA4+ methods" and the claim was false in two directions at once.** FoxIO publishes twelve, and `technical_details/README.md:5-16` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` lists all twelve as separate rows. The README table held ten rows and named JA4LS and JA4TScan in neither. **The issue body and `docs/specs/features/08-documentation.md:14-16` each state that this project implements ten of the twelve, and that number is wrong.** Twelve less the one decline is eleven, and `docs/specs/spec.md:42` already reads "**`ja4plus` implements eleven of them**". `JA4LFingerprinter` builds both JA4L and JA4LS, so ten fingerprinters carry eleven methods. The count of ten counts fingerprinters, and a reader who reads it as a count of methods reads it wrong. `ja4plus/fingerprinters/ja4l.py:446` writes `JA4L-S=` and `ja4plus/fingerprinters/ja4l.py:482` writes `JA4L-C=`. **The feature file, `docs/specs/spec.html:126` and the issue body all keep the stale count.** This round changes none of the three, because each sits outside the scope of #62. **The README method table now holds twelve rows and an `Implemented` column**, and JA4TScan alone reads `No`. `docs/specs/spec.md` § Non-goals holds that decline and #197 holds the reading. **`__all__` of `ja4plus/__init__.py` names 25 entries, measured on the base commit `0243e4e`**, and ten of them end in `Fingerprinter`. **The README states the four default bounds in one table, and a case reads each one out of the code rather than restating it.** `ja4plus/utils/state_table.py:46` sets 10000 entries and `:52` sets 600 seconds; `ja4plus/watch.py:77` sets 10000 connections and `:83` sets 300 seconds. The monitor therefore evicts an idle connection before a state table does, and round 101 records why the two differ. **The stated ceiling of 512 MiB is unchanged and no reading here re-measures it.** `tests/test_memory_bounds.py:83` holds `MEMORY_CEILING_MIB = 512.0`, #279 measured 383.47, 388.25, 392.05 and 394.94 MiB across four runs, and every earlier figure is superseded. **The concurrency paragraph and the independent-implementation sentence were already present**, so FR-documentation-2 needed no prose change. Two new cases record that fact rather than assert it. **`tests/test_readme_contracts.py` holds fifteen case functions and 35 collected cases, and each one reads its number out of `ja4plus/` or out of the FoxIO method tuple.** A case that restated the number would pass against a README that contradicts the code. Four mutations prove it. The first is a README age of 300 seconds. The second is a JA4TScan row that reads `Yes`. The third is an owner sentence that reads "FoxIO made JA4+". The fourth moves `DEFAULT_MAX_CONNECTION_AGE` to 900 in the code. Each killed exactly the one case that names it, and each was restored. **The fourth mutation is the one that matters**, because it proves the bound cases read `ja4plus/utils/state_table.py` and not the README twice. **The self-review then broke three of the cases and each break is now closed.** A reviewer removed `JA4TFingerprinter` from the import block and left its name in a comment, wrote "full coverage of every JA4+ method FoxIO publishes", and rewrote the independence sentence to undercut itself. All three passed the first form. The import case now reads the import lines and not the block text, the coverage case matches four patterns rather than three fixed strings, and the two ownership cases read one whole sentence out of the first 20 lines. **Two cases were added for the same reason**: one compares the count word of the prose against the rows the table marks `Yes`, and one compares the published count against the FoxIO tuple, so the prose and the table cannot drift apart. The three evasions now fail, and five cases fail against them. **The README license list is incomplete and this round leaves it.** `README.md` names JA4S, JA4H, JA4T, JA4TS, JA4L, JA4X and JA4SSH under the FoxIO License 1.1. The FoxIO `License FAQ.md:5` names JA4LS, JA4D and JA4D6 as well. A license statement is a decision for the user. **`.claude/rules/conformance.md` and `CLAUDE.md` each state that #179 records six unbounded state tables, and #179 is titled "Correct the maximum-age default in the concurrency-safety feature file".** Its Changelog row 43 does record the six, so the claim holds and the citation reads oddly; nothing here changes either file. No file under `ja4plus/` changes and no fingerprint moves. |
| 137 | 2026-08-09 | #64 landed and the documentation site builds from the Markdown files that already exist. **The site generator is MkDocs 1.6.1 with the Material theme 9.7.7, and the configuration is the new `mkdocs.yml` at the repository root.** `docs_dir` is `docs/`, so `FR-documentation-7` needs no page to move. **`docs/specs/` stays out of the site**, because the specification package is design material and it carries links that point outside `docs_dir`. `exclude_docs` holds that decision, and #65 reverses it if the user wants the package published. **The API reference is generated and it holds no copy of a signature.** `mkdocstrings-python` 1.15.0 reads the docstrings of the working tree, and the five pages under `docs/reference/` name objects rather than describe them, so the reference cannot fall behind the code. `FR-documentation-9` asks for the `Processor` page, and `site/reference/processor/index.html` carries `Run every fingerprinter on one packet, and return the results.` from the docstring of `process_packet`. **`FR-documentation-15` rests on two settings and neither one carries it alone.** `strict: true` fails the build on a warning. MkDocs reports a broken link as a warning and **a broken anchor as information**, so `validation.links.anchors: warn` raises the anchor case to a warning that strict mode fails. Without it the site builds while an anchor is dead. **The requirement is proven by its reversal, and both reversals ran live.** A link changed to `../usage-guide.md` fails with `Doc file 'reference/processor.md' contains a link '../usage-guide.md', but the target 'usage-guide.md' is not found among documentation files.` and exit code 1. An anchor changed to `#pcap-analysis-that-does-not-exist` fails with the matching anchor message and exit code 1. The restored file builds clean. **The first strict build reported 16 dead anchors and no page was wrong.** Python-Markdown collapses a run of hyphens and spaces into one hyphen, so `## JA4 - TLS Client` gains the anchor `ja4-tls-client`. GitHub keeps the run and writes `ja4---tls-client`, which is the form `docs/README.md` and `docs/usage.md` already link. `pymdownx.slugs.slugify` writes the GitHub form, so the repair is one configuration key and **no existing Markdown file changes**. **Every documentation dependency is pinned exactly and none of them reaches the runtime dependencies.** The new `docs` extra of `pyproject.toml` holds `mkdocs==1.6.1`, `mkdocs-material==9.7.7`, `mkdocstrings==1.0.6`, `mkdocstrings-python==1.15.0` and `griffe==2.1.0`. #378 records the defect a floating pin causes, and **this round met that defect while it worked.** `mkdocstrings-python` 1.15.0 accepts `mkdocstrings>=0.28`, the resolver chose `mkdocstrings` 1.0.6, and the build died with `ModuleNotFoundError: No module named 'mkdocstrings.handlers'`. The transitive pins therefore carry a version each, because the two of them decide what the API reference renders. **`mkdocstrings-python` stays on the 1.x line by necessity.** Version 2.0.0 requires Python 3.10 and the package floor is Python 3.9. **`tests/test_documentation_site.py` carries the unit-suite half of the link check**, because the unit suite installs the `dev` extra and not the `docs` extra, so no case imports `mkdocs`. Twelve cases read the configuration and the pages as text and resolve every internal link the way the build does. Five mutations prove them: a link to a page that does not exist, a link to an anchor that does not exist, a link into the excluded `docs/specs/`, a collapsed slug, and a floating pin each turn one case red, and the restored files turn all twelve green. **The self-review found a defect inside the anchor checker, and it is the fault this project records seventeen times.** `_slug` is a copy of the algorithm the site owns, and its first form replaced a run of spaces with one hyphen. `_uslugify` of `pymdownx/slugs.py` calls `str.replace`, which keeps every space, so `JA4 — TLS client` slugs to `ja4--tls-client` with two hyphens. **The copy disagreed with the build on 84 headings of 268, and no link pointed at one of the 84**, so the case compared the wrong value and still passed. `test_the_slug_of_a_case_matches_the_slug_of_the_build` now measures the copy against `pymdownx.slugs.slugify` on every published heading, and the two agree on all 268. That case reports a skip where the `docs` extra is absent, which names the gap rather than hiding it. **The self-review found a second gap, and it sits in the build and not in this branch.** A link into the excluded `docs/specs/` returns a 404 on the published site, and `mkdocs build --strict` succeeds on it. The build writes `Doc file 'reference/processor.md' contains a link to 'specs/spec.md' which is excluded from the built site.` at the information level, and strict mode fails on a warning alone. The `validation` tree holds no key that raises that message, so `_publishes` is the only guard against it and it is stricter than the build by measurement. **No file under `ja4plus/` changes and no fingerprint moves.** The conformance suite reports 1532 passed, 143 skipped and 134 xfailed, against 134 register keys. The unit suite rises from 2251 passed to 2263 passed against 2 skipped where the `docs` extra is installed, and 8 xfailed holds. **The continuous-integration job installs the `dev` extra alone, and there it reads 2262 passed against 3 skipped.** The third skip is the slug case, and a run with `pymdownx` hidden reports `SKIPPED [1] tests/test_documentation_site.py:215: the `docs` extra installs pymdownx, and `dev` does not`. **Coverage holds at 94.** **`.github/workflows/docs.yml` is not in this round**, because #66 owns the publication, and the site is not published. |
| 138 | 2026-08-09 | #63 landed and every code sample of the README, every code sample of `docs/`, and every script of `examples/` runs in continuous integration. **Nothing ran a sample before this round, so a sample that stopped working stopped working in silence.** That is the shape this project records seventeen times: a comparison that never runs reads as a comparison that passes. **The reader finds 157 fenced blocks across `README.md` and `docs/`, and a second reader agrees on every file.** `tests/documentation_samples.py` matches a fence grammar and tracks the fence width. `_second_reader_block_count` in `tests/test_documentation_samples.py` uses string operations alone. **The two readers disagreed on the first run, and the disagreement was a real defect.** The second reader counted one extra block in each of `docs/specs/foxio/JA4D.md`, `docs/specs/foxio/JA4SSH.md`, `docs/specs/foxio/JA4T.md` and `docs/specs/foxio/JA4X.md`, because each file holds an inline code span that opens a line with three backticks. `docs/specs/foxio/JA4D.md:137` is one. **The floor was then set from the corrected reader and not from the first draft.** #302 set its floor from a reader that skipped every wrapped row, and a floor taken from the wrong side of a defect can never report that defect. **The census reads 43 blocks that run, 2 that raise a named error, 94 that the harness skips and 18 that carry output.** `MINIMUM_BLOCKS` reads 157 and `MINIMUM_RUNNABLE_SAMPLES` reads 45. `BLOCKS_PER_SAMPLE_FILE` records the count of each user documentation page, so a page that loses a sample fails a case that names the page. **A block declares itself runnable by its language, and it declares itself unrunnable by a marker that names the reason.** A `python` block and a `bash` block run. `<!-- sample: skip <reason> -->` states why a block runs never, and `<!-- sample: raises <error> <reason> -->` states the error a block raises on purpose. A marker that names no known action raises, and a skip reason shorter than four words fails a case, so no skip is silent. **The default runs the sample.** A new `python` block with no marker runs, and a new block that cannot run fails until somebody writes the reason. **14 samples carry a skip marker and 80 blocks of `docs/specs/` carry a directory reason.** Five open a capture socket. Two reach the Python Package Index. Two reach the lookup service. One downloads the vectors from the FoxIO repository, and one clones the repository from GitHub. One names a process identity no host holds. One names a placeholder argument. One quotes the FoxIO reference implementation. `docs/specs/` holds the specification package and the verbatim FoxIO transcription, and `.claude/rules/ste.md` forbids a rewrite of FoxIO text. **The harness found one documentation defect on its first green run.** `ja4plus.generate_ja4` reads the `tls_info` dictionary that `extract_tls_info` returns, and `README.md` and `docs/api_reference.md` each documented a scapy packet. The call raised `AttributeError: get`. Eight of the nine other one-shot functions read a packet. `generate_ja4x` reads a dictionary that `docs/api_reference.md` already recorded. **This round repairs the two documents and changes no signature**, because #63 alters no behaviour under `ja4plus/`. **Five scripts of `examples/` run under six command lines.** `examples/live_traffic_fingerprinting.py` gained a `--pcap` option that passes `offline` to `sniff`, verified against the `scapy.sendrecv.sniff` docstring of the installed release. `examples/tcpdump_workflows.py` already carried an `analyze` mode and a `filters` mode, and both run. `EXAMPLE_COMMANDS` names every script, so a new script fails a case until somebody records how to run it. **No sample reaches the network.** A fixture refuses `socket.socket.connect` and `socket.create_connection` while a Python sample runs, and every sample that would reach `https://ja4db.com` carries a skip marker. #57 made the remote lookup opt-in and this harness honours that. **Four mutations prove the harness discriminates, and each was restored.** A README Python sample that reads `JA4Fingerprinterr` fails with `NameError`. A README shell sample that reads `ja4plus analyse` ends with the status 2. A `docs/usage.md` sample that reads `get_raw_fingerprintt` fails with `AttributeError`. `examples/pcap_analysis.py` that imports `rdpcapp` ends with the status 1. Each path reaches different code, so each path was broken on its own. **No file under `ja4plus/` changes and no fingerprint moves.** The conformance suite reports 1532 passed, 143 skipped and 134 xfailed, and `tests/foxio_deviations.json` holds 134 keys against 134 xfailed. The unit suite reports 2316 passed at 94% line coverage. **One case failed on a loaded host and passed on a quiet one.** `TestTheStatedMemoryCeiling::test_a_smaller_entry_count_holds_less_resident_memory` measures a resident-memory ratio in subprocesses. This round repaired it never, because it belongs to no issue here. **The self-review found four blind spots in the reader and closed each one.** A marker that a blank line parts from its fence now still reaches the block, because a reader that read one line alone would drop the marker the day somebody tidies the file. A marker on a block of another language now fails a case rather than vanishing. A Python sample now runs under a 300 second limit, which the shell samples already held through the subprocess. **A fence inside a blockquote reaches neither reader, and two such fences exist.** `docs/specs/features/00-foundation.md:49` and `:53` open and close one `.gitignore` fragment. `KNOWN_BLOCKQUOTE_FENCES` records both, so the miss is counted, and a third one fails a case. A blockquote fence in the user documentation fails a case outright, because it would hide a runnable sample. **The merge with #64 then turned the census red, and it exposed a real hole rather than a stale number.** #64 added `docs/reference/index.md`, `:/processor.md`, `:/fingerprinters.md`, `:/ja4db.md` and `:/types.md`. Each page holds `:::` directives and no fenced block, so **both counts stay at 157 and the census stays at 43 run, 2 raises, 94 skip and 18 output.** The user documentation grew from 7 pages to 12. **The hole is that the runner read a list somebody maintained by hand.** `PYTHON_SAMPLE_FILES` read the keys of the block-count table, so a page absent from that table was read, counted and classified as runnable, and then never run. A probe page under `docs/reference/` holding `from ja4plus import ThisNameDoesNotExist` proves it: the reader reported `docs/reference/probe.md:3 python run`, and the execution cases reported `3 passed`. **That is the shape #63 exists to remove, and it sat inside the harness built to remove it.** **The repair derives the file set and keeps the counts measured by hand.** The filesystem is the one record of which pages exist, so `user_documentation_files` reads it and `PYTHON_SAMPLE_FILES` reads the blocks. The counts stay hand-measured, because the only tool that could derive them is the reader the cases exist to check. `MINIMUM_BLOCKS_PER_PAGE` is now a floor for each page and no longer an exact count, so a page that grows passes and a page that loses a sample fails. `test_the_execution_cases_reach_every_sample_the_reader_calls_runnable` compares the reader against the runner, so the two lists cannot drift apart again. After the repair the same probe page fails with `ImportError: cannot import name 'ThisNameDoesNotExist'`. **`mkdocs build --strict` fails on this branch and the cause is not here.** `mkdocstrings==1.0.6` ships no `mkdocstrings.handlers` package and `mkdocstrings-python==1.15.0` imports `mkdocstrings.handlers.base`, so every `:::` directive fails. It reproduces on macOS under Python 3.14 and on Ubuntu 24.10 under Python 3.12, and `git diff origin/epic/19-documentation...HEAD -- pyproject.toml mkdocs.yml docs/reference/` is empty. **No workflow builds the site**, so no gate reads the failure. #394 records it and this round repairs it never. **The Linux host reports 31 passed under Python 3.12**, which is the platform continuous integration runs, and the census reads the filesystem. |
| 139 | 2026-08-09 | #65 landed and the site carries a page per method, the output schema and the migration page. **The count is the deliverable that a reader gets wrong first.** FoxIO publishes twelve methods, this project implements eleven, and ten fingerprinter classes carry the eleven, because `JA4LFingerprinter` writes `JA4L-S=` at `ja4plus/fingerprinters/ja4l.py:446` and `JA4L-C=` at `:482`. **This round measured the set on its own base commit `da2338d` and it carried no number from the issue body.** `__all__` names 25 entries and ten of them end in `Fingerprinter`. `VALID_TYPES` of `ja4plus/cli.py:51` names ten tokens. The README table of #62 marks eleven rows `Yes`. **The three agree, and the number of methods is eleven.** **The site therefore serves eleven method pages and no twelfth.** `docs/methods/index.md` holds the twelve-row table and states the JA4TScan decline, so a reader who searches for JA4TScan reaches the reason rather than a missing page. The alternative, a twelfth page that states the decline, was declined because the acceptance criterion reads "one page per implemented method" and a nav entry under `Methods` would claim JA4TScan is one. **Every value on a method page is a claim, and `tests/test_method_pages.py` holds 144 cases against the code.** Each page carries a `## The facts` table that a parser reads: the `--types` token against `VALID_TYPES`, the fingerprinter class and the one-shot function against `__all__`, the hash rule against the `hexdigest()[:12]` its own module holds, and the FoxIO file against the inventory table of `docs/specs/foxio/README.md`. **The strongest case is the example table.** Each page names a committed capture and a value, and the case reads the capture, runs the processor and compares. Six captures cover the eleven methods, and the whole file runs in 2.85 seconds. **The cases came first and 142 of the 144 failed on the unchanged base.** Five mutations prove they discriminate. **`docs/output-schema.md` already existed, so this round extended it rather than adding a second schema page.** Its `## The raw forms` table was prose that no case read, and a new case now runs the capture of each method page and compares the two raw fields of the result against the row. A `## The method of each output line` section links the ten `type` values to the eleven method pages. **The migration page reads the breaking changes from the record and not from the issue body.** It holds eleven rows with the old form, the new form, the reason and the round, plus seven fingerprints that move. **The issue body listed five breaking changes and the record holds eleven.** **Two gaps in the record are findings and this round repairs neither.** `CHANGELOG.md` holds no mention of `ja4plus.collector`, which round 71 records in this table alone. **The Python floor moved from 3.8 to 3.9 and no Changelog round records it anywhere**, in either file; it shipped in commit `02ee772` under #76, and round 4 names #27 alone. The migration page states that gap in the row rather than hide it. **`docs/concurrency.md` is new and the issue names it under `What to build`.** It restates the contract of `README.md`, and two cases read the two default bounds out of `ja4plus/utils/state_table.py`. **One docstring under `ja4plus/` changed and no fingerprint moved.** `FingerprintResult.type` read "One of the ten this library implements", which is the counting error of #387 inside a docstring the site renders through `docs/reference/types.md`. It now names the ten values `--types` accepts and states why ten carry eleven. `mkdocs build --strict` builds clean and the nav names all fourteen new pages. |
| 135 | 2026-08-09 | #76 landed and `requires-python` moved from `>=3.8` to `>=3.9`. **This row is the record that was missing.** Commit `02ee772` raised the floor on 2026-08-06, and no round recorded the move. Round 4 covers Epic 0 and it names #27 alone. **Neither `CHANGELOG.md` nor this table held the change, so the two files agreed and both were wrong.** #302 holds the two files to the same round for every entry that exists in both. A comparison between two records reaches no change that is absent from both sides. **#395 records the blind spot, and this row closes the gap.** Python 3.8 reached its end of life in October 2024. A user on Python 3.8 cannot install version 1.0.0, because `pip` reads `requires-python` and refuses the distribution. The `Programming Language :: Python :: 3.8` classifier left `pyproject.toml` in the same commit, and `README.md` states the same floor. Continuous integration runs Python 3.9 through Python 3.13. **`pyproject.toml` states `requires-python = ">=3.9"` today, and this round read the file rather than the issue body.** `tests/test_breaking_change_record.py` reads that value and requires both files to name it. The case failed on the unchanged branch, and it named the missing floor. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 140 | 2026-08-09 | #391 landed and the `docs` extra pins a `mkdocstrings` the site can build with. **The committed pins did not build the site they configure.** `mkdocstrings==1.0.6` installed, and `mkdocs build --strict` then failed with `ModuleNotFoundError: No module named 'mkdocstrings.handlers'`. `mkdocstrings_handlers/python/handler.py` of the 1.x line imports that module, and the changelog of `mkdocstrings` lists `mkdocstrings.handlers` under the breaking changes of 1.0.0. The pin is now `mkdocstrings==0.30.1`, and `mkdocs`, `mkdocs-material`, `mkdocstrings-python` and `griffe` hold the versions round TBD gave them. **The measured repair, from a clean environment on Linux with Python 3.12: `python3.12 -m venv .venv-docs`, `.venv-docs/bin/pip install -e ".[docs]"`, `.venv-docs/bin/mkdocs build --strict`, `Documentation built in 2.38 seconds`, exit 0.** The same commands on `da2338d` exit 1 with the import error above. **Two guards existed and neither could catch it.** `test_every_documentation_dependency_pins_one_version` reads the pins and an exact pin can still be an incompatible pin. The gate of round TBD then built the site inside the worker's virtual environment. An earlier resolution had already installed a working `mkdocstrings` there. The gate therefore measured the environment on hand and not the artifact that ships. **A unit case cannot close the gap**, because the suite runs inside an environment that another resolution already filled, and a case that imports `mkdocs` reports that environment. **The instrument is `.github/workflows/docs-build.yml`.** It starts from an empty runner, installs the `docs` extra alone, runs `mkdocs build --strict`, and reads two results back out of the generated HTML: the `Processor` docstring sentence on `site/reference/processor/index.html`, and the ten `generate_ja4*` one-shot functions on `site/reference/fingerprinters/index.html`. It runs on a change to `pyproject.toml`, to `mkdocs.yml`, to `docs/` or to itself. **The job never installs the `dev` extra**, which would bring a second resolution the release never installs. **The name `docs.yml` stays free for #66**, which owns the publication, so this job publishes nothing and the two never collide. `test_the_mkdocstrings_pin_holds_the_handler_module_its_handler_imports` states the upper bound as text, so a reader of `pyproject.toml` alone sees it, and each of the three new cases in `tests/test_documentation_site.py` failed before the repair. **The `docs` extra needs Python 3.10 or later, and the package floor of Python 3.9 stays.** `griffe` 2.1.0 requires 3.10 and `mkdocstrings` 1.0.0 requires it too. No user builds the site, the documentation job names one version, and the test matrix keeps 3.9. The comment of round TBD claimed the floor held the whole `docs` extra, and it is corrected. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 141 | 2026-08-09 | #395 landed and the two breaking changes the Changelog never captured hold a round in both files. **#65 found both gaps while it wrote `docs/migration-0.6-to-1.0.md`, and it filed nothing.** This round records them. **The removal of `ja4plus.collector` holds round 71 of this table, and `CHANGELOG.md` now carries an entry under that round.** No new round records the removal, because round 71 already records it, and a second row would give one change two citation targets. #258 settled that a round number is an identifier. **The move of the Python floor holds the new round 135 in both files**, because neither file recorded it. **The second half of this issue is the shape of the blind spot.** #302 compares `CHANGELOG.md` against this table, and a comparison between two records finds no change that is absent from both. That is a comparison that never runs, and this project now records the shape eighteen times. #302 records the sixteenth and #63 records the seventeenth. **`tests/test_breaking_change_record.py` compares the record against the package instead.** It holds the 25 modules of version 0.6.0, which `git ls-tree -r --name-only v0.6.0 ja4plus/` reports, and it reads each one with `importlib.util.find_spec`. `ja4plus.collector` is the one module the package drops. It reads `requires-python` out of `pyproject.toml`, and it requires both files to name the value. **Three of the six cases failed on the unchanged branch and three passed.** The three that passed are the census count, the removed module set, and the round 71 row of this table. **Two mutations prove the cases discriminate.** A floor of `>=3.10` in `pyproject.toml` fails the two floor cases, and `ja4plus/collector.py` restored from the tag fails `test_a_removed_module_reaches_no_importer`. **The sweep read `v0.6.0..HEAD` for a change that alters a published interface and carries no round.** The verdict of the pull request holds the whole table. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 142 | 2026-08-09 | #66 landed and the documentation site publishes to GitHub Pages, while `CHANGELOG.md` holds the release notes of version 1.0.0. **The publish workflow builds nothing of its own.** `.github/workflows/docs-build.yml` installs the committed `docs` pins into an empty environment and runs `mkdocs build --strict`, and a publish job that rebuilt the site with a second recipe would be a second source of truth. That job gains a `workflow_call` trigger and one conditional upload step, so `.github/workflows/docs.yml` calls it and deploys the artifact it uploads. The input is absent on a `push` trigger and on a `pull_request` trigger, so a pull request uploads nothing. No pin and no build step changes. **This repository holds no GitHub Pages site, and no run has ever deployed.** `gh api repos/Crank-Git/ja4plus` reports `has_pages: false`. The user decides whether to turn Pages on, because that change publishes a public website. The first job of the workflow reads `GET /repos/{owner}/{repo}/pages` and names the setting in the failure. A 404 reports that the site is absent, a `build_type` other than `workflow` reports the wrong source, and any other status reports the status it read. **The deployment stays unverified until the user changes the setting**, and no case claims otherwise. **The release notes name thirteen interface changes and eight fingerprints that move.** `tests/test_release_notes.py` holds the two tables against the `**BREAKING` entries of `CHANGELOG.md`, against `docs/migration-0.6-to-1.0.md` and against this table, so a breaking change that reaches the record and reaches no table fails a case. **Two defects of the record reached the release notes, and the notes report both.** #319 narrowed `compute_ja4x_from_pem` and `compute_ja4x_from_der` under round 133, and the migration page holds no row for it, which #399 records. The same page cites `Round 122, #59 and #364`, and row 123 of this table is the row that records #364, which #401 records. **A case that compared the two `Record` cells passed on the second defect, because both files held one error.** The case now reads this table, which is the authority both files copy. #65 owns the page, so the notes hold the correct citation and the page stays as it is. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 143 | 2026-08-09 | #399, #401 and #403 landed together and `docs/migration-0.6-to-1.0.md` answers to the record. **One worker carried all three, because all three change one page or the record of it, and two workers on one file collide for no gain.** #401 is a conflated citation: the page cited one row under `Round 122, #59 and #364`, and this table holds round 122 for #59 and round 123 for #364. Row 122 records `lookup_many` and the source field, row 123 records the item access of a `LookupResult`, and a reader who followed `Round 122` for the item-access rule reached the wrong row. #399 is a missing row: the page held eleven breaking changes and none recorded #319, which narrowed the handlers of `compute_ja4x_from_pem` and `compute_ja4x_from_der`, so an input that returned `None` in version 0.6.0 can now raise. **The page holds thirteen rows today.** The conflated row parts into two, each naming the round that records it, and the certificate readers gain one row that states the old form, the new form, the reason and `Round 133, #319`. **The two edits are the smaller half of this round.** Both defects sit in a page this epic created, and #66 found them only because it wrote the release notes against the page and compared. The question this round answers is why the cases of the page did not. **The first citation case of #66 compared two copies of one claim.** It read the `Record` cell of the release notes against the `Record` cell of the migration page. Both files held the same wrong round, so the two agreed. The case passed on the very error #401 records. #66 replaced it with a case that reads this table. **That is the eighteenth recorded instance of a comparison that never runs**, and #395 records the seventeenth. **`test_every_citation_of_the_migration_page_names_the_row_that_records_it` holds the page to the same authority.** It reads every `Round N, #M` citation of the page against this table, and it opens no second file. `_specification_rows`, `_citations` and `_wrong_citations` now serve the notes case and the page case, so one reader holds both files to one table. **`test_every_breaking_change_of_the_record_reaches_the_migration_page` reads the direction #66 left open.** `test_every_breaking_change_of_the_migration_page_reaches_the_release_notes` reads the page into the notes, and a page that holds fewer changes than the record passes it. That is why the gap of #319 survived a green suite. The record is the two release-notes tables of `CHANGELOG.md` and every entry that opens with the `**BREAKING` marker, which is 20 issues. **The `**BREAKING` marker alone would not have caught #319.** Eight entries carry it and all eight already reached the page; the interface table of the notes is the record that holds the thirteenth change. **The prose of the notes reaches neither reader.** It names #47 and #94, which the sweep of #395 judged marginal, and a reader that took the prose would demand a row for each. **Six mutations prove the three cases discriminate, and each was restored.** `Round 123, #364` back to `Round 122, #364` fails the citation case alone with `round 122 records no #364`. `Round 999, #364` fails it with `the migration page cites rounds the specification does not hold: [999]`. The `Record` cell of the new row moved from `Round 133, #319` to `Round 135, #76` holds the row count at thirteen. It fails the completeness case alone, with `the record names these breaking changes and the migration page omits them: [319]`. The removal of the whole row fails five cases, the row floor among them. **The floors rise with the page.** `MINIMUM_BREAKING_CHANGES` of `tests/test_method_pages.py` and `MINIMUM_MIGRATION_ROWS` of `tests/test_release_notes.py` read 13, `MINIMUM_MIGRATION_CITATIONS` reads 14 and `MINIMUM_RECORD_ISSUES` reads 20. **This round re-measured the record on its base commit `2a1fb33` and it carried no number from the brief.** This table holds 135 assigned rounds, numbered 1 through 135 with no gap and no duplicate, and 7 rows reading `TBD`. **#403 is the sentence this repair falsified, and the project manager widened the scope of this round to absorb it.** `CHANGELOG.md` read "One breaking change reaches this record and reaches no row of the migration page", and the new row of #319 made that untrue. The worker filed the finding and declined the edit, because #66 owned the release notes. **#66 sub-merged as `2a1fb33` before the worker finished, so the paragraph belonged to nobody and the worker held the context.** The paragraph now states that the record and the page name the same breaking changes, and it keeps the record that the gap existed and that #399 closed it. **The interesting half is that no case read the paragraph.** The prose asserted the same thing as `test_every_breaking_change_of_the_record_reaches_the_migration_page`, in the opposite direction, and nothing connected the two. `test_the_release_notes_report_the_gap_the_record_shows_and_no_other` connects them. It reads a present-tense gap claim of the notes against the set difference of the record and the page, and never against a second copy of the claim. **It fails in both directions, and two more mutations prove it.** The stale sentence restored fails it alone, with `the release notes claim the migration page misses a breaking change, and the record shows none: 'reaches no row of the migration page'`. The `Round 135, #76` mutation, which holds the row count at thirteen, fails it beside the completeness case, with `the record shows these breaking changes the migration page omits, and the release notes report no gap: [319]`. **The reader takes a present-tense claim alone, and that is deliberate.** A past-tense sentence records a gap an issue closed and stays true, so the paragraph keeps its history. A gap claim in words `GAP_CLAIM` does not hold escapes the case, and the docstring states the asymmetry: the failure this case stops is a sentence that survives a repair unchanged, and unchanged text matches. **Two findings reach no repair here.** `_citations` reads the first `Round` of a line and every issue behind it, which is right for a table row and over-strict for wrapped prose; it failed on this round's own corrected paragraph, where `Round 133, #319. #66 found` read as `round 133 records no #66`. It fails loud and never passes silently, so the paragraph drops the round rather than the reader loosening. The intro of the release notes states that a change the migration page records reaches a row or fails a case, and it names the new direction not at all; the sentence is true and incomplete. **The whole gate ran on this host.** The unit suite reports 2502 passed against the 2499 of the base commit, and the three new cases are the difference; 2 skipped, 8 xfailed and 98 subtests hold. **Coverage holds at 94**, and the missed line count reads 273 of 4232 statements. The conformance suite reports 1532 passed, 143 skipped and 134 xfailed, against the 134 keys of `tests/foxio_deviations.json`. `mypy --strict` finds no issue in 31 source files, `ruff check` and `ruff format --check` pass on 175 files, and `mkdocs build --strict` exits 0 with the two new citations rendered on `site/migration-0.6-to-1.0/index.html`. `TestTheStatedMemoryCeiling::test_a_smaller_entry_count_holds_less_resident_memory` passed on this quiet host, and #389 owns it. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 144 | 2026-08-09 | #389 landed and the memory ceiling control reads a quantity a busy host does not move. **`TestTheStatedMemoryCeiling::test_a_smaller_entry_count_holds_less_resident_memory` failed and then passed on an unchanged tree, and five workers reported it.** #62 saw one failure, #64 saw three of five, #65 saw one at load average 8.66 and a pass at 17.60, and #391 and #395 saw passes at 6.4 and 5.31. **The case is the control that proves the entry count bound holds the ceiling, and a control that fails when nothing is wrong teaches a reader to disregard it.** This project records eighteen instances of a comparison that is never made reading as a comparison that passes. **The mechanism is memory pressure, and it is not processor contention.** A resident reading states what the host left in memory, and a host under memory pressure reclaims the pages of a running process, reaching the run that holds the most memory first. The reclaim moves the two runs of the control by different amounts, and it moves the ratio toward one. **The margin explains the platform difference the issue recorded.** The growth of each run holds a term the entry count bound governs and a term it does not, because `BaseFingerprinter.fingerprints` grows with the packet count in both runs. The governed term is 57 percent of the Linux reading and 25 percent of the macOS reading, so the same perturbation crosses the threshold on macOS first. **The reproduction ran on `bigboy`, an Ubuntu 24.10 host with 56 cores, 137 GiB and `python3.12`.** Each measurement run ran under `systemd-run --user --scope -p MemoryMax=<limit>`, which reclaims the pages of that run alone and leaves the host untouched. **A deliberate processor load reproduced nothing, and that reading names the mechanism.** 56 processes that hold a processor busy held the load average at 58 on 56 cores for three rounds, and that matches the ratio of load to cores at every reported failure. The runs took 59 to 67 seconds against 27 quiet, and the resident ratio read 0.361, 0.445 and 0.407. **Memory pressure moved it at once.** The resident ratio read 0.434 at no limit, 0.962 at 80 MiB and 0.786 at 75 MiB, and at 70 MiB both runs added 0.00 MiB. The case that reads the resident growth fails at 80 MiB on the ratio and at 75 MiB and 70 MiB on the floor, and one base-commit run read `the shipped run added 0.87 MiB, which is too little to read`. **Three rounds at the 80 MiB limit read 1.047, 0.885 and 1.045**, so a rule that repeats the pair and takes the median fails at every round and costs three times the wall clock at 57 seconds a round. That reading declines shape 2 of the issue. **The block count read 0.392 at every one of the four limits**, and the block growth of the shipped run held between 376952 and 376965, so the four readings differ by 13 blocks. **The case now reads `sys.getallocatedblocks`, and it carries the name `test_a_smaller_entry_count_holds_fewer_memory_blocks`.** The run reports `idle_blocks` and `final_blocks` beside the unchanged resident readings, `traffic_growth_blocks` refuses a reading that holds no block pair, and `TestTheBlockReading` holds two cases that start no interpreter and read the recorded 80 MiB pair. **The run collects the cyclic garbage before it counts the blocks.** A count taken between two collections holds the garbage the collector has not reached, and the point it reaches moves with the allocation history. Without the collection two macOS readings gave a control growth of 230616 and 247991, at a ratio of 0.612 and 0.664, and with the collection three rounds read 147562, 147566 and 147569 at a ratio of 0.398. The collection runs after the resident reading at both points, so it moves no part of the ceiling reading. **`CONTROL_GROWTH_RATIO` stays 0.85 and the mutation proves the case still discriminates.** With the control bound raised from 100 to the shipped 10000 the case fails, at 1.361 on Linux and 1.367 on macOS, and it reads `a bound of 10000 added 506935 blocks and the shipped bounds added 370945 blocks, a ratio of 1.367, so the entry count changed nothing`. The file was restored after each mutation. **`CONTROL_FLOOR_MIB` is retired and the floor reads four blocks for each packet.** A MiB floor is the reading the host moves: under the 70 MiB limit the shipped run added 0.00 MiB and 376954 blocks. The measured rate is 12.37 blocks for each packet at 30000 packets, 13.99 at 5000 and 15.08 at 1000, so the reading sits 3.1 times above the floor at the default size. The retired MiB floor sat 2.9 times below its reading, so the rate holds the strength the MiB floor held. **The repaired case passes at all four limits, including the 70 MiB limit where the resident growth reads 0.00 MiB.** **The ceiling case and the two other controls are unchanged and each still passes under the mutation.** The ceiling reads a high-water mark and asserts an upper bound, so memory pressure only lowers that reading and never turns it red. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 145 | 2026-08-09 | #407 landed and the specification records Epic 11. **Nothing measures four statements version 1.0.0 makes**, so #406 opens the epic that measures them. No case runs the shipped package. Every Linux result comes from a runner. The sweep leaves 974 of its 976 candidates unsettled. The package states no throughput. `features/11-pre-release-validation.md` holds 33 requirements, and every sub-issue of #406 quotes one. **The file number equals the epic number.** Epic 10 (#194) holds no feature document, so `features/10-*.md` does not exist and the sequence loses no file. `Terms` gains `mutation`, `sweep`, `candidate`, `clean environment` and `throughput`. **A count of the table read 67 rows, and the table held none of the five under any spelling.** No row of this round therefore rotates a synonym of an existing row. `tests/test_specification_terms.py` now fails a term that two rows carry, because a reader who scrolls a 72-row table does not see the duplicate. **The evidence contradicts two premises of the epic, and this round works around neither.** `tests/mutation_sweep.py:396` groups the `candidates` key by test file and not by module. A candidate survives every mutation of every swept module, so the per-module census that #411 states needs one sweep for each module group. The report of 2026-08-07 also holds a `Date` field and no commit field, so #411 adds one. `Open questions` of the feature document records both. |
| 146 | 2026-08-09 | #408 landed and one case fingerprints a capture from the installed wheel. `tests/test_installed_wheel.py` builds both artifacts with `python -m build`, and it installs the wheel into a clean environment. It reads three paths from that environment: `ja4plus --version`, `ja4plus analyze --format json`, and one script that imports `ja4plus` and drives a `Processor`. The output of the last two equals the output of the source tree, byte for byte. **A run that resolves the working copy proves nothing.** The case therefore reads `ja4plus.__file__` of the clean environment, and it fails when that path is not below the `site-packages` directory of that environment. `test_the_import_check_fails_for_the_source_tree` holds the check against the interpreter that runs the suite, which the editable install resolves to the checkout, and it asserts the check rejects that path. **One mutation proved the failure live.** With the working directory moved to the repository root, the run reported `AssertionError: the clean environment imported /Users/.../ja4plus/__init__.py, which is not below /private/var/.../wheel-env0/env/lib/python3.14/site-packages`. **The control case is not optional**, because the byte-for-byte comparison passes on any wheel that imports. It removes `ja4plus/data/ja4plus-mapping.csv` from a second clean environment and reads one probe twice: `70 True` before the removal, and `0 False` after it. `.github/workflows/test.yml` gains the `installed-wheel` job, and `[tool.pytest.ini_options]` of `pyproject.toml` gains the `installed_wheel` marker. **That job is the only runner of the marker.** `tests/conftest.py` deselects the marker from a run that does not name it, and the `conformance` job holds the same relation to `spec_validation`. The unit gate therefore builds no wheel and reaches no index. **A `-m` expression on the command line replaces the one in `addopts`, so `addopts` cannot hold that rule.** With `addopts = "-m 'not installed_wheel'"` and the gate command `-m "not spec_validation"`, `pytest --collect-only` still collected all seven cases. `tests/test_installed_wheel_selection.py` holds the rule against three commands, so a rename of the marker fails a case rather than removing the wheel cases from the gate in silence. **The evidence contradicts one premise of #408, and this round works around none.** #408 states that eleven jobs of `.github/workflows/test.yml` install the working copy. That file held five job definitions, and the matrix expands them to ten job runs. **`build` joins the `dev` extra**, because the marker sits inside `pytest tests/ -m "not spec_validation"`, so every environment that runs that gate needs the package. **The case costs wall-clock time on every gate run.** Four consecutive runs on macOS with a warm `pip` cache measured 40.39 s, 24.25 s, 24.40 s and 22.22 s, so the settled figure is about 23 s. A runner starts with a cold `pip` cache and takes longer. This round changes no file under `ja4plus/` and it moves no fingerprint. |
| 147 | 2026-08-09 | #411 measured the whole-package sweep, and the measurement partitioned it. **The whole-package sweep costs 71.6 hours on one host, and it is not the instrument this project settles a candidate with.** `--dry-run --max-per-module 0` reads 3545 mutations over 31 modules, and one suite run takes 72.75 seconds over 4325 collected cases. The product is 257899 seconds. **`.claude/rules/conformance.md` stated about 17 seconds for one suite run, and the measured cost is 4.3 times that.** The suite grew from the tree #172 measured, and the stale figure is what let #411 plan an impossible run, so the rule now carries the measured number, its date and the instruction to measure both again. **A checkpoint makes such a run resumable and it removes no work**, so the split the issue offered reduces nothing, and the 15 slowest cases hold 37 seconds of the 73 so no reduction of the suite is available either. **The project manager partitioned the sweep across #412, #413 and #414 on 2026-08-09, and the reason is not only cost.** `tests/mutation_sweep.py:482` reads `tests = options.tests or ["tests/"]`, so a sweep runs the whole suite for each mutation when `--tests` names nothing. A sweep scoped to one module group and to the test files that read it runs a small suite for each mutation, and `tests/mutation_sweep.py:340` already directs that method in the preamble of the report. **The union of the per-module sweeps is larger than the candidate set of one whole-package sweep, and the union is the correct input for settlement.** A whole-package sweep names the cases that no mutation of any module kills, and a sweep of module X names the cases that no mutation of X kills, so a case that module Y kills still reaches the second list. A reader who compares the two counts reads a regression that did not happen, and the rule and the feature document both state that now. **A third premise fails: `git ls-files 'ja4plus/**/*.py'` lists 24 files and the package holds 31.** Git reads `**` in a pathspec as one or more directories, so the pattern matches no file of the top directory of the package, and it omits `ja4plus/cli.py`, `ja4plus/processor.py`, `ja4plus/ja4db.py`, `ja4plus/output.py`, `ja4plus/types.py`, `ja4plus/watch.py` and `ja4plus/__init__.py`. Those seven hold 556 of the 3545 mutations. The project manager confirmed the reading at the source. `FR-pre-release-validation-16` now names `git ls-files 'ja4plus/*.py' 'ja4plus/*/*.py'`, `FR-pre-release-validation-16a` asks a case to fail when the module list moves, and `tests/test_mutation_sweep_module_list.py` holds five cases that compare `DEFAULT_MODULE_PATTERNS` against the tracked Python files of the package. **The front matter of `.claude/rules/ste.md` and of `.claude/rules/external-apis.md` holds the same `**` string, that glob follows the gitignore rules, it matches every module, and this round repairs neither.** **`tests/mutation_sweep.py` writes a `commit` key and a `Commit` row**, read from `git rev-parse HEAD` before the first mutation lands, which `FR-pre-release-validation-17` asks for and which the report of 2026-08-07 states nowhere. **New file `tests/mutation_census.py` reads every `*.json` report of `docs/mutation_reports/`**, counts the candidates of each test file, and reads every `*.json` record of `docs/mutation_settlements/`. A candidate is keyed by the sweep that named it and by the case, because two sweeps may name the same case and each one then needs its own settlement. **It opens no Markdown file, and a case proves that by patching `builtins.open` and `Path.read_text` and reading every path the census opens.** It names every candidate that two records claim, every candidate that no record claims, every settlement of a case no report names a candidate, and every record that names a sweep no report holds. **The sweep collector was checked against pytest and it matches.** `collect_cases` and `pytest --collect-only -q` report the same identifiers on `tests/test_documentation_samples.py` at 22, `tests/test_ja4h_cookie_list.py` at 10 and `tests/test_foxio_rust_parity.py` at 202, including the parametrized identifier `test_every_shell_sample_runs[README.md:74]`. **This round runs no sweep and settles no candidate.** #412, #413 and #414 run the partitioned sweeps and write the reports. **No file under `ja4plus/` changes and no fingerprint moves.** The register holds 134 keys, and the conformance suite reports 1532 passed, 143 skipped and 134 xfailed. |
| 148 | 2026-08-09 | #409 landed and one case fingerprints a capture from the installed source distribution. `tests/test_installed_wheel.py` gains nine cases, and no new file appears. #408 wrote four functions that take the artifact as a parameter: the build, the clean-environment creation, the probe run and the comparison. This round passes the source distribution to the same four. The run repeats the three paths of #408: `ja4plus --version`, `ja4plus analyze --format json`, and one script that drives a `Processor`. **`pip install <the sdist>` must build the source distribution and not resolve a wheel.** A run that resolves a wheel measures the artifact of #408 again and reports a pass. The install passes `--no-binary ja4plus`, and `test_pip_built_the_package_from_the_source_distribution` reads the recorded output for three facts. `pip` names the source distribution as the input. `pip` writes `Building wheel for ja4plus`. `pip` writes no `Downloading ja4plus-<version>.whl` and no `Using cached ja4plus-<version>.whl`. **The option alone does not settle it.** The pip documentation states "Do not download binary packages. Cached binary packages may still be used." A measurement of 2026-08-09 ran the same install twice against one local source distribution, and the second run built the wheel again. A mutation that installed `artifacts["wheel"]` proved the case fails: `AssertionError: pip read another input than .../ja4plus-0.6.0.tar.gz`. **The mapping file is the likeliest failure**, so two cases read it. One compares the byte count of `ja4plus/data/ja4plus-mapping.csv` against the byte count in the repository. One looks a named fingerprint up, because a present file is not a read file. **The control case is not optional.** It removes the mapping file from a second clean environment. It reads one probe twice: `70 True` before the removal, and `0 False` after it. **The evidence contradicts one premise of #409, and this round works around it.** #409 states that a package-data rule carries the mapping file. A mutation that removed `data/*.csv` from `[tool.setuptools.package-data]` still shipped the file in both artifacts. A mutation that added `[tool.setuptools.exclude-package-data]` shipped it too. `include-package-data` defaults to true for a `pyproject.toml` project, and the `setuptools-scm` file finder lists every tracked file. A second rule therefore carries the same file. Only `include-package-data = false` beside the removal dropped it, and both cases then failed live. **The source distribution carries the test material.** It lists 396 members, and `tests/foxio_deviations.json` is one of them. `test_the_source_distribution_lists_the_deviation_register` reads that listing. `tests/test_installed_wheel_selection.py` raises `EXPECTED_CASE_COUNT` from 7 to 16. **The marked run costs about 26 s on macOS with a warm `pip` cache**, against about 23 s for the wheel alone. Two more clean environments install the artifact, and that is the whole difference. **`FR-pre-release-validation-26` and this round meet a conflict that #419 owns.** That requirement states that no case of this feature set opens a network connection, and `pip install` resolves the dependency list from an index. This round resolves nothing. It changes no file under `ja4plus/` and it moves no fingerprint. |
| 150 | 2026-08-09 | #419 landed and the network rule of Epic 11 names the cases it binds. **`FR-pre-release-validation-26` read `No case of this feature set opens a network connection.`, and every install case had to violate it.** `pip install <the wheel>` resolves the shipped dependency list, so an install into a clean environment reaches a package index by construction. **A measurement of 2026-08-09 states it rather than infers it.** A clean environment that installed `ja4plus-0.6.0-py3-none-any.whl` read `Downloading scapy-2.7.0-py3-none-any.whl (2.6 MB)` and `Downloading cryptography-50.0.0-cp311-abi3-macosx_11_0_arm64.whl (4.0 MB)`, and it installed `cffi` and `pycparser` beside them. **The rule came from #414, where it is correct and narrow**, so this round keeps it and names its scope. `FR-pre-release-validation-26` now binds a case of `tests/test_db_offline.py` that asserts that `ja4plus/ja4db.py` sends no request, and it names the recorded transport `RecordingRequests`. `FR-pre-release-validation-26a` bars the address `https://ja4db.com`. `FR-pre-release-validation-26b` permits the package index for the install cases of `tests/test_installed_wheel.py`, and `FR-pre-release-validation-26c` names `scapy` and `cryptography` as the two dependencies that index resolves. **The rule is now a condition a case tests, and not prose alone.** New file `tests/test_requirement_scope.py` reads the requirement text of every document under `docs/specs/features/`. A requirement that scopes itself to a feature set names one path under `tests/` inside backticks, and a requirement that names none fails `test_a_requirement_scoped_to_a_feature_set_names_a_case_file`. **The check reads a necessary condition and not a sufficient one**, because no parser reads which cases a sentence means; a writer who names the case file has decided the scope, and a reader follows the path. **The failing direction is proven live.** Against the base the case read `AssertionError: these requirements bind a feature set and name no case file: ['FR-pre-release-validation-26']`. An injected `FR-release-99 — No case of this feature set opens a network connection.` in `docs/specs/features/09-release.md` read the same message for `FR-release-99`, and the revert returned all four cases to green. **The self-review found one gap in the pattern and closed it.** The first form read `case` alone, so `No cases open a network connection` stated the defect again and passed. The pattern now reads `cases?`, and the plural injection fails the case. **The sweep found one widened rule of the 200 the base holds, and no second one.** `FR-pre-release-validation-33` is the only other requirement scoped to a feature set, it binds the issues rather than the cases, and it names `tests/foxio_deviations.json`, so it passes the check and stays as it reads. **Two findings reach no repair here.** `FR-pre-release-validation-23` states a procedure a worker follows, and no check can test it, and the `Two statements here are not checkable` list of the feature document does not name it. The issue body of #419 quotes `FR-pre-release-validation-1` in a merged form that the document does not hold: the file states the install, the two fingerprint paths and the comparison as `FR-pre-release-validation-1` through `FR-pre-release-validation-5`. **`FR-pre-release-validation-16` is untouched, because #411 owns it.** The `## Terms` table gains `recorded transport` and `package index`. No file under `ja4plus/` changes, no file under `tests/foxio_vectors/` changes, and no fingerprint moves. |
| 149 | 2026-08-09 | #410 ran the gates on the granted Linux host, and that host had never run one. **The host is `bigboy`: Ubuntu 24.10, kernel `6.11.0-29-generic`, 56 cores, 137 GiB, `python3.12` only.** `.issue-flow.json` records no checkout path, and the run found one at `/home/e/ja4plus-verify`. That checkout carried a single-branch fetch refspec for `batch/362-hygiene-three`, so `git fetch` failed with `fatal: couldn't find remote ref refs/heads/batch/362-hygiene-three`. The refspec now reads `+refs/heads/*:refs/remotes/origin/*`, and the checkout held `3991712f08fbb11996fad69a3b457150d585463b`, the head of `epic/406-pre-release-validation`. The environment is `/home/e/ja4plus-verify/.venv` on `Python 3.12.7`, built by `python3.12 -m venv` with `pip install -e ".[dev]"`. **The host reported `14:47:56 up 82 days, 21:42, 3 users, load average: 2.91, 2.85, 3.09` before the first gate and `14:59:13 up 82 days, 21:54, 3 users, load average: 3.88, 3.39, 3.20` after the last one.** The host is shared, it carries an Elasticsearch node and continuous-integration runners, and the run started no load generator. **Every gate reports no failure.** The unit suite reads `2547 passed, 3 skipped, 1825 deselected, 8 xfailed, 16 warnings in 393.15s (0:06:33)`. Coverage reads 94 percent, 273 missed of 4232 statements, against the `COVERAGE_FLOOR` of 70 in `.github/workflows/test.yml`. The conformance suite reads `1532 passed, 143 skipped, 2574 deselected, 134 xfailed, 1 warning in 16.21s`, and `tests/foxio_deviations.json` holds 134 keys on the host. **That matches every conformance number the brief stated, so the register invariant holds on Linux.** The fuzz job reads `127 passed, 1 warning in 4.54s`. `ruff check ja4plus/ tests/` reads `All checks passed!`, `ruff format --check ja4plus/ tests/` reads `183 files already formatted`, and `mypy --strict ja4plus/` reads `Success: no issues found in 31 source files`. `TestTheStatedMemoryCeiling` reads `4 passed, 25 deselected, 1 warning in 60.21s (0:01:00)`, and `test_a_smaller_entry_count_holds_fewer_memory_blocks` of round 144 passed here. `tests/memory_ceiling_run.py --packets 30000 --connections 3000 --bound 0` wrote `{"idle_resident_mib": 85.57, "final_resident_mib": 115.37, "idle_blocks": 597195, "final_blocks": 974150, "peak_mib": 115.37, "packets": 30000, "connections": 3000, "bound": 0}`, and `peak_mib` sits 4.4 times below the stated 512 MiB ceiling. **The project manager added the installed-wheel marker to the run, and that is the finding of this round.** `python -m pytest tests/test_installed_wheel.py -m installed_wheel` reads `16 passed in 71.56s (0:01:11)` at a wall clock of 71.99 s, against the 23 s #408 measured on macOS. The case builds an artifact and installs it, so its cost follows the host and not the case count. The Linux measurement exists before the batch gate rather than at it, and the session-11 handoff records the gate as the shape that failed four `ubuntu-latest` jobs on batch #361. **The caveat binds every measurement of this round.** The host verifies a Linux code path and predicts no `ubuntu-latest` result, because the runner holds another kernel, another processor count, another memory size and another Python version. The host measures `python3.12` alone, so the 3.9, the 3.10, the 3.11 and the 3.13 rows of the matrix get no measurement here. `docs/specs/features/11-pre-release-validation.md` carries both rules and settles the open question about the checkout path. **Three statements of #410 are not checkable, and each names its reason.** No check confirms that a transcript came from that host, so a reader trusts the transcript or repeats the run. No check confirms that no load generator ran, because this project owns no monitor on a shared host. No check turns a Linux pass into an `ubuntu-latest` prediction, and the caveat is the reason. **This round adds no continuous-integration job.** It changes no file under `ja4plus/`, it changes no file under `tests/foxio_vectors/`, and it moves no fingerprint. |
| 151 | 2026-08-09 | #415 landed and the package states a measured packet throughput. `throughput` of the `## Terms` table names the count of packets one processor reads for each second, for one named packet run on one named host, and nothing measured it before this round. **New file `tests/throughput_run.py` feeds one `Processor` a stated packet run in an interpreter of its own** and writes one JSON object holding `packets`, `connections`, `elapsed_seconds`, `packets_per_second`, `fingerprints`, `python_version`, `platform` and `commit`. `tests/test_throughput.py` reads it, and `docs/performance.md` publishes the measurements. **The clock covers `process_packet` and nothing else.** The traffic build and the capture parse sit outside it, because the seconds scapy spends to build a packet belong to scapy, and a clock that spanned both would report the speed of scapy under the name of this package. **The synthetic run reads 1000000 packets across 100000 connections at 2006 packets for each second**, in 498.39 seconds for 400000 fingerprints, on this laptop, `macOS-26.6.1-arm64-arm-64bit-Mach-O`, Python 3.14.3, commit `be91cc4`. It feeds `ceiling_traffic` of `tests/test_memory_bounds.py`, so this measurement and the memory ceiling of #279 describe the same packet run, and #279 read 481 seconds for that run against the 498.39 seconds here. **The run ran beside a mutation sweep and the rate is a lower bound**: the load average read 8.88 before it and 12.78 after it on a ten-core host, and an unqualified rate taken beside a running sweep is a misleading number. **The capture run reads all 38 captures on two hosts, and the two agree on the result and differ on the time**: 9062 packets and 777 fingerprints on each, at 1857 packets for each second on `bigboy` and 2183 on this laptop. That is the result a throughput measurement must show. **The one-million-packet synthetic run has one host only.** `bigboy` is shared, it carries an Elasticsearch node and continuous-integration runners, and #410 already spent a memory ceiling run there, so the project manager gave it the capture run alone on 2026-08-09. `docs/performance.md` states that gap and its reason, and no Linux figure for the synthetic run exists. `uptime` read 1.79 before the capture run and 1.90 after it, and the run started no load generator. **A timing case that measures the wrong thing reads as a fast package, so the measurement carries three controls and each one was proved able to fail.** The packet count control fails when the run feeds half the packets, and it fails two cases with `assert 1000 == 2000`. The work control fails when the clock reads a constant rather than the traffic, with `assert 1.0 > 1.0`. The result control fails when the run counts no fingerprint, with `assert 0 > 0`, because a processor that produced nothing reads the highest rate of all. Each defect ran on `bigboy`, turned its own control red and left the other cases green, and the restored file returned 7 passed. **A control that cannot fail is not a control**, and this project records that finding more than any other. **The issue names `tests/foxio_vector_manifest.json` as the source of the row count, and calls that a premise. The count contradicts the doubt**: the manifest holds the same 38 names the directory holds, with no name on either side alone, so the manifest is complete and the capture set is every file under `tests/foxio_vectors/`. **This round sets no target and adds no job.** `Non-goals` states that wire-speed performance is out of scope and that this project measures throughput and reports it. `grep -rn "packets_per_second" .github/workflows/` finds nothing, and `test_no_workflow_names_the_rate_field` holds that as a condition rather than as a sentence. **The measurement becomes no floor**, because a floor derived from this measurement cannot detect a fault in this measurement; a later issue derives a floor from a second measurement taken by another method on a named host. **Two statements about throughput are not checkable, and the page marks each one rather than reporting it as true.** No check tells a slow package from a slow host, because a rate is a measurement of one host at one moment and this project holds no second instrument; the two hosts differ in processor, in operating system and in interpreter, so the difference between their rates names no cause. No check states whether a rate is adequate, because adequacy is a decision the user makes against a use. **`mkdocs.yml` lists the new page**, because `omitted_files: warn` turns an unlisted page into a warning and `mkdocs build --strict` turns that warning into a failure. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 152 | 2026-08-09 | #424 landed and the capture privilege case guards on the state it reads. `tests/test_watch_capture.py::TheCommandNamesTheCapturePrivilege::test_the_message_reads_the_failure_this_host_reports` asks the real capture layer of this host for the failure it reports without the privilege. **It guarded on `sys.platform` and on `os.geteuid()`, and it did not guard on the grant it depends on.** The user ran `sudo chown $(whoami) /dev/bpf*` on 2026-08-09 for #423, and the case then read `AssertionError: the host granted the capture privilege` at `tests/test_watch_capture.py:132`. **The failure reproduces on the unchanged base**, so no change caused it, and the host state moved under a case that reads host state and does not control it. **A red gate that is normal is worse than no gate**, because the next real failure hides inside it. `the_privilege_failure` now returns `None` where the host grants the privilege, and the case calls `skipTest` with a reason that names the grant. **This round deletes no case and weakens none.** The case stays the only one that reads the failure from the real capture layer, and it still runs and still asserts the message on a host that denies the privilege. **A guard proved in one direction can skip on every host, and a case that always skips measures nothing.** New class `TheCapturePrivilegeCaseGuardsOnTheHostState` therefore runs the guarded case itself against a patched `scapy.arch.bpf.core.get_dev_bpf`, and it reads both directions. The denial direction reads one case run, no error, no failure and no skip. The grant direction reads one skip whose reason names the grant. **The denial direction already passed against the base, and that is why the defect reached the branch**; the grant direction read `AssertionError: Lists differ` there and it passes here. **The earlier form leaked a file descriptor on every granting host**, because `get_dev_bpf` returns an open descriptor at `scapy/arch/bpf/core.py:56` and no caller closed it. A third case reads `os.fstat` on that descriptor and requires an `OSError`. **The sweep for the same fault read all 154 files under `tests/`, it found four more cases, and this round repairs none of them.** #426 records all four. `the_filter_failure` calls `compile_filter`, which raises `ImportError("libpcap is not available. Cannot compile filter !")` on a host without `libpcap`, and the caller catches `Scapy_Exception` alone. `test_the_interface_list_of_this_host_holds_a_name` requires this host to hold one interface. `the_absent_interface_failure` requires this host to hold no interface named `nosuchif0`. `test_three_hundred_calls_hold_the_open_descriptor_count` of `tests/test_watch_stop.py` reads `os.listdir("/dev/fd")` under no platform guard. **Every number of the brief was measured again, and one reading of the brief was wrong.** The conformance suite reads 1532 passed, 143 skipped and 134 xfailed against 134 register keys, which the brief states and which holds. The unit gate reads those numbers nowhere, because it deselects the conformance marker; it reads 2579 passed, 4 skipped, 1825 deselected and 8 xfailed here against 2577 passed and 3 skipped on the base. Coverage holds at 94 percent, 273 missed of 4232 statements, unchanged from the base. **No file under `ja4plus/` changes, the privilege message does not move, and no fingerprint moves.** |
| 153 | 2026-08-09 | #412 landed and every sweep candidate of the eleven protocol parsing modules is settled. Eleven single-module sweeps read `ja4plus/utils/` with `--max-per-module 0` over **1420 mutations**, and the union of the eleven candidate sets is **205 candidates**: 203 settle `correct` with the reason and 2 settle `repaired` with the case. `docs/mutation_reports/412-utils.json` holds the merged report and `docs/mutation_settlements/412-utils.json` holds the record. **The scope is the minimal cover, and the user ruled it on 2026-08-09.** The rule "name every test file that reads the module" measured **32.18 hours** against a four-hour ceiling, so the stop condition fired and no sweep started until the ruling arrived; the minimal cover measures **0.52 hours** for the same 1420 mutations. **Three limits hold wherever that result is read.** Every mutation keeps a reader, so the surviving-mutation signal is preserved. The cover **over-reports survivors and never under-reports them**, because a test file it drops might have killed a mutation. The candidate set shrinks, 97 cases for `quic_utils.py` against 2268 under the wider rule, and **a case the cover drops stays unmeasured against that module** rather than measured clean. **Two cases are repaired.** `tests/test_tunnels.py` read `startswith("scapy.")` alone, so three mutations of the module names at `ja4plus/utils/tunnels.py:27` left it green. `tests/test_loopback_link_type.py` measured nothing for address family 30 on Darwin, because scapy binds `socket.AF_INET6` itself and that value is 30 there, so **the case was vacuous on that host and would have failed on Linux**; it now asserts that `ja4plus` wrote the bind. **One mutation can turn a loop bound into a loop that never ends.** `ja4plus/utils/ssh_utils.py:284` holds that bound, and the sweep stopped for good three times before the cause was read. `tests/mutation_sweep.py` gains `--timeout` and the status `timeout`, off by default, and **a mutation that times out is not a survivor** because a run that never finished measured nothing. Seven mutations of this module set reached the limit. **A criterion that reads "0 unsettled candidates" passes on an empty directory**, so `tests/test_mutation_census.py` gains `TestTheRecordsOfThisRepository`: the census names at least one candidate, and it names an unclaimed candidate when one row leaves the record. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 154 | 2026-08-09 | #413 landed and every sweep candidate of the twelve fingerprinter modules is settled. **Twelve single-module sweeps read `ja4plus/fingerprinters/` with `--max-per-module 0`, over 1569 mutations**: `ja4ssh.py` 265, `ja4l.py` 239, `ja4.py` 203, `ja4d6.py` 194, `ja4h.py` 169, `ja4s.py` 134, `ja4x.py` 130, `ja4d.py` 128, `ja4ts.py` 56, `ja4t.py` 31, `base.py` 11 and `__init__.py` 9. The sweeps killed 891 mutations, 675 survived and 3 passed the time limit. The union of the twelve candidate sets is **394 candidates**, and `docs/mutation_settlements/413-fingerprinters.json` settles every one with the verdict `correct` and the reason. **No file under `ja4plus/` changes**, because `CLAUDE.md` rule 1 binds every change there to a vector, and this round repairs no case that would move a fingerprint. **The scope is the minimal cover.** The rule "name every test file that reads the module" measured 43.86 hours for this module group against a four-hour ceiling, and the cover measured 0.69 hours before the run and 0.88 hours at it. The three limits #412 recorded hold here without change, and the record states them under its `scope` key. **The default coverage core of Python 3.14 records almost no test context, and a cover rests on that reading.** `coverage` 7.15.4 selects the `sysmon` core on Python 3.14.3, and one whole-suite run over 4112 passing cases recorded 294 contexts over 74 test files. The same run under `COVERAGE_CORE=ctrace` recorded one context for each case, and the reader set of this module group rose from 47 test files to 95. A cover built on the first reading names too few test files, so the record states the command that produces correct data. **94 of the 675 surviving mutations sit inside a type annotation, and no case can ever fail for one.** Every module of `ja4plus/fingerprinters/` carries `from __future__ import annotations`, so Python holds each annotation as a string and evaluates none of them. The sweep already skips a docstring and a logger argument for the same reason, and #431 records the finding. **`ja4plus/fingerprinters/__init__.py` reported 9 survived of 9, and the record states the reason rather than a new case.** The module holds nine export names, no case reads its `__all__`, and #413 was instructed to record the reason rather than to invent a case for a name. **Three mutations of `ja4plus/fingerprinters/ja4x.py` make the record scan run with no progress**, one at line 243 and two at line 284, and each run stopped at the 180-second limit #412 added. A run that never finished measured nothing, so each carries the status `timeout` and the candidate list over-reports rather than under-reports. **Eight candidates cannot fail**, because the unmutated suite state reports them as xfailed: seven of `tests/test_ja4_alpn.py` and one of `tests/test_ja4h_spec.py`. No candidate is skipped, and the 134 xfailed conformance cases reach this cover almost nowhere. **The census assertion is not vacuous, and both readings are recorded.** With the report present and no record, `test_every_candidate_of_this_repository_holds_exactly_one_settlement` failed on 394 unclaimed candidates; with one row removed it failed on one; with the record whole the file reports 32 passed. `python tests/mutation_census.py` reads 599 candidates over 41 test files, 0 unclaimed, 0 claimed twice, 0 unknown and no fault. **No fingerprint moves.** |
| 155 | 2026-08-09 | #414 landed and every sweep candidate of the seven interface modules is settled. Seven single-module sweeps read the seven Python modules of the top directory of the package, which `tests/mutation_sweep.py --module 'ja4plus/*.py'` selects with `Path.glob`, with `--max-per-module 0`, over **556 mutations**: `cli.py` 226, `watch.py` 97, `ja4db.py` 68, `output.py` 68, `processor.py` 54, `__init__.py` 32 and `types.py` 11. **The cost was measured before the run and not estimated**, as Amendment 2 asks: the minimal cover of each module, timed as a whole `pytest` command, gives a product of **1.27 hours** against the four-hour ceiling, so the stop condition did not fire. The seven runs took 4188.7 s and read **340 killed, 279 survived, 4 timeout and 0 unusable**. The candidate sets hold 307 rows and **236 distinct cases**, and the union is what `docs/mutation_settlements/414-interface.json` settles: 231 `correct` with a reason and 5 `repaired` with the case. **Do not compare 236 against the 976 of #206 or the 205 of #412**, because the three count different sets. **Coverage with one context for each case splits the 307 rows**: 301 run no mutated line of their module inside a function body, and 6 run one. Each of the 6 was proved live with `inspect.getsource` in both directions. **Two repairs came out of them, and each is a comparison that was never made.** `tests/test_documentation_samples.py` asserted the type of the error a `raises` sample block produces and never the line it came from, so the mutation `not in` -> `in` at `ja4plus/types.py:74` moved the raise from the second line of `docs/api_reference.md:284` to the first, kept the type `KeyError`, and left the case green; the harness now reads the line the error came from. `test_a_value_that_is_no_bool_raises_type_error` read the type of the error and no message, so the mutation at `ja4plus/ja4db.py:294` changed nothing it compared. **The first repair was itself unable to fail**, because `pytest.raises(match=...)` searches rather than compares and `allow_remote takes True or False_mutated` holds the unanchored pattern; the pattern now carries both anchors and the mutation fails all four parameters. **The census assertion is proved in both readings**: the census names 236 candidates of sweep `414-interface`, and one row removed from the record makes it name that candidate as unclaimed. **`FR-pre-release-validation-23` is split**, which #419 routed here. It stated a procedure a worker follows, no check could test it, and the `Two statements here are not checkable` list did not name it. The artefact keeps a check and the procedure keeps none: `FR-pre-release-validation-23` now states that a `repaired` verdict names a case the suite collects, new file `tests/test_settlement_procedure.py` reads it, and `FR-pre-release-validation-23a` carries the procedure into the uncheckable list as its third entry, because no case observes a worker. **The condition is necessary and not sufficient**, and the requirement, the list and the file each say so. **The cover rule holds a defect this round records and does not repair.** It drops the lines the import runs, and 95 of the 279 survivors sit on such a line; `ja4plus/__init__.py` measures the cost, because the cover the rule builds is `tests/test_parity.py` at 1 killed and 31 survived while the same 32 mutations against `tests/test_public_interface.py` read 25 killed and 7 survived, so 24 survivors are over-reported. #433 carries it. **`tests/test_cli.py` runs all 27 reachable mutation lines of `ja4plus/output.py` on its own**, so the cover of that module holds it alone and no case of `tests/test_output_schema.py` or of `tests/test_cli_output_option.py` is measured against that module; a case the cover drops is unmeasured and not clean. **The sweep was stopped once by a signal and it left no change behind**: the handler of #412 turned the signal into `SystemExit`, the restore ran, `git status --porcelain ja4plus/` reported nothing, and the checkpoint resumed the run at mutation 37 of 54. No file under `ja4plus/` changes, no file under `tests/foxio_vectors/` changes, and no fingerprint moves. |
| 156 | 2026-08-09 | #429 landed and a change set that records no round fails a case. New file `tests/test_round_entry_existence.py` reads the change set of the branch and requires one new round entry in `CHANGELOG.md` and one new Changelog row in this table. **The three cases of `tests/test_changelog_round_agreement.py` hold the two records against each other, and two absent records agree**, so all three passed the change set of #412: eleven sweeps, two repairs, a new test file, no entry and no row. The project manager read the diff at the merge gate and caught it, and that is not a check. **The reference point is the merge base of `HEAD` and the integration branch**, which `git merge-base` reads against `origin/dev` and then `dev`. The merge base is a condition on the commit graph and it reads no local configuration. The change set joins `git diff --name-only <merge base>`, which reports an uncommitted change to a tracked file, and `git ls-files --others --exclude-standard`, which reports the new file that #412 shipped. A change set of `CHANGELOG.md` and this page alone demands no entry, because the round assignment of the project manager edits those two files alone. **The case counts an unassigned entry and a numbered entry alike**, because the project manager assigns the number at the batch gate on one batch and hands the number to the worker on another, and a case that demanded `TBD` would fail on the second. **Where the change set cannot be read, the case skips and the reason names the state.** A shallow clone carries no `origin/dev` ref, so the case skips on the GitHub runner and runs on the local gate of a worker. #438 owns the ref the runner needs. **The case was proved in both directions on this repository**, and one case reads commit `46aa502` against its parent and fails the change set of #412 itself, which the whole unit gate passed. Thirteen more cases prove the same pair against a scratch repository over a committed change, an uncommitted change, an entry that reaches one record alone, a change set of each record alone, a file an ignore rule covers, a repository that holds no integration branch and a directory that holds no repository. **A case here cannot test that an entry is true**, only that one exists, and the widest of the four recorded limits is that the count reads the whole branch, so an integration branch whose members each recorded a round satisfies a member that recorded none. No file under `ja4plus/` changes, no fingerprint moves, and the register holds 134 keys against 134 xfailed. |
| 157 | 2026-08-09 | #371 landed and the periodic statistics case states the schedule rather than samples it. **`TheStatisticsGoToStandardError::test_the_periodic_line_reaches_standard_error` held the second case of the fault #369 repaired.** It ran `ja4plus watch` with `--stats-interval 0.05`, slept 0.3 seconds inside the capture, and asserted that at least 2 statistics lines reached standard error. Six wake-ups were due and the case needed 2 of them, where the case #369 repaired needed 3 of 7, so the margin was wider and the mechanism was the same one. **#369 could not reach this case with the seam it added.** The case builds no reporter. It reaches the reporter through `cmd_watch`, so a deterministic form needs a wait that reaches `report_statistics` from the command. **The seam is one parameter and the shipped behaviour does not move.** `report_statistics` gains `wait`, it forwards that call to `StatisticsReporter`, and `ja4plus/cli.py` passes nothing, so the default still waits on the stop event. One line of `ja4plus/` changed, and no file under `ja4plus/fingerprinters/` changed. **The case reads the two arguments the command passed, so it proves more than the earlier form proved.** `ScriptedReport` records the interval and the stream `cmd_watch` gave to `report_statistics`, and the scripted wait records the timeout the reporter asked for. The case asserts `[0.05]` for the interval, `[0.05, 0.05, 0.05, 0.05]` for the four timeouts of three intervals, that the stream is standard error, that four statistics lines reach standard error, and that no statistics line reaches standard output. **The count is exact, where the earlier form asserted a lower bound**, so a reporter that writes an extra line now fails. **No assertion of the case reads the wall clock and the case holds no sleep.** **The tests came first**, and the case reported `Error during capture: report_statistics() got an unexpected keyword argument 'wait'` against the unrepaired package. **Six mutations prove the case discriminates, and each one was proved live by reading the loaded source back with `PYTHONDONTWRITEBYTECODE=1` in both directions.** A `report_statistics` that drops the injected wait fails the case, a `cmd_watch` that passes a constant interval fails it, a `cmd_watch` that passes standard output fails it, and a reporter that writes two lines for one interval fails it. The fifth mutation binds the seam to the shipped path. A default wait that never reports the stop fails `test_the_thread_ends_before_the_command_returns`, and that case measures the real stop event through the command. **The sixth mutation holds a kill that this case alone carries.** The sweep of #414 recorded the mutation `0` to `1` at `ja4plus/cli.py:871` as killed, at a killed count of 1. This case is the sample it names. The guard reads `seconds <= 0`, and `--stats-interval 0.05` is the one interval below one that any case of the suite passes. The repair keeps that argument, and the mutated guard fails the case as it did before. The repair therefore loses no coverage of the interval guard. **The class runs 20 times in a row and passes 20 times**, at 7 cases for each run. **No fingerprint moves**, and the conformance suite reports 1532 passed, 143 skipped and 134 xfailed, with 134 keys in `tests/foxio_deviations.json` against those 134. The unit suite holds at 2595 passed, 4 skipped and 8 xfailed, because the case count does not change. **Coverage holds at 94%**, and `ja4plus/watch.py` reads 99% before and after. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 158 | 2026-08-09 | #430 landed and the two load-sensitive timing cases read the work performed rather than the seconds elapsed. **The reassembler case was vacuous as well as flaky.** It compared `elapsed(20000)` against `elapsed(5000) * 8`. `DEFAULT_MAX_STREAM_SEGMENTS` stores 4096 segments and refuses the rest. Both readings therefore stopped at the same 4096 stored segments. A duplicate check that scans the stored segments read a ratio of 5.975 against that threshold of 8, and the case passed in 7.66 seconds. `test_the_cost_of_a_segment_does_not_grow_with_the_segment_count` now holds no clock. It raises the segment cap to the count it feeds. It substitutes `CountingSegmentList` for the segment list. It then counts the reads of the stored segments, and it costs 1.18 seconds. The set of seen segments reads 0.0 segments for each segment at 5000 and at 20000. The scan reads 2499.5 and 9999.5. The bound is 4, and the scan fails the case with `AssertionError: 2499.5 not less than or equal to 4`. **The counting list charges a slice by the segments it spans.** A count of one for a slice would leave a scan of the last 4096 segments below the bound. A scan written as `stream["segments"][-4096:]` fails the case with `AssertionError: 2417.8688 not less than or equal to 4`. The list charges a reverse iteration by the whole list for the same reason. **The work control keeps its clock.** The rise of the elapsed time is the property the control exists to prove. It now reads the fastest of several runs. A loaded host adds seconds to a run and removes none, so the fastest run of a count is the run the load moved least. The control feeds 1000 packets and then 2000, three runs of each count. It compares the fastest run of one count against the fastest run of the other. It states no tolerance. A runner that writes a constant elapsed time still fails it with `assert 1.0 > 1.0`. **The control reads a count of its own rather than the published count.** The rise holds at any count. A reader who raises `JA4PLUS_THROUGHPUT_PACKETS` then pays nothing for the control. The 20000-packet run of the earlier form leaves the unit suite, and `DOUBLE_RUN_PACKETS` and the `double_measurement` fixture leave with it. **The load was produced deliberately and it was bounded.** Four worker processes on this ten-core laptop performed a fixed count of arithmetic iterations, and each one stopped on its own block count. `bigboy` ran nothing. The two named cases passed under that load twice, at `3 passed, 1 warning in 17.76s` and at `3 passed, 1 warning in 26.05s`. **The second measurement reproduced the failure of #412.** The load average read 14.37 during it and 20.29 after it. The 1000-packet runs read 0.7825, 0.8261 and 0.65 seconds against 0.31 on a quiet host. The 2000-packet runs read 0.9065, 0.9117 and 0.7926 against 0.64. **One reading pair of the nine inverted**: 0.8261 seconds for 1000 packets against 0.7926 for 2000. The earlier form compared one reading of each count, so it fails on that pair. The fastest runs kept the order, at 0.7926 against 0.65, which is a margin of 22%. `WORK_CONTROL_RUNS` is the knob a later flake turns, because each added run lowers the fastest reading of both counts toward the reading of a quiet host. The first measurement ran at a lighter load, 11.66 before and 13.03 after, and all nine pairs kept the order there. **No tolerance widened, and three losses are named.** The reassembler case no longer fails on a cost that grows inside a structure other than the segment list. The earlier duration form did not fail on the scan of that list either. The work control no longer reads the elapsed field of the published measurement, so it proves the rise at 1000 and 2000 packets. The control reads a rise and it reads no slope. A clock that carries a large constant and a small share of the traffic still rises, so the control passes it. **A floor on the ratio of the two readings is declined.** The two fastest runs read a ratio of 1.219 under the load average of 20, against 1.96 on a quiet host. A floor of 1.5 fails there. That floor trades one defect for a flaky case, and the flaky case is the defect this round repairs. #369 and #371 record the same defect class, and #371 holds the third case. **No file under `ja4plus/` changes and no fingerprint moves.** Unit: 2612 passed, 4 skipped, 8 xfailed, 98 subtests, coverage 94%. Conformance: 1532 passed, 143 skipped and 134 xfailed against 134 register keys. |
| 159 | 2026-08-09 | #426 landed and the four remaining cases that read ambient host state carry a guard. The sweep of #424 read all 154 files under `tests/` and found four cases of the shape it repaired. **This round guards all four and deletes none of them**, because each case states a real requirement. `the_filter_failure` of `tests/test_watch_capture.py` calls `compile_filter`, and `scapy` 2.7.0 raises `ImportError("libpcap is not available. Cannot compile filter !")` at `scapy/arch/common.py:87` where the loader cannot open `libpcap`. The call caught `Scapy_Exception` alone, so a host without `libpcap` ended four cases with an error rather than with a failure. **A minimal Linux container is such a host**, and the helper now returns `None` there while `the_filter_failure_or_skip` skips the four cases that read it. `the_absent_interface_failure` raised `AssertionError` where the host holds an interface named `nosuchif0`, and it now returns `None` and skips the four cases that read it. `test_the_interface_list_of_this_host_holds_a_name` required this host to hold one interface, and it now skips where the capture layer reports none. `test_three_hundred_calls_hold_the_open_descriptor_count` of `tests/test_watch_stop.py` read `os.listdir("/dev/fd")` under no platform guard, and Windows holds no such directory, so `the_open_descriptor_count` now returns `None` there and the case skips. **A guard proved in one direction can skip on every host, and a case that always skips measures nothing.** Four new classes therefore run the guarded cases themselves and read both directions. **The `libpcap` direction reaches the real `scapy` translation and no stand-in.** A finder on `sys.meta_path` raises `OSError` from the import of `scapy.libs.winpcapy`, which is the failure the loader reports, and `scapy` raises the `ImportError` itself. **Every run-direction prover reads the ambient state by a route that passes the guard it proves.** The first form read the state through the guard. A mutation that made the guard skip always then made the prover skip as well, so it measured nothing. That mutation now turns four subtests red. **The count case carried a second defect and this round repairs it too.** The reading covers every descriptor of the process, so another part of the run that opens one moved it, and the case compared for equality. `DESCRIPTOR_TOLERANCE` is 16 against the 600 descriptors the leak opens over 300 calls. `test_the_case_fails_where_the_monitor_leaks_a_descriptor_for_each_call` opens one descriptor inside each capture call and requires the count case to fail. **The tolerance states one limit**, because a leak below it passes where the equality failed, and the case states that trade. No file under `ja4plus/` changes and no fingerprint moves. |
| 160 | 2026-08-09 | #380 landed and the precedence exception states the register key count the register holds. `.claude/rules/external-apis.md` read **The exception reaches 6 rows of the 135 the register holds**, and the register holds 134 keys. #272 removed `ssh2.pcapng/JA4L-S` on 2026-08-09, because the repair to `ja4plus/fingerprinters/ja4l.py` made the comparison pass, and the prose kept the earlier count. **The reach of 6 was correct, and the key count alone was stale.** The two new cases prove that split. The reach case passed on the unrepaired prose. The key-count case failed with `assert 135 == 134`. **A count that no case measures goes stale.** #345 repaired that shape in `tests/foxio_deviations.py`, and this round repairs the second sentence of the pair. `stated_exception_reach_counts` in `tests/test_precedence_exception.py` reads both numbers out of the sentence with one regular expression. `measured_exception_reach_counts` reads the same two counts out of the register. `TestTheExceptionReachCounts` holds the two against each other, and it follows the `TestTheMarkerRuleCounts` shape of #345. **The reader raises `ValueError` on a document that states the counts on no sentence, and on one that states them twice.** A rewording that removes the sentence therefore fails the gate. **A check that a rewording defeats is not a check**, and two further cases prove the reader in both directions. **Both counts were proved live**, with `PYTHONDONTWRITEBYTECODE=1` and a cleared `__pycache__`. A stale key count fails with `assert 135 == 134`. A stale reach fails with `assert 5 == 6`. A removed sentence fails with `ValueError: the rule states the reach counts on 0 sentences, and it states them on one`. **The sweep decided the size of the repair, and it changed one sentence.** Four other live claims state 134, and each one agrees with the register: `docs/specs/spec.md:539`, `docs/specs/features/11-pre-release-validation.md:172` in `FR-pre-release-validation-33`, the same page at line 370, and the marker rule of `tests/foxio_deviations.py:63`. `.claude/rules/external-apis.md` states 43 capability entries and 37 decided value-form keys of #129. The register measures 43, 43 under #129, and 37, so all three agree. **A dated record of a past measurement is quoted, not rewritten.** `tests/test_foxio_deviations.py:660` records what #341 measured. The module docstring of `tests/test_precedence_exception.py` records the 135 keys the run of #334 read at the pinned commit. `docs/implementation_notes.md` records earlier readings at 33, 99, 104, 116, 117 and 120 keys. Every page under `docs/specs/foxio/` records a reading against the stated base of that page. Every Changelog row that states a past count records that round. **A correction inside a record falsifies the record**, so all of them stay as they read. No file under `ja4plus/` changes, no file under `tests/foxio_vectors/` changes, and no fingerprint moves. |
| 161 | 2026-08-09 | #378 landed and the `dev` extra of `pyproject.toml` states one exact `ruff` version. **The lint gate could turn red without a commit.** The extra declared `ruff>=0.6`, so `pip` resolved the newest release at the moment of each install. `ruff check ja4plus/ tests/` therefore read a different tool on two days. The entry now reads `ruff==0.16.2`. **#297 measured the drift on this repository.** Against `ruff` 0.14.5 it read 58 `F401` findings, 28 files and 82 `I001` findings. Against 0.16.2 it read 54, 27 and 76. The tree did not change and every number differed. #297 changed no pin, because the pin sat outside its scope. **The project manager ruled the exact pin.** It declined a compatible range and it declined the recorded drift. A gate whose result depends on the day it runs measures something other than what it names. **The pin moves what this gate installs today not at all.** That is a measurement and not an assumption. `pip index versions ruff` reports 0.16.2 as the newest release, so the floating specifier already resolved it. Both lint commands report no finding before the change and after it. **The pinned release therefore reports no finding the floating one did not.** That is the condition the plan asked a worker to report rather than repair. **The `dev` extra is the one place a tool reads the version from, and three readings state it.** No file under `.github/workflows/` names `ruff` with a version specifier. Every job of `.github/workflows/test.yml` installs the extra. The tracked `requirements.txt` names no `ruff`. **New file `tests/test_lint_gate_pin.py` holds 6 cases.** The entry is one exact pin. The comment beside it cites #378. That comment states that a version change is a commit. No workflow states a version. No second dependency file states one. The installed release equals the pin. **The tests came first.** The unrepaired tree failed 4 of the 6 with `AssertionError: the dev extra states no exact version for ruff: 'ruff>=0.6'`. **Eleven mutations prove the cases discriminate, and each one isolates to the case it targets.** A pin of 0.16.1 fails the installed-release case alone, at `this environment holds ruff 0.16.2 and pyproject.toml pins 0.16.1`. A comment that drops `#378` fails the citation case alone. A comment that drops the word `commit` fails the commit rule alone. A workflow that appends a specifier fails the workflow case alone. A `ruff` line appended to `requirements.txt` fails the dependency-file case alone. A second `ruff` entry fails the pin case, at `the dev extra names ruff 2 times`. The floating specifier restored fails the pin case and the installed-release case. **Each mutation restored from a snapshot of the implemented tree and not from the index.** The first harness restored from the index, and it contaminated six of its own readings. **The self-review found three defects in the first form of the new file, and three more mutations prove the repairs.** All three are the shape this project records most often, a case that cannot fail. **A substring test read the comment of the wrong entry.** The reader matched the line that held `"ruff`. A decoy `ruff-lsp` entry above the pin, with a one-line comment of its own, satisfied both comment cases while the pin carried no comment. `_distribution` now compares the whole name, and the decoy fails both cases. **The specifier pattern missed a one-character operator.** It read `[=<>!~]=` and demanded a trailing `=`, so `ruff>0.6` escaped it, and that is the shape of the specifier this pin replaced. The pattern now reads `ruff>0.6`, `ruff===0.16.2` and `ruff[extra]==1.0`. **The shared list parser counted a string inside a comment.** `_dependency_block` of `tests/test_documentation_site.py` collects every double-quoted substring of the block, and the `dev` extra carries comments inside its brackets. It already returned `not spec_validation` from the comment beside `build>=1.0`. A comment that quoted `"ruff==0.14.5"` would have failed a case on the wording of a comment, which is a check a rewording defeats. `_dev_entries` strips the comment lines first, and such a comment now moves no case. `_dependency_block` stays correct where it is used, because the `docs` list and the runtime list carry no comment inside the brackets. **The case that reads the installed release catches a drifted environment.** It skips where the environment holds no `ruff`, so a reader of the skip sees the gap. **A case here reads no prose.** `CHANGELOG.md` and this row quote the pin inside backticks, and no tool installs from a record, so the reader names the files a tool resolves a dependency from. **`FR-foundation-8b` and `FR-foundation-8c` carry the two requirements.** Two behaviour rules of `docs/specs/features/00-foundation.md` state the cost. A pinned tool falls behind, and a bump re-measures the lint gate and the `ignore` list of `[tool.ruff.lint]`. The `Terms` table gains `pin` and `lint gate`. **Two findings reach no repair here, because both sit outside the acceptance criteria of #378.** `mypy>=1.11` resolves 2.3.0 and `pytest>=7.0` resolves 9.1.1, so the same defect stands on the rest of the `dev` extra. The tracked `requirements.txt` states `pytest-cov>=3.0.0` against the `>=5.0` of `pyproject.toml`, and the comment beside that entry states why the coverage floor needs 5.0. #446 carries both, and it holds the `mypy` question open rather than pin by symmetry. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 162 | 2026-08-09 | #387 landed and every document that states a count of implemented methods states eleven. **`JA4LFingerprinter` writes `JA4L-S=` at `ja4plus/fingerprinters/ja4l.py:446` and `JA4L-C=` at `:482`, so ten fingerprinter classes carry eleven methods.** **This round re-measured the set on its own base commit `d123ab9` rather than trusting the count the issue body carries.** `__all__` names ten `generate_` functions and the module of `generate_ja4l` writes both value prefixes, so eleven of the twelve names of `FOXIO_METHODS` reach a generator and JA4TScan reaches none. Round 139 read the same eleven from three other places, and the two measurements agree. **The issue named three documents and a sweep of the whole corpus found six.** `docs/specs/features/08-documentation.md` and `docs/specs/spec.html` each stated that this project implements ten of the twelve. Four more documents called the ten fingerprinters ten methods: `docs/api_reference.md` at eight places, `docs/specs/features/03-concurrency-safety.md` at two, `docs/usage.md` at one, and the `Data model` table of this file at one. **A count of the ten things the processor drives is a real count, and the word `method` carried two meanings.** The prose now counts fingerprinters and `--types` values where it counted methods, which is the wording the `FingerprintResult.type` docstring already held after round 139. **The reason stands beside the number wherever a document states it**, because a bare eleven invites the next reader to count the classes and correct it back to ten. `Overview` gains that sentence, and `README.md` and `docs/methods/index.md` already held one. **A new case set reads the count out of the package, because prose carries no other gate.** `tests/test_documented_method_count.py` holds 151 cases. `stated_counts` reads a claim about the implemented count in three word orders, over a set of claim verbs rather than the one verb `implement`, and `class_counts_of_methods` reads the class count applied to the word `method`. **The forbidden word is not the literal `ten`**: it is the count of names of `__all__` that end in `Fingerprinter`, so the case stops forbidding the word on the day the two counts agree. **The cases were written first and they bite**: against the unchanged documents they failed 10 and passed 128, and the failing set named exactly the six documents this round repairs. **Seven mutations prove they discriminate.** The first replaces `JA4L-S=` with `JA4L-X=` on the return lines of `ja4plus/fingerprinters/ja4l.py` and it leaves the docstring that states the same format. It drops the count to ten and fails all five documents that state eleven. **That mutation is the one that matters**, because it proves the cases read `ja4plus/` and not the documents twice. Removing `generate_ja4d6` from `__all__` fails the same set. The other five restate ten in one document, remove the reason from one document, and apply the class count to `method` on `docs/usage.md` and on `docs/api_reference.md`. Every mutation was restored, `inspect.getsource` confirmed the live source in both directions, and `git diff` reports no change under `ja4plus/`. **A dated record of a past measurement is quoted and not rewritten.** `readable_text` cuts `CHANGELOG.md`, the `Changelog` section of this file and the `Risks & open questions` section of this file, so rounds 49, 70, 136 and 139 keep the counts they record, and the risk row that reads "The README claims ten methods" keeps the condition it recorded. **The self-review found two defects in the reader and each is now closed.** The first read the whole module source for a value prefix, so the docstring of `ja4plus/fingerprinters/ja4l.py:6`, which states the format `JA4L-S=` in prose, satisfied the check on its own. A change that deleted every emitter would have kept the count at eleven, which is the right answer for the wrong reason. The reader now reads the return lines of the module alone, and `test_the_emitter_reader_reads_no_prefix_that_a_docstring_alone_states` fails the first form. The second searched the joined page for a quoted passage, so it paired a quotation mark inside a command of `docs/usage.md` with a mark 40 lines below and dropped every word between them. The reader matched nothing on that page, and a mutation of that page passed against a document that held the defect. It now drops a quotation one line at a time, and `test_the_class_count_reader_reads_a_page_that_holds_a_quotation_mark_elsewhere` fails the first form. **The review also proposed five rewordings that the first reader read nothing in**, among them "supports ten of FoxIO's methods" and "ten of twelve implemented", and `SUPERSEDED_SENTENCES` now holds all of them. **Five findings reach no repair here.** #449 records that this page's rendered form states "Seven of twelve FoxIO methods are specified only as images" where the same page states eleven, and that `SUPERSEDED_COUNT` of `tests/test_documentation_image_count.py` reads over it because the page writes no `the`. #450 records the ten files under `tests/` that call the ten fingerprinters ten methods, and it names the decision the field `ProcessorStats.method` needs, because that repair reads a public field name and this round does not touch it. **Four docstrings of `ja4plus/watch.py` carry the same counting error**, at lines 13, 81, 115 and 518, and a comment on #450 records all four, because the criteria of #387 forbid a change under `ja4plus/`. #453 records a separate stale number the sweep met: `docs/specs/features/03-concurrency-safety.md` states sixteen state tables where `docs/api_reference.md` states seventeen, and a live `Processor().stats()` reports seventeen. The body of #62 still states ten of the twelve, and it is a tracker record rather than a document of this repository. **`docs/specs/spec.html` is not generated.** No command in the repository writes it, its own `How this page is maintained` card states that a person keeps it by hand, and round 70 records the same reading, so the correction is a hand edit and no build undoes it. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 163 | 2026-08-09 | #393 landed and the documentation behaviour rule names a command that runs. The Behaviour rules of `docs/specs/features/08-documentation.md` read **A code sample is tested by `pytest --doctest-glob`**, and the Acceptance criteria named `pytest --doctest-glob="*.md" README.md docs/`. **That command collects nothing.** It writes `no tests ran` and it exits with the status 5. `--doctest-glob` collects an interactive session, written as `>>>` lines above the expected output, and every code sample of this project is a fenced code block. The two forms share nothing, so the rule named no check at all, and a reader of the page believed the rule covered the samples. **The requirements themselves hold.** `tests/test_documentation_samples.py` runs 45 samples for FR-documentation-4 and FR-documentation-5, and `tests/test_examples.py` runs the five scripts of `examples/` under nine cases for FR-documentation-6. #63 built that harness, met the requirements, and stopped at the rule rather than editing the specification inside a feature diff, which is why this amendment is its own issue. **The project manager ruled on 2026-08-09 that the criterion is amended and the samples stay fenced.** A rewrite of 44 fenced samples into doctest sessions would make several unrunnable and would buy no coverage the harness already provides, and a token doctest added to make the command green would prove nothing. **The defect lived in a second file.** The Documentation row of the Testing strategy table of this page named the same command, so one rule stood in two records and this round repairs both. `CLAUDE.md` carries no doctest command, and its `## Commands` section was read for the same fault and holds none. **Three sentences of the feature page change and one row of this page changes.** The Behaviour rules now name `pytest tests/test_documentation_samples.py` and `pytest tests/test_examples.py`, they state that `--doctest-glob` reads an interactive session and collects nothing here, and they record that the samples stay fenced because a fenced block is what a reader copies. The Edge cases row reads `The harness case fails` where it read `The doctest run fails`. **New file `tests/test_documentation_behaviour_rule.py` holds 20 cases in four groups.** The command cases run every backticked `pytest` command of the Behaviour rules under `--collect-only` and require each one to collect at least one case, so a fourth rule that named a dead command fails whatever sentence carried it. The floor case requires two such commands, because **an aggregate over an empty set passes** and a deleted rule would satisfy the command cases in silence. The discrimination cases prove the measurement fails where it must: the doctest command collects 0 cases and exits 5, and the counter reads 0 for a file that holds no case. The reader cases prove that `stated_command` raises on an absent sentence, on a repeated sentence, and on the text the rules held before this round. **The recorded reason takes two clauses, and a case reads each one.** A reader of the first clause alone would accept a sentence that reverses the claim, because the words `collects an interactive session` refuse nothing. **The reader parses the case count and never greps it.** A page that held the words `0 tests collected` would match a substring search, so the reader matches the summary line and reads the exit status beside it. **Six cases failed on the unamended records, and seven mutations prove the new cases discriminate.** This round wrote each mutation to disk, measured it, and restored it, and the file compared equal after every restore. A mutation that deletes the sample rule fails two cases, including the floor. A mutation that names the doctest command in the rule sentence fails two. A mutation that names `tests/test_no_such_harness.py` fails the same two. A mutation that deletes the recorded reason fails one. A mutation that keeps the first clause of the reason and drops the consequence fails the consequence case alone. A mutation that reverts the row of this page fails one alone, which proves the second-file repair is held. A mutation that renames the `## Behaviour rules` heading fails five. **One premise of the issue was stale and this round re-measured it.** The issue reported 45 samples, which is right, and 14 samples carrying a marker, which is not: `disposition` reads 15 explicit `skip` markers today, and 17 markers with the two `raises` markers of `docs/api_reference.md`. This round names no page as the fifteenth, because the marker set moved across several rounds of this epic and the attribution would be a guess. The four-word reason case reaches all 95 skipped blocks and not the marked ones alone. **The sweep read every command of `docs/specs/features/*.md`, and three findings reach no repair here.** The Behaviour rules also name `mkdocs build --strict`, which is no `pytest` command, so the aggregate case passes over it and `tests/test_documentation_site.py` holds it instead; round 140 measured it green from a clean environment, and this round did not run it, because the worker environment holds no `docs` extra. Two acceptance criteria of other feature files state counts the repository no longer holds: `00-foundation.md:214` expects 75 files under `tests/foxio_vectors/` where `git ls-files` reports 90, and `09-release.md:144` expects the string `0.6.0` in one file where `grep -rn` reports eight. **One measurement is a defect and not a stale count, and #69 already owns it.** `09-release.md:156` requires the wheel to carry no file under `docs/`, and `python -m build` on this base produces a wheel of 96 entries that holds 56 of them, so this round files no duplicate and reports the measurement. No file under `ja4plus/` changes and no fingerprint moves. |
| 164 | 2026-08-09 | #388 landed and the FoxIO License 1.1 list of `README.md` names every method it covers. The list named JA4S, JA4H, JA4T, JA4TS, JA4L, JA4X and JA4SSH, and it named neither JA4LS, nor JA4D, nor JA4D6, which this project builds under that license. **A published statement about another party's license is the user's decision.** #62 therefore measured the difference and changed nothing. The user ruled on 2026-08-09 that the list follows FoxIO's list. **The premise of the issue is that `License FAQ.md:5` names ten methods, and the source names twelve.** Read at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, from a clean checkout outside this repository, that line names JA4S, JA4L, JA4LS, JA4H, JA4X, JA4SSH, JA4T, JA4TS, JA4TScan, JA4Scan, JA4D and JA4D6. **Two of those names reach no row of this list.** This project builds neither JA4TScan nor JA4Scan. The list therefore holds the names the user named, and it is a subset of FoxIO's. **Three FoxIO records at that commit disagree with each other.** This round records the three readings and chooses among them for nothing. `License FAQ.md:5` names the twelve above. `README.md:293` omits JA4Scan, JA4D and JA4D6. `LICENSE:3` spells the scanner `JA4SScan`, beside a `JA4E` that the other two never name. **The user decides what a wider list would assert.** This round asserts nothing about the three disagreeing records beyond the reading above. **The list now cites the document and the commit**, so a reader checks it against the source without a second measurement. **The license list is not the implemented set.** This project implements eleven methods, and JA4 is one of them. FoxIO publishes JA4 under BSD-3-Clause and not under the FoxIO License 1.1. JA4 therefore reaches the method table of the README and reaches no row of this list. Round 162 landed that implemented count with the cases that derive it from `ja4plus/`, and this round contradicts none of them. **Two new cases in `tests/test_readme_contracts.py` parse the paragraph and never search for a name.** `JA4L` is a substring of `JA4LS`, so a substring reader reports JA4L on a paragraph that names JA4LS alone. **The first form of the reader lost `JA4S` for the opposite reason.** An underscore is a word character, so `__JA4S__` hid the name from the word boundary. The reader now removes the Markdown emphasis characters before it reads. **The set case derives the expected names from the `Implemented` column of the README method table**, without the BSD-3-Clause method. A name that table gains therefore reaches the list or fails the case, and `tests/test_documented_method_count.py` holds that table to `ja4plus/`. **No transcription under `docs/specs/foxio/` carries the license list.** The transcriptions cover `technical_details/`, and `docs/specs/foxio/README.md` bars a copy of FoxIO material into this repository. No case here therefore reads the FoxIO document itself, and the citation carries the provenance instead. **Both new cases were proved in both directions on this host.** The removal of JA4D6 from the list fails the set case alone, with `the license paragraph names no ['JA4D6']`. The deletion of the whole paragraph fails both cases. Each mutation ran with `PYTHONDONTWRITEBYTECODE=1`, and each run restored the file it moved. No file under `ja4plus/` changes, no fingerprint moves, and `__all__` is untouched. |
| 165 | 2026-08-09 | #459 landed and a batch merge fails where the pull-request event created no run. **The failure mode is not a missing run. It is an absent run read as a passed run.** `gh pr checks` writes "no checks reported", which refuses no merge, so a reader takes the absence for a pass and merges a batch whose members never ran on Linux. Every member commit carries a skip keyword by design, so the batch pull request is the first run of a member's cases on that platform. **The cause is measured and it is not the throttled webhook path the workflow comment named.** A skip keyword anywhere in a commit message creates no run for that commit. GitHub reads five keywords, matches the subject and the body, and reads no intent. The head commit `2a589ca` of pull request #444 and the head commit `b593de5` of pull request #458 each held the line ``This commit carries no `[skip ci]`, so it is the head that starts the full run``, so the sentence that states the absence of the keyword is the keyword. **The correlation is perfect across four heads.** The two heads that hold the keyword created no run. The two control heads `ec697c0e` of the Epic 11 pull request and `b21604ce` of the Epic 8 pull request hold no keyword and both created their runs. Verified against https://docs.github.com/en/actions/how-tos/manage-workflow-runs/skip-workflow-runs (retrieved 2026-08-09), which states that a keyword anywhere in the head commit message of a pull request creates no run for a push event or a pull-request event. **All four observations of the issue re-measured true and one premise re-measured false.** `gh run list` reports `workflow_dispatch` runs alone on both batch branches, both green, and it reports the push runs on `dev` after each merge. The premise that neither head commit carries the keyword is false, and it is the premise that ruled the documented skip path out. **New file `tests/batch_gate.py` holds the condition and `python -m tests.batch_gate --pr <number>` reads it.** The command exits 0 where every required workflow holds a terminal successful run at the head commit and every other run of that commit concluded `success`. It exits 1 on an absent run, on a run that has not finished, on any other conclusion, and on a failed read of the provider. **`.github/workflows/test.yml` is the one required workflow**, because it accepts every pull request into `dev` and it filters no path, and `.github/workflows/docs-build.yml` filters four paths so an absent run of it proves nothing. A red run of that second workflow still refuses the merge, so the gate needs no path reader. **The run reader takes `GET /repos/{owner}/{repo}/actions/runs`**, verified against https://docs.github.com/en/rest/actions/workflow-runs (API version 2022-11-28, retrieved 2026-08-09) for the `head_sha` parameter and the `path`, `status`, `conclusion`, `head_sha`, `event` and `created_at` fields. **New file `tests/test_batch_gate.py` holds 48 cases and every one failed before the module existed.** They cover an absent run, a run of another commit, a queued run, a run in progress, four terminal conclusions that are not `success`, a `skipped` conclusion, a re-run that passed after a failure, a failure after a success, a red run of the unrequired workflow, an abbreviated commit identifier, and the head commit message of both measured pull requests against both controls. **The empty required set carries a case of its own, because an aggregate over an empty set passes**, and a required set emptied by an edit would make this gate pass on every input. **The self-review found three defects in the first form of this gate and each is closed.** **The first is the fault this project records most often, and it reached the gate built to stop it.** One workflow holds two runs at one commit where a push event and a pull-request event both reach it, and two runs can carry one creation time. `max` returns the first of several equal keys, so a green run listed before a red one made the gate report a pass. **The verdict rested on the order of the response**, and a measurement proves it: on the unrepaired reader the order `success, failure` reads `PASSED` and the order `failure, success` reads `REFUSED`, on one input. `_newest` now breaks that tie toward a refusal and `test_two_runs_of_one_time_break_the_tie_toward_a_refusal` reads both orders, while `test_an_older_red_run_moves_no_verdict` holds the rule to one creation time. **The second is a check name that names no check.** The rule file listed the six `jobs:` keys of `.github/workflows/test.yml` as the required checks, and the `test` job runs a matrix, so the provider publishes one check for each combination. A read of commit `b593de5` reports `lint`, `test (ubuntu-latest, 3.9)` and five more `test` names, `fuzz`, `samples`, `installed-wheel`, `conformance` and `build`. **A required check named `test` would have matched nothing**, so the rule now lists the eleven names and bars `build`, which belongs to the path-filtered workflow and would block every batch that touches none of its four paths. `test_the_rule_names_every_job_of_the_required_workflow` reads the `jobs:` keys against the rule file, and removing one name from that file leaves it named in the failure. **The third is a reader that tested a region it never found.** The two path-filter cases sliced the file on the text `pull_request:`, and a slice that landed on a comment would have satisfied `paths: not in trigger` on a claim nobody read. `_trigger_block` now takes the `on:` block by indent, it requires exactly one line that reads `on:`, and `_pull_request_trigger` raises where the region it took holds no `branches:` key. Against `.github/workflows/publish.yml` it raises `accepts no pull-request event` rather than returning a region. **A caller that passed the upper form of a commit identifier read an absent run**, which sends a reader after a cause that is not there, so the comparison folds the case and a fourth new case reads it. **The gate names the cause it cannot otherwise show.** Where a required run is absent and the head commit message holds a keyword, the failure names the keyword and this issue, so a reader reaches the cause without a second investigation. It names no keyword where a run exists and failed, because a red run has a cause in its log. **The condition was proved against the provider in both directions.** Against pull request #444, #458 and #405 the command exits 0 today, because the manual run of each ref now holds a terminal successful run at that head, and that is the accepted procedure rather than a defect. Against the head of this branch, which carries no run at all, it exits 1 and names the keyword. **The procedure reaches the loop and not an issue alone.** New file `.claude/rules/batch-gate.md` states it, `CLAUDE.md` names that file under `## Branch model`, and `.issue-flow.json` replaces the note that read "A batch pull request into dev starts a run, so provider continuous integration is the batch gate". That sentence is the belief that turns an absence into a pass. **The header comment of `.github/workflows/test.yml` named a cause nobody measured**, and it now records the keyword and cites this issue, held by a case. **The strongest shape needs the user and it is written down rather than left in a verdict.** `gh api repos/Crank-Git/ja4plus/branches/dev/protection` returns `404` with `Branch not protected` and `gh api repos/Crank-Git/ja4plus/rulesets` returns `[]`, so this repository holds no required status check. The token carries the `repo` scope and both reads returned a determinate answer, so this is a measurement and no stated limit. **A change to branch protection changes the repository configuration, so the user makes it**, and the rule file names the four steps and the eleven check names. **Two limits stand.** A run of the branch head is no run of the merge result, and this gate reads a run rather than the code. No file under `ja4plus/` changes and no fingerprint moves. |
| 166 | 2026-08-09 | #455 landed and the built wheel carries the `ja4plus` package alone. **The requirement stood in the specification and the artifact disagreed with it.** `docs/specs/features/09-release.md:156` requires the wheel to carry no file under `docs/`. `python -m build` on the base commit `d94d8c1` produced a wheel of 96 entries. 56 of those entries lay under `docs/`. #393 measured that state on 2026-08-09, and this round re-measured both counts on its own base and read the same two numbers. **Two defaults produced the defect together.** A `pyproject.toml` project reads implicit namespaces by default, so `docs` and every directory below it are discovered packages although none of them holds an `__init__.py`. `include-package-data` is true by default since `setuptools` 61. The file finder of `setuptools-scm` reports every file that git tracks. Each tracked file below a discovered package therefore shipped. `ja4plus-0.6.0.dist-info/top_level.txt` named `assets`, `docs` and `ja4plus`, which is the reading that proves the discovery. **The repair adds two entries to the one list that already excludes `tests/` and `examples/`**, and it adds no second mechanism beside it. The `exclude` key of `[tool.setuptools.packages.find]` now reads `["tests", "tests.*", "examples", "docs", "docs.*", "assets", "assets.*"]`. A pattern matches one package and no subpackage, so the entry `docs.*` carries `docs.specs` and `docs.specs.features`. The repository holds no `MANIFEST.in`. **The project manager widened this round to `assets/`, which is the same defect on another tree.** `assets/` holds one tracked file, `assets/logo.png` at 2423363 bytes. It holds no `__init__.py`, so cause 1 above discovered it, and cause 2 shipped it. `README.md` is its one reader, and a logo is no run-time dependency of a fingerprinting library. **`assets` carries the wildcard entry although it holds no subdirectory today**, because the requirement is that no file of the tree ever ships. `examples` keeps the bare name it already had, and `test_the_wheel_carries_no_file_outside_the_package_and_its_metadata` covers it. **The wheel falls from 96 entries to 39, and it carries the `ja4plus` package and the metadata directory alone.** The reading after the `docs/` repair was 40 entries, so the three counts are 96, 40 and 39. The file falls from 3139637 bytes to 2551426 and then to 152780, which is 5% of the first reading. `top_level.txt` fell from `assets`, `docs` and `ja4plus` to `ja4plus` alone. The wheel still carries `ja4plus/data/ja4plus-mapping.csv` and `ja4plus/py.typed`. **The source distribution does not change, and that is the intended state.** It holds 417 members before the repair and 417 after. 64 of those members lie under `docs/`, and `assets/logo.png` is among the rest. The file finder adds every tracked file whatever the package list holds. `tests/` and `examples/` prove the same split: the list excludes both from the wheel and both ship in the source distribution. `FR-release-11b` and `FR-release-11c` name both trees, and `FR-release-11d` states the top-level name, because `FR-release-11` named the test suite, the examples and the vectors and named neither tree. The `Terms` table gains `documentation tree` and `assets tree`. **The tests came first.** `test_the_wheel_carries_no_file_under_the_documentation_tree` failed on the unrepaired tree with `AssertionError: the wheel carries 56 of its 96 entries under docs/, and it must carry none`. The three cases of the widened scope failed with `the wheel carries 1 of its 40 entries under assets/`, with `the wheel declares the top-level names ['assets', 'ja4plus'], and it must declare ['ja4plus']`, and with `the wheel carries 1 of its 40 entries outside ja4plus/ and the metadata directory`. **Each new case reads the artifact and never the working tree**, because the defect lives in what the build includes. **An assertion over an empty entry list passes**, so each wheel case holds `ja4plus/__init__.py` against the entry list before it reads the rest. `test_the_wheel_carries_the_mapping_file`, `test_the_source_distribution_carries_the_documentation_tree` and `test_the_source_distribution_carries_the_assets_tree` hold the states a wrong exclusion rule would produce. **Two cases hold the whole wheel rather than one named tree, and they fail on two different faults.** `test_the_wheel_declares_one_top_level_name` reads the declaration of `top_level.txt`, and `test_the_wheel_carries_no_file_outside_the_package_and_its_metadata` reads the payload. A file that ships outside `ja4plus/` under no discovered package reaches no `top_level.txt`, so the payload case catches what the declaration case cannot. **A legitimate new top-level package moves both cases, and that cost is intended**, because a new importable name changes what a user of the release can import. **A mutation proves the three new cases discriminate, and it isolates them.** The removal of the two `assets` entries from the exclusion list fails exactly those three and passes the other 20. The mutation was written to disk, measured, and restored, and the file compared equal by digest after the restore. `EXPECTED_CASE_COUNT` of `tests/test_installed_wheel_selection.py` moves from 16 to 23, and the `installed-wheel` job stays the only runner of the marker. **#462 filed the `assets` finding before the project manager widened this round, so #462 closes as absorbed and this round holds the repair.** No file under `ja4plus/` changes and no fingerprint moves. |
| 167 | 2026-08-09 | #398 landed and `docs/migration-0.6-to-1.0.md` states the count its own fingerprint table holds. **The page stated six twice and the table held seven rows.** #395 measured the difference and filed the issue rather than repair it, because the defect is a wording defect and not a missing change. **The count lived in prose and no case read it.** #399 added a row to the breaking-change table of the same page and #401 corrected a citation of it, so two workers edited the page after the filing and neither edit moved either sentence. **The record already stated seven, so the page is the half that is wrong.** `CHANGELOG.md` reads "That table holds eight rows. Seven of them move a value a tool may have stored", and row 139 of this table reads "plus seven fingerprints that move". Both are dated records of a past measurement, so both keep their text and the repair is in the page. **The two premises of the brief were re-measured on the base commit `d4a1305`, and one was stale.** The table holds seven rows and the two sentences state six, which holds. The issue body states that `CHANGELOG.md` reads "plus seven fingerprints that move", and that wording is on row 139 of this table alone; the round assignment of batch #445 rewrote the entry. The 2026-08-09 comment of the issue records the same correction. **The trailing paragraph adds a value and it moves none, and that is the decision this round records.** `Processor.close_open_windows` emits every window a connection left open under #214, so a capture produces one more JA4SSH value than version 0.6.0 produced. A value the earlier version never produced moves no stored value, so the count of seven excludes it and the table holds no row for it. A reader who counts the paragraph reaches eight, and the page now states the decision rather than leave it to the reader. **The release notes hold #214 as a row and the page holds it as a paragraph, so the two tables differ by exactly that one row.** #214 already reaches `test_every_fingerprint_that_moves_reaches_the_release_notes`, which reads the prose of the section beside its table, so the decision breaks no case. **Three new cases in `tests/test_release_notes.py` read each count against the table that carries it.** `test_the_migration_page_states_the_count_its_own_fingerprint_table_holds` reads the two sentences of the page against the row count of the table below them. **It opens no second file, and round 143 states why.** The first citation case of #66 compared the `Record` cell of the release notes against the cell of the page, both files held one error, and the case passed on the very error it exists to catch. `test_the_migration_page_states_that_the_trailing_fingerprint_moves_no_value` reads the decision, and `test_the_release_notes_state_the_counts_their_fingerprint_table_holds` reads the eight rows and the seven moves of the notes against the two tables. **Row 139 of this table reaches no case, on purpose.** A dated row records what a past round measured, and a case that held it to the page today would fail on the day the page gains a row. **Two floors bar an aggregate over an empty set.** `MINIMUM_MOVE_COUNT_CLAIMS` reads 2, because a comparison over no sentence passes and a rewording the pattern misses would otherwise pass in silence. `MINIMUM_MIGRATION_FINGERPRINT_ROWS` reads 7, because a count sentence compared against an emptied table is the same fault in the other direction. **The self-review found this round building the second copy round 143 warns about.** The `CHANGELOG.md` entry of #398 quotes the live count sentence of the release notes verbatim as evidence, the first form of the notes case searched the whole version section, and `re.search` takes the first match. The live claim would therefore go and the quote would answer for it. `_release_notes_introduction` reads the text above the first third-level heading, so the case reads the claim where it lives and no case here reads a second copy of any claim. **Fourteen mutations prove the three cases discriminate, and each run restored the file it moved.** Either sentence back to six fails the count case alone, with `the migration page states ['Six'] fingerprints that move, and its own table holds 7 rows`. The deletion of the top sentence, the deletion of the section sentence, the deletion of both, and the rewording `Seven changes alter a fingerprint.` each fail the sentence floor alone. One row deleted from the table fails four cases, the row floor among them. Every data row deleted fails the row floor with `the fingerprint table of the migration page holds 0 rows, and the floor is 7`. The decision sentence deleted fails the trailing case alone. The count of the notes moved to nine rows, moved to six moves, and replaced by a sentence that states no count, each fails the notes case alone. **Two mutations prove the scope of the notes reader.** The live claim deleted while the verbatim quote of the entry stands fails with `the release notes state no row count for their fingerprint table`, and the live claim moved to nine rows fails with `the release notes state 9 rows of moved fingerprints, and their table holds 8 rows`. **The whole gate ran twice, and once on each base.** On the base commit `d4a1305` the unit suite reports 2815 passed against the 2812 of that commit, and the three new cases are the difference. **This branch then merged the integration branch twice**, at `b0bab60` and at `ab73bd6`, because #459 and #455 landed after the branch left `d4a1305` and each round appends an unassigned row to this table. Each resolution keeps both rows, and the earlier row stands first. Every commit of this branch ends with the skip keyword, so the two merges started no run. After both merges the unit suite reports 2865 passed, and the 50 cases the merges brought are the difference; 48 of them belong to `tests/test_batch_gate.py`, which #459 owns, and the cases of #455 carry the `installed_wheel` marker that this selection deselects. 4 skipped, 8 xfailed and 114 subtests hold on every run. Coverage holds at 94 on every run, with 273 lines missed of 4232 statements. The conformance suite reports 1532 passed, 143 skipped and 134 xfailed against the 134 keys of `tests/foxio_deviations.json`. `mypy --strict` finds no issue in 31 source files, and `ruff check` and `ruff format --check` pass. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 168 | 2026-08-10 | #431 landed and `tests/mutation_sweep.py` builds no mutation inside a type annotation. The sweep built one mutation for each `BinOp` node whose operator sits in `BINOP_SWAP`, and `ast.BitOr` is one of them, so it changed an annotation written `str | None` to `str & None`. **28 of the 31 modules of `ja4plus/` carry `from __future__ import annotations`**, so Python holds each annotation as a string and a changed annotation reaches no run-time value. The three modules that carry no such import hold one annotation node between them, the `-> None` of `ja4plus/utils/loopback.py`, and no operator sits in it. **#413 measured the cost.** 94 of the 675 surviving mutations of `ja4plus/fingerprinters/` sat inside an annotation, and all 94 survived. New reader `_annotation_nodes` names the nodes of an argument annotation, of a return annotation and of an `AnnAssign` annotation, in the shape `_docstring_nodes` and `_logging_argument_nodes` already set. `generate_mutations` reads it for every node kind, because the earlier `skip` set reaches the constant branch alone. **The reader removes 95 mutations and not 94**, because `ja4plus/fingerprinters/base.py:43` holds `-> Literal[False]:` and that constant reaches no run-time value either. `--dry-run --max-per-module 0` over `ja4plus/fingerprinters/*.py` reported 1569 mutations before the reader and it reports 1474 after it. `FR-pre-release-validation-34` and `FR-pre-release-validation-34a` state the rule, and `tests/test_mutation_sweep_annotation_skip.py` holds nine cases against it. Every case was proved in both directions. No file under `ja4plus/` changes and no fingerprint moves. |
| 169 | 2026-08-10 | #433 landed and a mutation of a module body keeps a reader in the cover rule. Step 2 of the cover procedure in `.claude/rules/conformance.md` subtracts the lines the import runs. Every mutation of a module body then lost the case that reads it, and the cost rule held the cheapest test file. **The defect follows from the subtraction of step 2 and not from the cost measurement**, because that subtraction removes the whole line class at once. #414 measured it on `ja4plus/__init__.py`: the cover the cost rule builds is `tests/test_parity.py`, that sweep reads 1 killed and 31 survived, and all 25 entries of `__all__` survive it. New step 4 names a reader by the value rather than by the cost, and new file `tests/mutation_cover.py` reads the names a module body binds and the identifier strings those statements build. It names the test file whose own source holds the most of those tokens. The module body of `ja4plus/__init__.py` holds 28 tokens, and the tool names `tests/test_public_interface.py` at 26 of them against 15 for the next file. **The cost is a run and not an estimate.** At commit `00a0c42`, over all 32 mutations of that module, the repaired cover reads 26 killed and 6 survived in 48.0 seconds, and it leaves no survivor on the 25 entries of `__all__`. One sweep of the reader alone costs 39.9 seconds. **The rule states a fourth limit**: the reader kills a mutation of the value it reads and no other mutation of the module body, so `__version__`, `__author__` and `__license__` survive it. **Two cases prove the prose prover in the failing direction.** The rule text with every line naming `tests/mutation_cover.py` removed fails the reader case. The rule text with every line naming a second count removed fails the cost case. A reworded step passes both, so a rewording defeats neither. A floor case reads the ranked list, because an aggregate over an empty set passes. `docs/mutation_settlements/414-interface.json` keeps the numbers #414 measured, because that record states the sweeps that ran. No file under `ja4plus/` changes and no fingerprint moves. |
| 170 | 2026-08-10 | #436 landed and `FR-pre-release-validation-16` names a pathspec whose plain reading equals what it matches. The requirement named `git ls-files 'ja4plus/*.py' 'ja4plus/*/*.py'`, which lists all 31 modules, and its first term alone lists all 31 too. **In a default git pathspec, `*` crosses `/`, and only `:(glob)` magic stops it at a separator.** A read of 2026-08-09 reports four counts: `git ls-files 'ja4plus/*.py'` lists 31, the pair lists 31, `git ls-files 'ja4plus/**/*.py'` lists 24, and `git ls-files ':(glob)ja4plus/*.py'` lists 7. **The count was right and the rule was not.** The second term read as proof that `*` stops at a separator, and #411 and #414 each met that reading. The requirement now names `git ls-files 'ja4plus/*.py'` alone, and it states the separator rule. New `FR-pre-release-validation-16b` binds the cases, and new `FR-pre-release-validation-16c` records the four counts. **`tests/test_mutation_sweep_module_list.py` reads the pathspec out of the requirement text and runs it**, so a requirement that names a pathspec listing a different set than the sweep reads fails a case. It gains four cases. Two compare the file set of the stated pathspec against the module set the sweep reads and against the tracked Python files. One fails where a term of the pathspec lists no file the other terms miss. One reads the separator rule out of the requirement. **The failing direction is proven live.** Against the unrepaired requirement the redundancy case read `AssertionError: the term ja4plus/*/*.py lists no file the other terms miss`, and the separator case read a missing sentence. A `**` form restored into the requirement failed the two comparison cases, which named the seven top-directory modules as extra items, and the revert returned all nine cases to green. `DEFAULT_MODULE_PATTERNS` keeps its two patterns, because it reaches `Path.glob`, where `*` stops at `/`. `.claude/rules/conformance.md` and the acceptance list of `docs/specs/features/11-pre-release-validation.md` state the same reading. **No file under `ja4plus/` changes and no fingerprint moves.** The register holds 134 keys, and the conformance suite reports 1532 passed, 143 skipped and 134 xfailed. |
| 171 | 2026-08-10 | #461 landed and a state table reads the clock of the thread that touches it. Eight sharded threads wrote a JA4TS value that one thread does not write, and the reading is `At index 10 diff: '64240_2-1-1-4-1-3_1460_7' != '64240_2-1-1-4-1-3_1460_7_0'` on `test (macos-latest, 3.12)`, run `31342645430`, merge commit `d94d8c1` of batch #445. The sharded value lost part e, the delay list of the SYN-ACK retransmissions. **`BoundedStateTable` held one clock for every thread, and the age pass read every entry.** `SynAckTracker.times` runs one pass on every packet and it holds a maximum age of 120 seconds. On the concatenated timeline of `TestTheConcurrencyContract`, packet 578 opens `('184.150.157.177', 80, '172.16.225.48', 57380)` at 19.2378 seconds and packet 581 retransmits the SYN-ACK 5.8 milliseconds later. Packet 1773 belongs to another connection and another shard, and it stands at 151.678 seconds. It announced its timestamp to the shared clock, the pass read 132.44 seconds of age, and it evicted the connection. Packet 581 then found no entry, `SynAckTracker.record` returned an empty delay list, and `_part_e` wrote an empty string. **The reproduction is three packets on two threads and it holds no race**, so it fails every run rather than one run in four. **The repair scopes the clock and the pass to the thread.** `on_packet` writes the timestamp into a `threading.local`, each entry carries the identifier of the thread that read it last, and `evict_aged` passes over every entry of another thread. One thread owns every entry it stores, so a caller that runs one thread reads the pass this project always ran. **The entry of a thread that ends now stays until the entry count bound removes it**, and that bound is the one that holds the memory. **One wall clock serves every thread, so the pass still evicts an entry that the wall clock dated, whichever thread stored it.** `JA4DBClient` builds the one table that reads the wall clock, and its callers share every entry under one lock rather than owning whole connections. A self-review found that a pass scoped by thread alone would age out nothing there. `FR-concurrency-safety-8`, `FR-concurrency-safety-8a`, `FR-concurrency-safety-8b` and `FR-concurrency-safety-8c` state the rule. **`TestTheConcurrencyContract` passed 50 of 50 consecutive runs on macOS**, against the one failure in four runs the issue records. **Nine new cases hold the repair and five live mutations prove them.** Every run cleared every `__pycache__` outside `.venv/` and set `PYTHONDONTWRITEBYTECODE=1`, and `inspect.getsource` read the loaded source in both directions. The shard filter of `evict_aged` removed reads 1 occurrence clean and 0 mutated, and it kills 2 cases. The thread clock of `on_packet` removed kills 1 case. The owner write of `__contains__` removed kills 1 case, and the owner write of `__getitem__` removed kills 1 case. The wall-clock exemption of the pass removed kills 1 case. Every case passes clean and after the restore. **No file under `ja4plus/fingerprinters/` changes and no fingerprint moves.** A replay of all 38 committed captures produced 783 values, and the SHA-256 of that output is `04014a1d7a1863bc3244e1348ce61b5f39f9805a988310af657a6a931a2d8907` before the change and after it. The conformance suite reports 1532 passed, 143 skipped and 134 xfailed against 134 register keys. |
| 172 | 2026-08-10 | #477 landed and every statement of the vector-fallback rule names a reader. Round 49 corrected the rule on 2026-08-08: the fallback needs an image that a person read and found ambiguous, and an image nobody read is not a license to use it. `.claude/rules/external-apis.md` and `.claude/rules/conformance.md` carried the correction, and three pages kept the superseded premise, which gives the decision to the expected-output files and names no reader. **The issue reported one occurrence and the sweep measured three.** `docs/specs/spec.html` held one in its `Open, but not blocking` card, 118 lines below the corrected form in its own `01 — Spec conformance` card, so the page contradicted itself. `docs/specs/features/01-spec-conformance.md` held one in its `Behaviour rules` list, and `docs/implementation_notes.md` held one in the paragraph that states why the file exists. All three now state the form `.claude/rules/external-apis.md` states. **The issue also stated two line numbers that a re-measurement disproved.** It named `docs/specs/spec.html:444` and `:316`, and the base commit `17858bf` carries the two statements at `:436` and `:317`. **The case reads a shape and no phrase**, because #211 and #449 each proved that one forbidden phrase guards one spelling. New file `tests/test_documentation_fallback_rule.py` holds 48 cases. `fallback_statements` matches a condition about an image that settles no question, bound to a sentence that gives the decision to the expected-output files, in four word orders. It then reports whether a reader premise stands inside a window of 600 characters before the statement and 200 after it. **A rewording that drops the reader therefore fails, and the words it chooses do not matter.** Twenty-two spellings of the superseded rule reach the reader, six spellings of the corrected rule pass it, and ten control sentences that name the fallback for one transcription rule reach no case. **The trailing window is 200 characters and not 600**, because a wider window reads the premise of the next rule and passes a statement that names no reader of its own. **An aggregate over an empty set passes**, so six pages are named and each one holds a statement the reader finds. **A dated record of a past measurement is quoted and not rewritten.** Round 49 above records the superseded wording word for word, and this row quotes the sentence this round corrects. `readable_text` cuts the `## Changelog` section, and one case reads the cut in both directions. **A self-review drove the reader with sentences it had not seen.** It found seven evasions and two false reports. The evasions were the conjunctions `because`, `since`, `given that` and `unless`, a semicolon, the word `means`, and a passive sentence, so the pattern now needs no conjunction at all and it reads `decided by the expected-output files`. The false reports were a rendering fallback of an image and a sentence about a slide deck, so `REQUIREMENT_FORM` names the vector fallback and `AMBIGUOUS_IMAGE_FIRST` needs a verb of decision after a bare `vector`. Six evasions and two controls became cases. **The cases were written first and they bite**: against the unrepaired corpus they failed 2 of 48, and the failure named all three pages. No file under `ja4plus/` changes and no fingerprint moves. |
| 173 | 2026-08-10 | #473 landed and the document set of the count cases reads the tracked file list of git. `tests/test_documented_method_count.py` built `DOCUMENTS` from a walk of `docs/`, `.claude/`, `tests/` and the repository root. **The agent harness places a worker worktree at `.claude/worktrees/agent-<id>`, and that worktree is a whole checkout**, so the walk of `.claude/` read every document of every live worker. The reading counted the host and not the commit. **The cases passed on such a checkout, because a worktree holds a valid copy that agrees with the rule.** That is the shape this project records more than eighteen times: a comparison whose result does not depend on the thing under test. A measurement of 2026-08-10 inside a worker worktree reports 153 collected cases with no worktree page present and 156 with one page of one worktree present, and two of the three extra cases failed on that page. **`documents()` now reads `git ls-files -z '*.md'`**, which names the tracked pages alone and names no worktree copy, no ignored file and no build output. `tests/mutation_sweep.py` already carried this correction for its module list. **In a default git pathspec `*` crosses `/`**, so the one term reaches every depth and its plain reading equals what it matches, and `FR-pre-release-validation-16` states the rule that #436 repaired. A read of 2026-08-10 reports three counts: `git ls-files '*.md'` lists 59 files, `git ls-files '**/*.md'` lists 56, and `git ls-files ':(glob)*.md'` lists 3. **The tracked list and the walk name the same 59 pages on a clean checkout**, so the runner reads the set it read before. **The repair is proven in both directions.** `test_the_reader_names_no_markdown_page_of_a_worktree` writes one page below `.claude/worktrees/`, it removes the directory it created and no other, and it fails against the walk with `AssertionError: .claude/worktrees/issue-473-qnt_uybr/docs/copy.md reaches a parametrized case`. Nine anchor cases name one page of each depth and one page of each root, and `test_the_anchor_set_fails_a_reader_that_names_one_depth_of_one_directory` proves the anchor set fails a reader that drops a root. **A floor fails the reader that names no document**, because an aggregate over an empty set passes. The collected count of the file reads 166 with a worktree page present and 166 with none. New `FR-documentation-16` and its four parts state the rule. No file under `ja4plus/` changes and no fingerprint moves. |
| 174 | 2026-08-10 | #480 landed and the batch-gate rule states the branch protection the provider now holds. #459 wrote the protection section of `.claude/rules/batch-gate.md` from a read of 2026-08-09, which returned `404`, `Branch not protected`, and #468 turned the rule on. **A reader who followed that wording concluded that nothing at the provider refuses an ungated merge, and that conclusion is false.** **This round re-took the measurement rather than quoting the issue body.** `gh api repos/Crank-Git/ja4plus/branches/dev/protection` returns `200` with eleven required contexts, each carrying `app_id` 15368, and `gh api repos/Crank-Git/ja4plus/rulesets` returns `[]`. The eleven names match the eleven the file already listed, so no name moved, and no context reads `build` or the bare `test`. The reading agrees with the issue body on every field, and the ruleset list stays empty, so the branch protection rule is the one place the provider holds this condition. **The section is renamed to `The provider refuses an ungated merge`**, because the heading `The strongest shape needs the user` named a shape the repository did not hold. **A dated record of a past measurement is quoted and not rewritten**, so the 2026-08-09 sentence stays as a quotation under a paragraph that marks it superseded, beside a table that holds both dates. **The same call returned two limits and the file states each one as a measurement.** `enforce_admins` reads `false`, so the rule binds a contributor and binds no repository administrator, and an administrator merges past the eleven contexts with no run at all, which is why the local gate stays the procedure for every merge. `strict` reads `false`, so a branch merges where it is behind `dev`, and that reading suits the batch model: `strict: true` would demand that every integration branch take `dev` again after another batch lands, the new head would carry no run, and the run that proved the batch would then not be a run of the head. `python -m tests.batch_gate --pr <number>` stays in the procedure, because it reads the same condition before the merge rather than at it. **A case reads the claim against the provider, because prose carries no other gate.** New file `tests/test_batch_gate_protection_rule.py` holds 93 cases. `protection_reading` runs the same `gh api` command, and it returns nothing where the host holds no `gh`, where the command exits non-zero, or where the body is no JSON object. **Where the call cannot be made a live case skips, and it does not pass.** `test_the_live_context_case_skips_where_the_provider_returns_no_reading` drives the live case with a refused call and requires the skip, because an aggregate over an unreachable provider that passes is the defect this repository has recorded more than eighteen times. **Every live case reads its floor before it reads the provider**, so a file that lists no context fails rather than comparing two empty sets. **The sweep reads a shape and no phrase.** **A self-review drove the sweep with fifteen candidate sentences and eight reached no match**, among them `The repository lacks a required status check.`, ``The branch `dev` is unprotected.`` and `Nothing at the provider refuses an ungated merge.` `superseded_claims` therefore reads three shapes: a check term paired with a negation, a sentence that states the provider refuses nothing, and a sentence that calls the branch unprotected. Fifteen spellings of the superseded claim reach it, three live wordings reach no case, and eight control sentences reach neither reader, four about a workflow run and four true sentences of the repaired file. Against the unrepaired file the sweep reports two statements where the first form reported one. `readable_text` cuts a quotation, a paragraph that marks itself superseded, and the `## Changelog` section that quotes round 165, and one case reads that cut in both directions. **An aggregate over an empty set passes**, so a case requires the file set to name both `.claude/rules/batch-gate.md` and `CLAUDE.md`. **The cases were written first and they bite**: against the unrepaired file they failed 18 of 59, and the sweep named exactly one document, `.claude/rules/batch-gate.md`. **Two defects of the section reader were found by that first run and each is closed.** The reader ended a section at the next heading of any level, so a `###` subsection cut the list of check names out of the body and four cases read a body that stopped early. It then read `#468 turned the rule on` as a first-level heading and returned one paragraph, because a heading needs a space after its `#` characters and an issue reference does not. Three cases now hold the reader in both directions. **Two mutations prove the live cases discriminate.** Adding `build` to the listed names and writing ``` `strict` reads `true` ``` killed five cases, among them both provider cases, and deleting the superseded record killed two. Each mutation was restored. **No sentence of `CLAUDE.md` rested on the superseded reading**, which the sweep measures rather than asserts, so that file changes nothing. No file under `ja4plus/` changes and no fingerprint moves. |
| 175 | 2026-08-10 | #482 landed and one pass at the batch gate assigns every round number of a batch. The project manager assigned a round number at each sub-merge. A sub-merge is an event of one batch, and the round sequence is global to the repository. **One live integration branch hides that, because it is then the only writer, and two live branches make the assignment a race.** The project manager measured it on 2026-08-10 at the sub-merge gate of #456: `batch/470-sweep-tools-and-shard-bug` held 171 rows and assigned 168 to 171, while `batch/476-count-repairs` started from a `dev` of 167 rows and reported `the Changelog holds 169 rows and its highest round is 173`. **Neither member was at fault and the merged state of each member was correct.** `.claude/rules/batch-gate.md` now states where the assignment happens. One pass on the integration branch assigns it, immediately before the batch pull request, when the row count of `dev` is fixed. A member writes the literal `TBD` in `CHANGELOG.md` and in the Changelog row of this page, and it keeps `TBD` through every sub-merge. **One integration branch at a time also removes the race, and this project declines that order**, because it costs the concurrency the batch model exists to provide. **The rule alone leaves a shape that no case could read.** `row count == highest round` passes on a table that carries 168 twice and 170 nowhere. The count and the maximum both still read right under the wrong pairing. `tests/test_specification_changelog.py` gains `test_the_changelog_assigns_every_round_from_one_to_the_row_count`, which requires the rounds 1 to the row count, each on one row. **The shipped page proves the measurement, and a case keeps it.** A copy of this page whose round 170 reads 168 failed the new case with `these round numbers open more than one row: [168]` and `these rounds from 1 to 171 open no row: [170]`, and `test_the_changelog_row_count_equals_the_highest_round_number` passed on that same page. `test_the_row_count_rule_passes_on_a_table_that_repeats_a_round` holds that measurement, so no later reader takes the older rule for a complete one. **A gap alone raises the maximum above the count**, so the older rule reads a pure gap. The shape that hides from the older rule carries a repeat beside the gap. **An aggregate over an empty list passes**, so the reader reports a table from which it read no numbered row. `MINIMUM_ROWS` holds the second floor at 124. **The cases came first, and they bite.** Against the module that held no reader they failed 6 of 9. The three that passed were the two older rules and the record of what the older rule reads as correct. Five cases drive the reader over five fixed tables. One table repeats a round and one holds a gap. One assigns each round once, and one names no numbered row at all. One carries four numbered rows and two rows that read `TBD`, which an integration branch always carries and which stays legal. No file under `ja4plus/` changes and no fingerprint moves. |
| 176 | 2026-08-09 | #449 landed and `docs/specs/spec.html` states one image count. **The page stated eleven at line 316 and seven at line 436**, and `tests/test_documentation_image_count.py` existed to forbid the second sentence. It held the fixed phrase `seven of the twelve` and searched each line for it. The page writes `Seven of twelve`, so a missing article carried the superseded count through 97 rounds of a passing case. The user states the rule: **"A check that a rewording defeats is not a check."** **This round re-measured `technical_details/` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` rather than quoting round 70.** The directory holds twelve files: three text files of 1567, 9153 and 278 bytes, and nine images. The upstream `README.md` lists twelve methods and embeds nine image links. Every count reproduces `docs/specs/foxio/README.md`, so eleven is the measured count and seven is superseded. **The case reads a shape and no phrase.** `image_count_claims` matches a count that binds to the word `method`, followed by a claim about an image or about a complete text specification. It reads the whole page with the line breaks removed, so a claim that a line break splits reaches it. **The number comes out of the inventory.** `measured_image_count` is the method count less the count of complete text specifications, and it reads both halves out of `docs/specs/foxio/README.md`. A case that restated eleven would pass on the day the inventory moves. **Sixteen spellings of the superseded count reach the reader and eight control sentences reach no wrong count.** The controls include the `7 of the 12 moved values` of `docs/specs/foxio/JA4T.md` and `All but one FoxIO method is specified only as an image`, whose count is the complement and which `COMPLEMENT_COUNT` skips. **A self-review drove the first form of the reader with more than 40 candidate sentences.** It found fourteen evasions and one false positive. Six evasions became spellings of the case set, and the false positive became the two complement controls. **The module docstring records the six holes that stay**, among them a synonym of `method` and a claim further than 40 characters from the noun. **A dated record of a past measurement is quoted and not rewritten.** Round 162 above quotes the defective sentence word for word, and `readable_text` cuts the `## Changelog` section, which one case reads in both directions. **The cases were written first and they bite**: against the unrepaired page they failed 2 of 47. **Two premises of the issue body are false and the measurement disproves both.** The body states that the two counts sit 60 lines apart, and its own table names lines 314 and 434, which are 120 apart. The two sentences sit at lines 316 and 436 on the base commit `00a0c42`. #477 records one finding outside the scope: the same bullet states the fallback rule in the form round 49 superseded. No file under `ja4plus/` changes and no fingerprint moves. |
| 177 | 2026-08-09 | #456 landed and a case measures the count an acceptance criterion states. **Two criteria of `docs/specs/features/*.md` stated a count this repository contradicts.** #393 measured both on 2026-08-09. **Both premises of the issue were stale, and this round measured each one again.** `git ls-files tests/foxio_vectors | wc -l` reports 90 files, and `docs/specs/features/00-foundation.md` stated 75. The issue reported eight files that hold the text `0.6.0`. `git ls-files pyproject.toml ja4plus/` reports seven. A read of the whole repository reports 35. Neither read gives eight. **The criterion of `docs/specs/features/09-release.md` states an end state, and it is not a stale count.** That judgment is what this round records. Four facts settle it. #67 is open. Its own acceptance criterion is the same sentence. `FR-release-1` states that the version appears in one place. The page carries criteria that name version 1.0.0, which is unreleased. **A page of an unbuilt feature states what that feature will hold.** A reader who takes one such criterion for a stale measurement makes a category error. The criterion therefore keeps its count. It now states plainly that #67 builds it. **The command of that criterion counts files and not declarations, and #67 needs that reading.** Two of the seven files declare the version. Five name it in prose that records the released behaviour. The command therefore reads six files after #67 removes one declaration. `docs/specs/features/09-release.md` now holds the table of all seven, and #67 owns the decision. **The repair is a case and not a corrected count.** This project has corrected a stale count in prose more than seven times, and a corrected count goes stale again. New file `tests/test_criterion_counts.py` reads each measured criterion from its feature page. It takes the count out of that sentence. It compares that count against a measurement of this repository. A built criterion states the measured count. A pending criterion states a count this repository does not hold. `test_a_pending_criterion_states_a_count_the_repository_does_not_hold` therefore fails on the day #67 lands, and that failure instructs the writer to drop the end-state marker. **The count comes from the page and never from a copy inside the case.** A reader that held a copy of the sentence passed with the page restored to the stale 75. The shipped reader fails against the same page. The direction is measured and not asserted. **Eight mutations prove the six cases discriminate, and each file was restored.** The stale 75 fails the built case alone. A criterion that loses its command fails the locator case and the built case. The end-state marker without `#67` fails the naming case alone. That marker deleted fails the naming case alone. A pending criterion raised to seven fails the end-state case alone. A parser that reads no criterion fails five cases. A measurement that reads no file fails the floor case and the built case. **A comparison over an empty set passes, so both floors are present.** `MINIMUM_CRITERIA` reads 151, and `test_every_measurement_reads_at_least_one_file` requires one file from each measurement. The parser closes the criteria section on any heading, because this round put a sub-heading inside that section of `docs/specs/features/09-release.md`. The `Terms` table gains `end state`, `built criterion` and `pending criterion`. No file under `ja4plus/` changes, no fingerprint moves, and the register holds 134 keys against 134 xfailed. |
| 178 | 2026-08-09 | #450 landed and the prose under `tests/` counts fingerprinters where it counted methods. **The word `method` carried two meanings and the two counts differ.** FoxIO publishes twelve methods and this project implements eleven. The processor drives ten fingerprinters, because `JA4LFingerprinter` writes both `JA4L-C=` and `JA4L-S=`. **The user chose the prose repair and declined the rename.** `ProcessorStats.method` keeps its name, its docstring and every value it holds. It is a published output field, so a rename of it is a breaking change and the user owns that decision. **The ten places the issue body names are all present, and one line number moved.** `tests/test_thread_safety.py:372` reads 373 today. Ten docstrings and comments across six files under `tests/` now count fingerprinters, and the four docstrings and comments of `ja4plus/watch.py` take the same repair under the ruling the issue thread records. **`tests/test_documented_method_count.py` now reads Python source, and it found three more places in itself.** `python_prose` extracts every comment with `tokenize` and every docstring with `ast`. **A docstring of one line sits inside quotation marks.** `_unquoted` drops the whole of it, so a search of the raw source reads nothing in it. Three of the ten places hold that shape, and the raw search missed all three. A case fixture reaches no case, because it is a string literal and no docstring. The class-count case runs over each of the 166 Python files under `tests/`, and it failed on seven of them before the repair. `PYTHON_SOURCE_FLOOR` refuses a corpus that shrank, because an aggregate over an empty set passes. The `Terms` table gains `method name`, and the `method` row and the `fingerprinter` row now state the two counts. **Two identifiers keep their names.** `test_the_processor_reports_one_entry_for_each_method` is cited as evidence by `docs/specs/features/03-concurrency-safety.md`, and the writing standard reproduces a test name verbatim. `make_every_method_answer` is an identifier and no prose. The eight places that remain under `ja4plus/` are #484. **No file under `ja4plus/fingerprinters/` changes, no fingerprint moves, and the conformance counts do not change.** |
| 179 | 2026-08-10 | #453 landed and a case reads every stated state-table count against a live `Processor`. **Two documents stated a different count and the code holds seventeen.** `docs/specs/features/03-concurrency-safety.md` stated sixteen state tables, 47400 remembered keys and 8.5 MiB, and `docs/api_reference.md` stated seventeen, 57400 and 10.2 MiB. This round re-measured the code rather than quoting either page: `sum(len(report.tables) for report in Processor().stats().values())` reads 17. **The two other numbers rest on the table count**, so a repair of the count alone leaves both wrong. The remembered-key count is the sum of `max_entries` over the same tables, which reads 57400. 57400 keys at the 187 bytes #41 measured cost 10.2 MiB. **The state-bound table of the feature file lost one row, and that row is the whole difference.** #215 added `JA4TFingerprinter.connections` at 10000 entries and 600 seconds. That table never gained the row. 47400 plus 10000 is 57400. **A count in prose that nothing measures goes stale on the day the code moves.** This project has repaired that shape more than seven times. New file `tests/test_documented_state_table_count.py` reads the three numbers out of a live `Processor`. It compares every Markdown page of the corpus against them. It reads the state-bound table row by row. **Four reversals prove the cases fail, and each one was restored.** The removed `JA4TFingerprinter.connections` row fails the two table cases with `states the bound of 16 state tables, and one processor holds 17`. The superseded sentence restored fails three cases, one for each number. Those three read `states the state-table count as ['sixteen'], and one processor holds 17`, `states ['47400'] remembered keys, and one processor bounds 57400` and `states ['8.5'] MiB for the remembered keys`. The same mutation on `docs/api_reference.md` fails two, so the reader reaches both pages. **Floors refuse a reader that matched nothing**, because an aggregate over an empty set passes. Eleven more cases prove the readers take each wording and take no count of another thing. **A dated record keeps its numbers.** `readable_text` cuts `CHANGELOG.md` and the two recording sections of this page, so round 85 keeps fifteen tables, 46400 keys and 8.3 MiB. The #279 reading of 40200 entries across the seventeen state tables is a dated measurement that already agrees with the code, so it stands unchanged. The `Terms` table gains `remembered key`, because this repair counts them in three places. **The prose of `docs/specs/features/03-concurrency-safety.md` now states the move that #215 made**, so the count history names five values and four moves. No file under `ja4plus/` changes, no fingerprint moves, and the conformance counts do not change. |
| 180 | 2026-08-10 | #457 landed and the sentence-length rule of `.claude/rules/ste.md` exempts the two records and no other document. #393 found about twelve sentences of its own Changelog row past the 25-word limit, and that finding is correct against the letter of the rule. All 159 numbered rows read the same way, so a row written to the limit is a house-style change for the whole record. **The user ruled on 2026-08-10 that the record is exempt.** A dated record of a past measurement is quoted and not rewritten: a rewrite of the rows falsifies nothing they record, and it does mean that the text a past reader saw is not the text a future reader sees. The rule gains the section `## The one exemption`, which names the entries of `CHANGELOG.md` and the `## Changelog` table of this page. **The exemption names a path and never a class of files.** A blanket exemption for any file whose name holds the word `changelog` reaches `docs/CHANGELOG.md`, which this ruling never read. New file `tests/test_changelog_sentence_exemption.py` holds 27 cases. `exempt_records` reads the list of that section alone, `reaches` matches one document against one record, and `evaluate` reports three states: a record the ruling names nowhere, a document the exemption reaches outside the two records, and a document set below the floor of 40. **An aggregate over an empty set passes**, so a reading over no document fails there rather than reporting a green result. The document set reads `git ls-files -z '*.md'`, which names 59 pages, and an anchor case holds one page of each depth and of each root. **The reader reads a path before a blanket word.** The sentence reader also drops the emphasis marks, because this project opens a paragraph with a bold sentence and the mark stands between the period and the space. The first item holds the word `each` in the clause `which each record one round`, and the first form of the reader read that item as a class of files. **The cases came first and they bite**: against the unrepaired rule they failed 5 of 27. **Two live mutations prove the failing direction.** A third record, the entries of `docs/CHANGELOG.md`, added to the shipped rule kills 2 cases, and one failure reads `the exemption reaches these documents outside the two records: ['docs/CHANGELOG.md']`. A blanket item that exempts any file whose name holds the word `changelog` kills 4 cases. Each mutation was restored and the 27 cases are green after each restore. **A self-review found a third widening and two cases now close it.** The first form of the reader kept the first path of an item alone, so the item that names the entries of `CHANGELOG.md` and `docs/CHANGELOG.md` reported nothing and the exemption reached a third document. `exempt_records` now returns one record for each path of an item. **A case measures the two records, and it measures no other document of this repository.** The entries of `CHANGELOG.md` hold 191 sentences past the limit and the `## Changelog` table of this page holds 787, so the exemption does work. Both counts include the entry and the row of this round. `.claude/rules/ste.md` holds no sentence past the limit, and a case reads that file against the limit it states. Every other measurement runs on a document the case writes. **A sweep that measured every sentence of every page is separate work, and this ruling orders none.** A worker's self-review that reaches a Changelog row's sentence length now reads the exemption and records nothing. #393 raised the second such finding and this round ends them. No file under `ja4plus/` changes and no fingerprint moves. |
| 181 | 2026-08-10 | #446 landed and the `dev` extra of `pyproject.toml` states one recorded shape for every entry. **#378 pinned `ruff` alone**, so `build`, `mypy`, `pytest` and `pytest-cov` still floated, and `pytest` and `pytest-cov` had each drifted two major lines above their floor. **The user ruled the shape on 2026-08-10 and this round builds that ruling.** `build`, `pytest` and `pytest-cov` each carry an exact version beside `ruff==0.16.2`. `mypy` floats. The tracked `requirements.txt` is deleted. **The `mypy` reading is the opposite of the four pins, on purpose.** A pinned type checker falls behind, and a frozen release hides a defect that a later release reports. A floating formatter turns a green tree red, which is noisy and safe. A frozen type checker keeps a red tree green, which is quiet and unsafe. A red `mypy --strict` on an unchanged tree is information this project wants. **The pinned versions are a measurement of 2026-08-10 and not a copy of the issue body.** A clean environment that ran `pip install -e ".[dev]"` resolved `pytest` 9.1.1, `pytest-cov` 7.1.0, `build` 1.5.0, `mypy` 2.3.0 and `ruff` 0.16.2. **Two of those five versions would turn every Python 3.9 job red, and this round declines both.** `pytest` 9.1.1 states `requires_python` `>=3.10` and `build` 1.5.0 states the same, while `FR-foundation-13` runs the matrix from Python 3.9. A read of the PyPI interface reports the newest release of each distribution that carries a wheel for Python 3.9 through 3.13: `pytest` 8.4.2, `pytest-cov` 7.1.0 and `build` 1.4.4. The extra therefore reads `pytest==8.4.2`, `pytest-cov==7.1.0` and `build==1.4.4`. Verified against https://pypi.org/pypi/pytest/9.1.1/json, https://pypi.org/pypi/build/1.5.0/json and https://pypi.org/pypi/pytest-cov/7.1.0/json, retrieved 2026-08-10. **The deletion of `requirements.txt` left two instructions pointing at a deleted file, and this round repairs both.** The `ruff` comment of `pyproject.toml` named it and the note under `FR-foundation-8c` named it. A read of every tracked file found no third one: no workflow reads it, no `Makefile` exists, no `tox.ini` exists, and `README.md` names it nowhere. `CHANGELOG.md` and this page name it inside a record, and no reader installs from a record. **`tests/test_lint_gate_pin.py` widens from 6 cases to 30**, because the Notes of #446 asked for one reader over the extra rather than a second one. The file keeps its name, because `pyproject.toml`, `docs/specs/features/00-foundation.md` and `CHANGELOG.md` each name that path, and a rename would move three records for no reading a reader gains. **The tests came first**, and the unrepaired tree failed 18 of the 30. Two of the failures read `AssertionError: the dev extra states no exact version for pytest: 'pytest>=7.0'` and `AssertionError: these files name requirements.txt: ['docs/specs/features/00-foundation.md', 'pyproject.toml']`. **Eight mutations prove the cases discriminate, and each one isolates to the case it targets.** The floating `pytest` specifier restored fails the pin case and the installed-release case. A `build` comment that drops `#446` fails the citation case alone. A `pytest` comment that drops the word `commit` fails the commit rule alone. A `mypy` comment that drops the word `hides` fails the hidden-defect case alone. `mypy==2.3.0` fails the floating case alone. A `tox>=4.0` entry appended to the extra fails the coverage case alone. A restored `requirements.txt` fails three cases. An install line appended to `README.md` fails the instruction case alone. Each mutation restored from a snapshot of the implemented tree, and the restored form passes 30 of 30 every time. **The reader reads dependency entries and never every quoted substring**, which is the defect #452 records and leaves open. `_dev_entries` strips the comment lines of the list before it collects an entry, and every comment this round writes quotes a version. **Two floors fail a reader that collected nothing**, because an aggregate over an empty set passes. `MINIMUM_DEV_ENTRIES` holds the extra at five entries, and `MINIMUM_INSTRUCTION_FILES` holds the instruction set at ten files. **A case fails a `dev` entry whose shape no decision chose.** An entry that a later round appends reaches `test_the_decision_covers_every_entry_of_the_dev_extra` rather than the next install. **One statement of this round reaches no case, and the test file states that.** No case reads whether a pinned version installs on Python 3.9, because that read needs the package index and this suite opens no network connection. `FR-foundation-8d` through `FR-foundation-8g` carry the four new requirements, and the `Terms` table gains `dependency record`. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 182 | 2026-08-10 | #466 landed and the divergence register carries the FoxIO License 1.1 contradiction. **Three FoxIO records at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8` name a different set of methods.** `License FAQ.md:5` names twelve, the FoxIO `README.md:293` names nine, and `LICENSE:3` names thirteen. `LICENSE:3` spells the scanner `JA4SScan`, and it names a `JA4E` that neither other record names. **This round read all three lines again, from a checkout outside this repository.** #388 measured them on 2026-08-09, and a premise of this repository has read false many times. All three counts hold. The three files carry the SHA-256 values `2bcb0b7c83d4c46cd3f47184d56d6894a0aa884d1428afeb24c1064a34ab965d`, `de909ea6856feb43b711f6403bb30ce6af26bc7aaf4499243becc268b5798bf4` and `7781abaf8b8f1cd550de05de5cba4387886dabd493e618b630b359c450f4d8f0` at that commit. **No statement of equality with FoxIO's list can be true**, so the subset wording of `README.md` stands and the file does not change. **The user ruled on 2026-08-10**, and the register carries the record rather than a closed issue. A measurement that lives only in a closed issue is a measurement the next reader repeats. **This is a candidate reference defect outside the two shapes `.claude/rules/conformance.md` declines**, because it moves no fingerprint. That rule sends such a case to the user. **The register quotes each FoxIO line verbatim**, because `.claude/rules/ste.md` bars a rewording of text copied from the FoxIO material. **The second row records `JA4Scan` as not implemented, with no decision taken.** That is the true state, and the row writes no reason for a decline because no round holds one. **JA4TScan keeps its existing entry** under `Non-goals`. `README.md:42` states that it sends crafted packets, so it reaches a network the operator did not capture. That decision is unrelated to the license. New file `tests/test_foxio_license_register.py` holds seven cases against the two rows. All seven failed before the rows landed, with `the divergence register holds no row named 'The methods the FoxIO License 1.1 covers'` and `the divergence register holds no row named 'JA4Scan'`. **A floor refuses a reader that found no row**, because an aggregate over an empty set passes, and `MINIMUM_REGISTER_ROWS` reads 28. One case bars a decline phrase from the `JA4Scan` row, so a later writer cannot turn a recorded absence into a recorded decision. No file under `ja4plus/` changes and no fingerprint moves. |
| 183 | 2026-08-10 | #441 landed and the prose names the statistics thread by its controlled term. **The `## Terms` table rejects the word `reporter` for the statistics thread, and the prose of the package used `the reporter` throughout.** The self-review of #371 found the disagreement and declined the repair, because a repair inside that diff would have stood `the reporter` and `the statistics thread` in adjacent sentences of one paragraph, which rule 7 of `.claude/rules/ste.md` bars. **The user ruled on 2026-08-10 that the prose changes and every identifier holds its name.** `.claude/rules/ste.md` exempts an identifier from the standard, so `StatisticsReporter`, `report_statistics` and `TheReporterWritesOneLinePerInterval` keep their names, and a rename of a published name is a breaking change this ruling does not make. **A dated record of a past measurement is quoted, not rewritten.** The Changelog rows of #55, #369 and #371 each name the rejected word, this round read all three, and it leaves all three exactly as they read. **A case that reads the corpus for the rejected word is worth more than a corrected sentence, so the case came first.** New file `tests/test_statistics_thread_term.py` holds 283 cases at this commit. `rejected_words` reads the fourth column of the `## Terms` row, so the row stays the authority and a case fails where the row stops rejecting the word. Two parametrized cases read the Markdown corpus of `git ls-files '*.md'` and the Python corpus of `git ls-files 'ja4plus/*.py' 'tests/*.py'`. The Markdown corpus holds `docs/specs/spec.html` beside the Markdown pages, because a writer edits that page by hand. `python_prose` of `tests/test_documented_method_count.py` extracts the comments and the docstrings, so no line of code reaches a case. **The reader reads a whole word alone**, so no part of a compound identifier reaches a case, and six exempt identifiers hold that boundary. **It drops every fenced block and every code span first**, because the standard reproduces an identifier verbatim, and it drops a span one line at a time because a search over a whole page pairs a backtick of one line with a backtick far below it. **An aggregate over an empty set passes**, so each corpus carries a floor and a case proves the floor fails a corpus of no file. `readable_prose` cuts `CHANGELOG.md`, the `## Changelog` table of this page and the `## Terms` section, which is the authority the cases read. **The cases bite**: against the unrepaired prose they failed 3 cases and named `docs/api_reference.md`, `ja4plus/watch.py` and `tests/test_watch_statistics.py`. **This round read each paragraph whole after the change, and not line by line**, which is the defect rule 7 names. The repair removes 24 prose occurrences of the rejected word, and 17 of them now read `the statistics thread`. The other 7 read `the thread` or a pronoun, where the full term would repeat inside one sentence. One message of an assertion reads the term too. **A string literal that is no docstring reaches no case**, so that message is a hole the module records rather than closes. **The row rejects `timer` and `ticker` beside `reporter`, and no case forbids either one.** Two feature pages state that eviction runs on packet arrival and on no timer, each sentence names a general mechanism rather than the statistics thread, and the ruling names `reporter` alone. No file under `ja4plus/fingerprinters/` changes and no fingerprint moves. |
| 184 | 2026-08-10 | #484 landed and the prose of the package counts fingerprinters where it counted methods. **Eight comments and docstrings under `ja4plus/` stated the count of fingerprinter classes as a count of methods.** FoxIO publishes twelve methods and this project implements eleven. `JA4LFingerprinter` writes `JA4L-C=` and `JA4L-S=`, so ten fingerprinter classes carry eleven methods, and a count of classes read as a count of methods reads one short. #387 records the measurement, #450 built the reader, and the ruling of #450 scoped that issue to `tests/` and to four lines of `ja4plus/watch.py`. **This round re-measured the eight places rather than trusting the list, because a premise of this repository has read false many times.** `python_prose` and `class_counts_of_methods` of `tests/test_documented_method_count.py` report `ja4plus/__init__.py:113`, `ja4plus/cli.py:160`, `ja4plus/fingerprinters/base.py:76` and `ja4plus/processor.py:124`, `:264`, `:283`, `:295` and `:332`. Every line number of the issue holds. **The corpus came first, and it made the failure real.** `python_sources` read `tests/` alone, so no case reached the package, and the eight places sat outside every gate. It now reads `ja4plus/` beside `tests/`. `test_no_python_file_states_the_count_of_classes_as_a_count_of_methods` then failed 4 new cases, one for each file, and one read `AssertionError: ja4plus/processor.py holds ['ten methods'], and ten counts the fingerprinter classes rather than the methods they carry`. **The suite alone meets `PYTHON_SOURCE_FLOOR`, so the package carries its own floor.** A read of 2026-08-10 counts 203 files in the corpus, 31 under `ja4plus/` and 172 under `tests/`, against a floor of 120. A corpus that dropped the package again would meet that floor and read no package file, and every case above it would pass. `PACKAGE_SOURCE_FLOOR` reads 25, and `test_the_python_corpus_holds_the_package` holds the count and names `ja4plus/processor.py`. **Its mutation proves that case.** Against a `python_sources` restored to `tests/` alone it fails with `AssertionError: the corpus holds 0 package files, below the floor of 25, so a case over it proves little`, and this round restored the reader after the measurement. **`ProcessorStats.method` keeps its name, its docstring and every value it holds.** It is a published output field, a rename of one is a breaking change, and #450 records that the decision belongs to the user. **The `## Terms` table decides each word of the repair.** `method name` names the lowercase token and the processor holds ten of them, `fingerprinter` names the class that implements one method or two, and the report of `Processor.stats` therefore holds one entry for each of the ten fingerprinters. **The phrase `ten method names` states the truth and still fails the gate**, because `METHOD_NOUN` allows two words of room before the noun, so the repair writes `ten fingerprinters` at every site. **This round read each paragraph whole after the change, and not line by line**, which is the defect rule 7 of `.claude/rules/ste.md` names and which raised #441. The `Processor.stats` docstring reads `fingerprinter` in all four of its paragraphs, where two of them had rotated between `fingerprinter` and `method` for one thing. **The diff under `ja4plus/` touches comment and docstring lines alone.** No statement of the package changes, no fingerprint moves, and the conformance counts hold. |
| 185 | 2026-08-10 | #452 landed and one reader reads the entries of a dependency block of `pyproject.toml`. **`_dependency_block` of `tests/test_documentation_site.py` collected every double-quoted substring of a dependency block**, so a comment inside the block read as an entry. #378 found the defect and stopped importing the helper rather than change it, because a change to a shared reader reaches callers outside its scope. **A reader that is correct only because of what its callers happen to read is the defect**, and the two callers of #378 read the runtime block and the `docs` extra, neither of which carries a comment inside its brackets. **The premise was re-measured against the current tree and it holds.** The unrepaired reader returns `['pytest==8.4.2', 'pytest-cov==7.1.0', 'ruff==0.16.2', 'mypy>=1.11', 'not spec_validation', 'build==1.4.4']` for the `dev` extra. `not spec_validation` is a fragment of the command `pytest tests/ -m "not spec_validation"`, which stands inside the comment beside `build==1.4.4`. **New file `tests/dependency_entries.py` is the one reader**, and it holds `dependency_lines` and `dependency_entries`. `_dependency_block` calls it, and `_dev_lines` and `_dev_entries` of `tests/test_lint_gate_pin.py` call it too, so no caller parses a block of its own. **#446 widened the hazard and answered it with a second reader inside that file.** Every entry of the extra now carries a comment above it, four of those comments quote a version, and two readers of one block can disagree. **The module reads lines and it reads no TOML.** `tomllib` reaches the standard library at Python 3.11, `pyproject.toml` states `requires-python = ">=3.9"`, and `FR-foundation-13` runs the matrix from Python 3.9, so an import of `tomllib` fails two jobs of that matrix. The `dev` extra names no `tomli`, so no third-party reader is present either. **A comment starts at a `#` that stands outside a string**, which is the rule TOML states. One scan therefore drops a comment line and a comment after an entry, and an entry that carries a fragment marker in a URL survives it. **The tests came first**, and the unrepaired tree failed `tests/test_documentation_site.py::test_the_reader_returns_the_entries_of_the_dev_extra_and_no_comment_fragment` with `AssertionError: At index 4 diff: 'not spec_validation' != 'build==1.4.4'`. **Each case names the entries it expects**, because a repair that returns an empty list passes a case that reads the absence of the comment fragment alone. Three cases of `tests/test_documentation_site.py` hold the `dev` extra, the runtime block and the `docs` extra by name, in file order. **New file `tests/test_dependency_entries.py` holds eleven cases**, and each one writes the block it reads, because no dependency block of `pyproject.toml` carries a comment after an entry today. **The reader refuses a block that holds no entry.** An aggregate over an empty set passes, so a reader that parses nothing would report a clean block to every caller, and the floor sits in the reader rather than in each caller. `MINIMUM_DEV_ENTRIES` of `tests/test_lint_gate_pin.py` stays, and it holds the count that the decisions of #378 and #446 cover. **The self-review found one hole and the reader now closes it.** TOML holds a literal string in single quotes, the first form of the reader read the double-quoted form alone, and an entry of the other form would have disappeared with no offender reported. The reader holds both forms, and it holds the quote that opened a string, so the apostrophe of an environment marker closes no basic string. **The reader reads no escape sequence, and a line that holds one raises**, because a silent half of an entry is the worse result: `"foo\"bar"` fails rather than returns `foo\`. **Five mutations prove the cases discriminate, and each one isolates to the cases it targets.** A reader that keeps every comment fails 4 cases, among them the `dev` extra case and `test_the_decision_covers_every_entry_of_the_dev_extra`. A reader that cuts a line at the first `#` fails the fragment-marker case alone, at `AssertionError: the 'dev = [' block holds no entry`. A reader without the floor fails the empty-block case alone, at `Failed: DID NOT RAISE <class 'AssertionError'>`. A reader of the double-quoted form alone fails the literal-string case alone. A reader without the open-string guard fails the escape case alone. Each mutation restored from a snapshot of the implemented tree, and the restored form passes 58 of 58 every time. `docs/specs/features/00-foundation.md` names the module under `FR-foundation-8g`, and it warns a later writer against a second reader. The `Terms` table gains `dependency block`, because the table already rejects `dependency list` as a synonym of `dependency record`. **No file under `ja4plus/` changes and no fingerprint moves.** |
| 186 | 2026-08-10 | #423 landed and the `dropped` field of the statistics line reports the drop count of the capture socket on macOS. **#55 built the field and no caller ever filled it.** `MonitorStats` accepted a `dropped_source`, `ja4plus/cli.py` passed none, and every monitor therefore read `dropped=null` whatever the capture layer knew. `read_interface` now attaches the socket it opened to a `CaptureDropCount`, and `MonitorStats` calls that object for each line. `_L2bpfSocket.get_stats` reads the `BIOCGSTATS` ioctl at `scapy/arch/bpf/supersocket.py:297`, and `AsyncSniffer._run` holds the socket it opens in a local name at `scapy/sendrecv.py:1205`, so a caller reaches that socket through the `opened_socket` argument alone. Both readings were taken again against the installed `scapy` rather than trusted from the issue. **The measurement came from a real capture socket of the development host, and #423 bars an injected one.** This project has recorded sixteen instances of a comparison that is never made reading as a comparison that passes, and a criterion marked met against a fake socket would be the seventeenth. **A `bpf` device opens exclusively, so the grant is the one a run measures at the moment it runs.** This run measured it first: `/dev/bpf0` through `/dev/bpf3` opened, `/dev/bpf4` answered `[Errno 13] Permission denied`, and `/dev/bpf5` and above answered `[Errno 2] No such file or directory`. **Each live case captures on the loopback interface and it generates every packet it reads.** A monitor on `lo0` read 4 packets of one TCP connection this run opened, over an `L2bpfListenSocket`, and reported `dropped=0`. **A count of 0 on a clean capture proves nothing**, because a field no code writes reads the same way. A second case therefore opens a real capture socket with a 4096-byte kernel buffer, sends 50 UDP packets of 1400 bytes to the loopback address, and reads `received=55 dropped=46`. **Two mutations prove that case measures a real drop**: the whole 65535-byte buffer and a burst of one packet each fail it with `AssertionError: 0 not greater than 0`. **The `BIOCGSTATS` ioctl resets neither counter**, so the macOS monitor reports the count of the socket and it accumulates nothing, and two reads of one socket returned 29910 drops and then 59910 drops. That reading is the opposite of the Linux one, where `getsockopt(SOL_PACKET, PACKET_STATISTICS)` resets the counter as it reads, and #326 owns the Linux half. **`read_interface` reads the count once more before it closes the socket**, because the exit summary of FR-live-capture-8 runs after the capture returned, and the kernel gives the file descriptor of a closed socket to the next file the process opens. New file `tests/test_watch_drop_count.py` holds 22 cases. **The cases came first**, and the whole file failed to collect against the base commit `cf77598` with `ImportError: cannot import name 'CaptureDropCount' from 'ja4plus.watch'`. **Where no `/dev/bpf` node opens, the two live cases skip and neither one passes.** A probe that refused the device reported the skip reason `this host opened no capture socket on 'lo0': [Errno 13] Permission denied`, so a green run on a host that grants no capture device reports no met criterion. **Five more mutations prove the wiring, and each was restored.** A drop count that reports nothing fails 10 cases, a capture that attaches no socket fails 3, a capture that reads no count before the close fails 3, a command that passes no drop count fails 2, and a holder that keeps no last count fails 4. **The self-review found a race between the statistics thread and the close, and `CaptureDropCount` now holds a lock.** The statistics thread reads the socket while the capture closes it, and `SuperSocket.close` at `scapy/arch/bpf/supersocket.py:324` calls `os.close` and then sets `closed`, so a reader between the two statements reads a released file descriptor and the ioctl reaches a file this project does not own. `CaptureDropCount.release` holds the lock over the last read and over the drop of the socket, `read_interface` calls it before the close, and the call sits inside its own `try` because an exception there would leave the capture socket open for as long as the process runs. **Two of the three mutations that cover the guard passed against the first form of the cases.** That is the fault this project records sixteen times, found inside the guard written to stop it, and both cases were repaired until each mutation failed one. A socket that made every read wait blocked the release inside the ioctl rather than on the lock, so only the first read waits now, and a release that acquires no lock then fails one case with `AssertionError: Lists differ: [4] != []`. A case that read a closed socket could not part a release from a refresh, so it reopens the socket under another count and models the reused file descriptor, and a capture that refreshes rather than releases then fails one case with `AssertionError: 9 != 5`. A capture that closes the socket before it releases it fails 4 cases. **Four stubs of `read_interface` in the test suite gained the new argument**, and one case of `tests/test_watch.py` now reads the two bounds it names rather than the whole argument set. `docs/specs/features/06-live-capture.md` gains FR-live-capture-15, FR-live-capture-16, two acceptance criteria and the section `## The macOS drop count`, and the `## Terms` table of this page gains `drop count`. **The macOS half of the open question of that feature page is closed and the Linux half stays open.** The measurements ran on macOS 26.6.1, build 25G76, against `scapy` 2.7.0 and Python 3.14.3, on 2026-08-10. **No file under `ja4plus/fingerprinters/` changes and no fingerprint moves.** The conformance suite reports 1532 passed, 143 skipped and 134 xfailed on the base commit and the same three counts after. The unit suite rises from 3827 passed to 3851 passed, which is the 22 cases this issue adds and the two cases the record gates read, and 8 xfailed holds. **Coverage holds at 94% and no file falls.** The total misses hold at 273 while the statement count rises from 4253 to 4292, `ja4plus/watch.py` holds 99% and `ja4plus/cli.py` holds 88%. **Every count above measures this change against the base commit `cf77598`.** The branch then took the integration branch of batch #499, the Changelog table held an append conflict of three rows that read TBD, and the resolution keeps all three. The merged tree reports 3901 passed on the unit suite and the same three conformance counts. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 187 | 2026-08-10 | #67 landed and one file declares the version. **`pyproject.toml` declared `version = "0.6.0"` at line 7 and `ja4plus/__init__.py` declared it again at line 94, so the two records could disagree.** `ja4plus/__init__.py` is now the one declaration, and `pyproject.toml` names `version` in its `dynamic` list and reads the value from `ja4plus.__version__`. **This round declines the `importlib.metadata` reader that `docs/specs/features/09-release.md` proposed, and it measured three reasons.** That call reads the metadata of an installed distribution and it does not read the source tree. A checkout that carries no install raises `PackageNotFoundError`, so the reader needs a literal fallback, and a fallback is the second declaration `FR-release-1` bars. An editable install freezes its metadata at install time: with `pyproject.toml` changed to `0.7.0` and no reinstall, `importlib.metadata.version("ja4plus")` returned `0.6.0`. That metadata resolves through `ja4plus.egg-info` of the working directory, so the answer follows the directory a command runs in. **A gate that reads the version out of an install compares a value against itself**, because continuous integration installs the project from `pyproject.toml`. `version_disagreement` of `tests/version_gate.py` holds `pyproject.toml` against `ja4plus/__init__.py` for `FR-release-2`, and `changelog_disagreement` requires a `## [<version>]` section for `FR-release-3`. **Each gate carries a control case that feeds it a contradicting pair**, because a comparison that is never made reads as a comparison that passes. **The acceptance criterion now counts the declaration and no longer the text `0.6.0`.** #456 measured seven files that hold that text and handed the decision here: five state in prose what version 0.6.0 did, and a count of the text can therefore never reach one. A read of 2026-08-10 reports one declaration file and six files that hold the text. |
| 188 | 2026-08-10 | #68 landed and the publish workflow builds the release and verifies it in a clean environment before it publishes. **`FR-release-4` to `FR-release-9` state the checks, and `.github/workflows/publish.yml` held two steps before this round**: `python -m build` and the upload. Nothing stood between the build and PyPI, and a publish to PyPI cannot be undone. New file `tests/release_verification.py` holds the whole check, and the workflow runs it in one step in front of the upload step. It builds both artifacts, reads them with `twine check`, installs the wheel into a clean environment, runs the console script of that environment, and runs the conformance suite against the installed package. **The conformance suite lives under `tests/`, and #455 removed `tests/` from the wheel**, so the suite comes from the checkout and the package comes from the environment. **A run that starts in the repository root reads the source tree and proves nothing about the wheel.** `python -m` puts the working directory on `sys.path`, and `pytest` inserts the parent of the `tests` package there as well, and both paths hold `ja4plus/`. `verification_root` copies the suite and `pyproject.toml` to a directory that holds no package source, and the check reads `ja4plus.__file__` from that directory before it runs one case. A measurement of 2026-08-10 reports both directions from one clean environment: the copied root resolves `<site-packages>/ja4plus/__init__.py` and the repository root resolves the checkout. **This round rewrites no part of the repair #408 built.** It imports `build_artifacts`, `create_clean_environment`, `package_file_of` and `run_probe` of `tests/test_installed_wheel.py`, which scrub `PYTHONPATH`, set `PYTHONNOUSERSITE` and assert that `pip` wrote the package into `site-packages`. A `pip` success message establishes no install, because `pip` reads the requirement as satisfied where the package already imports. **An aggregate over an empty set passes**, so three floors stand in the check: `twine check` refuses an empty file list, the `PASSED` count reads 2 against two files, and `compare_collections` refuses a clean environment whose case list differs from the checkout. The clean environment collected 1809 cases against 1809 in the checkout, and the run reported 1532 passed, 143 skipped and 134 xfailed, which are the three counts the checkout reports. **A fourth floor came from the self-review, and it is the one that mattered.** The first form of the check read the exit status of the conformance run alone, and `pytest` reports a run whose every case skipped as a success. A vector tree the copy missed would then have printed `release check: PASSED`. `passing_summary` now reads the summary line, refuses a passed count of zero and refuses a summary that names a failure, and `verify` keeps the line it read. A run reduced to a collection refuses the release with `RuntimeError: the conformance run wrote no passed count: '1809 tests collected in 0.23s'`. **The self-review also narrowed two readers of the workflow.** The step reader now starts at the `steps:` key and stops at the next key of the job, so a list elsewhere in the job adds no step, and one case now reads the whole key set of the `on:` block rather than two names, so a trigger this project never reviewed fails a case. New file `tests/test_publish_workflow.py` holds 28 cases, 10 of them under the `installed_wheel` marker. **The cases came first**, and the whole file failed to collect against the base commit `589c5ee` with `ModuleNotFoundError: No module named 'tests.release_verification'`. **Three mutations prove the cases bite, and each one was restored.** The workflow of the base commit fails 2 cases, one reading `publish.yml runs 'python -m tests.release_verification' in 0 steps`. A check that reads `ja4plus.__file__` from the repository root fails 1 case, which reports the checkout path against the `site-packages` path. A file list cut to its first entry refuses the release with `the clean environment collected 202 cases against 1809 in the checkout`. **A workflow step that never runs cannot fail**, and no case here starts the publish workflow, because a run of it would publish. The cases read the step order as text and run the check itself, and one case holds the check command of the workflow against the command the cases run. **The publish step is the last step and no step carries `continue-on-error` or `if: always()`**, so GitHub stops the job at the first failed check and the upload never runs. The `installed-wheel` job of `.github/workflows/test.yml` now runs the marker over `tests/` rather than over one file, because a command that names one file runs none of the cases this round adds. The `dev` extra of `pyproject.toml` gains `twine==6.2.0`, `PINNED` of `tests/test_lint_gate_pin.py` gains the same name, and `MINIMUM_DEV_ENTRIES` rises from 5 to 6. **7.0.0 is the newest `twine` release and this pin declines it**, because it requires Python 3.10 and the matrix runs Python 3.9, which is the reading #446 records for `pytest` and `build`. Verified against https://pypi.org/pypi/twine/json, retrieved 2026-08-10. `.claude/skills/release/SKILL.md` held the same source-tree trap in its step 5 and it now runs the same command. `docs/specs/features/09-release.md` gains six behaviour rules, six edge cases and two file entries. **This round changes no version-resolution mechanism**, which #67 owns; it reads `declared_version` of `tests/test_installed_wheel.py` and changes nothing there. **No file under `ja4plus/` changes and no fingerprint moves.** The conformance suite reports 1532 passed, 143 skipped and 134 xfailed on the base commit and the same three counts after. **Coverage holds at 94%** with 4292 statements and 273 misses, which are the counts the base commit reports. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 189 | 2026-08-10 | #69 landed and the built wheel carries the mapping file and the `py.typed` marker, and no file of an excluded tree. **`FR-release-10` and `FR-release-11` state the two requirements, and #455 already repaired the wheel these cases read**, so this round measures a state that holds and it changes no packaging rule. New file `tests/test_packaging.py` holds nine cases against the built wheel. A read of 2026-08-10 reports 39 entries and 155900 bytes, and #455 reports the same entry count. **The value of a case here is the direction it fails in.** Four mutation cases prove both directions on a copy of the wheel: a copy without `ja4plus/py.typed`, a copy without `ja4plus/data/ja4plus-mapping.csv`, a copy that gains one entry under `tests/`, and an archive that lists no entry at all. **An aggregate over an empty set passes**, so `packaging_faults` reads the entry count before the exclusion rule and the floor stands at 30 entries. **A mutation writes a copy and it edits no byte of the built wheel**, which `test_the_mutation_leaves_the_built_wheel_unchanged` measures by digest. **A second read of 2026-08-10 states which mechanism ships each required entry, and it corrects a plausible reading.** With `[tool.setuptools.package-data]` cut to `ja4plus = ["data/*.csv"]` and the exclusion list cut to `["examples", "assets", "assets.*"]`, the build still listed `ja4plus/py.typed`. `include-package-data` is true by default and the file finder of `setuptools-scm` reports every tracked file, so a tracked file below the package ships whatever the package-data list holds. That weakened build listed 376 entries and 337 of them lay under `tests/`, `examples/` and `docs/`, and the excluded-tree case failed with `the wheel carries 337 of its 376 entries under ['tests/', 'examples/', 'docs/']`. The measurement restored `pyproject.toml` and `git status` reported the file unchanged after the restore. **This round repeats one reading of `tests/test_installed_wheel.py` for the mapping file and one for the documentation tree**, because `FR-release-10` and `FR-release-11` each name them beside another entry and a case reads a whole requirement. **It repeats no other reading of that file**, which holds the assets tree, the top-level name and the whole payload; the new file imports `build_artifacts` and `wheel_entry_names` from it. **The user ruled on 2026-08-10 that the project holds `Development Status :: 3 - Alpha` until the maintainer tags version 1.0.0.** The classifier is a promise about the interface, and the release commit of 1.0.0 makes that promise true. `FR-release-12` therefore stays open, and this round changes no line of `pyproject.toml`. **The ruling declines the third acceptance criterion of #69**, so this round builds no part of it. `docs/specs/features/09-release.md` gains two sections, three edge cases and two ticked criteria. **No file under `ja4plus/` changes and no fingerprint moves.** The unit suite reports 3950 passed, 4 skipped and 8 xfailed, the conformance suite reports 1532 passed, 143 skipped and 134 xfailed, and the `installed_wheel` marker reports 43 passed. **Coverage holds at 94%** with 4292 statements and 273 misses, which are the counts the base commit reports. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 190 | 2026-08-10 | #502 landed and the exemption of `.claude/rules/ste.md` covers rule 3 as well as rule 1. **Rule 3 reads `One topic per paragraph, six sentences at most.` and the self-review of #484 found its own Changelog row past that limit.** That finding is correct against the letter of the rule, and pull request #501 records the decline of the repair. **One row records one round, which is one topic**, so the sentence count of a row follows from how much that round measured. **The user ruled on 2026-08-10 that the exemption widens to rule 3 for the same two records**, the entries of `CHANGELOG.md` and this table. #457 exempted rule 1 on that same reason and this round repeats it for rule 3. **The exemption covers rule 1 and rule 3, and it covers no other rule of the standard.** A read of 2026-08-10 reports 178 of the 190 rows of this table past six sentences, and 113 of the 136 entries of `CHANGELOG.md` past six, so the exemption does work. Both counts include the entry and the row of this round. **The section is now `## The exemption` rather than `## The one exemption`**, and it names the rules it covers, the fourteen rules that reach both records, and the two rulings by issue. The checklist of the standard gains one item for the paragraph limit. **The reader holds the section against the numbered rule list rather than against a transcribed count.** `standard_rules` reads the 16 rules of `## The rules`, `exempt_rules` and `rules_that_reach_the_records` read the two placement sentences, and `rule_failures` reports five states: a rule set below the floor of 16, a covered set that is not rule 1 and rule 3, a rule the section places on both sides, a rule the section places nowhere, and a rule the section names that the standard states nowhere. **A rule this project adds later therefore needs a reading before it ships**, because a seventeenth rule reaches the records until the section places it. `uncovered_failures` measures the exempt region of each record under every rule the exemption drops, so a record that breaks an uncovered rule fails a case rather than passes one. `paragraphs` and `record_units` read one entry of `CHANGELOG.md` and one row of this table as one paragraph. **A list is not a paragraph**, so the paragraph reader parts one item from the next, and a reader that joined them would report the 16 rules of the standard as one paragraph of 16 sentences. The reader drops every fenced block first, and it joins the wrapped line of one item to the item above it. **The cases came first.** The 51 cases of `tests/test_changelog_sentence_exemption.py` failed 17 against the writing standard of the base commit, among them `test_the_exemption_covers_rule_one_and_rule_three`, which read `the ruling exempts the rules [1, 3], and the exemption covers []`. **Five mutations of the shipped standard prove that the cases bite in the failing direction**, and each one runs inside a case rather than against the working tree. An exemption that drops rule 3 reports every long entry and every long row, an exemption that drops rule 1 reports every long sentence, an exemption that names rule 2 on both sides fails the placement with `the exemption both covers these rules and states that they reach: [2]`, and an exemption that places rule 2 on neither side fails it with `places these rules of the standard nowhere: [2]`. `mutated_rule` refuses a mutation that matches no span, because a mutation that matches nothing proves nothing and it leaves the case reading an unmutated file. **An aggregate over an empty set passes**, so the rule floor of 16 and the document floor of 40 each carry a case that feeds the reader nothing. **No other prose loses rule 3.** The standard itself holds the paragraph limit under a case, and a page outside the two records reports its paragraph of seven sentences. **This round widens no rule of the standard beyond the two the user named**, and it rewrites no row of this table. **No file under `ja4plus/` changes and no fingerprint moves.** The unit suite reports 3974 passed, 4 skipped and 8 xfailed, and the conformance suite reports 1532 passed, 143 skipped and 134 xfailed, which are the three counts the base commit reports. **Coverage holds at 94%** with 4292 statements and 273 misses, which are the counts the base commit reports. `ruff check`, `ruff format --check` and `mypy --strict ja4plus/` each report no issue. |
| 191 | 2026-08-10 | #70 landed and the publish workflow reaches TestPyPI on a manual event, and the release body holds the summary and the breaking-change tables of the version. `FR-release-13` and `FR-release-14` state the two requirements. **A manual trigger on a publish workflow creates a path to PyPI**, so the project manager ruled that the dry run must be structurally incapable of reaching the real index. `.github/workflows/publish.yml` now holds two events and two jobs, and each job accepts one event. The published-release event runs the `publish` job, which names the `pypi` environment and states no repository URL. The manual event runs the `dry-run` job, which names the `testpypi` environment and states `https://test.pypi.org/legacy/`. **The manual trigger declares no input**, because an input that selected the index is the hazard the separation removes. Each environment name and the repository URL are literals of the file, so no value a caller chooses reaches either one. The dry run runs the same verification as the release. New file `tests/release_body.py` builds the release body. **The whole `## [1.0.0]` section is 242778 characters and the provider accepts 125000**, so `FR-release-14` as first written cannot hold. The user ruled on 2026-08-10 that the body holds a named part and links `CHANGELOG.md` at the tag for the rest, and the named part is the summary and the two breaking-change tables. `CHANGELOG.md` keeps every row it holds and no round entry moves. The body of version 1.0.0 measures 4727 characters. **The reader truncates nothing**, and a named part above the limit fails the release at a named step. **A `####` heading of a breaking-change table carries one more mark than a part heading**, so the reader keeps both tables and stops at the entry list. A section that names no breaking change reaches the body in whole. The link names the tag and never the default branch. **The value of a case here is the direction it fails in**, so six mutations of the workflow and five of the reader prove both directions, each written to disk, measured and restored; the workflow compared equal by digest `36941ecb201aed94a19d8c63ee920144beafbda00e205e6c55059f7f5d0916d1`. **One defect of the recovered work reached the repair.** `main` read the module docstring for its parser description, and **`python -OO` sets `__doc__` to None on every module**, so the command raised `AttributeError: 'NoneType' object has no attribute 'splitlines'`. The case came first and it failed on that message. #513 records the same pattern at `tests/mutation_cover.py:211` and `tests/mutation_sweep.py:520`. The second reported `pyright` diagnostic, an unresolved import of `tests.version_gate`, does not reproduce from the repository root or from `tests/`. **A workflow step that never runs cannot fail**, and no case starts the publish workflow, because a run of it would publish. **No dry run has run.** The `testpypi` environment holds zero deployments, no agent reads the TestPyPI publisher page, an absent publisher fails the index at `invalid-publisher`, and the literal repository URL keeps the job away from PyPI whatever the publisher state is. `python -m tests.release_verification --dist dist` ran on this host against a real build and reported `release check: PASSED. The release is ready to publish.` #512 records the stale step 1 of `.claude/skills/release/SKILL.md`. No file under `ja4plus/` changes and no fingerprint moves. |
| 192 | 2026-08-10 | #326 landed and the `dropped` field of the statistics line reports the drop count of the capture socket on Linux. #423 closed the macOS half and it left the Linux half open, because no Linux host had run the reading. **`scapy` 2.7.0 calls `getsockopt` in no file**, which a `grep` over the installed package of the granted host confirms. The kernel reports a drop count, `scapy` reads it nowhere, and `ja4plus` now reads the socket option itself in `packet_statistics_drops`. That reader reaches the packet socket through `SuperSocket.ins`, which holds the `socket.socket` the Linux capture opened. **The Python `socket` module publishes neither constant**, and both names read `None` there, so `ja4plus/watch.py` defines them from the kernel headers of that host. `/usr/include/x86_64-linux-gnu/bits/socket.h:151` reads `SOL_PACKET 263` and `/usr/include/linux/if_packet.h:44` reads `PACKET_STATISTICS 6`. `struct tpacket_stats` at `/usr/include/linux/if_packet.h:77` holds `tp_packets` and then `tp_drops`, each one an `unsigned int` of the host byte order, so the reader unpacks eight bytes and takes the second field. A reader of the first field would report the received count as the drop count. **Warning: the kernel resets both counters as the read returns them.** `man 7 packet` of that host states the rule: `Receiving statistics resets the internal counters.` `CaptureDropCount` therefore adds each Linux reading to a running total, where the macOS reader reports the count of the socket and accumulates nothing. **The reset is a measurement here and not a premise.** One packet socket on `lo`, with a 2304-byte receive buffer, answered `first=(100, 99)` and then answered `second=(0, 0)`, with no packet between the two reads. **Each live case captures on the loopback interface and it generates every packet it reads.** A monitor on `lo` read 4 packets of one TCP connection this run opened, over an `L2ListenSocket`, and reported `dropped=0`. **A count of 0 on a clean capture proves nothing**, because a field no code writes reads the same way. A second case therefore sends 50 UDP packets of 1400 bytes to the loopback address and reads a total of 99 drops, then sends 8 more packets and reads a total of 115. **The second burst is far smaller than the first one**, so a monitor that reported the last reading would report a smaller count after it. **A mutation proves that the case bites**: `self._dropped = increment` in place of the sum fails 5 cases, among them the live one, which reports `AssertionError: 0 not greater than 0`. The mutation was restored. **The cases came first**, and the whole file failed to collect against the base implementation with `ImportError: cannot import name 'PACKET_STATISTICS' from 'ja4plus.watch'`. **Where the host grants no `CAP_NET_RAW`, each live case skips and none of them passes.** `docs/specs/features/06-live-capture.md` gains FR-live-capture-17, one behaviour rule, two acceptance criteria and the section `## The Linux drop count`, and it now records no open question. `tests/test_watch_drop_count.py` rises from 22 cases to 38. **The live measurements ran on Linux 6.11.0-29-generic, against `scapy` 2.7.0 and Python 3.12.7, on 2026-08-10.** The host is shared, so the run read `uptime` before and after: `load average: 6.08` and then `load average: 5.91`, on 56 cores. The five gates ran on macOS 26.6.1, build 25G76, against `scapy` 2.7.0 and Python 3.14.3. **No file under `ja4plus/fingerprinters/` changes and no fingerprint moves.** The conformance suite reports 1532 passed, 143 skipped and 134 xfailed before and after. The unit suite rises from 3950 passed and 4 skipped to 3963 passed and 7 skipped, and the three new skips are the Linux cases on a macOS host. **Coverage holds at 94%**, the total misses hold at 273 while the statement count rises from 4292 to 4316, and `ja4plus/watch.py` holds 99%. |
| 193 | 2026-08-10 | #438 landed and a member pull request reaches the round-entry check, whose change set the runner now reads. **The finding that outranks the rest of this round is that the check passed by construction.** The base-branch filter of `.github/workflows/test.yml` read `branches: [master, dev]`, and that filter matches the base branch of a pull request, so the only pull requests that reached the `test` job were the batch pull request and the promotion. A batch pull request reads its change set against the tip of `dev`, and every member of a batch has already recorded a round, so the check found an entry whatever one member did. **The member pull request is the change set the check exists to refuse, and the filter kept it away from the job.** The user ruled on 2026-08-10 that the filter widens, and it now reads `branches: [master, dev, "batch/**", "epic/**"]`. **The widened filter costs no run that the batch model saved**, because a member commit ends with a skip keyword and #459 measured that such a commit creates no run for any event, so a keyword-free head is the one head that now starts one. `.claude/rules/batch-gate.md` gains the section `## Which pull request creates a run`, which states the two conditions that each stop a run and states this shape so that a reader holds the next check against it. #429 built `tests/test_round_entry_existence.py`, which fails a change set that edits a tracked file and records no round. **That case also skipped on every job of `.github/workflows/test.yml`, so it guarded the local gate of a worker and it guarded no pull request.** The `actions/checkout` step of that workflow names no `fetch-depth`, so it makes a clone of depth 1, and that clone carries no `origin/dev` ref and no history behind the checked-out commit. `git merge-base` fails on such a clone. **The `test` job now fetches the base commit of the pull request at depth 1 and writes it into the `ROUND_ENTRY_REFERENCE` environment variable.** `environment_reference` reads that variable, `named_commit` resolves it against the repository, and `reference_commit` prefers it over the merge base. A name the repository cannot read reads as no name, so a stale value leaves the local gate of a worker exactly as it is, and the reading of row 156 stands where the environment names nothing. **`git diff` and `git show` read a commit that no history connects to `HEAD`, and `git merge-base` does not.** A diff compares two trees, so one extra commit is the whole requirement, and a read of 2026-08-10 against a clone of depth 1 reports that the diff and the show each answered and that `git merge-base` exited 1. **The measured cost decided the reading.** A clone of depth 1 holds 14844 KB, the extra fetch of one commit raises it to 15452 KB, and `fetch-depth: 0` raises it to 18728 KB, so this step costs 608 KB against 3884 KB on each of the six jobs of the matrix. The `fetch-depth: 0` cost rises with every commit the project makes, and the cost of one commit does not. **#438 declined a second fetch of `dev` at depth 1 on a measurement**, because two shallow histories share no commit, `git merge-base` fails between them, and the case would skip exactly as before. **The base commit is a stricter reference than the merge base with `dev`**, because it reads the change set of one pull request rather than the change set of a whole integration branch, so the widest limit row 156 records does not reach the runner reading. A push event carries no base commit, so the case skips there, and a push to the integration branch has no change set to read. **The fetch writes the ref `refs/ja4plus/round-entry-base`, because a commit that no ref reaches is unreachable.** A bare `git fetch origin <sha>` writes `FETCH_HEAD` and no ref, and a read of 2026-08-10 on a clone of depth 1 reports that the ref form survives `git gc --prune=now`. **The self-review raised `continue-on-error` on the fetch step and #438 declined it**, because a failed fetch leaves the variable empty, the case skips, and a green job over a silent skip is the failure this round exists to remove. **The expression reaches the `env` block of the step and never its `run` block**, because an expression inside a shell command is a script-injection path. **Twelve new cases hold the reading, and two of them hold the widened filter and the section of the rule file.** The self-review also found that `named_commit` rested on its one caller to drop the space around a name, and the reader now drops it itself. Two of them build an orphan branch, which reproduces the runner state where `git merge-base` reports nothing and the diff still answers, and two read `.github/workflows/test.yml` for the fetch step. **The cases came first**, and `test_the_test_job_fetches_the_base_commit_of_a_pull_request` failed against the workflow of the base commit. **The runner read the check in both directions, on one pull request that landed in no branch.** The user approved one deliberately red pull request for this proof and no other issue of this project may open one. #520 targeted `batch/510-dry-run-and-gates`, it targeted neither `dev` nor `master`, and each of its two heads carried no skip keyword. **A green run alone proves nothing here**, because the case skipped before this round and a skip is not a pass. Head `8702322` edited three tracked files and recorded no round, and run https://github.com/Crank-Git/ja4plus/actions/runs/31402557575 concluded `failure` with `tests/test_round_entry_existence.py::test_the_change_set_of_this_branch_records_a_round` reported as `FAILED` rather than `SKIPPED`, on the assertion `the change set holds these paths outside the two records: .claude/rules/batch-gate.md, .github/workflows/test.yml, tests/test_round_entry_existence.py. CHANGELOG.md holds 105 round entries against 105 at the reference commit`. Head `ecd1677` added the entry and the row and changed nothing else, and run https://github.com/Crank-Git/ja4plus/actions/runs/31403217183 concluded `success` with the same case reported as `PASSED` on all six jobs of the matrix. #520 is closed and its branch is deleted. **GitHub read the widened filter from the merge commit of that pull request and not from the base branch, and the run itself is that measurement.** `batch/510-dry-run-and-gates` carries the old filter and the run started anyway, so a filter change takes effect inside the pull request that carries it. The documentation states the same of `pull_request_target`: `This event runs in the context of the default branch of the base repository, rather than in the context of the merge commit, as the pull_request event does.` **#519 measured the older filter before the ruling**, with a keyword-free head into the same integration branch, and the provider held no run for it after 150 seconds. No file under `ja4plus/` changes and no fingerprint moves. |
| 194 | 2026-08-10 | #511 landed and the branch-protection claims of `.claude/rules/batch-gate.md` reach a case. **The rule file, `CHANGELOG.md` and the round 174 row of this table each state that every required context carries `app_id` 15368, and no case read the number.** `provider_contexts` reads `required_status_checks.contexts`, which holds the context names alone, and the provider holds the application under `required_status_checks.checks[]`. The new reader `check_applications` reads that list, and `test_the_provider_carries_the_stated_application_on_every_context` requires `app_id` 15368 on each of the eleven contexts. **The case reads its floor before it reads the provider**, so a file that states another number fails the case rather than compares two empty sets, and a run against a rule file that states `app_id` 99999 reported `assert 99999 == 15368`. A read of the provider on 2026-08-10 reports `app_id` 15368 on all eleven entries of `checks`. Where `gh` returns no reading the case skips and it does not pass, and `test_the_application_case_skips_where_the_provider_returns_no_reading` measures that state. **The same round repairs three statements of the rule file.** The opening claim of `## The provider refuses an ungated merge` stated the refusal without a condition, and the same section measures `enforce_admins` as `false`, so it names that reading and states that an administrator merge reaches `dev` with no run. The steps intro promised a change of the rule and the four steps below it are four reads, so it states what the steps do, and a case holds the verb of every step against `Open` and `Read`. One sentence dated its warning against the day a reader reads it, and it names the read of 2026-08-10 instead. **A floating word is the defect this file exists to record**, so the case sweeps every live sentence of the file and the sentence of round 193 that carried the same word reads from the widened filter instead. `tests/test_batch_gate_protection_rule.py` gains 26 cases and it holds 119. The coupling in `test_the_live_context_case_skips_where_the_provider_returns_no_reading` and the one-directional proof of `SUPERSEDED_PHRASINGS` stay open, and #511 records both as out of scope. No file under `ja4plus/` changes and no fingerprint moves. |
| 195 | 2026-08-10 | #522 landed and the divergence register records the JA4 ALPN ruling. **The user ruled on 2026-08-10 that the form of `ja4plus` stands.** A first ALPN value that is not alphanumeric writes `99`, and a first ALPN value of one byte writes `hh`. The conformance audit of the same date named this the one condition where `ja4plus` matches no FoxIO implementation. **The two references disagree with each other**, and each one reads its own tooling rather than the packet. `ja4plus` reads the packet bytes, and it writes `99` or `hh`. FoxIO Python writes `U+FFFD`, which is a replacement character that no packet byte holds. FoxIO Rust writes `h9`, which is the escape text of `tshark`. **A match with either reference copies an artifact of that reference tool into the fingerprint**, and the user declines that. **No FoxIO reading is available to adopt here**, so the new row states the ruling rather than a value to adopt. #141 holds the measurement of the disputed inputs and #162 records the readings of 2026-08-07, and the two rows those issues wrote stay as they are. **This round moves no fingerprint and it repairs no defect.** New file `tests/test_alpn_ruling_register.py` holds eight cases, and seven of them failed before the row landed. **Four mutations prove that the cases bite.** A row that drops the ruling reports ``AssertionError: the ruling row holds no 'the form of `ja4plus` stands'``. A row that gives FoxIO Rust the value `hh` reports ``AssertionError: the sentence that names FoxIO Rust states no value `h9` ``. A row that renames the item reports `AssertionError: the divergence register holds no row named 'The JA4 ALPN form that matches no FoxIO implementation'`. A row that moves the date out of the ruling sentence into the sentence after it reports `AssertionError: the sentence that states the ruling names no date 2026-08-10`. **The self-review found that fourth reading, because the ruling sentence closed its bold mark after the period and the reader therefore read it together with the sentence after it.** The row now closes the bold mark before the period, so the reader holds the date against the ruling alone. The run restored each mutation, and `git diff --stat` then reported one insertion. **A floor refuses a reader that found no row**, because an aggregate over an empty set passes. **The sixteen entries of `tests/foxio_deviations.json` that name #162 stand**, and one case reads that count. The deviation register holds 134 keys before and after, and the conformance suite reports 1532 passed, 143 skipped and 134 xfailed before and after. **No file under `ja4plus/` changes.** The unit suite reports 4076 passed, 6 skipped and 8 xfailed, and the eight new cases stand among them. **All six skips read the platform or the host**: two name the Linux cache convention, three name the `AF_PACKET` sockets of Linux, and one names the capture privilege of this account. Coverage holds at 94% with 4316 statements and 273 misses. |

| 196 | 2026-08-10 | #515 landed and JA4TS reaches a FoxIO reference value. **The audit of 2026-08-10 read JA4TS as the one method of twelve that reached no FoxIO-produced expected output**, and it stood on transcribed prose of a deleted file plus a capture this project writes itself. `tests/build_ja4ts_rst.py` writes that capture and `tests/test_ja4ts_reset.py` reads it back, so the fixture proved the implementation against itself. **The finding that outranks the rest of this round is that this project recorded a wrong measurement, and that record is what hid the reference.** The table `The search for a reference value` in `docs/specs/foxio/JA4T.md` read `No ja4t value and no ja4ts value` for `wireshark/test/testdata/`. **That directory holds 118 `ja4.ja4t` values and 58 `ja4.ja4ts` values, in 24 of its 37 files.** The earlier search read the key `ja4t`, and the FoxIO Wireshark dissector writes the key `ja4.ja4t`, so the search matched nothing and the reader wrote a negative result. `## The search for a JA4TS reference value` above records every source this round read and what each one returned, so no later reader repeats the search. **`tests/foxio_vectors/wireshark_expected/` rises from 2 files to 26**, and `tests/test_foxio_wireshark_ja4ts.py` compares all 58 values. **52 match byte for byte and 6 reach the register.** The dissector names a frame rather than a stream, so the reader takes the frame out of the capture and reads the address pair and the port pair of that packet, then counts the occurrence in frame order. **A tunneled capture needs the `ja4plus` import before the read**, because `ja4plus` loads the scapy contrib layers that dissect ERSPAN, VXLAN and Geneve; a read of 2026-08-10 reports `gre-erspan-vxlan.pcap` frame 2 as `Ether / IP / GRE / Raw` without it and as a chain that ends in `TCP` with it. **All 6 registered values are the RST decline that R13 of `docs/specs/foxio/JA4T.md` already records**, and #246 owns each one. The dissector writes a second JA4TS value on a RST packet of a connection with no delay, that value repeats the value the SYN-ACK produced, and `.claude/rules/conformance.md` declines a value that describes no packet of its own. **The decline stays proven rather than asserted**: `TestTheDifferencesAreTheRecordedResetDecline` reads the TCP flags of each of the six frames as `R`, reads each value against the value of the first SYN-ACK of that stream, and reads the `ja4plus` value count of the stream. **`tests/foxio_vectors/zeek_expected/` is new and it holds all seven Zeek baselines**, which `docs/specs/foxio/zeek.md` asked a later issue to commit. `tests/test_foxio_zeek_ja4ts.py` compares 9 of their 10 JA4TS values and `ja4plus` produces all nine. **The tenth is barred and the bar is measured, not asserted.** `zeek/ja4t/main.zeek:66-68` returns an empty option record when the link layer is not Ethernet, the link type of `ipv6.pcapng` is `DLT_NULL`, and the Zeek baseline writes `65535_00_00_00` where `ja4plus` writes `65535_2-1-1-4-1-3_1346_10`. **The FoxIO Wireshark dissector writes the `ja4plus` value for that same connection**, so a second FoxIO implementation corroborates the bar and it does not rest on this project's own reading. **`tests/compare_zeek_baselines.py` ran by hand until this round and no gate read it**, because it needed a `FoxIO-LLC/ja4` checkout that a worktree holds none of. It now reads the committed baselines when it gets no path, and `TestTheCommittedBaselinesRunAsAGate` states the counts of the whole comparison. **The run against the committed copy reads 98 rows, 63 matches and 35 differences, which are the three counts the run of #327 measured against a checkout**, so the committed copy reproduces the reference. `tests/conformance_index.py` gains the JA4TS fingerprinter, and `REPORTED_METHODS` in `tests/test_spec_validation.py` names neither JA4T nor JA4TS, so the addition adds no case there. **The cases came first**, and the ten comparison cases of `tests/test_foxio_zeek_ja4ts.py` failed against the base index with `assert () == ('65535_2-1-1-4-1-3_1346_10',)`. **One mutation of `ja4plus/fingerprinters/ja4ts.py` proves that the comparison bites, and the mutation was restored.** `return prefix + "x" + _part_e(packet, tracker, prefix)` in place of `return prefix + _part_e(packet, tracker, prefix)` moves every JA4TS value and no value of another method, and it kills 62 cases: the 52 Wireshark comparisons, the 9 Zeek comparisons and the one case that reads the `ja4plus` value of the barred row. The six registered cases stay `xfailed` under it, which is correct, because a mutation moves no declined value onto the dissector value. **The self-review found one gap and it belongs to the FoxIO material.** A second mutation, of the delay-list separator from `"-".join` to `",".join`, killed no case of either new module, because no value of either source carries more than one delay. No capture of the vector set holds a connection the server answered three times, so `tests/test_ja4ts_part_e.py` keeps the constructed cases that measure the separator, the rounding rule and both bounds, and `docs/specs/foxio/JA4T.md` records the reading as a warning. **The criterion of `docs/specs/features/00-foundation.md` reported the vector-set change rather than a reviewer**, and its count rises from 90 files to 121. `tests/download_test_vectors.py` fetches and validates both new file sets, and it rewrites `tests/foxio_vectors/NOTICE` with them, so a refresh at a newer commit carries them. A baseline that names no column and a baseline that holds no data row each fail the refresh. **No file under `ja4plus/` changes and no fingerprint moves.** `tests/foxio_deviations.json` rises from 134 keys to 140 and the conformance suite rises from 134 `xfailed` to 140, so the register invariant holds on both sides. The conformance suite reports 1635 passed where it reported 1532, and 143 skipped on both. |
| 197 | 2026-08-10 | #524 landed and a case that skips on every job of the matrix fails the run. **A skip is not a pass, and a case that runs nowhere is not a case.** #438 measured that shape: `tests/test_round_entry_existence.py` reported a skip on every job of `.github/workflows/test.yml` from the day it was written, and every job stayed green while the case refused nothing. **One job reads no such case, because one job reads one environment.** A macOS case that skips on Linux is correct, and a Linux case that skips on macOS is correct. **The union of the six reports of the `test` job is the first reading that tells a correct skip from a case the suite runs nowhere.** `.github/workflows/test.yml` gains the job `skip-gate`, which depends on the `test` job, downloads the six `test-results-*` artifacts and fails a case that every report records as skipped. `tests/skip_gate.py` holds the condition and `tests/test_skip_gate.py` holds that file. **The reading takes no new dependency**, because the workflow already writes one JUnit report for each matrix job with `pytest --junitxml`. `tests/universal_skips.json` holds the allowlist, each entry names the environment limit that stops its case, and the gate fails an entry that names no limit whatever the reports hold. **A report the download omits is not a passed job**, so the gate also fails a download smaller than the matrix. **The gate reads one download directory and never the checkout**, because #473 measured a reader that walked the repository root and picked up a worker worktree under `.claude/`. **The gate is proven on the runner in both directions**, on pull request #526 into `batch/523-conformance-and-claims`, which merged into nothing and closed with its branch deleted. The red direction ran at https://github.com/Crank-Git/ja4plus/actions/runs/31408763259, where every other job concluded `success` and `skip-gate` concluded `failure`. The green direction ran at https://github.com/Crank-Git/ja4plus/actions/runs/31412758369, with all twelve jobs green. **A pull request that conflicts with its base creates no run at all**, which this proof measured three times, because the workflow runs against a merge commit that the provider does not build for a conflicting pull request. **The census classifies all 40 skip sites of `tests/` in the four forms `skipTest(`, `pytest.skip(`, `skipIf(` and `skipUnless(`: 28 of class a, 10 of class b and 2 of class c.** The 2 of class c are `tests/test_round_entry_existence.py:521` and `:525`, and #528 holds them. One further class c site sits outside the four forms, `tests/test_documentation_site.py:222`, and #529 holds it. **The gate reads the six matrix jobs and no other job**, so a conformance case that skips everywhere reaches it nowhere, and #530 holds that reach. `FR-pre-release-validation-35` through `FR-pre-release-validation-38` state the requirement, and `.claude/rules/batch-gate.md` gains the section `## A case that skips on every job fails the run`. **`skip-gate` is a twelfth check and the required list of `dev` holds eleven names**, so the branch protection rule reaches this check once the user adds the name to it, and a red `skip-gate` fails the run whatever that rule holds. No file under `ja4plus/` changes and no fingerprint moves. |
| 198 | 2026-08-10 | #513 landed and every argument parser of the repository states a description that a stripped docstring does not remove. **`python -OO` sets `__doc__` to None on every module**, so a parser that reads `__doc__.splitlines()[0]` for its description raises `AttributeError: 'NoneType' object has no attribute 'splitlines'` before it reads one argument. **#70 repaired that line at `tests/release_body.py:139` at round 191, and the same line reached `tests/skip_gate.py:333` at round 197.** A repair of the known sites stops no new one, so the deliverable of this round is the case and not the repair. New file `tests/test_parser_description.py` parses every tracked Python file with `ast` and fails a parser whose description reads `__doc__`. **The case came first and it failed on the four sites a read of `dev` at `31cca24` named**: `['tests/memory_ceiling_run.py:109', 'tests/mutation_cover.py:211', 'tests/mutation_sweep.py:520', 'tests/skip_gate.py:333']`. The first three raise and the fourth passes `__doc__` itself, which argparse accepts as None, so that one reports no description at all under `-OO`. Each of the four now names a `DESCRIPTION` constant, which is the repair `tests/release_body.py` already carries. **A second case runs every tool module of `tests/` twice, once plain and once under `-OO`, and it requires the same `--help` from both runs.** That comparison covers both failure modes at once, because the first raises and the second drops the description. It reads the words and never the wrap, because argparse wraps to a terminal width that a captured run does not hold. `test_the_help_comparison_fails_a_module_that_reads_the_module_docstring` writes a module that reads `__doc__` and measures the two outputs apart, so the comparison bites rather than passes on an empty set. **The reader takes its file list from `git ls-files` and it walks the checkout nowhere.** #473 measured a reader that walked the repository root and picked up a worker worktree under `.claude/`, so its corpus grew with the number of live workers. `test_the_tracked_corpus_holds_an_argument_parser` and `test_the_tracked_corpus_holds_a_tool_module` state the floor, so a pathspec that lists nothing fails rather than reports a clean corpus. The reader reads an `add_parser` call beside an `ArgumentParser` call, because a subcommand parser accepts the same keyword. `FR-pre-release-validation-39`, `FR-pre-release-validation-39a` and `FR-pre-release-validation-39b` state the three requirements. **The self-review found two latent gaps in the reader itself, and `FR-pre-release-validation-39c` states the repair.** `ArgumentParser` takes `description` third, so `ArgumentParser(prog, usage, __doc__)` reached no report while the reader read the keyword alone. The tool reader matched the text `__name__ == "__main__"`, so a guard of another quote style or another operand order reached no run case. Both gaps were latent, because every call site of the repository writes the form each reader expected, and **a latent gap in this reader is the defect this round exists to remove**. The reader now reads the third position beside the keyword, and it reads the main guard from the syntax tree. The reader reports `description=__doc__ or "Read the run."`, which raises nothing, and that stricter reading is a decision rather than a defect: a module states one description, and a fallback beside `__doc__` states it twice. `pyright` reports no `reportOptionalMemberAccess` against the four files. No file under `ja4plus/` changes and no fingerprint moves. |
| 199 | 2026-08-10 | #528 landed and the change-set defect case runs on the runner. **A skip is not a pass, and this case had never run on any job.** `tests/test_round_entry_existence.py::test_the_reading_fails_the_change_set_of_the_defect` reads commit `46aa502` against its parent, which is the change set #412 shipped: eleven sweeps, two repairs, a new test file, no `CHANGELOG.md` entry and a green gate. It is the one case that proves the reading finds a real change set which records no round. **The `actions/checkout` step makes a clone of depth 1, and that clone holds neither the commit nor its parent**, so the case reported `this clone holds no parent of commit 46aa502` on all six jobs of the matrix. The census of #524 measured it in run https://github.com/Crank-Git/ja4plus/actions/runs/31408763259 and classed it c, which names a site that no limit of the runner explains. **The `test` job now fetches the two recorded commits, each at depth 2**, into `refs/ja4plus/recorded-defect` and `refs/ja4plus/recorded-control`, because a fetch of depth 2 carries the commit and its parent and that pair is what a diff needs. **A green reading of one commit proves nothing on its own**, so the case set holds two directions and the runner reads both. `CONTROL_COMMIT` is commit `f140a5c`, which #429 shipped to add `tests/test_round_entry_existence.py`, and it recorded one round, so `test_the_reading_passes_the_change_set_that_records_a_round` reads it and expects no failure. **The measured cost decided the reading**, taken on 2026-08-10 after `git gc --prune=now` on each read: the clone of depth 1 holds 14884 KB, the defect fetch raises it to 14912 KB and the control fetch raises it to 14916 KB, so the two commands cost 32 KB together against the 3884 KB of `fetch-depth: 0` that row 193 records. **The fetch names the whole identifier, because git resolves no abbreviation at the remote**, and `+46aa502:refs/ja4plus/abbrev` answered `fatal: couldn't find remote ref 46aa502` on a clone of depth 1. **The step carries no `if` condition**, because the two commits stand in the recorded history whatever event starts the run, where the step of row 193 reads the base commit of a pull request and skips on a push. **#528 declined `continue-on-error` for the reason #438 states**: a failed fetch leaves the two cases skipped, and the `skip-gate` job then fails the run on a case that ran nowhere. **The cases came first**, and `test_the_test_job_fetches_the_two_recorded_change_sets` failed against the workflow of the base commit. **The runner read the check in both directions, on one pull request that landed in no branch.** The user approved that deliberately red pull request under the standing rules, and #538 targeted `batch/535-skip-gate-repairs` and neither `dev` nor `master`, and each of its two heads carried no skip keyword. **The red head `4da756b` raised in place of each assertion, so the run states the reading the runner made.** It ran at https://github.com/Crank-Git/ja4plus/actions/runs/31416675503, where every one of the six matrix jobs reported both cases as `FAILED` rather than `SKIPPED`, on `AssertionError: the runner read the defect commit: the change set holds these paths outside the two records: .claude/rules/conformance.md, docs/mutation_reports/412-utils.json, docs/mutation_settlements/412-utils.json and 3 more. CHANGELOG.md holds 64 round entries against 64 at the reference commit` and on `AssertionError: the runner read the control commit: None`. **The two readings on the runner match the two readings of the local gate word for word.** The green head `837f88e` carried the cases as this round ships them, and it ran at https://github.com/Crank-Git/ja4plus/actions/runs/31417379359, where all twelve jobs concluded `success`, both cases reported `PASSED`, and the `skip-gate` job read 6 reports and found 10 cases that ran on no job of the matrix, all 10 allowed and neither of these two among them. #538 is closed and its branch is deleted. `tests/universal_skips.json` loses the entry that named this case, so the allowlist holds one open finding and #529 names it. `docs/specs/features/11-pre-release-validation.md` gains `FR-pre-release-validation-40` and one acceptance criterion, and `.claude/rules/batch-gate.md` records the removal beside the entry rule. No file under `ja4plus/` changes and no fingerprint moves. |
| 200 | 2026-08-10 | #529 landed and the documentation slug case runs on one job of the matrix. **The census of #524 read `tests/test_documentation_site.py:222` as a class c site**, because the case skipped on all six jobs of `.github/workflows/test.yml` and no limit of the runner explained it. Run https://github.com/Crank-Git/ja4plus/actions/runs/31408763259 holds that measurement, and the report named the reason: the `docs` extra installs pymdownx, and `dev` does not. **The case holds `_slug` against `pymdownx.slugs.slugify` on every published heading**, and a slug that moves breaks every link into that anchor. The `test` job gains the step `Install the documentation extra on one job of the matrix`, which runs `pip install -e ".[docs]"` under `if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.13'`. **The extra reaches one job and not six.** `griffe` 2.1.0 requires Python 3.10 and the matrix runs Python 3.9, so the Python 3.9 job can never install this extra. The skip gate reads the union of the six reports, so one run of the case is the whole requirement. A read of 2026-08-10 measured the step at 28 packages and 125476 KB, against 197200 KB for the `dev` extra alone, so five more installs would buy 627380 KB and nothing else. **The two extras resolve together**, which the same read measured on one environment, and the unit suite reported 4136 passed and 6 skipped there. **The step names one job rather than a matrix variable, because a matrix variable reaches the job name**, and the branch protection rule of `dev` holds each of eleven required check names. **The third reading runs the case in `.github/workflows/docs-build.yml`, and this round declines it.** That workflow filters four paths, so a batch that touches no documentation path creates no run of it, and the case would run sometimes rather than always. The skip gate downloads the `test-results-*` artifacts of its own run, so a case that runs in another workflow leaves the gate naming it. `tests/universal_skips.json` loses its entry for the case, and `test_one_job_of_the_test_matrix_installs_the_documentation_extra` and `test_the_skip_allowlist_holds_no_entry_for_the_slug_case` hold the repair. `FR-pre-release-validation-38a` states the requirement. **The repair is proven on the runner at https://github.com/Crank-Git/ja4plus/actions/runs/31418704343**, a manual run of the branch head, where the report of `test (ubuntu-latest, 3.13)` records the case as run in 0.048 s and the five other reports carry the skip. **The census of `skip-gate` reads 11 cases that ran on no job of the matrix, and this case is none of them.** **A manual run carries no pull request**, so the step `Fetch the base commit of the pull request` runs nowhere and `tests.test_round_entry_existence::test_the_change_set_of_this_branch_records_a_round` skips on all six jobs, and that case reads a pull-request event. No file under `ja4plus/` changes and no fingerprint moves. |
| 201 | 2026-08-10 | #530 landed and the skip gate reads the report of every job that runs cases. **A reader that covers part of the suite reports a clean corpus over the part it cannot see**, and #524 bound the gate to the six reports of the `test` job. That job runs `pytest tests/ -m "not spec_validation"` and `tests/conftest.py` deselects the `installed_wheel` marker, so it collects 4150 cases of the 6111 the suite holds. **A read of 2026-08-10 reports 1918 cases of the `conformance` job and 43 of the `installed-wheel` job that no report of the `test` job holds**, which is 32 percent of the suite. The `fuzz` job holds 127 cases and the `samples` job holds 31, and the `test` job holds every one of those 158, so neither job widens the corpus today. **The `skip-gate` job now waits for all five jobs and downloads ten reports** under the pattern `*-results*`. **The gate binds every job that runs cases, and not the jobs that add a case today**, because a list of the second kind goes stale on the day a job changes its selection. `tests/test_skip_gate.py` reads `.github/workflows/test.yml` instead: every job that runs `pytest` writes one JUnit report and uploads it, the download pattern matches the artifact name of each one, and the `needs` list of `skip-gate` names all of them. **The verdict reads over the jobs that select the case and over no other job**, so a conformance case rests on one environment where a matrix case rests on six. The census names the report that holds each case it lists, and **#530 declined a job field on an allowlist entry on that reading**, because a scope the run derives needs no hand maintenance and disagrees with no report. **A case that no job selects at all reaches no report**, which is a different finding, and `.claude/rules/batch-gate.md` records that limit so that no reader takes a green gate for a statement about it. **The conformance suite reports 143 skips and every one of them reads `not applicable:`**, which one cross product produces where the vector holds no value for the method and this project produces none. **All 143 belong to `tests.test_spec_validation::test_the_produced_occurrence_keys_equal_the_reference`, which ran 199 other parameter sets in the same job**, so the function asserts and no case runs nowhere. A pass in place of the skip would assert an equality over two empty sets, which #524 put out of scope, so this round repairs no conformance case. An entry of `tests/universal_skips.json` therefore names one case under `case`, or one class of skip under `skip_message_prefix`, and one prefix entry stands in place of 143 case entries. **A prefix entry covers a case only where every report that skips it states that same prefix**, so a second reason on one job takes the case back out of the class and the gate names it. The census reports the count each prefix entry covers and it names none of those cases one by one, because 143 near-identical lines would bury every case-level line of the census. The census also names the corpus the reports hold, which is the reach of the reader: 6111 cases against the 4150 the six reports held. **The gate is proven on the runner in both directions, on the branch `issue/530-skip-gate-beyond-the-matrix`.** The red direction ran at https://github.com/Crank-Git/ja4plus/actions/runs/31419291939 with the widened reader and no prefix entry. Every other job of that run concluded `success` and `skip-gate` concluded `failure`, and its census reads `The skip gate read 10 reports that hold 6129 cases between them, and found 155 cases that ran on no job.` 143 of the 155 are parameter sets of `tests.test_spec_validation::test_the_produced_occurrence_keys_equal_the_reference`, and the six-report reader accepted every one of them. **A manual run carries no base commit**, because the fetch step of the `test` job runs on a pull-request event alone, so `tests.test_round_entry_existence::test_the_change_set_of_this_branch_records_a_round` skipped on all six matrix jobs of that run and stands among the 155. That case runs on a pull-request run, and the push run of `dev` at `31cca24` concluded `success` on `skip-gate`, so this reading names the manual run and it names no defect of the gate. The green direction ran at https://github.com/Crank-Git/ja4plus/actions/runs/31420269200, which the pull-request event of #540 started with the prefix entry in place. All twelve jobs of that run concluded `success`, and its census reads `The skip gate read 10 reports that hold 6132 cases between them, and found 153 cases that ran on no job.` beside the line `- 143 cases: allowed by the skip message prefix 'not applicable:'`. The unit suite reports 4156 passed, 7 skipped and 8 xfailed, the conformance suite reports 1635 passed, 143 skipped and 140 xfailed, and coverage holds at 94% with 4316 statements and 273 misses. **The sub-merge gate of batch #535 then found a case that passes on each branch and fails on the merge result**, which is the shape that gate exists to catch. #529 added `test_the_skip_allowlist_holds_no_entry_for_the_slug_case`, which reads every entry of `tests/universal_skips.json` as `entry["case"]`, and the prefix entry of this round holds no `case` key, so that reader raised `KeyError` on the merged tree. **Both intents stand and the reader is what accepts both**, so `tests/test_documentation_site.py:349` reads `entry.get("case")` and the assertion of #529 stays exactly as strong, because None equals no case identifier. `test_the_slug_reader_accepts_an_entry_that_names_a_class_of_skip` holds the repair, and `test_the_slug_reader_still_finds_the_slug_case_in_an_entry` states the floor so that a reader which found nothing fails rather than passes. **The merge result runs green on the runner at https://github.com/Crank-Git/ja4plus/actions/runs/31423103064**, where all twelve jobs concluded `success` and the census reads `The skip gate read 10 reports that hold 6137 cases between them, and found 152 cases that ran on no job.` The five gates run green on the merge result as well: 4161 passed, 7 skipped and 8 xfailed on the unit suite, 1635 passed, 143 skipped and 140 xfailed on the conformance suite, 43 passed on the `installed_wheel` marker, and no issue from `ruff check`, `ruff format --check` or `mypy --strict ja4plus/`. **#529 and #530 each allocated `FR-pre-release-validation-38a` to a different requirement, and #529 landed first and keeps it.** `FR-pre-release-validation-35`, `FR-pre-release-validation-36` and `FR-pre-release-validation-37` change their wording, `FR-pre-release-validation-35a`, `FR-pre-release-validation-36a`, `FR-pre-release-validation-38b` and `FR-pre-release-validation-38c` are new, and `.claude/rules/batch-gate.md` gains the subsections `### The gate reads ten reports, and #530 measured what the six missed` and `### One entry covers one class of skip`. No file under `ja4plus/` changes and no fingerprint moves. |
| TBD | 2026-08-10 | #546 landed and `.claude/rules/batch-gate.md` records the twelfth required context of `dev`. **The user added `skip-gate` to the branch protection rule of `dev` on 2026-08-10**, and the rule file stated eleven required contexts. Two cases of `tests/test_batch_gate_protection_rule.py` failed on `dev` at `61e04f1`: `test_the_provider_requires_the_contexts_the_rule_file_lists` and `test_the_provider_carries_the_stated_application_on_every_context`. **The repair is the record, and no assertion of those cases moved.** The read of 2026-08-10 that #546 took returns `200` with twelve required contexts: `lint`, `fuzz`, `samples`, `conformance`, `installed-wheel`, the six `test (...)` entries of the matrix, and `skip-gate`. Every entry of `required_status_checks.checks[]` carries `app_id` 15368, `enforce_admins` reads `false`, `strict` reads `false`, and `gh api repos/Crank-Git/ja4plus/rulesets` returns `[]`. **The protection section holds two reads of one date**, so each record names the issue that took it: #480 read eleven contexts and #546 read twelve. The eleven-context reading stays as a quoted record under a paragraph that marks it superseded, and the 2026-08-09 reading already holds that form. **A reader who meets a new required context needs the condition that turns it red**, so the section states that `skip-gate` fails a case which every report records as skipped, and it names Round 201 as the round that built the job. New case `test_the_rule_file_states_what_the_twelfth_required_context_refuses` holds that statement, and it failed before the repair. **A self-review read the first form of that case as satisfiable by prose that names the check and states no condition.** The case now cuts every fenced block before it reads a sentence, because the check-name list holds no full stop and joins the sentence beside it. It also requires the word `skipped`, which names the input that turns the check red. **Two mutations prove that it bites**: prose that drops `skipped` and prose that names no check each fail it. `.github/workflows/test.yml` and `docs/specs/features/09-release.md` each stated the eleven in live prose, and both now state the twelve. **The dated records keep the eleven**: the entry of Round 197 in `CHANGELOG.md`, the row of Round 174 above and the quoted reading of the rule file each record a past measurement, and this round supersedes such a record rather than rewrites it. **No file under `ja4plus/` changes and no fingerprint moves.** |

## Issue map

Created by `spec-to-issues` on 2026-08-06 against `Crank-Git/ja4plus`.

### Epics

| Epic | Feature file | Epic issue | Sub-issues |
|---|---|---|---|
| Epic 0: Foundation | `features/00-foundation.md` | #11 | #21, #22, #23, #24, #25, #26 |
| Epic 1: Spec conformance | `features/01-spec-conformance.md` | #12 | #27, #28, #29, #30, #31, #78, #80 |
| Epic 2: Correctness audit | `features/02-correctness-audit.md` | #13 | #32, #33, #34, #35, #36, #37 |
| Epic 3: Concurrency and resource safety | `features/03-concurrency-safety.md` | #14 | #38, #39, #40, #41, #42, #43, #285 |
| Epic 4: Typed public interface | `features/04-typed-api.md` | #15 | #44, #45, #46, #47, #48 |
| Epic 5: Structured output | `features/05-structured-output.md` | #16 | #49, #50, #51, #52 |
| Epic 6: Live capture | `features/06-live-capture.md` | #17 | #53, #54, #55, #56 |
| Epic 7: Database enrichment | `features/07-db-enrichment.md` | #18 | #57, #58, #59, #60, #61 |
| Epic 8: Documentation | `features/08-documentation.md` | #19 | #62, #63, #64, #65, #66 |
| Epic 9: Release | `features/09-release.md` | #20 | #67, #68, #69, #70 |
| Epic 11: Pre-release validation | `features/11-pre-release-validation.md` | #406 | #407, #408, #409, #410, #411, #412, #413, #414, #415, #419 |

### Requirements

| Requirement | Issues |
|---|---|
| `FR-concurrency-safety-1` | #40 |
| `FR-concurrency-safety-2` | #40 |
| `FR-concurrency-safety-3` | #40 |
| `FR-concurrency-safety-4` | #40 |
| `FR-concurrency-safety-5` | #40 |
| `FR-concurrency-safety-6` | #40 |
| `FR-concurrency-safety-7` | #38, #39 |
| `FR-concurrency-safety-8` | #38, #39 |
| `FR-concurrency-safety-9` | #38 |
| `FR-concurrency-safety-10` | #38 |
| `FR-concurrency-safety-11` | #41 |
| `FR-concurrency-safety-12` | #41 |
| `FR-concurrency-safety-13` | #42 |
| `FR-concurrency-safety-14` | #42 |
| `FR-concurrency-safety-15` | #43 |
| `FR-correctness-audit-1` | #32 |
| `FR-correctness-audit-2` | #33 |
| `FR-correctness-audit-3` | #32 |
| `FR-correctness-audit-4` | #34 |
| `FR-correctness-audit-5` | #89 withdrew it |
| `FR-correctness-audit-6` | #35 |
| `FR-correctness-audit-7` | #35 |
| `FR-correctness-audit-8` | #37 |
| `FR-correctness-audit-9` | #37 |
| `FR-correctness-audit-10` | #37 |
| `FR-correctness-audit-11` | #34 |
| `FR-correctness-audit-12` | #36 |
| `FR-correctness-audit-13` | #37 |
| `FR-db-enrichment-1` | #57 |
| `FR-db-enrichment-2` | #57 |
| `FR-db-enrichment-3` | #58 |
| `FR-db-enrichment-4` | #58 |
| `FR-db-enrichment-5` | #58 |
| `FR-db-enrichment-6` | #58 |
| `FR-db-enrichment-7` | #59 |
| `FR-db-enrichment-8` | #59 |
| `FR-db-enrichment-9` | #60 |
| `FR-db-enrichment-10` | #60 |
| `FR-db-enrichment-11` | #61 |
| `FR-db-enrichment-12` | #61 |
| `FR-db-enrichment-13` | #61 |
| `FR-db-enrichment-14` | #57 |
| `FR-db-enrichment-15` | #57 |
| `FR-db-enrichment-16` | #364 |
| `FR-db-enrichment-17` | #364 |
| `FR-documentation-1` | #62 |
| `FR-documentation-2` | #62 |
| `FR-documentation-3` | #62 |
| `FR-documentation-4` | #63 |
| `FR-documentation-5` | #63 |
| `FR-documentation-6` | #63 |
| `FR-documentation-7` | #64 |
| `FR-documentation-8` | #66 |
| `FR-documentation-9` | #64 |
| `FR-documentation-10` | #65 |
| `FR-documentation-11` | #65 |
| `FR-documentation-12` | #65 |
| `FR-documentation-13` | #66 |
| `FR-documentation-14` | #62 |
| `FR-documentation-15` | #64 |
| `FR-foundation-1` | #22 |
| `FR-foundation-2` | #22 |
| `FR-foundation-3` | #22 |
| `FR-foundation-3a` | #22 |
| `FR-foundation-4` | #23 |
| `FR-foundation-5` | #23 |
| `FR-foundation-6` | #23 |
| `FR-foundation-7` | #24 |
| `FR-foundation-8` | #24 |
| `FR-foundation-8a` | #21 |
| `FR-foundation-9` | #24 |
| `FR-foundation-10` | #24 |
| `FR-foundation-11` | #24 |
| `FR-foundation-12` | #24 |
| `FR-foundation-13` | #24 |
| `FR-foundation-14` | #25 |
| `FR-foundation-15` | #26 |
| `FR-foundation-15a` | #26 |
| `FR-foundation-15b` | #26 |
| `FR-foundation-16` | #26 |
| `FR-foundation-17` | #26 |
| `FR-live-capture-1` | #53 |
| `FR-live-capture-2` | #53 |
| `FR-live-capture-3` | #53 |
| `FR-live-capture-4` | #53 |
| `FR-live-capture-5` | #54 |
| `FR-live-capture-6` | #54 |
| `FR-live-capture-7` | #54 |
| `FR-live-capture-8` | #55 |
| `FR-live-capture-9` | #55 |
| `FR-live-capture-10` | #55 |
| `FR-live-capture-11` | #56 |
| `FR-live-capture-12` | #56 |
| `FR-live-capture-13` | #56 |
| `FR-live-capture-14` | #53 |
| `FR-pre-release-validation-1` | #408 |
| `FR-pre-release-validation-2` | #408 |
| `FR-pre-release-validation-3` | #408 |
| `FR-pre-release-validation-4` | #408 |
| `FR-pre-release-validation-5` | #408 |
| `FR-pre-release-validation-6` | #408 |
| `FR-pre-release-validation-7` | #408 |
| `FR-pre-release-validation-8` | #409 |
| `FR-pre-release-validation-9` | #409 |
| `FR-pre-release-validation-10` | #409 |
| `FR-pre-release-validation-11` | #409 |
| `FR-pre-release-validation-12` | #410 |
| `FR-pre-release-validation-13` | #410 |
| `FR-pre-release-validation-14` | #410 |
| `FR-pre-release-validation-15` | #410 |
| `FR-pre-release-validation-16` | #411, #436 |
| `FR-pre-release-validation-16a` | #411 |
| `FR-pre-release-validation-16b` | #436 |
| `FR-pre-release-validation-16c` | #436 |
| `FR-pre-release-validation-17` | #411 |
| `FR-pre-release-validation-18` | #411 |
| `FR-pre-release-validation-19` | #411 |
| `FR-pre-release-validation-20` | #411 |
| `FR-pre-release-validation-21` | #411, #412, #413, #414 |
| `FR-pre-release-validation-22` | #412, #413, #414 |
| `FR-pre-release-validation-23` | #412, #413, #414 |
| `FR-pre-release-validation-24` | #412 |
| `FR-pre-release-validation-25` | #414 |
| `FR-pre-release-validation-26` | #414, #419 |
| `FR-pre-release-validation-26a` | #414, #419 |
| `FR-pre-release-validation-26b` | #419 |
| `FR-pre-release-validation-26c` | #419 |
| `FR-pre-release-validation-27` | #415 |
| `FR-pre-release-validation-28` | #415 |
| `FR-pre-release-validation-29` | #415 |
| `FR-pre-release-validation-30` | #415 |
| `FR-pre-release-validation-31` | #415 |
| `FR-pre-release-validation-32` | #415 |
| `FR-pre-release-validation-33` | #407, #408, #409, #410, #411, #412, #413, #414, #415 |
| `FR-release-1` | #67 |
| `FR-release-2` | #67 |
| `FR-release-3` | #67 |
| `FR-release-4` | #68 |
| `FR-release-5` | #68 |
| `FR-release-6` | #68 |
| `FR-release-7` | #68 |
| `FR-release-8` | #68 |
| `FR-release-9` | #68 |
| `FR-release-10` | #69 |
| `FR-release-11` | #69 |
| `FR-release-12` | #69 |
| `FR-release-13` | #70 |
| `FR-release-14` | #70 |
| `FR-spec-conformance-1` | #27 |
| `FR-spec-conformance-2` | #27 |
| `FR-spec-conformance-3` | #27 |
| `FR-spec-conformance-4` | #28 |
| `FR-spec-conformance-5` | #28 |
| `FR-spec-conformance-6` | #28 |
| `FR-spec-conformance-7` | #29 |
| `FR-spec-conformance-8` | #29 |
| `FR-spec-conformance-9` | #30 |
| `FR-spec-conformance-10` | #30 |
| `FR-spec-conformance-11` | #31 |
| `FR-spec-conformance-12` | #31 |
| `FR-spec-conformance-17` | #88 |
| `FR-spec-conformance-18` | #88 |
| `FR-spec-conformance-19` | #88 |
| `FR-spec-conformance-20` | #88 |
| `FR-spec-conformance-21` | #88 |
| `FR-structured-output-1` | #49 |
| `FR-structured-output-2` | #49 |
| `FR-structured-output-3` | #50 |
| `FR-structured-output-4` | #49 |
| `FR-structured-output-5` | #49 |
| `FR-structured-output-6` | #52 |
| `FR-structured-output-7` | #50 |
| `FR-structured-output-8` | #50 |
| `FR-structured-output-9` | #52 |
| `FR-structured-output-10` | #52 |
| `FR-structured-output-11` | #51 |
| `FR-structured-output-12` | #51 |
| `FR-structured-output-13` | #50 |
| `FR-typed-api-1` | #44 |
| `FR-typed-api-2` | #44 |
| `FR-typed-api-3` | #45 |
| `FR-typed-api-4` | #45 |
| `FR-typed-api-5` | #44 |
| `FR-typed-api-6` | #44 |
| `FR-typed-api-7` | #46 |
| `FR-typed-api-8` | #46 |
| `FR-typed-api-9` | #47 |
| `FR-typed-api-10` | #47 |
| `FR-typed-api-11` | #191 |
| `FR-typed-api-12` | #47 |
| `FR-typed-api-13` | #47 |
