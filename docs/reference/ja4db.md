# Lookup

The lookup identifies the application behind a fingerprint. It reads the bundled FoxIO
mapping file. A lookup that reaches the network needs an opt-in, because a fingerprint
describes traffic the operator observed.

::: ja4plus.ja4db
